# Purpose

- Own typed, focusable UI components for serializable `InteractionRequest` kinds.

# Ownership

- Components translate an existing request payload into controls and emit the exact response payload selected by the player.
- `TextChoiceInteraction` renders yes/no and authored indexed/scenario options as one backed content-width semantic response row. `InteractionPresenter` mounts that row in a compact floating pane immediately above the narrative well; it never consumes the narrative text measure or claims unused stage width. The shared presenter invents no generic title for these requests. A positive acknowledgement with visible authored text has no formal button or helper labels: clicking the narrative surface or pressing Enter/Space emits its one typed response. An authored empty positive message keeps that typed boundary and receives one content-width `Continue` action. Journal-eligible acknowledgements never add a separate Take Note button: source-backed default-off Auto Note leaves Castle's proven `N` key available, while an explicit enabled preference makes the narrative acknowledgement emit `{ "takeNote": true }`. An already-recorded message exposes no duplicate mutation.
- `InteractionPresenter` always presents a Complex Encounter's explicit authored prompt; prior AP narration or a prior result cannot replace the active Encounter request text.
- `EncounterInteraction` preserves one backed stable original-art Action, Items, Skills, Speak, Spells, and Stop dock directly above the narrative well. A new or repeated Complex Encounter starts with no command selected so its authored prompt retains the complete well. Action replaces the roster with Castle's actorless toggle-set sidebar: the request-owned choices scroll independently above fixed square Done and local Stop controls, Done enables only at the exact supplied count, and local Stop returns to the idle dock without backing out of the Encounter. Submitting an action removes the dock and sidebar while an authored click-result owns the narrative well; acknowledging a continuing result reconstructs the idle dock. Presentation must not invent a party-member choice for an authored Action such as COB's `Knock on the door`; Castle commits only the selected slot set. Authored Simple choices remain canvas-limited in one compact vertical list, while Speak alone uses the narrative well beneath the dock. A repeated Encounter keeps the latest non-click authored result in the well and uses a transparent pointer-blocking shield so the surrounding application remains legible without accepting background input. Items temporarily cover the application with the real `InventoryScreenController`; Spells temporarily replace the roster with a scroll-bounded `SpellsScreenController` in Encounter-selection mode; closing either restores the intact Encounter. Both submit the exact request-owned owner and identity. Skills uses a content-sized map-stage dialog whose previous/portrait/next controls form one centered cluster above the source-valued grid. Speak uses the established Theldrow input/action treatment, bounds its word-or-phrase entry to 39 characters so Castle's 40-byte destination remains terminable, and exposes Castle's one proven submit path. Escape first cancels an open action/item/spell task and otherwise emits only an authored Back/Stop response; it cannot invent an unavailable exit. Unavailable commands remain disabled in place with an exact reason, and every submission retains the typed complex-encounter body. Castle's Scroll-versus-Spells/Stop control mapping remains unresolved evidence and must not be guessed through presentation remapping.
- `InteractionPresenter` owns request identity, modal visibility, and construction of `InteractionResponse`.
- An interaction component may supply one semantic `preferred_initial_focus`; the presenter uses it before tree-order fallback. Treasure uses the already-selected recipient so opening the workspace does not falsely hover or focus-ring the first loot item.
- `SelectionInteraction` keeps the active character prompt and optional spell context in the Classic textbox while `ClassicPartyRoster` is the sole character picker, matching Castle's accessible right rail. The exact requested count auto-submits in stable Party order; no duplicate portrait grid or Continue control may consume the narrative well. Ally body-count selection stays in its dedicated list.
- `LifecycleInteraction` renders only the host-supplied Main Menu/End Adventure or process-Quit operations in a compact locked floating modal. Process Quit shows one question and one centered content-width Save and Quit / Quit / Cancel row without duplicate headings or explanatory prose; every lifecycle request disables its outer interaction scrollbar, and Main Menu retains at most one short consequence line while showing every declared operation. It owns stable fixed actions, restrained destructive emphasis, and Escape routing to a declared Cancel action, then emits one declared action identity. It does not save, close a session, infer combat state, or reuse scenario-choice semantics.
- `AgeUpdateInteraction` presents exact typed portrait/tactical media, source-backed band/range, and nonzero fifteen-column age changes in one contained inset and emits only the empty `age_update` acknowledgement payload.
- `PlayerMapInteraction` owns the negative-opcode immediate display stage. It renders the detached player-map payload through the same presenter used by Maps/Notes and emits only the empty acknowledgement for the matching request ID; closing it cannot mutate acquisition state or bypass the issuing VM frame.
- `ThiefEncounterInteraction` owns the dedicated eight-action workspace supplied by the typed request. It uses the same compact previous/next character navigation as Encounter Items, keeps the selected portrait/name above one full-height action grid, and emits the exact selected character and source action index. `PickLockInteraction` renders exact character identity, chance and countdown above its deterministic tumbler frames at the supplied cadence; the red, gold, and green track zones mirror Castle's thresholds, the final source second is held, and only the selected frame index is emitted. Neither component rolls RNG, recomputes ability values, mutates encounter flags, or invents a cancel outcome absent from Castle's picker.
- `pick_lock_interaction.tscn` owns the Pick Lock identity, mechanism, legend, action, and timer layout. Its script binds only the request values and instantiates `pick_lock_tumbler_row.tscn` once per supplied tumbler.
- Encounter item mode uses the full application inventory workspace, including its standard portrait selector, ledger, character rail, item inspector, and explicit Back to encounter action. Its request-owned catalog binds the exact owner/instance pair to the hidden Classic identity without exposing hidden facts. Encounter spell mode filters the standard spellbook to request-supplied owner and packed spell identities; the controller changes only temporary caster, level, and spell selection before returning the exact typed pair. Disabled reasons come only from the request.
- `TreasureDistributionInteraction` owns `ordinary` and `completion-confirmation`. Ordinary mode renders Castle's single ordered booty queue—including leading fumbled items—in a responsive white 50×60-cell field (six columns in the compact profile and as many whole cells as the wide field permits), with a transient source-cell hover oval, centered native-size item art, a typed item inspector, the shared Alt-hover floating detail pane, and a separately selected portrait-led recipient roster showing item count, movement, and load. The floating pane consumes only request-detached public naming, description, facts, state, charges, and exact CICN; unidentified input therefore remains unidentified. A request-revealed magical item receives the exact blue-green loot glow behind its icon without enlarging that item art; unrevealed and nonmagical cells remain plain. Pool, Share, exact denomination transfers, stable Detect/Identify controls, and Done remain visible in the same workspace at 1280×720. Detect/Identify caster selectors use the established Theldrow picker role and display authoritative remaining SP separately from cost; a used method stays disabled with its supplied reason while the sibling remains usable when its independently recomputed caster list permits it. Identified magical items visibly retain both facts. Only an unidentified item's name receives Castle's source-backed hollow outline treatment. Clicking an eligible item emits one exact-instance assignment; after the session commits it, presentation alone runs the source-ported 24-step pickup burst around the actual item CICN—a contracting white oval and independently colored expanding oval—then plays sound 6002 without withholding the resulting request/view, so another item may be assigned immediately. Its color generator is presentation-local and never consumes session RNG. The animation never mutates, advances, or gates simulation. `fumbled-item-recovery` is retained only as a save-v4 visual adapter. Done retains the ordinary component across its response/rerender boundary; completion mode then explicitly confirms or cancels abandonment in a presenter-owned nested modal over that exact retained workspace.
- `TreasureDisplayText` formats only the detached player-visible Treasure summaries, recipient facts, wealth, and item-detail dictionaries consumed by that component.
- `ShopInteraction` owns the canonical two-ledger exchange workspace: a Castle-white Adventurer Pack field stays left, Shop Stock stays right, and a narrow slate control spine between them contains the five native source-category bitmap filters. The ledgers own all surplus application height while remaining the only scrolling region, so the compact transaction strip and fixed description/mechanical-fact wells stay bottom-aligned at every interface scale. The lower region keeps separately framed left load, selected shopper, Cost/Offer/Weight transaction, route-control, and right load groups; equal expanding left/right control regions center the independent two-by-three portrait selector directly beneath the filter spine; source-art Shop Keeper/Items/Money controls plus Done use compact square stages. Selecting a right shopper replaces stock with that pack until Shop Keeper restores it. Rows open unpressed; Buy/Sell/Identify and bidirectional drag recheck request-supplied availability and emit the same typed response. Alt-hover uses the shared floating item-detail pane and cannot expose anything beyond the request's public transaction facts. The compact profile uses pack/stock tabs, a five-row minimum ledger viewport, and narrower framed groups so every fixed lower action remains visible without outer-page scrolling; larger profiles retain the nine-row minimum and expand their ledgers first. Shopper, stock, and carried-item selection are presentation-only; details, categories, portraits, load, and legality come from the request rather than inference.
- `classic_exchange_item_button.tscn`, its script, and `ClassicExchangeLedger` provide presentation-only drag payload and drop-target plumbing for Trade and Shop. The button's drag preview is also a reusable scene rather than an ad hoc control. Their callers must recheck the supplied exact instance, destination, and availability before emitting the existing typed mutation; drag never becomes a second gameplay path, and visible click actions remain available.
- `LevelUpInteraction` owns locked floating `result` and `spell-selection` modals. Result renders the committed level and four supplied gains with exact character portrait when the detached party view proves it. Spell selection uses the application region rather than roster height, consumes the available height at canonical and compact resolutions, caps excess height on tall displays, stays vertically centered, reuses the exact gradient `Spell Level` heading, level-colored `Level #` parent filters, pressed Theldrow spell controls and inset record chrome, and renders the complete supplied candidate set, existing selections, exact point accounting, supplied application-owned description, and exact spell CICNs only when the detached character spell view proves their resource identity. Switching levels refreshes the selected record and description while filtering the visible list without discarding hidden selections. A selected spell remains removable; an unselected spell whose supplied cost exceeds the remaining allowance stays visible but disabled with an exact reason. Confirmation remains available with bankable unspent points and becomes disabled with an explicit warning only when a restored selection already exceeds the allowance. It acknowledges the exact character result or returns one set of stable spell IDs; it never rolls, applies gains, computes eligibility, overrides the runtime budget validation, invents missing before/after facts, or changes character state.
- `ScrollingTextInteraction` owns opcode 62's opaque complete-stage use of the shared `ClassicScrollingTextSurface`. The surface resolves the request's exact signed scenario `TEXT` identity and optional same-ID `styl` resource through the media chain, fills the available stage instead of collapsing to content height, applies the authored color/face/size runs, and renders over exact application `ppat` 129 tiled at native 64-by-64 scale. It adds no invented heading, starts at the top, automatically advances 20 pixels per second, applies Castle's signed 25-pixel vertical drag steps, and acknowledges only through double-click, Enter/Space, Escape, or visible Done; a single click may begin a drag but never dismisses the request. Offset, timing, and drag state are presentation-only and never enter the save.
- `TempleInteraction` owns a full-stage three-pane temple workspace: exact portrait-led adventurer selection, all supplied source-priced services, and a selected character/service inspector with fixed Purchase/Pool/Share/Leave actions. The optional 800×600 profile replaces the adventurer pane with a compact selector. It may disable Purchase only from the supplied selected character's available-gold fact, shows at most the first five supplied conditions, and emits only stable character/service identity or the named wealth action. It does not invent service effects or reuse unproven art.
- `BankInteraction` owns a full-stage three-pane Bank-backed Swap / pooled-departure workspace with separate account, adventurer, and selected-character transfer panes; the optional 800×600 profile replaces the adventurer pane with a compact selector. It renders only supplied pooled, banked, personal, load, Pool/Share, exact-increment, selection, and unavailable-reason facts. It emits Pool, Share, one stable character/denomination transfer, or Done. It never performs bank import/return, clears a pool, resumes movement, recomputes capacity, invents arbitrary deposit/withdraw controls, or turns Done into a deposit.
- `BattleInteraction` consumes the request's weapon mode, rules-owned melee/projectile targets, legal-action list, typed Escape availability, staged spell/power/cost choices, and source-probed charged-item/power/target combinations. Cast and item responses carry only actor, action, and stable item/spell/target identities plus spell power, one selected battlefield coordinate/rotation, an ordered actor sequence, or an ordered summon-anchor sequence where supplied. A random-power item first submits its target-free roll action, then labels the regenerated request's authoritative staged power before ordinary targeting. Automatic groups use an empty target ID plus the core-provided group label and omit individual HP. Memorized-spell targeting may select any request-provided living battlefield footprint, center, or ordered summon anchors, and leaves final actor/range/LOS/mask/footprint legality to the core response transaction. Repeated charged items and scrolls retain their pre-probed candidates, accept one through the supplied power limit, and emit the explicit selection order as `targetIds`. The component never recalculates spell or item legality, masks, occupants, range, LOS, charges, or effects. Disabled Escape, Fire, spell, and item actions use typed reasons and cannot synthesize targets or tactical facts. Guard emits Castle's persistent defend command, while Finish is a separate response that clears Guard and remaining movement.
- `BattleInteraction` also consumes typed Auto Turn, Delay, Bandage, and Turn Undead probes. It emits the ordinary combat action plus at most one core-listed Bandage recipient; persistent character Auto is routed separately by the party roster and never masquerades as an activation command. Its fixed-height tactical record resolves exact active, initiative, and inspected combat icons through the supplied media map, collapses missing identities to text, and gives restrained emphasis only to Attack/Spells/Finish without changing command order or availability. View, Action, and Tactics spend only their command-shelf height budget when scaling from 1x to 2x; a live application resize must reapply group, control, text, and spacing scale without rebuilding the interaction or resetting its current mode.
- `battle_initiative_panel.tscn` owns the stable initiative frame, heading, and turn row. `BattleInitiativePanelBuilder` fills only its request-ordered combatant buttons; selection returns the stable identity to `BattleInteraction` for inspection and camera focus.
- A battlefield Ctrl/Command-click may replace the command overview with `BattleInteraction`'s read-only Items, Conditions, and Attacks record for the selected party or monster combatant. The persistent Back action restores the unchanged battle overview; inspection changes neither the active actor nor session state and renders only request-supplied rows.
- `fast_spell_dock.tscn` and `fast_spell_dock_slot.tscn` own the editable transient dock and repeated slot structure over the battle canvas. While Alt is physically held and at least one binding is assigned, `FastSpellDock` binds up to ten assigned slot instances in 1–0 order using only complete exact eight-frame spell-effect sequences from detached spell views. Disabled assignments retain their request-owned reason; enabled pointer or top-row activation delegates to `BattleInteraction.handle_fast_spell` and therefore cannot bypass ordinary power, cost, action, or target handling. A nonautomatic Fast Spell makes the ordinary targeting panel visible even though the spellbook was not opened first; `T` selects/cycles request-supplied combatants or marks the current/default area center, and Space presses the same confirmation boundary as the visible Cast action. The dock owns no gameplay state and closes on activation, modifier release, focus loss, playback, or request replacement.
- `SelectionInteraction` keeps mandatory field-spell, scroll, and charged-item target selection in the Classic textbox/Party-roster composition. It renders only the request's strict spell-target context—spell identity, selected power, effective cost, icon key, and target facts—while the roster owns numbered target picks and emits the existing stable-ID response. It uses the same native-slate target badge as field and combat spellbooks only for catalog-proven donor gradient lettering; unproven target types retain ordinary semantic facts without invented badge art. It does not parse prompts, recalculate costs, or invent target art for unproven Classic target types.
- `SelectionInteraction` also owns a locked floating surviving-allies modal with supplied candidates, health, required selections, maximum capacity, exact CICNs joined by stable ID from the detached completed-combat monsters or retained `party_allies`, and fixed Continue. It owns temporary selection and maximum enforcement and emits only selected stable IDs. An empty request has a non-trapping fallback, but core/session flow remains responsible for skipping ally selection when no allies exist.
- The battle component is a full-window-width bottom command deck, not a full-stage modal. Its compact beveled overview keeps active and inspected facts beside a request-ordered initiative strip rotated to `NOW` and `NEXT`; presentation-supplied combat icons identify actors, with short names retained as the missing-icon fallback. Below that record, one centered Classic console groups natural-width controls as View, Action, and Tactics rather than stretching every label or inset across equal screen fractions. Each content-sized group retains two centered fixed rows beneath a larger centered and divided heading across actor and availability changes; semantic Classic color distinguishes viewing, action, and tactical commands without changing legality or order. Canonical geometry remains 1×, while surplus combat-region width and height proportionally enlarge those three group panels, their controls, spacing, and text together up to 2×; exact combat icons remain independently native-sampled. Battle-specific theme margins keep the console inside the canonical 1280x720 combat region; unavailable commands stay in place with their supplied reason instead of making neighboring controls reflow. One target/spell/item workflow may replace that overview at a time and must provide a visible natural-width Back to battle action in both supported profiles. While the full-height spellbook rail is open, this deck ends at that rail instead of continuing invisibly beneath it, so its status and Back action center within the still-visible battlefield width. When battlefield targeting begins, that workflow hides its setup selectors and shows only Back, an explicit `Targeting` status, Confirm, and Cancel; cancellation restores the selectors. Spatial movement remains with `ClassicBattlefieldPresenter` and stays suppressed until the secondary workflow is closed or committed.
- Memorized combat spell selection replaces the persistent party rail and its inert lower command rectangle with one full-height Classic-shaped spellbook: the shared exact gradient `Spell Level` heading and level-colored `Level #` rail, unique level-filtered known-combat-spell list, legal power choices, supplied cost/target facts, and explicit Aim/Cast plus Back actions. Known spells without a supplied legal option remain inspectable and disable Cast with an explicit current-activation explanation; presentation never fabricates a `CastOption`. The bottom deck retains only workflow/targeting status and confirmation. The spellbook passes the exact selected `CastOption` back to `BattleInteraction`; it never reconstructs legality or submits directly to the session.

# Local Contracts

- Never add gameplay facts, targets, prices, eligibility, or service availability that are absent from the request.
- Disabled controls require a specific player-facing reason.
- Every pointer action must also be keyboard focusable.

# Work Guidance

- Keep request-kind logic in the narrowest component and preserve payload field names exactly.
- Repeated combat spells retain explicit addition order, permit one through `maximumTargets`, and emit that order as `targetIds`. Summons retain clicked order, permit one anchor through `maximumTargets`, and emit that order as `targetCoordinates`. Memorized spells use the request's living combatant roster or battlefield pointer for selection and defer final legality to submit; scrolls retain core-probed candidates. Presentation cannot reorder the selection or commit gameplay state.
- Battlefield target cancellation emits no response. Confirmation preserves the existing combat payload ABI: one stable `targetId`, ordered `targetIds`, one legal `targetCoordinate` plus rotation, or ordered `targetCoordinates`, according to the configured mode.

# Verification

- Run `tools/verify.ps1`; interaction fixtures verify request-ID and payload preservation.

# Child DOX Index
- No child AGENTS.md files are currently required.
