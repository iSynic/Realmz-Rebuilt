# Human-maintainability architecture record

The boundary foundation and major-scene authoring pass are complete, but the human-centered architecture overhaul is not yet fully certified. Realmz Rebuilt has six sound source areas, recognizable production-backed scene previews, and a verified behavioral baseline. No production file, function, or top-level class exceeds its final limit, and the private-call, generic-alias, missing-scene, missing-preview, and obsolete shell-marker debts are all zero. A later audit correctly added physical feature-layout ratchets: missing target directories, undeclared legacy directories, and production files left loose at boundary roots must also reach zero. Gallery-wide visual review and repository-local clean-clone onboarding have passed; Beta 1 remains blocked on that physical migration, the independent maintainer exercise, ordinary-play walkthroughs, and final native-platform evidence.

This record describes the foundation inherited by the active migration. Current ownership is machine-checked in [the system manifest](system-manifest.json), and the newcomer tour remains [The Realmz Rebuilt Builder's Manual](builders-manual.md).

## The six source areas

| Area | Owns | Must not own |
|---|---|---|
| `src/app` | Startup, dependency construction, host lifecycle, and input translation | Realmz rules or screen layout |
| `src/game` | Pure definitions, mutable state, fixed rules, topology, clock, RNG, and detached views | Nodes, files, audio, or platform services |
| `src/playthrough` | `GameSession`, transactions, continuations, restore coordination, and player workflows | Presentation or filesystem access |
| `src/scenarios` | Classic instruction execution, Safe Scenario Actions, and the scenario VM | Host UI or package I/O |
| `src/storage` | Package loading, saves, Character Files, settings, validation, and external adapters | Gameplay decisions or screen behavior |
| `src/ui` | Scenes, screens, dialogs, components, rendering, animation, audio, and themes | Mutable game truth or direct storage access |

Dependencies point toward the model and command boundary:

```text
game <- scenarios <- playthrough <- app
game/playthrough/scenarios <- storage <- app
game/playthrough <- ui <- app
```

The architecture verifier enforces these directions and the typed boundaries between them. A later move includes its scripts, paired `.uid` files, resource paths, tests, verification rules, and public ownership documentation in one coherent change.

## Foundation already in place

- Internal source moved from framework-shaped `core`, `session`, `scenario`, `infrastructure`, and `presentation` folders into the six product-facing areas above.
- Major routes gained named `*_screen.tscn` scenes, but several expose only a frame or mounting regions. They are migration anchors, not evidence that the complete runtime composition is editor-authored.
- Reusable inventory regions, party-setup rows, Fast Spell controls, exchange controls, Debug Tools, the action console, music playlist, shared content and spell visuals, scrolling text, player maps, Pick Lock, Age Update, lifecycle and text choices, Bank, Temple, and Thief workspaces now have editable scenes. Request-sized item, spell, character, statistic, transfer, service, and tumbler records use reusable row scenes.
- Exploration and Combat are typed shell modes and no longer have placeholder scenes. Their persistent visible structure remains authored once in `game_shell.tscn`.
- Realmz Builder recognizes each existing major scene and links its guide, controller, detached view, and tests. Registration remains distinct from production-bound preview completion, so the 24-surface preview debt is still reported honestly.
- Campaign selection and the retained party-assembly/character-creation workspace now have editor-authored scenes under `src/ui/setup`; their controllers bind stable nodes and create only variable campaign and Character File records. This lowers missing major scenes from eight to five and runtime-created Controls from 664 to 629.
- Level Up now has one recognizable two-mode scene: committed gains and spell learning share the scene-authored header, with their complete stable pane structures visible in Godot. Only the level-filtered spell buttons remain dynamic, lowering missing major scenes to four and runtime-created Controls to 611.
- Complex Encounter now exposes its six-command dock in `encounter_interaction.tscn` and keeps its editable Action, Items, Spells, and Speak structures in neighboring workspace scenes. Its script selects and binds those scenes and creates only authored action records, lowering missing major scenes to three and runtime-created Controls to 587.
- Shop now exposes its wide two-ledger and compact tabbed browsers, five category controls, transaction strip, shopper selectors, route controls, and detail wells in `shop_interaction.tscn`. Item and portrait records are reusable scenes, lowering missing major scenes to two and runtime-created Controls to 549; the controller also falls below both the file-size and class-method limits.
- Treasure now exposes its ordinary distribution workspace, legacy exact-item recovery adapter, completion confirmation, wealth transfers, loot cells, recipient rows, and lore controls through editable scenes. Its controller binds detached request values and creates only scene instances, lowering missing major scenes to one and runtime-created Controls to 483; it also lowers the outstanding file, function, and class limits to 26, 99, and 36 respectively.
- Combat now exposes the complete turn summary, initiative host, View/Action/Tactics deck, fixed command slots, attack/spell/scroll/item/bandage modes, inspection record, and reusable targeting controls in `battle_interaction.tscn` and `battle_targeting_controls.tscn`. The controller binds supplied legality and detached records without constructing Controls, lowering missing major scenes to zero and runtime-created Controls to 442.
- Allies and Bestiary now share the fully authored `creature_library_workspace.tscn`: list and detail panes, identity header, facts, empty state, and three state cards are visible in Godot, while `creature_library_row.tscn` supplies the only variable record. Their controller only binds detached creature data and exact media, lowering runtime-created Controls to 417.
- The Character route exposes its party-order summary, reorder editor, persistent shared sheet, empty state, identity, complete tab rail, and content frame through authored scenes. Character Files likewise exposes its complete library, empty state, history, eligibility, and inspection composition; exported `party_order_row.tscn`, `vault_character_card.tscn`, and `vault_history_row.tscn` provide the variable records.
- Overview, Conditions & Saves, Abilities, and Lifetime Record now expose their complete stable sheet regions through `character_sheet_stat_tabs.tscn`. Metric rows and lifetime cards instantiate exported component scenes, lowering runtime-created Controls to 361; Equipment, Spells, Appearance, and Race/Caste/Aging remain the explicitly incomplete sheet tabs.
- Equipment and Spells expose their fixed headings, split records, list hosts, empty states, and scroll-case regions through `character_sheet_inventory_magic_tabs.tscn`. Exact item and spell records use exported card scenes.
- Appearance and Race, Class & Aging now expose their complete preview, picker, action, definition, and age-band structures through `character_sheet_identity_tabs.tscn`. All eight character-sheet tabs are therefore recognizable and editable in Godot; their controller binds detached values and instantiates only the exported metric, record, item, and spell components. This completes the character-sheet scene migration and lowers runtime-created Controls to 324.
- Inventory now shares one editor-authored `inventory_workspace.tscn` between its route and Encounter selection. Its normal split, empty state, character record and selector, seven-action dock, confirmation stage, item inspector, two-ledger Trade workspace, portrait matrix, and fixed Money/Items/Done spine are visible in Godot; only exported party, item, fact, and portrait record scenes vary at runtime. The controller constructs no Controls, lowering runtime-created Controls to 274 and the outstanding file/function limits to 25 and 95.
- Spells now shares one complete editor-authored `spells_workspace.tscn` between the route and Encounter selection. Its caster and section selectors, blocked and empty states, Classic level/power rails, known-spell browser, selected record, Fast Spell grid, Scroll Case, and action dock are visible in Godot; only exported spell, Fast Spell, and scroll record scenes vary at runtime. The controller constructs no Controls, lowering runtime-created Controls to 229 and the outstanding file limit to 24.
- Maps / Notes now embeds the complete `maps_notes_workspace.tscn`: saved-place and current-note panes, historical-map viewport, acquired-map selector, cartographic stage and zoom tools, scrolling-text host, and two-page journal are all visible in Godot. Location, map, and journal records instantiate exported row scenes, while map canvases remain algorithmic presenters. The controller constructs no Controls, lowering runtime-created Controls to 193, oversized functions to 93, and generic preload aliases to 130.
- System now embeds the complete `system_workspace.tscn`: Save/Load columns, selected-record and empty states, fixed host actions, every preferences tab, picker, slider, toggle, help card, and Wide/Compact reflow are visible in Godot. Only detached save records instantiate the exported `save_slot_row.tscn`; the controller constructs no Controls, lowering runtime-created Controls to 161.
- Party Wealth now embeds the complete `services_workspace.tscn`: pooled and banked wealth, adventurer ledger, selected-character exchange, all three denomination transfers, Done, location services, alternate states, and Wide/Compact reflow are visible in Godot. Detached characters, transfers, and location services instantiate exported row scenes; the controller constructs no Controls, lowering runtime-created Controls to 138.
- The persistent Party roster now authors its member record, empty position, countdown cursor, and complete combat spellbook as scenes. `ClassicPartyRoster` retains incremental ordinary-movement updates and portrait compositing, while `CombatRosterSpellbook` owns level/list/detail/power binding; neither constructs Controls. This lowers runtime-created Controls to 111 and removes the former 800-line roster from the oversized file and class-method budgets.
- Startup now authors the complete deferred `front_door_menu.tscn`, and campaign selection authors its selected record, alternate library states, package-operation status, install dialog, and refresh actions. `CampaignLibraryController` binds both scenes and instantiates only the campaign selector's exported package-row scene; it constructs no Controls, lowering runtime-created Controls to 91.
- Character creation now exposes five recognizable step scenes from the retained party-setup workspace. Identity, Race/Caste, Appearance, Review, and Starting Spells expose every stable preview, selector, detail, alternate state, level rail, and allowance in Godot; the production controller constructs no Controls and instantiates only exported variable rows or the shared sheet.
- Party assembly exposes its Character Files heading, row host, empty state, and pager through `party_assembly_browser.tscn`; party inspection exposes its complete overlay through `party_setup_inspection_overlay.tscn`. Both controllers construct no Controls and instantiate only exported records or the shared sheet.
- Character targeting and surviving-allies selection now share the complete editor-authored `selection_interaction.tscn`; the latter instantiates only its exported candidate-row scene. The selection binder constructs no Controls, lowering the remaining classified runtime-created Controls to 34. The current structural debt is 22 oversized files, 88 oversized functions, 35 oversized classes, 192 cross-object private calls, and 128 generic preload aliases elsewhere in the repository.
- Interaction presentation now exports scene-authored modal shields, flash messages, nested Treasure confirmation, Encounter dock, side and application workspaces, and hint components from `interaction_presenter.tscn`. The presenter owns only typed request routing, placement, retention, and teardown, lowering the remaining classified runtime-created Controls to 19.
- Shared item-detail records, Party-effect slots, combat initiative entries, interaction hints and character pickers, and Race/Caste option hosts are now exported or directly scene-authored components. Their binders construct no Controls, lowering the remaining classified runtime-created Controls to 10 and generic preload aliases to 127.
- Three warmed startup samples for that shared-component batch kept first-frame median at 144.587 ms versus 149.084 ms and application-ready median at 6,713.574 ms versus 6,512.257 ms. The latter 3.1-percent change remains below the joint five-percent/100 ms rejection rule; all first-frame, retained-intro, and queued-transition contracts passed.
- Navigation hosts, generic summary cards/messages, and the field Spells footer are now scene-authored. `ScreenNavigator`, `ScreenContentPresenter`, and `ScreenFrame` bind those components without constructing Controls, lowering runtime-created Controls from 10 to 2. The two remaining calls are named algorithmic render-target allocations in the retained map and first-person dungeon presenters; no route, screen, interaction, or controller script creates stable layout.
- Realmz Builder now previews all 24 registered major surfaces—including the application shell, campaign selection, party assembly, and character creation—by placing an ownerless clone over the edited scene and feeding all six deterministic profiles through each production binder. The actual scene is never mutated or made dirty, variable records come from the production row scenes, and the owning test exercises the same fixture catalog. No registered major surface lacks a production-bound preview.
- Three warmed startup samples for the navigation batch improved first-frame median to 147.017 ms from 149.678 ms, application-ready median to 6,411.604 ms from 7,101.360 ms, and background-load median to 6,272.779 ms from 6,951.907 ms. All retained-intro and queued-transition contracts passed.
- `ScreenNavigator` owns navigation and mounting through its production scene. Named screen controllers bind detached views and populate genuinely variable collections from exported row/card scenes.
- The application composition root and its package, save, Character Files, and standalone-creator controllers now construct dependencies through their public class names. Removing all 22 `*Type`/`*Script` preload aliases from this boundary lowers generic alias debt from 127 to 105 without changing dependency construction or startup timing.
- Every remaining production preload alias pointed to an already registered public class. Replacing those 105 indirect names with the actual class names removes the generic-alias debt entirely across game rules, playthrough workflows, scenario runtime, storage, and presentation; dependency construction and serialized identities remain unchanged.
- Party setup now has explicit collaborator contracts: assembly, inspection, creation, and shared-state operations are public where another controller genuinely depends on them, while dead proxy helpers were removed. This eliminates all 24 cross-object private calls from that feature and lowers the repository debt from 192 to 168.
- Application input now crosses three named host operations for intent submission, field fast spells, and the full-party Auto abort; the router no longer invokes composition-root private methods. This removes seven more cross-object calls and lowers that debt to 161.
- Package assembly now uses explicit decoder and validator APIs for story records, rule catalogs, world records, scenario construction, media resolution, and cross-reference checks. Internal parsing helpers remain private, while the 34 operations used between package collaborators are named contracts; cross-object private-call debt is now 127.
- Ten remaining shared codec, projection, exploration-event, scenario-handoff, and shell/row helpers now have narrow named contracts instead of being called as another object's private implementation. Only the session and combat seams retain cross-object private calls, lowering that debt to 117.
- `GameSession` now owns and addresses one durable `SessionContext` directly, containing content, mutable state, deterministic RNG, rules, VM/action state, runtime API, typed continuations, pending interaction, and revision. Exploration, scenario, response, and debug coordinators receive that same context by reference through named operations; the former copy-in/copy-out coordinator context and all 35 session-to-private calls are gone. Only the combat seam remains, at 82 calls.
- Combat now shares one explicit `CombatContext` instead of retaining its root facade in every collaborator. `CombatFlow` exposes only mutation commands; callers use the named round, action, reaction, magic, field, summoning, Phase, or automation owner for probes, and physical-action event policy has its own `CombatActionEvents` collaborator. This removes the remaining 82 production cross-object private calls and lowers the outstanding file and class limits to 21 and 34.
- Combat AI now enters through a 15-line `CombatAiScoring` facade. Party Auto policy, monster policy, shared deterministic scoring, and read-only target facts have separate named owners without changing their public calls or RNG order. This lowers the outstanding file and class limits to 20 and 33.
- Physical combat now enters through a small `CombatRules` facade. Character attacks, monster attacks and specials, shared weapon/save calculations, and initiative have separate named rules owners without changing the existing command API or RNG order. This lowers the outstanding file and class limits to 19 and 32.
- Magic now enters through a small `MagicRules` facade. Character casts, monster casts, projectiles, field/scenario effects, and shared resolution mechanics have separate named owners without changing the public calls, effect ordering, or RNG tags. This lowers the outstanding file and class limits to 18 and 31.
- Combat automation now separates party Auto activation and pursuit, monster phases and actions, and battlefield occupancy cleanup behind its stable entry point. The split preserves command order and RNG use while lowering the outstanding file limit to 17.
- Scenario reward handling now separates battle settlement from Treasure distribution and progression behind the opcode-facing reward entry point. Shared transaction rollback is explicit, and the evidence inventory points to each real owner. This lowers the outstanding file and class limits to 16 and 30.
- Package content decoding now routes campaign story records, character/rule catalogs, and encounter catalogs to separate strict decoders that share one diagnostic boundary. Character, encounter, map, and player-map collection operations preserve source order while delegating each record to a focused validator/builder; player-map marker validation is likewise isolated. Safe Scenario Action bytecode construction dispatches named instruction and expression families while retaining the same bounded recursion, node count, capability checks, errors, and wire format. Scenario-reference validation now names its Simple Encounter, Complex/Thief Encounter, and program-instruction stages. Package installation names existing-install resolution separately from temporary-copy, readback, atomic-rename, and receipt commit. The public APIs and failure semantics are unchanged.
- Restore validation keeps `SessionRestoreValidator` as the transactional entry point and session-continuation owner, while `SessionRestoreStateValidator` validates detached game truth and `SessionScenarioRestoreValidator` validates pending VM workflows. Header, core, and continuation stages preserve the former failure order and never mutate the live aggregate. The former 800-line validator is now a 484-line coordinator, lowering the outstanding limits to 14 files and 76 functions; 30 top-level scripts still exceed the final method-count limits.
- Monster automation now names turn-action dispatch, fallback casting, contact reactions, target refresh, route probing, spell targeting, spell execution, and character-versus-monster attack resolution as separate private operations. Its transient spell target plan replaces parallel local arrays without entering state or wire formats. Combat characterization remains 279 assertions; three warmed AOGM Battle 46 samples measured median Auto activation at 26.448 ms, complete monster phase at 7.082 ms, and warm monster phase at 6.974 ms, all below the branch baseline. This lowers oversized-function debt to 72 while file and class debt remain 14 and 30.
- Acknowledge, age update, yes/no, indexed choice, character selection, general selection, Complex Encounter, Thief Encounter, and Pick Lock requests now use feature-named top-level payload files under `src/game/session/requests`. `InteractionRequest` remains the stable kind registry and wire envelope rather than an oversized nested protocol catalog; serialized kinds, fields, response matching, and save forms are unchanged. Focused session, presentation, scenario, persistence, and field-spell characterization passed 1,228 assertions, and oversized-file debt falls to 13.
- Shop, Temple, Bank, Treasure, Level Up, combat, and application-lifecycle payloads now join the other top-level request bodies. The registry contains no feature payload implementation or compatibility alias; each caller names the actual value it consumes, while the strict central codec preserves the exact wire representation. Focused transaction, UI, scenario, and persistence characterization passed 1,155 assertions.
- Classic banking now has a named request-and-transfer owner, while Shop and Temple remain in the opcode-facing service handler. A shared money-transfer collaborator owns package-backed movement validation and recalculation for both Bank and Temple; no wealth behavior or event order changed. Scenario and money characterization passed 427 assertions, lowering architecture debt to 12 oversized files and 71 oversized functions.
- The battle command deck now delegates only responsive sizing to `BattleCommandScaleController`; its scene remains the complete stable hierarchy and `BattleInteraction` remains the request/response binder. Wide, Compact, and all Realmz Builder preview profiles passed 728 presentation assertions. The binder now satisfies both the 600-line and class-method limits, lowering debt to 11 oversized files and 29 oversized classes.
- The 2D map renderer now delegates decoded atlas, overlay, darkness-mask, and land-marker retention to `MapTextureCache`. `ClassicMapPresenter` remains the drawing and pointer-input owner, and `ClassicRetainedMapSurface` uses the same transparent-tile operation without a presenter dependency. Focused map geometry and presentation characterization passed 486 assertions, lowering oversized-file debt to 10.
- Map viewport, discovery, LOS-mask, debug-outline, pointer-region, and facing calculations now live in the stateless `MapPresentationGeometry`; exact map asset identities and tile composition live beside their decoded cache. Renderers and the application host call those named public collaborators instead of treating `ClassicMapPresenter` as a utility namespace. Focused map characterization passed 486 assertions, and the presenter falls from 58 methods to 33, lowering oversized-class debt to 28.
- Battlefield combatant lookup, camera tracking, footprint interpolation, click sectors, and coordinate calculations now live in `BattlefieldPresentationGeometry`; `BattlefieldTextureCache` retains the base and upper atlases, combatant textures, and keyed effect matte results. `ClassicBattlefieldPresenter` remains the tactical drawing and input owner rather than a shared utility namespace or decoder cache. Focused combat and presentation characterization passed 694 assertions, lowering oversized-file debt to 9.
- Tactical battlefield input is now a named controller boundary. `BattlefieldInteractionController` owns hover, inspection focus, Reveal Friends, movement-cost aids, targeting, and typed movement/target response emission, while `ClassicBattlefieldPresenter` retains rendering, playback, and its incremental rendered camera. Focused shell, battlefield, and Realmz Builder characterization passed 816 assertions with the principal UI suite holding its prior 7,283 ms runtime. This removes the final oversized top-level class without adding per-frame construction or changing combat truth.
- Detached view construction now reads as complete party, combat, campaign, setup, collection, and action phases instead of one monolithic assembly. `SessionOrdinaryProjectionPolicy` owns the exact conservative event-sequence admission for incremental movement and turn reuse, while `SessionActionViewProjector` names exploration, magic, setup, inventory/service, combat-movement, learned-spell, scroll, and Fast Spell availability phases. Seven affected core, exploration, Inventory, field-magic, scroll/camp, creation, and presentation suites passed 1,280 assertions, lowering oversized-function debt from 48 to 43. Three immediate-parent and three changed AOGM movement samples changed median transaction-plus-projection p95 from 1.294 ms to 1.282 ms, overall projection p95 from 0.808 ms to 0.834 ms, ordinary projection p95 from 0.470 ms to 0.519 ms, and hourly projection p95 from 1.066 ms to 0.945 ms. The only percentage increase was 0.049 ms, below the 0.20 ms absolute rejection floor; all six samples retained 60 incremental projections and the same event-sequence counts.
- Scenario execution now separates program-identity resolution from instruction dispatch, Complex Action response validation from exact authored-group evaluation, and opcode 86 descriptor validation from its pure party matcher. The VM remains the sole owner of frame mutation and tracing, while the runtime and control-flow operation retain their existing public boundaries. Four scenario, reward, exploration, and persistence suites passed 1,450 assertions, lowering oversized-function debt from 43 to 40. This extraction changes no loop frequency, resource load, rendering, or per-frame work and therefore requires no performance probe.
- Character creation now reads in Castle's exact RNG order as attribute rolling, initial saves/specials, age-profile application, rare special bonuses, state population, spellcaster setup, abilities, and requested starting-level advancement. The strength calculation is an explicit Classic brawn chart, and `CharacterView.copy_from` groups its detached transfer into identity, statistics, and immutable component phases. Five character, session, persistence, and presentation suites passed 1,229 assertions, lowering oversized-function debt from 40 to 37. Three warmed AOGM samples changed median transaction-plus-projection p95 from 1.239 ms to 1.231 ms, ordinary projection from 0.459 ms to 0.471 ms, and hourly projection from 0.817 ms to 0.835 ms; all changes are below the 0.20 ms rejection floor and every run retained sixty incremental projections.
- Party setup now reads as three visible responsibilities instead of three oversized binding/import routines. Assembly configuration, visible revision media, and browser replacement are named phases around the retained six party slots; Starting Spells binds its authored level rail, variable spell rows, and selected detail through `CharacterCreationSpellsStep`; Character Files import admits the detached copy through definition/load, magic, appearance, and final party-identity phases in the existing order. Four creation, persistence, UI, and Realmz Builder suites passed 1,149 assertions, lowering oversized-function debt from 37 to 34. These operations run only during setup navigation or a committed import and add no movement, package-loading, rendered-frame, or per-frame work, so they do not require a performance probe.
- Character combat magic now exposes the same source and target families that its rules already execute. Charged-item and scroll admission dispatches repeated, area, group, and actor validation through named helpers; their resolution dispatches summon, Phase, area, party, group, repeated, ray, and targeted transactions without changing source-specific charge, scroll, spell-point, rollback, field, or event ownership. Learned repeated casting has its own readable phase beside the existing shared resolver. Four combat, scroll/camp, scenario, and persistence suites passed 1,321 assertions, lowering oversized-function debt from 34 to 29. Three immediate-parent and three changed AOGM Battle 46 samples kept median Auto activation at 24.604 ms versus 24.610 ms, battle setup at 75.413 ms versus 75.477 ms, spell-option construction at 2.133 ms versus 2.145 ms, combat-view construction at 1.220 ms versus 1.232 ms, complete monster phase at 6.404 ms versus 6.435 ms, warm monster phase at 6.204 ms versus 6.166 ms, and checkpoint serialization at 0.270 ms versus 0.276 ms. Every change is below the absolute rejection floor; events, 2,718 RNG draws, and the 84-option probe remained identical.
- Shop presentation data now reads as stock projection plus character inventory projection, and resumed transactions dispatch directly to Buy, Sell, Identify, or Leave. Session response routing names its random-surprise fallback and separates pooled-wealth Warning from Distribution, Pool, Share, Swap, and leave-behind phases. The stable requests, continuation records, mutations, event order, and movement resumption are unchanged. Four scenario, money, persistence, and session suites passed 1,046 assertions, lowering oversized-function debt from 29 to 25. These operations run only when opening or responding to an interaction and add no movement-loop, rendering, package-loading, startup, or per-frame work, so they do not require a performance probe.
- Exploration continuation now names contextual program selection/start, post-move preparation/completed-trigger/next-trigger, and random-region/door/surprise phases. Detached map construction separately names LOS visibility, viewport bounds, cached or patched window projection, complete-window construction, and movement options while retaining the established cache keys and incremental window. Five topology, exploration, persistence, scenario, and UI suites passed 1,767 assertions, lowering oversized-function debt from 25 to 21. Three immediate-parent and three changed AOGM samples moved median transaction-plus-projection p95 from 1.231 ms to 1.292 ms, projection p95 from 0.785 ms to 0.837 ms, ordinary projection p95 from 0.446 ms to 0.469 ms, and hourly transaction p95 from 0.479 ms to 0.571 ms. Every delta remains below the 0.20 ms absolute rejection floor; all samples retained sixty incremental projections, the same final coordinate, event sequences, and fatigue payload.
- Reward opening now separates experience scaling, scenario-overlay item admission, item-instance creation, and recipient awards. Terminal battle settlement first builds a validated reward plan, then performs the unchanged source-ordered money, loot, and incidental-item draws; Treasure responses dispatch completion, item custody, wealth, lore, and progression through named phases. Reward, combat, scenario, persistence, and money characterization passed 1,413 assertions, lowering oversized-function debt from 21 to 18 without changing transaction rollback, continuation records, RNG order, event order, or reward ownership. Reward operations run only at player interactions or terminal battle settlement and add no movement-loop, rendering, package-loading, startup, or per-frame work, so this extraction does not require a performance probe.
- `GameSession` is now a 200-line transaction boundary rather than a feature dispatcher. `SessionContext` owns initialization, accepted restore assignment, teardown, and snapshot construction; `SessionIntentCoordinator` routes validated commands to the existing feature workflows and returns typed rejected or committed results. Dead session proxy methods were removed, failure revision semantics remain explicit, and focused session, combat, exploration, persistence, inventory, magic, camp, and money characterization passed. This lowers oversized-file debt to 6 and oversized-function debt to 68.
- Classic VM program transfer is now a named control-flow collaborator. `ScenarioClassicControlFlow` applies XAP/program branches, GOSUB depth, encounter entry, result/repeat loops, and encounter exit to the existing typed frame array, returning `ScenarioDirectiveTransition` for the VM-owned halt/return/trace boundary. Focused VM, exploration, and persistence characterization passed without changing snapshots or trace order, lowering debt to 5 oversized files and 67 oversized functions.
- `GameState` now exposes scenario truth through two recognizable state owners instead of implementing more than twenty unrelated progress operations itself. `ScenarioProgressState` owns searches, quests, journal discovery, and character selection; `ScenarioEncounterState` owns timed records, elimination, attempts, thief flags, and program redirects. The collaborators preserve the exact existing top-level save fields and lower architecture debt to 4 oversized files and 66 oversized functions while keeping class debt at 27.
- Interaction request binding is now separate from conditional overlay hosting. `InteractionPresenter` preserves request identity, binds one typed component, and translates its response; `InteractionOverlayHost` owns the scene-authored shield, nested confirmation, Encounter dock, and side/application workspace lifecycles; `InteractionFlashController` owns queued Classic flash messages. Focused shell, workspace, and Realmz Builder characterization passed 816 assertions, lowering debt to 3 oversized files and 65 oversized functions while keeping class debt at 27.
- Character combat magic now separates source-specific learned-spell, charged-item, and scroll transactions from their shared target collection and ordered commit. `CombatCharacterSpellResolution` owns ray/group/area collection, result-event construction, defeated-actor cleanup, death-macro handoff, and turn advancement without changing payment, rollback, RNG, or event order. Focused combat, session, and persistence characterization passed 715 assertions, lowering debt to 2 oversized files and 64 oversized functions while keeping class debt at 27. Three warmed AOGM Battle 46 samples measured median Auto activation at 26.869 ms, complete monster phase at 7.117 ms, and warm monster phase at 7.022 ms; each remains below the combined 5-percent and absolute regression thresholds against the branch baseline.
- The scene-backed application shell now delegates its distinct presentation responsibilities instead of combining them in one binding script. `GameShellLayoutController` applies responsive sizing from `GameShellLayoutPolicy`, `GameShellMenuController` owns the actual menu definitions and dispatch, `GameShellAvailability` owns disabled-action explanations, and `GameShellPicturePresenter` owns scenario picture decoding and display. The authored `game_shell.tscn` hierarchy and retained command/roster behavior are unchanged. Focused shell, workspace, and Realmz Builder characterization passed 816 assertions, lowering debt to 1 oversized file and 62 oversized functions while keeping class debt at 27. Three paired warmed AOGM 1280x720 samples reduced median shell presentation p95 from 0.665 ms to 0.581 ms and rendered frame p95 from 6.537 ms to 6.152 ms; frame p99 changed from 8.084 ms to 8.434 ms, remaining below the combined five-percent and 0.50 ms rejection threshold. Transaction-plus-projection and package-preparation medians remained within their acceptance thresholds.
- `RealmzApplication` is now a 566-line composition root rather than the owner of every host workflow. `ApplicationLifecycleHost` owns process quit, End Adventure, Save and Quit, and deferred battle-close state; `ApplicationCharacterFilesHost` owns the application library, vault, and standalone creator; and `ApplicationSpatialLayout` aligns the authored shell with the map, battlefield, dungeon, and interaction presenters. Input routing and characterization call these public owners directly instead of retaining composition-root policy proxies. Focused application, persistence, and creator characterization passed 836 assertions, removing the final oversized production file while preserving the 62-function and 27-class migration ceilings for later batches. Three paired warmed startup samples improved first-frame median from 42.126 ms to 40.133 ms, application-ready median from 8,129.463 ms to 7,853.000 ms, and background-load median from 7,940.096 ms to 7,677.585 ms; the retained intro and queued scenario transition passed in every run.
- Application construction now reads as five explicit startup phases instead of one 91-line callback, and screen-content signal binding is grouped by its source controller instead of one 97-line relay block. `WorkspaceFocusController` owns focus identity and scroll restoration independently from route history and mounting. The authored scenes, retained workspace lifecycle, and route behavior are unchanged; 816 focused shell, navigation, and Realmz Builder assertions passed, lowering oversized-function debt from 62 to 60 while class debt remains 27. Three paired warmed startup samples improved first-frame median from 180.684 ms to 166.657 ms, application-ready median from 8,348.664 ms to 7,891.862 ms, and background-load median from 8,176.640 ms to 7,725.459 ms; the retained intro and queued scenario transition passed in every run.
- Adventure persistence now enters through `ApplicationAdventureStorageHost`: save, backup restore, session restore, preview refresh, and the transient post-load Auto cleanup no longer add seven forwarding operations to the composition root. `RealmzApplication` now meets the 40-method class limit, and input/tests address the actual lifecycle and Character Files owners instead of policy proxies. Save repository, host composition, and session round-trip characterization passed 848 assertions, lowering oversized-class debt from 27 to 26 without changing save envelopes, slots, backups, or restore semantics.
- `ScreenNavigator` now exposes only route/history/mounting behavior instead of forwarding setup, save-preview, and character-selection calls. `GameShell`, tests, and gallery capture address `CampaignPartySetupController` or `ScreenContentPresenter` as the actual state owner and request one explicit workspace refresh only when a bound visible route needs it. Focused shell, navigation, and Builder preview characterization passed 816 assertions; the navigator falls to 37 methods and 20 public operations, lowering oversized-class debt from 26 to 25 without changing retained scene instances, route history, focus, or scroll behavior.
- `GameShell` is now a 30-method scene composition root instead of a second application facade. `GameShellStatusController` owns narrative, world facts, transient activity feedback, and field-time playback; `GameShellPartyEffectsPresenter` owns effect animation and media caching; and `GameShellRoutePolicy` owns automatic workflow routing. Application hosts address status, commands, roster, setup, content, and navigation through their named public owners, while tests name the same utility and policy classes. Focused shell, UI-system, and Realmz Builder characterization passed 816 assertions, lowering oversized-class debt from 25 to 24 with every zero-debt architecture check unchanged. Three paired warmed AOGM 1280x720 samples improved median movement-frame p95 from 6.967 ms to 6.940 ms, frame p99 from 8.396 ms to 8.076 ms, shell presentation p95 from 0.748 ms to 0.666 ms, and package preparation from 3,312.543 ms to 3,072.484 ms; transaction-plus-projection remained effectively flat at 2.201 ms versus 2.193 ms.
- The former 79-method Classic spell capability catalog now has six explicit rule owners: packed application identity, mechanical classification/signatures, source-context disposition, source-only structures, healing/condition/field structures, and special-effect structures. Production, restore, projection, tests, and package probes address the actual owner directly; no compatibility facade or duplicate capability list remains. Combat, field, scroll/camp, and scenario characterization passed 818 assertions, the complete 420-spell parity inventory remained byte-for-byte stable, and oversized-class debt falls from 24 to 23. Nine paired warmed AOGM Battle 46 samples were used after the initial three exposed timing variance: median Auto activation changed from 31.353 ms to 31.289 ms, complete monster phase from 9.611 ms to 8.616 ms, warm monster phase from 8.750 ms to 9.004 ms, spell-option construction from 3.422 ms to 3.595 ms, combat-view construction from 1.510 ms to 1.343 ms, and battle setup from 91.248 ms to 90.859 ms. Every metric remains within the combined relative and absolute rejection thresholds.
- Scenario battle lifecycle now owns only battle start, response dispatch, retreat, macro, age, and terminal transitions. `CombatInteractionRequestBuilder` separately projects the authoritative battle into legal actions, movement, targets, combatant inspection, spell/item/scroll choices, and the transient detached `CombatView`. Battle response dispatch now names Auto, explicit retreat, edge retreat, direct command execution, and post-command continuation instead of combining them in one 90-line branch. Focused combat, scenario, session, reward, and persistence characterization passed 1,150 assertions, lowering architecture debt to 8 oversized files and 69 oversized functions.
- The misleading `InventoryMagicServicesWorkflow` is gone. `InventoryWorkflow` owns carried-item transactions, `FieldItemWorkflow` owns Identify/Torch/door/charged-item operations, and `FieldMagicWorkflow` owns Fast Spells, scrolls, and learned field spells. `FieldMagicTargetRequestBuilder`, `FieldMagicResolver`, and the top-level `MagicTransitionResult` provide their shared typed seams without compatibility aliases. Focused inventory, field-spell, scroll, session, and persistence characterization passed 805 assertions, lowering architecture debt to 7 oversized files and 27 oversized classes.
- `CombatState` is now a small saved aggregate instead of a 68-method battle utility. `CombatRosterState`, `CombatTurnSequenceState`, `CombatActorStatusState`, `CombatDroppedItemState`, and `CombatSpellRuntimeState` own the actual mutable facts, while `CombatStateCodec` preserves the exact flat save keys and validation rules. The aggregate coordinates only turn-order/status registration and turn-boundary invariants; production, scenario, presentation, tests, and probes address factual owners directly without a compatibility facade. Focused combat, session, persistence, scenario, and reward characterization passed 1,150 assertions with clean teardown, lowering oversized-class debt from 23 to 22 while oversized-function debt remains 60. Five final alternating AOGM Battle 46 pairs measured median Auto activation at 29.645 ms versus 29.140 ms, complete monster phase at 8.624 ms versus 8.684 ms, warm monster phase at 8.156 ms versus 8.377 ms, spell-option construction at 2.311 ms versus 3.160 ms, combat-view construction at 1.581 ms versus 1.407 ms, battle setup at 85.712 ms versus 86.467 ms, and checkpoint serialization at 0.457 ms versus 0.368 ms. View and checkpoint deltas remain below the absolute rejection floor; every sample preserved 21 monster-phase events and 2,718 RNG draws.
- `PlayerIntent` is now the stable 36-kind command envelope instead of a 42-method constructor and nested-payload catalog. `ExplorationIntents`, `InventoryIntents`, `MagicIntents`, `CombatIntents`, `PartyIntents`, and `EconomyIntents` are the feature entry points; their payload families carry copied detached values, and `PlayerIntentRegistry` alone validates each stable kind/payload pairing. Production, tools, and tests address those owners directly without forwarding aliases. A direct factory/registry characterization plus every affected gameplay, persistence, scenario, and presentation suite passed 2,486 assertions, lowering oversized-class debt from 22 to 21 while oversized-function debt remains 60. Three alternating AOGM movement samples changed median transaction-plus-projection p95 from 1.709 ms to 1.741 ms, a 0.032 ms/1.87 percent change below both rejection thresholds; each retained the same 4,096-cell history.
- `SessionContinuation` is now the stable saved envelope for 21 resumable transaction kinds instead of a 46-method factory, payload catalog, validator, and codec. Exploration, application, inventory, magic, service, and combat factories own live construction; their named payloads retain typed values, and `SessionContinuationCodec` alone admits the unchanged strict wire representation. The complete kind denominator plus affected session, combat, scenario, exploration, persistence, inventory, magic, camp, and money characterization passed 2,024 assertions, lowering oversized-class debt from 21 to 20 while oversized-function debt remains 60. Three alternating AOGM Battle 46 samples changed median checkpoint serialization from 0.275 ms to 0.291 ms, a 0.016 ms change below the absolute rejection floor; battle setup, combat view, Auto activation, and complete and warm monster phases also remained within their combined thresholds, with every sample preserving the same 21 monster-phase events and 2,718 RNG draws.
- `ScenarioRuntimeContinuation` is now the stable 34-kind scenario envelope rather than the owner of every runtime factory, typed payload, validator, and codec branch. Interaction, age, service, combat, and reward factories construct nine named payload families, while `ScenarioRuntimeContinuationCodec` alone validates and restores the unchanged wire representation. The complete kind denominator plus affected session, combat, persistence, inventory, magic, camp, money, reward, and scenario characterization passed 1,820 assertions, lowering oversized-class debt from 20 to 19 while oversized-function debt remains 60. Reversed-order warmed AOGM Battle 46 samples kept median setup at 79.396 ms versus 79.134 ms, Auto at 26.646 ms versus 26.688 ms, complete monster phase at 7.431 ms versus 7.919 ms, warm monster phase at 7.440 ms versus 7.512 ms, and checkpoint serialization at 0.322 ms versus 0.290 ms; all changes remain within the combined thresholds, and every sample preserved 21 monster-phase events and 2,718 RNG draws.
- Nested interaction request records now decode through five feature owners—common, service, combat, reward, and selection—instead of a 39-method public catch-all class. One shared support class contains only strict shape predicates, and callers name the decoder responsible for the record they consume; no forwarding facade remains. A 34-value denominator preserves every alternate service and cast mode plus the historical unknown cast-source admission, while eight affected gameplay, restore, scenario, service, reward, combat, and presentation suites passed 2,004 assertions. Oversized-function debt falls from 60 to 59 and oversized-class debt from 19 to 18. This boundary-only codec batch adds no movement, rendering, package-loading, or per-frame work, so it does not require a performance probe.
- `CasteDefinition` now presents three authored concepts instead of a 24-method flat record and 35-argument constructor: the root owns identity and broad policy, `AttributeDefinition` owns save, attribute, condition, and strength facts, and `ProgressionDefinition` owns stamina, combat, magic, ability, and victory growth. Providence's package schema remains flat and unchanged; its decoder constructs the named records after strict validation. Eight affected character, package, combat, magic, persistence, and scenario suites passed 2,068 assertions, lowering oversized-class debt from 18 to 17 while function debt remains 59. Three paired warmed AOGM package samples kept median load at 2,752 ms versus 2,766 ms, content construction at 1,252 ms versus 1,248 ms, and first view at 370 ms versus 372 ms, all within the combined regression thresholds.
- `ScenarioExecutionContext` now owns only live typed VM provenance, copying, merging, and feature construction. `ScenarioExecutionContextCodec` alone owns the unchanged sparse save dictionary and strict restoration, and frames/directives address that codec directly rather than preserving serialization as a value-object facade. Scenario VM, reward, exploration, and session-persistence characterization passed 1,450 assertions, lowering oversized-class debt from 17 to 16 while function debt remains 59. This serialization-boundary extraction adds no movement, rendering, package-loading, or per-frame work, so it does not require a performance probe.
- `SessionContext` now owns only the live session aggregate, lifecycle, revision, snapshot, continuation, and shared coordinator capabilities. Intent, exploration, scenario, response, and debug coordinators construct their outcomes directly through `SessionCoordinatorResult` instead of asking the context to forward five result factories. Eight affected session, exploration, persistence, inventory, magic, camp, money, and party-order suites passed 1,402 assertions, lowering oversized-class debt from 16 to 15 while function debt remains 59. This ownership-only call-site change adds no movement, rendering, package-loading, or per-frame work, so it does not require a performance probe.
- Effective Classic media now has one explicit `PresentationMediaController`. It composes application, character-library, and scenario sources, installs the resulting catalog on map, battlefield, and shell consumers, and selects route-aware music; `PresentationCoordinator` retains committed-view, interaction, spatial-visibility, and combat-playback coordination without forwarding the media owner's API. Application hosts and the gallery address the media owner directly. Focused shell and application characterization passed 503 assertions, lowering oversized-class debt from 15 to 14 while function debt remains 59. This ownership-only dependency split adds no movement, rendered-frame, or package-loading work, so it does not require a performance probe.
- Carried-item and wearable-equipment rules now have separate named owners. `InventoryRules` retains item use, capacity, transfer, stack, charge, and custody transactions; `EquipmentRules` owns wearable admission, equip/unequip mutation, scroll-case presence, and combat loadout projection. `RealmzRules`, `CombatContext`, workflows, detached views, scenario operations, tests, and probes address the actual owner without a forwarding facade. Six affected rules, inventory, combat, magic, and persistence suites passed 1,405 assertions, lowering oversized-function debt from 59 to 58 and oversized-class debt from 14 to 13. Three reversed-order warmed AOGM Battle 46 samples changed median battle setup from 80.956 ms to 81.291 ms, combat view from 1.224 ms to 1.364 ms, spell-option construction from 2.576 ms to 2.331 ms, Auto activation from 27.190 ms to 27.530 ms, complete monster phase from 8.061 ms to 8.041 ms, warm monster phase from 7.834 ms to 7.782 ms, and checkpoint serialization from 0.326 ms to 0.333 ms. Every change remains below the combined rejection thresholds, and all samples preserved 21 monster-phase events and 2,718 RNG draws.
- `WorldState` is now the small flat-save aggregate for three direct owners instead of a 55-method world utility. `WorldTopologyState` owns physical and appearance overlays, `WorldTriggerState` owns Action Point and random-region changes, and `WorldExplorationState` owns sight/travel memory, acquired maps, and location notes; `ClassicLandTileRules` separately owns signed tile decoding. Production, scenario, projection, tests, and probes address those collaborators directly while the established `worldOverlays` keys and order remain unchanged. Five affected topology, exploration, persistence, scenario, and presentation suites passed 1,767 assertions, lowering oversized-function debt from 58 to 57 and oversized-class debt from 13 to 12. Three warmed AOGM movement samples changed median transaction p95 from 0.945 ms to 0.651 ms, projection p95 from 1.142 ms to 1.004 ms, and combined p95 from 1.965 ms to 1.559 ms; all samples retained 50 measured steps, the same source event sequences, the same final coordinate, and the 4,096-cell history.
- Presentation settings, party, monster, Classic reward, and interaction-response restoration now read as named envelope, version, scalar, nested-record, and population phases rather than five 67–83-line decoder blocks. The same owners retain their exact wire fields, legacy defaults, strict rejection rules, and transactional population; no compatibility codec or mutable dictionary enters runtime behavior. Five affected UI-system, persistence, combat, reward, and scenario suites passed 1,705 assertions, lowering oversized-function debt from 57 to 52 while class debt remains 12. This decode-boundary-only batch adds no movement, rendering, package-loading, or per-frame work, so it does not require a performance probe.
- Top-level interaction requests now decode through five feature owners—dialog, selection, thief, service/combat, and reward—while `InteractionRequest` retains only its stable kind registry, wire envelope, convenience construction, and decoder dispatch. Each feature decoder validates the unchanged exact fields and optional-presence rules before constructing the existing typed body; no compatibility alias or second wire representation exists. Five affected UI-system, persistence, combat, reward, and scenario suites passed 1,705 assertions, lowering oversized-function debt from 52 to 49 while class debt remains 12. This codec-boundary extraction adds no movement, rendering, package-loading, or per-frame work, so it does not require a performance probe.
- The field spellbook now occupies one recognizable `src/ui/magic` feature root containing its route scene, reusable ordinary/Encounter workspace, repeated record scenes, binding controller, shared level/list chrome, display-only formatter, maintainer guide, and local ownership contract. `SpellsScreenController` delegates pure fact-label formatting to `SpellDetailFormatter` and falls to 34 methods without changing spell legality, targeting, costs, or typed intent payloads. Six affected creation, UI, Builder preview, reward, field-magic, and scroll/camp suites passed 1,108 assertions, lowering oversized-class debt from 12 to 11 while function debt remains 49. The route remains retained and this organizational/display-only slice adds no movement, package-loading, or per-frame work, so it does not require a performance probe.
- Inventory now occupies one recognizable `src/ui/inventory` feature root containing its ordinary route, reusable Encounter workspace, item browser, command rail, inspector, operation stage, two-pack Trade surface, repeated record scenes, binding controller, view queries, display text, maintainer guide, and local ownership contract. `InventorySceneBinding` owns repeated label, media, signal-cleanup, child-cleanup, and scroll-restoration mechanics, bringing `InventoryScreenController` to the 40-method limit without changing detached facts, selection, typed intents, or Trade behavior. Four affected Inventory, shell, UI, and Builder preview suites passed 950 assertions, lowering oversized-class debt from 11 to 10 while function debt remains 49. The retained route and presentation-only extraction add no movement, package-loading, or per-frame work, so this slice does not require a performance probe.
- Characters now occupy one recognizable `src/game/characters` feature root containing mutable truth, lifetime history, pure rules, detached presentation records, and the stable save codec. `CharacterStateCodec` alone owns the unchanged character dictionary and strict legacy defaults; Party, draft, vault, application, workflow, test, and tool callers name that boundary directly instead of asking state to serialize itself. Read-only copied save, special, and ability collections avoid per-index codec dispatch without exposing mutable backing arrays. Ten affected rules, Character Files, persistence, creation, appearance, party-order, Inventory, field-magic, and scroll/camp suites passed 1,281 assertions, lowering oversized-function debt from 49 to 48 and oversized-class debt from 10 to 9. Nine immediate-parent and nine optimized AOGM Battle 46 samples kept median battle setup at 103.773 ms versus 104.497 ms, complete monster phase at 11.046 ms versus 11.242 ms, and checkpoint serialization at 0.354 ms versus 0.381 ms. The directly affected checkpoint changed by 0.027 ms, below the 0.20 ms absolute rejection floor, while every sample preserved the same five Auto events, 21 monster-phase events, and 2,718 RNG draws.
- Economy now occupies one recognizable `src/game/economy` feature root containing money and temple rules, immutable shop definitions, wealth state, detached service records, and mutable `LocationServiceState`. `GameState` remains the save aggregate but no longer forwards fourteen shop, temple, and bank operations; callers address `state.location_services`, while `LocationServiceStateCodec` preserves the exact existing flat availability, stock, inflation, buyback, and native-slot fields. Four affected session, persistence, economy, and scenario suites passed 1,046 assertions, lowering oversized-class debt from 9 to 8 while function debt remains 48. Three warmed AOGM Battle 46 samples changed median checkpoint serialization from 0.399 ms to 0.458 ms, below the 0.20 ms absolute rejection floor; battle setup changed from 115.834 ms to 117.040 ms, while Auto activation and the complete monster phase improved. Every sample retained five Auto events, 21 monster-phase events, and 2,718 RNG draws.
- Immutable campaign records now have feature-owned lookup surfaces instead of a forty-method `RealmzContent` catalog facade. `ScenarioContentCatalog`, `CharacterCatalog`, `ItemCatalog`, `SpellCatalog`, `CombatCatalog`, and `EconomyContentCatalog` own their respective indexes; callers navigate through `content.scenario_records`, `characters`, `items`, `magic`, `combat`, or `economy`, while the root retains only identity, start location, world, compiled program graph, and those collaborators. Package assembly still composes application definitions with legal scenario exact-ID overlays before catalog construction, and the appearance fallback remains explicit. Twelve affected package, session, character, combat, scenario, persistence, inventory, magic, reward, and service suites passed 2,057 assertions, lowering oversized-class debt from 8 to 7 while function debt remains 48. Three warmed AOGM package samples reduced median package load from 3,303 ms to 3,282 ms and content construction from 1,512 ms to 1,485 ms; first-view construction changed from 457 ms to 459 ms, below the 100 ms absolute rejection floor.
- Exploration now enters through five workflow owners instead of one 33-method utility. `ExplorationMovementWorkflow` owns travel, headings, fatigue admission, camp departure, and boats; `ExplorationSearchWorkflow` owns Search mode and secret checks; `ExplorationTimeWorkflow` owns Camp, Rest, and Heal; `ExplorationContinuationWorkflow` owns post-clock/post-move construction and validation; and `LocationNoteWorkflow` owns player-authored map notes. The session coordinators address those owners directly, and restore validators share only the continuation owner's explicit placed-trigger policy. Four affected exploration, persistence, session, and scenario suites passed 1,362 assertions, lowering oversized-class debt from 7 to 6 while function debt remains 48. An alternating immediate-parent comparison under the same warmed machine load reduced median AOGM transaction p95 from 1.458 ms to 1.379 ms, projection p95 from 1.980 ms to 1.916 ms, and combined p95 from 2.883 ms to 2.839 ms; every sample retained the same event sequences, final coordinate, fatigue payload, and 4,096-cell history.
- The shared Character Sheet now separates screen coordination from repeated content binding. `ClassicCharacterSheet` owns character/tab selection, responsive tab choice, and appearance drafts, while `CharacterSheetSceneBinding` binds detached labels, media, metrics, items, spells, and lifetime cards through the exported component scenes. The authored eight-tab hierarchy is unchanged, and focused UI plus Realmz Builder coverage passed 728 assertions, lowering oversized-class debt from 6 to 5 while function debt remains 48. This route-bind-only extraction adds no movement, package-loading, or per-frame work, so it does not require a performance probe.
- Campaign and party setup now expose only genuine composition operations plus named read-only campaign-library and character-creation collaborators. Workspace buttons already connect directly to `PartySetupAssemblyController`, `PartySetupInspectionController`, or `PartySetupCharacterCreationController`; removing nine unused methods and sixteen forwarding methods leaves `CampaignPartySetupController` responsible for setup construction, combined visibility, focus, layout, and host coordination. The authored setup scenes and typed signals are unchanged, lowering oversized-class debt from 5 to 4 while function debt remains 48. This call-routing change adds no runtime work and does not require a performance probe.
- Character creation now separates five-step wizard lifecycle from the Race/Caste scene binder. `PartySetupCharacterCreationController` retains draft navigation, validation, typed submissions, and the Identity, Appearance, Review, and Starting Spells transitions; `PartySetupRaceCasteBinding` owns package-definition filtering, compatible-first Caste ordering, detail labels, and selection updates for the authored Race/Caste step. Focused UI and Realmz Builder characterization passed 728 assertions, lowering oversized-class debt from 4 to 3 while function debt remains 48. The split preserves the same retained workspace and adds no movement, package-loading, startup, or per-frame work, so it does not require a performance probe.
- Combat interaction now has one named command owner instead of widening the general request presenter. `CombatInteractionController` binds the active `BattleInteraction`, targeting and combatant-focus signals, roster spellbook state, direct combat responses, and the scene-exported Fast Spell dock; application input and presentation coordination address that owner directly. `InteractionPresenter` remains the request identity, component mounting, region, and response-translation boundary and now meets both class-method limits. Focused UI and Realmz Builder characterization passed 728 assertions, lowering oversized-class debt from 3 to 2 while function debt remains 48. The extraction preserves the same retained battle component and adds no simulation, package-loading, movement, or per-frame drawing work, so it does not require a performance probe.
- Battlefield state now reads as a small battle-map aggregate with direct terrain and actor owners. `BattlefieldTerrainState` owns cells and the nonserialized navigation revision, `BattlefieldActorState` owns anchors, footprints, occupancy, placement, movement, and swaps, `BattlefieldGrid` owns fixed geometry, and `BattlefieldStateCodec` alone preserves the existing flat save representation. Focused battlefield, combat, scenario, and persistence characterization passed 1,201 assertions, lowering oversized-class debt from 2 to 1 while function debt remains 48. Three warmed AOGM Battle 46 samples changed median setup from 74.331 ms to 76.958 ms, combat-view construction from 1.167 ms to 1.230 ms, checkpoint serialization from 0.295 ms to 0.293 ms, and cold routing from 11.662 ms to 11.647 ms. Every change remains within the combined thresholds, and all samples preserved five Auto events, 21 monster-phase events, and 2,718 RNG draws.
- Combat setup and interruption handling now read as named phases rather than five long nested transactions. `CombatBattleSetup` owns validated inputs, terrain and formation construction, party/ally/authored-monster placement, stable identity commit, initiative, and opening automatic turns; `CombatFlow` retains the stable `start_battle` command. Lifecycle age-update restoration, friendly-contact choices, hostile reactions, and persistent-field collision dispatch each delegate to their actual phase owner without changing wire continuations, event order, rollback, or RNG order. Six affected combat, battlefield, scenario, persistence, reward, and session suites passed 1,348 assertions, lowering oversized-function debt from 18 to 13 while file and class debt remain zero. Three immediate-parent and three changed AOGM Battle 46 samples kept median setup at 75.325 ms versus 75.601 ms, Auto activation at 24.404 ms versus 24.881 ms, combat-view construction at 1.236 ms versus 1.215 ms, spell-option construction at 2.142 ms versus 2.137 ms, complete monster phase at 6.357 ms versus 6.295 ms, warm monster phase at 6.273 ms versus 6.187 ms, and checkpoint serialization at 0.273 ms versus 0.267 ms. Every change remains within the combined rejection thresholds, and all samples preserved the exact event sequences, 2,718 RNG draws, and 84 spell options.
- Character command dispatch, Turn Undead, and all character/monster physical target families now expose their actual phases. `CombatFlowActions` validates the active actor, delegates one named command mutation, recognizes saveable interruptions, and only then performs battle completion and automatic monster continuation. Attack resolvers prepare typed attempts before source-ordered damage, special, condition, health, and lifetime-record commits. Six affected combat, battlefield, scenario, persistence, reward, and session suites passed 1,348 assertions, lowering oversized-function debt from 13 to 8 while all other structural debt remains zero. Three immediate-parent and three final AOGM Battle 46 samples kept median setup at 75.601 ms versus 76.323 ms, Auto activation at 24.881 ms versus 24.462 ms, combat-view construction at 1.215 ms versus 1.186 ms, spell-option construction at 2.137 ms versus 2.123 ms, complete monster phase at 6.295 ms versus 6.449 ms, warm monster phase at 6.187 ms versus 6.216 ms, and checkpoint serialization at 0.267 ms versus 0.265 ms. Every change remains within the combined rejection thresholds, and all samples preserved the exact event sequences, 2,718 RNG draws, and 84 spell options.
- Scenario Actions now dispatch operation, nested call, state assignment, condition, loop, return, and halt as named VM phases; reusable ordered argument evaluation belongs to `SafeExpressionEvaluator`, while `ScenarioVm` remains at the exact 40-method ceiling and retains every frame, continuation, trace, and pacing mutation. Field/scenario character effects now name their save/protection, condition, special, aging, and health phases. Restore targeting separately validates scroll discard, item use, learned casting, and scroll casting against the reconstructed request. Five affected scenario, persistence, field-magic, scroll/camp, and session suites passed 1,158 assertions, lowering oversized-function debt from 8 to 5 while all other structural debt remains zero. These are existing action, spell, and restore transactions with no new loops, rendering, package loading, startup, or per-frame work, so the batch does not require a performance probe.
- Battlefield pursuit now names target admission, target-footprint collection, goal seeding, and current-occupancy marking before entering the unchanged retained A* loop. `SpellView` separately names immutable-definition population and reusable-view copying. Four affected battlefield, combat, field-magic, and presentation suites passed 782 assertions, lowering oversized-function debt from 5 to 3 while all other structural debt remains zero. Three ten-iteration, 24-shape/fixture navigation samples changed the custom planner median from 4.067 seconds to 4.020 seconds across 240 decisions; all 24 outcome/first-step rows agreed in every changed sample.
- The final presentation hot spots now expose their natural phases. Combat playback tracks source-sequence cadence before dispatching event families to named appenders; interaction construction routes full-stage, narrative, party, workspace, and combat requests to their scene-specific binders; and dungeon mesh construction separates cell admission, stable surfaces, boundary completion, columns, and final mesh materialization while retaining one shared buffer and material cache. Three affected presentation suites passed 799 assertions, and the architecture verifier now reports zero debt in every structural category. Three dungeon-transition samples changed median unique-step p95 from 4.326 to 4.590 ms, turn p95 from 0.018 to 0.016 ms, and backtrack p95 from 0.034 to 0.043 ms. The only increase above five percent is 0.264 ms on the rendered unique-step path, below the plan's 0.50 ms absolute rejection floor; cache build and retention counts remained exactly 32 and 78.
- The obsolete `ClassicWorkspaceView` mounting-point scene was removed. Product files use screen, panel, dialog, row, card, canvas, renderer, definition, state, rules, repository, loader, decoder, result, and flow according to their actual role.
- Every internal rename was decisive. Stable wire identities—including `.realmz2`, `.r2save`, `.r2char`, package keys, opcode identities, save fields, and the user-data directory—were intentionally preserved.

## Scene and script agreement

Stable layout belongs in `.tscn`. Scripts bind data, respond to input, coordinate behavior, and build only variable collections. Repeated rows and cards should become small `PackedScene` components when their structure is stable enough to edit visually.

Custom map, battlefield, first-person dungeon, animation, and effect drawing remain valid script-owned exceptions because their structure is algorithmic. Their viewport, cameras, layers, materials, masks, and surrounding controls still belong in scenes. Shell modes are typed resources rather than duplicate or placeholder scenes.

## Readability budgets

The repository rejects growth from the verified baseline and is converging on stricter final limits:

- final production scripts over 600 substantive lines;
- final functions over 60 substantive lines;
- final classes over 40 total or 20 public methods;
- test suites over 1,200 substantive lines;
- a regression in the measured test-line ratchet or test-to-production ratio;
- any compressed statement line or production script without a purpose header;
- any runtime-created ordinary control outside an exact reviewed function classification, any stale classification, or any per-owner count change that has not been reviewed;
- class/file-name mismatches and unapproved one-node route scenes.

The former 800/100 caps and 664-call construction inventory remain useful no-growth guards during migration, but they are not final acceptance. `verify_architecture_overhaul.ps1` separately ratchets the 600/60/method limits, cross-object private calls, generic preload aliases, missing major scenes, missing preview registrations, one-node shell-mode markers, missing or undeclared feature directories, and misplaced boundary-root production files toward zero. A converted UI collection instantiates an exported row scene; only named algorithmic renderers retain code-created rendering nodes.

## Acceptance trail

The final acceptance exercise requires a newcomer to locate and explain fatigue movement, character state, portable item resolution, one scenario opcode, Inventory layout, and combat Auto flow without private guidance. That exercise has not yet passed. Each migration batch must lower its checked-in ceilings and leave source, scene, guide, tests, and system-manifest ownership in agreement.
