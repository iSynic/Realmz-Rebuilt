# Classic functional differential

Classic-visible behavior is developed through a rolling differential gate. Current Remake identifies existing functionality, mappings, tests, and likely differences. Castle source controls the behavioral decision; a controlled Castle runtime fixture is used only when complete source flow is insufficient. Providence must preserve the authored inputs, and Realmz Remake 2.0 must own the result through typed runtime boundaries.

The machine-readable record is `tests/fixtures/oracle/classic-functional-differential.json`. It contains the detailed paths, symbols, commits, expected traces, tests, and decision state. This document summarizes only the current conclusions.

## Current cases

| Case | Current decision | Durable conclusion |
| --- | --- | --- |
| `movement.land-eight-direction` | Aligned | Land input, availability, and pathfinding use the same eight destination probes; diagonal corner exits follow compiled Layout neighbors. Dungeon movement remains cardinal and edge-based. |
| `action-point.placed-first-match` | Aligned | A placed AP selects the lowest native record at the coordinate. Disabled state or chance failure ends the placed-AP check without falling through; percent below one consumes no draw. |
| `media.classic-resource-key` | Aligned | Classic media identity is the exact four-character `(resourceType, resourceId)` pair. Scenario media wins over stock fallback; Providence rejects unresolved duplicate keys and runtime never performs numeric-ID-only lookup. |
| `interaction.classic-staged-presentation` | Aligned | The active textbox, picture, and response remain staged. Completed narrative may enter the Chronicle, but the Chronicle is not the interaction surface. |
| `campaign.party-setup-commit` | Aligned | Campaign startup begins with an empty session-owned party. Add, vault import, remove, save, and restore remain in setup across revisions; only explicit Begin enters exploration. |
| `character.creation-starting-defenses` | Aligned | Level-one creation combines race/caste saves within `-99…120`, copies racial conditions verbatim, and replaces caste condition-level `1` with permanent `-1`; later caste thresholds wait for level-up. |
| `character.creation-aging-and-rng-order` | Aligned | Creation preserves Castle's discarded seventh attribute roll, cumulative race aging through the caste minimum group, first-seven-save adjustments, three special-bonus checks, and subsequent stamina/age/spell-point draw order. |
| `character.live-aging-and-maximum-age` | Aligned | Midnight and age-changing spells mutate exact age days and invoke at most one adjacent age row. Live rows are unbounded, maximum movement floors at two, and maximum-age battle XP truncates to two thirds; `doesNotDie` does not add behavior absent from the pinned source. |
| `character.age-update-interaction` | Aligned | Every changed band yields a dedicated typed dialog in party/target order with character/race identity, resulting band/range, fifteen applied deltas, and sound 3002. Save/resume retains the exact clock, post-move, or spell continuation. |
| `combat.monster-aging-special` | Aligned | A hit with monster special 17 preserves Castle's damage/potency/save draw order, tests save slot seven, applies the attack-row lifespan formula and one live age operation, and pauses direct or scenario combat at a saveable age update before the next actor. |
| `combat.monster-status-specials` | Aligned | Specials 1–7 and 16 use Castle's exact save/condition map after the shared potency draw. Permanent conditions block after the save; party durations add once only when their starting value is below 30, monster durations remain uncapped, disease uses monster-only sound 684, and monster resistance above 100 turns the entire special attack into a miss. |
| `combat.monster-resource-drain-specials` | Aligned | Special 8 transfers up to three spell points per attacker hit die after save six and does not cap the attacker at its normal maximum. Special 9 subtracts twenty times attacker maximum stamina from a party character's direct earned experience after save five, requests sound 630, and has no monster-target branch. |
| `combat.monster-charm-special` | Aligned | Special 10 uses save zero plus party charm resistance, copies attacker allegiance, gives charmed party actors automatic opposed-side turns, persists mid-battle, and restores party allegiance during battle cleanup. |
| `combat.monster-elemental-specials` | Intentional correction | Specials 11–15 retain Castle's potency, elemental draw, save, protection, and truncation order. `FD-COMBAT-001` makes monster-target cold-through-mental protection reduce committed damage as well as the reported amount. |
| `combat.character-melee-resolution` | Intentional correction | Character melee identifies the actual type-2 weapon independently from its magic plus, derives equipped combat modifiers, and preserves Castle's weapon/condition/critical draw order. `FD-COMBAT-002` makes elemental weapon damage test the defender's save. |
| `combat.character-required-weapon-correction` | Intentional correction | Battle `distance` remains initial monster placement. Data MD `requiredWeapon` and `magicToHit` are separate attack gates. `FD-COMBAT-003` follows Divinity's Item Number contract because Castle's minus-1024 comparison cannot match the shipped item IDs, and it prevents unarmed attacks from bypassing authored weapon-family restrictions. |
| `combat.monster-permanent-affliction-specials` | Aligned | Specials 18 and 19 use save seven. Blindness writes permanent `-1` and allows physical damage; petrification uses Castle's party/monster stone sentinels, sets health to zero, and skips the already-rolled physical commit. |
| `combat.monster-ordinary-melee` | Intentional correction | With fumbles disabled, ordinary monster melee now uses damage plus for accuracy and damage, effective defender luck/armor, day scaling, evil-conditional protection, the ten-percent floor, carried item metadata, and Dragon Hide reduction/sound feedback. Weapon conditions and sound survive a blocking age update. `FD-COMBAT-004` floors a negative physical total at zero instead of allowing Castle's subtraction to heal the defender. |
| `combat.active-turn-attack-cadence` | Runtime defect corrected | Player-controlled character melee uses signed integer half-attack units, carries at most one positive leftover into the next activation, and costs two units per attempt. Monsters choose once, retain one living target, and execute their ordered authored attack rows; the active actor/action/target/row cursor survives save and age-update boundaries. Remake's floating budget is one full action too generous for ordinary values and is not used. |
| `combat.fumble-and-battle-recovery` | Intentional correction | Character and armed-monster attacks consume Castle's separate level/HD-scaled fumble draws. Character weapons enter a bounded battle queue; monster fumbles clear only the active weapon. Body count precedes typed player-directed recovery after victory, defeat, or retreat; dead recipients are not silently excluded. Opcode 122 retains Castle's party-active-only outer guard rather than Remake's reachable monster path. `FD-COMBAT-005` preserves the exact remaining charges instead of reconstructing definition defaults. |
| `combat.character-weapon-mode-selection` | Runtime defect corrected | Battle state now owns and saves Castle's melee/missile toggle. Setup prefers type 2, otherwise type 15; switching costs no attacks, movement, or turn. Missile mode cannot silently invoke melee, and impossible duplicate type-10/type-15 slots fail explicitly. |
| `combat.battlefield-generation-and-placement` | Intentional correction | Battle setup now builds, decorates, and saves Castle's 90×90 tactical field; places party, allies, and row-major authored monsters in source order; and constructs authored monsters with `combatsetup` rolls. `FD-COMBAT-006` terminates impossible placement with exact state/RNG rollback instead of reproducing Castle's unbounded search. |
| `combat.tactical-movement-and-melee-adjacency` | Runtime defect corrected | Party actors take eight-direction steps using complete footprint legality and Castle's maximum destination-terrain cost plus changed axes. Melee targets are adjacent-only, and nonadjacent monsters cannot apply remote melee. Hostile-perimeter reactions are owned by the Guard/withdrawal case rather than blocking otherwise legal steps. |
| `combat.death-macro-battlefield-occupancy` | Intentional correction | `FD-COMBAT-007` keeps a death-macro subject positioned until its nested macro completes. A still-dead monster is then removed; CODE 119 revival keeps the original footprint instead of reproducing Castle's live-but-unoccupied result. |
| `combat.monster-targeting-los-and-movement` | Intentional correction | Monsters retain or select visible targets in Classic combat-slot order, use repeated complete-footprint steps and exact bounded `shift` retries, resolve party/allied Guard at contact, and attack only after adjacency. `FD-COMBAT-008` removes the presentation-delay term from Castle's 128-sample LOS so UI speed cannot alter AI. Permanent Tangle remains explicitly unresolved because Castle's `-1` increases movement. |
| `combat.monster-target-scan-safety` | Intentional correction | `FD-COMBAT-009` bounds Castle's ascending unseen-target scan to validated party/gap/monster slots, preserving order while preventing uninitialized monster access and invalid movement targets. |
| `combat.monster-projectile-weapon-replacement` | Intentional correction | `FD-COMBAT-010` replaces a projectile weapon from the actual attacker's native slot zero. Castle's `attack2` can instead read and mutate global `monsterup`, which may identify a different mover during Guard or withdrawal reactions. |
| `combat.tactical-placement-and-ranged-resolution` | Ordinary physical projectiles implemented; special branches explicit | Character Fire and monster missiles share the battlefield's integer Euclidean range and fixed LOS. Ordinary class-nine physical missiles preserve projectile shield, missile dodge, repeated-hit draw order, distinct player/monster costs, exact monster slot one, finite player charges, and Guard clearing. Random-power, elemental, special, charmed-character, and allied-monster paths remain unavailable with exact reasons. |
| `combat.withdrawal-opportunity-not-facing` | Runtime defect corrected; movement-entry substate unresolved | Guard is explicit combat state, not Remake's once-per-round economy. A legal player step serializes source-slot-ordered guards before movement, every original enemy left behind with temporary +20, movement commit for a survivor, and destination guards. Monster movement resolves party/allied guards at contact. Age dialogs and CODE 119 revival resume the exact cursor. Character `face` and monster `lr` remain presentation-only. Castle's separate pre-direction movement substate still cannot fire Guard on a cancelled or blocked direction through the current one-step intent; later ordinary missile and spell passes clear Guard on successful monster execution. |
| `combat.character-and-monster-retreat` | Runtime defect corrected; two unsafe Castle branches remain explicit | Escape is per-character, permits exact integer range ten, enforces Castle's explicit-command conditions, asks for confirmation, adds 200 prestige penalty, and persists the escaped identity without ending battle while another loyal character remains. Edge movement prompts or forces Escape before collision gates. Routed monsters retreat from their retained target, receive withdrawal attacks before each step, and leave at the edge without a death macro. Castle's missing-target `pos[-1]` path and ineffective mandatory-ally reversal are not guessed. |
| `combat.monster-spell-slot-selection` | Compiler and runtime defects corrected | Providence and the runtime retain ten exact spell positions. Empty slots consume selection draws; they are never packed away. The ordinary single-party-target caster now consumes this contract, while broader shapes remain explicit. |
| `combat.character-spell-activation` | Runtime and presentation defects corrected for ordinary single-target damage | The core owns spell admission, tier, cost, range/LOS, same-round hit and per-activation limits. The battle request exposes only core-proven spell/power/target combinations and the presenter returns stable IDs without calculating rules. |
| `combat.monster-ordinary-spell-casting` | Runtime defect corrected; anomalous and broader branches explicit | Ordinary monster damage spells preserve thirty fixed-slot retries, initial range power, separately lowered affordability power, native target-space draws, source-ordered resistance/save/protection, multiple magical attacks, Guard clearing, and one post-movement retry. Failed charm saves continue into later defenses. Negative cost, ineffective late size range, target-everybody Guard omission, friendly/charmed-monster targets, group/area, class-nine, and special paths remain separately adjudicated. |
| `combat.spell-death-macro-queue` | Runtime defect corrected; group spell resolution remains explicit | Spell-triggered monster death macros are save-owned and drain in source target order before battle outcome or caster advancement. The host does not invent an allegiance reset; authored opcode 119 still revives and explicitly clears allegiance. |
| `combat.character-automatic-group-spells` | Player runtime and Providence editor defects corrected; monster branch disabled | Ordinary player target types 9, 10, and 12 select all-friendly, all-enemy, and everybody in party-then-monster order, spend and roll once, resolve defenses per target, and feed the ordered death-macro queue. Castle's monster branch skips power/resource setup and remains explicitly unavailable. |
| `combat.character-spatial-area-spells` | Bounded player runtime and presentation defects corrected; unsafe/anomalous branches explicit | Ordinary player target types 3 and 4 select one battlefield point, use Castle's source-ordered Data AD mask, deduplicate footprints, remove over-100 resistant monsters during targeting, and spend/roll once before party-then-monster defenses. Type 0 instead repeats selection and resolution, so it is not folded into this transaction. Reflection, adjacent-mask rotation, persistent fields, and masks crossing the battlefield boundary remain explicitly unavailable. |

The rule, clock, spell, complete monster-special attack table, battle-allegiance, battle-reward, persistence, `showageupdate` click boundaries, character and ordinary monster melee, physical attack cadence, natural/forced fumble recovery, battle-owned weapon-mode selection, initial battlefield construction, adjacent melee, source-ordered Guard/withdrawal reactions, per-character Escape, routed-monster withdrawal/edge flight, deterministic tactical LOS, ordinary monster target/movement flow, ordinary physical projectiles, ordinary single-target and bounded group/area character damage spells, ordinary single-target monster damage spells, and the spell-death-macro queue at Castle's default difficulty are covered. Multi-open-space selection, reflection, rotation, queued fields, and spell specials remain explicit. Castle's saved party-selected `howhard`, allow-fumbles XP coupling, opcode 122 live `inspell`, charmed firing, friendly swaps, permanent Tangle, critical ability values, full equipment side effects, and the recorded unsafe or contradictory source branches remain separate boundaries. A separate exploration defect is also flagged: terrain-replacement overlays currently do not consistently replace passability, LOS, and render identity outside battlefield generation, so opcode 12 is not yet complete topology-fidelity proof.

## Rolling gate

For each functional slice:

1. Inventory the pinned Remake implementation, mappings, and tests.
2. Trace the complete Castle control flow and authored fields that determine the observable behavior.
3. Add a controlled synthetic Castle fixture only if source flow cannot settle the observation.
4. Verify Providence preserves the required data in the Realmz 2 package.
5. Add the failing 2.0 characterization, implement through the owning typed boundary, and verify save/resume and RNG where applicable.
6. Update the machine record and the owning domain evidence document before closing the slice.

An unresolved behavior remains disabled with an explicit reason. Remake host architecture, direct Godot mutation, service locators, compatibility profiles, and inferred Classic behavior are not accepted as implementations.
