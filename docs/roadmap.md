# Delivery roadmap

Status values describe current evidence, not intent. A phase completes only when its exit gate is proven.

- **Architecture complete** means the approved ownership and boundary contracts exist and pass their architecture gates.
- **Route-certified** means one bounded scripted route completed through the normal runtime; it does not prove optional branches or the surrounding application.
- **Functionally complete** means every required axis and variant for a workflow passes the generated application audit.
- **Live-certified** means a functionally complete workflow also has accepted presentation and the required ordinary-play or cross-platform evidence.

## Current phase — Audit-guided parity convergence

The completed application audit remains the fixed denominator, and the rolling differential ledger remains the authority for disputed behavior. Delivery now emphasizes convergence: each 3–5-workflow batch includes ordinary-play certification, prioritizes missing and partial workflows over deeper edge cases in functional workflows, and reports functional/certified count deltas from an explicit baseline. The 60/25/15 planning allocation and archaeology stop rule are defined in `docs/development.md` and enforced structurally by the workflow-inventory validator.

The bounded architectural-hardening tranche that began at Rebuilt commit `a918a6a35b49646e14b41571c9fb7c09230d756e` is complete. Package v3/save v4, receipt-backed startup, typed request/response payloads, typed nested scenario/session continuations, package and presentation decomposition, domain-owned Classic opcode handlers, session workflow extraction, transactional restore validation, and bounded RNG/package/projection resources are established. The live presentation boundary uses typed response and combat-targeting variants end to end; mandatory interactions reclaim pointer ownership, and terminal host navigation waits for committed combat playback to settle. Movement/time and field item/spell/scroll transitions execute in their owning workflows, the campaign library is separate from party setup, and `ClassicScreenRouter` delegates route-domain rendering/state/audio to `ClassicWorkspacePresenter` mounted under explicit scene-owned hosts. Receipt-backed AOGM startup verifies the complete archive hash while reaching party setup in 1.17–1.18 seconds across three warm runs. Providence v3 exporter/unit/type/build checks, deterministic repeated export, Rebuilt consumer loading, clean-reference evidence validation, final aggregate/source-intelligence checks, and the MCP walkthrough pass. The ordinary AOGM regression exercised installed-package startup, a six-character vault party, opening-AP pacing, schema-v4 Quick Save/load restoration, Battle 46, persistent Auto, fumble recovery, ordinary treasure, two level results with spell decisions, exact return to exploration, and a post-battle save. The maintenance pause is removed and the unchanged `aogm-application-entry-exit-certification` parity batch resumes. Workflow states remain 68 Classic plus 8 host; architecture work does not upgrade parity counts.

The bounded `rebuilt-hotspot-test-performance` tranche from commit `7e91e06bf6ff70d575c362afef26169f83a155e3` is complete. Presentation-owned held movement now supports a 25–400 percent exploration-only cadence and conservatively reuses unchanged detached view domains. Combat, session continuation, campaign setup, and battle/reward hotspots are split by responsibility behind stable facades. Permanent ratchets now enforce at most 1,000 substantive lines per handwritten product file, 150 per function, 1,200 per test suite, 7,900 total test lines, and a 20-percent test-to-production ratio. The selected AOGM certification batch and all delivery counts remained unchanged throughout maintenance and now resume.

The completed `aogm-application-entry-exit-certification` batch delivered its bounded creator, typed picker, and End Adventure implementation work. Ordinary AOGM play proves the creator context and End Adventure Cancel/Save-and-end paths; the latter now returns to campaign setup with an empty `0/6` party rather than retaining stale roster presentation. A later MCP route exposed malformed local Character Files whose distinct characters shared exact item-instance IDs: exploration had accepted the aggregate, while persistent Auto rollback first detected it after combat began. Party import and aggregate restore now reject duplicate ownership before mutation, the local six-character certification set was republished with unique exact instances, and the repaired route completed both southern-gate battles, rewards, the transition to land level 4, and the opcode-31 AP at coordinate 4,16 with valid snapshots throughout. The corrected mandatory picker used the persistent Party roster, disabled dead members, survived Escape without cancelling, and resumed the VM exactly once after selecting Brom; the resulting session had no pending interaction at revision 306. A completed eventless response now clears its obsolete prompt status instead of leaving `Choose an option` in the ordinary shell. This is ordinary AOGM evidence, but presentation remains functional rather than accepted and the batch does not claim a certification-count increase. The same frame exposed two unexplained solid-green dungeon cells, retained as `GAP-EXP-007` pending Providence/topology/Castle tracing. Full composition-root save-repository failure injection remains the minor `GAP-START-006` test gap.

The completed `classic-inventory-split-join-certification` batch implemented the two missing player-visible inventory transformations—Split and Join—and exercised all five targets through the ordinary installed AOGM item workspace. The live route inspected a package item, confirmed an exact Drop, transferred a charged Dagger to Durin with the expected load change, split Durin's six-charge Torch into two three-charge records without changing load, and joined it back into one six-charge record. `FD-INVENTORY-002` preserves Castle's charge division, all-match merge, copied identification, equipment retention, item limit, and integrated sounds while correcting its stale carried-load bookkeeping and signed-short overflow. Providence package v3 already preserves every required immutable field. These workflows remain functional rather than certified until their presentation is user-accepted; Equipment remains outside this batch because its workflow still requires a completed Castle oracle.

The active `aogm-field-application-certification` batch now targets ordinary charged-item use, field/camp spell and scroll use, and acquired-map viewing. The first two are implementation targets because their common source-backed paths work but deferred special branches keep their audit state partial; acquired-map viewing is the batch's ordinary-play certification target. The batch will exercise reachable AOGM behavior before opening rare discard, case-transfer, random-power, malformed-data, or exact-pixel archaeology.

- Split now keeps the selected stable instance with the ceiling half, creates one deterministic unequipped sibling with the floor half, copies identification, enforces the thirty-record limit, updates exact load immediately, and requests integrated sound 678.
- Join now absorbs every matching definition into the selected stable instance, ORs equipped/identified state, updates exact load immediately, requests integrated sound 663, and rejects infinite or over-32,767 totals without mutation.
- The owning public inventory proof covers detached availability, synchronous typed intents, exact identities, load, save restoration, integrated sounds, and overflow rejection. One coherent MCP walkthrough now supplies ordinary AOGM evidence for Inspect, Drop, Trade, Split, and Join; no additional stack edge-case expansion is scheduled.

Hardening baseline: 226 production GDScript files contain 39,944 lines; 27 test GDScript files contain 13,000 lines and 175 named cases. The baseline aggregate gate passed 4,540 assertions across 23 suites in 43.0 seconds with no teardown leak. The original largest coordinators were `GameSession`, `RealmzRuntimeApi`, `ClassicScreenRouter`, `PackageRepository`, and `RealmzApplication`. Three pre-hardening warm-process probes of the installed local AOGM package reached party setup in 3.383, 3.290, and 3.653 seconds; manifest/integrity work took 0.803–0.927 seconds, document decoding 0.808–1.144 seconds, and typed construction 1.413–1.446 seconds. These are historical comparison measurements, not current completion evidence.

Hotspot/test-performance closeout: 40,434 substantive production lines and 6,782 substantive test lines yield a 16.77-percent ratio. Test code fell 42.6 percent from the approved 11,807-line baseline while retaining one owning proof for all current workflow and differential links. Both grandfather lists are empty, no test reaches a private product method, and all file/function/suite caps pass. A warm six-character AOGM probe measured 0.377 ms transaction p95, 23.336 ms projection p95, and 23.713 ms combined p95 across 50 steps. The MCP walkthrough sustained eight clear steps in 0.85 seconds at 100 percent and nine in 0.45 seconds at 400 percent with no post-release burst, then exercised opening-AP pacing, schema-v4 Quick Save/load, Battle 143, persistent Auto, ordinary rewards, a real level-up and spell-selection boundary, exact return to exploration, and a post-battle save. The aggregate gate passes 2,438 assertions across 23 suites without teardown leaks.

Post-closeout performance correction: the earlier benchmark hid presentation churn and sampled only 43 of 60 moves on the incremental path. Hour-boundary fatigue updates are now conservatively classified, overlapping detached land cells and static combat map projections are reused, and ordinary movement retains its command controls. A current 60-step six-character AOGM run reports 60 incremental projections with approximately 0.25 ms transaction p95, 1.42 ms projection p95, and 1.65 ms combined p95 while preserving the exact event sequences and final coordinate. Character-file rows and shared images are retained across roster changes, and combat entry reuses its request-owned detached combat projection for the matching live revision while route presentation occurs once. These are performance corrections only and do not change workflow counts or Classic behavior.

The completed **Ordinary AOGM combat certification** batch exercised representative physical attack, combat spell, explicit character retreat, ordinary rewards, an actual level result, spell selection, save/restore, and terminal return through the ordinary UI. The completed **Final ordinary AOGM service lifecycle certification** batch then exercised an exact charged-item Trade, every visible Temple service with a real successful mutation and Castle's paid no-effect behavior, and the full Shop lifecycle: buy, paid identification, sale to normalized buyback stock, save/reload while the Shop request remained pending, buyback purchase, and Leave to exploration. All three workflows now carry ordinary AOGM evidence.

Shop and Temple are no longer standing implementation priorities. The only retained Shop gap is low-priority Providence parity for native empty-slot ordering; it does not block the normalized buyback workflow or AOGM play. Future service work requires an observed campaign defect or content that demonstrably depends on that native ordering. The next batch is selected from the regenerated non-service audit queue rather than extending Shop or Temple edge cases.

Current combat-interface convergence restores Castle's far-click one-step direction projection, detached shown-target facts, Reveal Friends, camera Center/Next/Previous, source-ordered physical attack feedback, Auto, Delay, Bandage, Turn Undead, bounded movement-only Undo, fixed-power combat scroll use through battlefield-native targeting, and character-owned Fast Spell bindings/shortcuts through ordinary typed casting. Realmz Rebuilt now owns the complete pinned 142-resource integrated sound bank; Providence commit `8fead345c0d13b74fe08b26060ab538073a77df3` packages only scenario-owned exact sound overrides. A clean AOGM export and live MCP session prove application fallback for integrated sound 147, scenario ownership for sound 24000, and a non-overlapping 960x600 setup shell. Human auditory comparison and ordinary combat-layout acceptance remain batch closeout evidence; Auto Weapon Switch policy and Undo's runtime-unobserved cursor edges remain current work.

Missing Fast Spell and Allies/Bestiary remain leading non-service implementation candidates unless the regenerated audit identifies a more reachable AOGM application gap. Historical rolling-pass entries below remain evidence of completed slices, not standing instructions to continue their old “next priority” notes.

## Phase 0 — Foundation and tooling (completed)

- Godot project and explicit composition root.
- DOX hierarchy, architecture docs, ADRs, provenance lock, test runner, CI, MCP integration.
- Clean pinned Castle and Remake reference worktrees.

Exit evidence: the application launched through MCP Pro 1.16.0 on Godot 4.7.1; runtime UI discovery and simulated input changed observed state; a 960×600 smoke screenshot was captured; the final editor error list was empty; headless verification passed; pinned clean reference worktrees were created; and the DOX hierarchy was re-read and reconciled.

## Phase 1 — Package contract and deterministic kernel (completed)

- Implemented: additive Providence `.realmz2` exporter, deterministic ZIP/JSON/hashes, desktop target/report, authoritative schema, byte-identical runtime mirror, and a 3×3 synthetic package generator.
- Implemented: independent ZIP/hash/capability/reference/topology validation into typed Realmz content; direct map/AP/Classic-action models; deterministic session, party/state, clock, QuickDraw-compatible RNG and traces; detached save envelope and transactional save repository.
- Implemented evidence: repeated Providence exports compare byte-for-byte; runtime accepts the generated package and rejects a valid-ZIP stale-hash mutation; deterministic search/save/reload resumes the same RNG branch and state revision.
Exit evidence: repeated Providence exports are byte-identical; the runtime independently accepts the generated package and rejects a valid-ZIP stale-hash mutation; seed 1 produces the verified QuickDraw/Castle search roll 52; save/reload preserves the session revision, one-minute clock advance, RNG state, and draw count; and failed restore leaves the active session untouched. Through MCP Pro, the composition root loaded the package, committed the search through simulated input, observed the expected runtime state, saved `mcp-phase1`, and captured the 960×600 loaded-session screenshot. The final editor error list was empty and the full 43-assertion verification suite passed.

## Phase 2 — Authoritative world and exploration (completed)

- Implemented: Providence normalization of land and packed dungeon representations into explicit cells, directional edges/features, random regions, and Layout transitions; strict independent runtime construction and validation.
- Implemented: typed world overlays, movement, deterministic pathfinding, LOS/visibility, search, secrets, doors, transitions, random rectangles, clock advancement, trigger execution, and terrain replacement through one topology.
- Implemented: a `GameView`-only Classic 2D presenter with topology-derived minimap and movement/LOS/trigger debug facts.

Exit evidence: the synthetic three-map fixture exercises message AP execution, terrain replacement, random-region gating, hidden-secret blocking/discovery, directional dungeon secrets, door opening, land transition, pathfinding, visibility, and save/reload through one topology. The 93-assertion suite passes across six suites. MCP Pro keyboard/mouse input traversed the message AP, search roll 52, secret, and transition; restart/restore retained map, coordinate, clock, RNG state/draw count, and overlays; the 960×600 topology/minimap screenshot was inspected; and a fresh editor inspection reported zero errors.

## Phase 3 — Scenario VM and Actions (completed)

- Implemented: a serializable, session-owned VM with preserved Classic instructions, signed GOSUB, CODE 111/112 stack behavior, XAP replacement, Simple Encounter yields, one runtime API, and explicit unsupported behavior.
- Implemented: compiled Safe Scenario Actions with typed calls, persistent state, private helpers, bounded flow/arrays/steps, separate Classic/Safe frame limits, and save/resume at interaction boundaries.
- Implemented: Providence ordinary Action calls and generated call editors, project-wide Scenario Action library, Safe bytecode compilation, derived export readiness, and removal of visible scripting tiers, gameplay profiles, behavior anchors, and sandbox creation from the normal 2.0 path.

Exit evidence: the synthetic route executes AP opcode -4 to a serializable Simple Encounter request, selected result message, `CallScenarioAction`, Classic opcode 39 to an XAP, and CODE 111 return as one ordered trace. Save/reload reproduces the pending request and exact continuation. The 132-assertion runtime suite passes across seven suites; Providence passes 905 frontend tests plus its Rust exporter, typecheck, lint, architecture, module-size, production-build, and cargo-check gates. Repeated Phase 3 exports are byte-identical. MCP Pro exercised the pending-save, response, restore, repeated response, and final route state with a clean editor error inspection. Castle source/control-flow evidence and the precise proof boundary are recorded in `docs/scenario-vm-evidence.md`.

## Phase 4 — Realmz gameplay domains (opcode/capability ownership gate completed)

This historical gate proves that the active capabilities in the bounded fixture and AOGM inventory have source-backed owners. It is not a claim that every player-visible Realmz application workflow exists or is complete.

- Implemented: direct race, caste, item, spell, monster, battle, treasure, shop, and Complex/Thief/Timed Encounter models with independently validated package construction.
- Implemented: fixed character, condition/time, inventory/economy, combat, magic, monster, and combat-flow rules; save-owned domain state; packed standard/scenario spell identities; opcode 7/8 program mutation; opcode 36 equipment escrow; and stateful opcode 122 fumble behavior.
- Implemented evidence: the 452-assertion suite covers rules, package construction, domain mutation, RNG ordering, save/restore, VM routing, and explicit error paths. A bounded hash-labelled Assault on Giant Mountain inventory declares 58 active normalized opcodes; every one has one owner, passes package readiness, and dispatches without top-level fallback. This is content-inventory evidence, not live-route proof.

Exit: every active capability needed by the synthetic fixture and Assault on Giant Mountain has executable source-backed behavior with no fallback or silent no-op.

Exit evidence: Providence commit `364755324fae1685b82bbf0c751572045ee6888d` passes its Rust library/exporter/example checks, 321 library tests, frontend typecheck, and production build. The runtime's full gate passes 452 assertions across eight suites plus schema, fixture-provenance, architecture, export-exclusion, and whitespace checks. A fresh MCP Pro editor session reported zero errors; the main scene played, exposed the expected runtime tree, accepted simulated mouse input, changed the observed status, captured a 960×600 frame, and stopped cleanly. Phase 4 domain behavior is proven by deterministic headless tests; the current minimal host does not yet expose those domain screens, which is Phase 5 work.

## Phase 5 — Classic shell and first campaign (bounded AOGM route certified; not application completion)

- Implemented: Classic-first 2D shell with campaign discovery/install, party creation, exploration HUD, character/inventory/spell views, typed encounter/combat/shop/temple/bank presenters, package pictures/audio, settings, and save/load surfaces.
- Scope note: those are functional routes and surfaces, not a claim that the current shell reproduces the density, composition, interaction affordances, or finish of the Classic Realmz UI. Presentation fidelity remains active work even though the bounded AOGM route gate passed.
- Implemented: source-backed random-rectangle continuation, active-combat intent lockout, save v3 migration, Castle-correct AP post-action destinations, and deterministic synthetic interaction/save routes. The runtime gate passes 531 assertions across eight suites.
- Package/route evidence: a fresh local Providence export of Assault on Giant Mountain independently validates and starts with 21 decoded media assets. Package hash `7561aa2d2f1c2acf4dc57bc12cef1d79049473a2e9bd19fe8c6e377acf087e84` and route hash `b84ff7d68d2d203cb03aca65f94600d4f7c87e01054d8b9a97aba6d68ef19a95` remain local-only evidence; no campaign payload is committed. The route passes opening presentation, Battle 60, Baron briefing/macro, Battle 274, four fortress tile mutations, quest 17, rewards 68/467/625, final report, and final position land 0 (84,8).
- Compiler evidence: Providence commit `a06f2ef152139dfd3fccfc5e563ba9ee58b62d3a` passes 323 Rust library tests with one audit test ignored, frontend typecheck, production build, and `cargo check`.
- MCP evidence: Godot MCP Pro discovered the unfixed port automatically, opened and inspected the main scene, started the synthetic campaign, created the party, entered the typed random-surprise request, saved it as `quick`, declined it, loaded the save, observed the identical prompt and options again, and accepted the restored decline response. A 960×600 frame was captured. A fresh post-fix editor session repeated project/scene inspection, play, runtime text inspection, screenshot capture, and stop with zero editor errors.

Exit: a fresh export completes the certified route with no unsupported active mechanic or save/reload divergence.

Exit evidence: the source-backed route completes through the ordinary 2.0 session/VM API with no fallback, the synthetic suite covers save/reload at its typed interaction boundaries, and the compiler/runtime gates above pass. This certifies the bounded completion spine; it does not claim exhaustive Castle parity for every optional branch.

## Phase 6 — Corpus, releases, and extensions (implemented; cross-platform certification pending)

- Implemented: Windows, Linux, and macOS export presets plus three-runner verify/export CI matrices. Release exports exclude MCP, tests, references, and local evidence.
- Implemented: optional Classic dungeon 3D whose floor, edges, doors, secrets, stairs, and columns are projected from the same `MapView` facts as the 2D map. No second collision or discovery model exists.
- Implemented: explicit rejection of packages requiring `realmz.scenario.gdscript-actions-v1`. Safe packages remain portable; an in-process or token-scanned GDScript fallback was not introduced.
- Implemented: generic external corpus probing/routing, expanded source-backed opcode ownership, held-over ally combat with serializable Castle body-count selection, serializable battle-round/death macros, and Providence reachability retention for direct opcode 88/89 monster references.
- Local campaign evidence: a fresh War in the Sword Lands package at hash `4509fcda9f3684292b5406c151b7108979f4f6211eb1ee198b808ce798e464dd` passes all eight external route stages and observes Battles 377, 473, 474, and 212 before ending at land 16 (78,36) with quest flags 1, 42, and 75. Route hash `fa08b47bd53e0f506333c3c488ccb609b7387fc324b7784586b88d9899e5b1d3`; package, route, and report remain local-only.
- Compiler evidence: Providence commit `84a5afde2ec13b218289fcb02ace20adfc71b6f2` retains direct ally monster definitions, preserves Classic column-major land storage, separates negative-land `cicn` overlays from base terrain, packages every referenced map atlas, resolves reachable shared Classic sounds, and normalizes odd terminal WAV chunks. Its focused twelve-test Realmz 2.0 exporter gate and focused WAV decoder/round-trip tests pass. Runtime unit/integration coverage currently passes 815 assertions across nine suites.
- Prior AOGM visual/audio evidence: local package hash `45dd3caa73b58b29afdbe071ea120a812f64595b716468ecede61b27c1e69ae3` independently validated with 119 media assets, including 40 then-packaged scenario/shared Classic sounds. That package predates application-owned integrated sounds and is retained only as historical route evidence. Its unchanged world document retained the verified 8,100-cell land 0 layer, tile 155 start at (48,15), and 59 special-cell overlays. The view retained native 32×32 art, a clipped 530×452 map viewport, the unadorned built-in CICN 186 party image, and click/hold plus numpad/arrow/WASD input. The opening AP preserved Castle opcode 1 pacing: message 896 and message 897 each yielded a dedicated, saveable Continue textbox before later actions executed; negative message 898 remained in the same textbox without adding a click. MCP observed no dispatched sound at either positive textbox, then observed the 1.480-second Horn, 1.726-second Angry Mob, and 1.440-second Camp streams playing simultaneously on three channels immediately after the second acknowledgement. The concise shell status no longer expanded to the message text, and internal picture/sound event names never entered player-facing textbox history. A 960×600 frame was captured. This does not replace the earlier route package's `live-route` evidence or constitute a human auditory comparison.
- Local release-pack evidence: Windows, Linux, and macOS presets each produce a 497,592-byte Safe runtime PCK with SHA-256 `530c3d07f020fa90ee0cb9da5223086d8d1d745b052795665643f65b33494b5a`; inspection of Godot's export inventory found no MCP configuration/addon, tests, tools, docs, contract mirrors, references, or GitHub workflow entries.
- Current Windows handoff evidence: the exported `Realmz Remake 2.0.exe` is 109,071,360 bytes with SHA-256 `04baf75cc1d69dd93eb709533ecab4fd7770bb8a530645717017a06a9d9809fc`; its 526,344-byte PCK has SHA-256 `678dcea7e2daa765894a9562dba8d554085929be7c8e6ac416d172962515e1b8`. A hidden headless launch smoke exited successfully. The current AOGM package remains local and separate from the executable.
- MCP evidence: the 1.16.0 doctor check passed; automatic port discovery inspected the Godot 4.7.1 project and scene tree; the main scene played; the latest AOGM package loaded and created a party through simulated input; ten ordinary movement intents reached the public-well `yes_no` request; runtime UI inspection returned `Yes` and `No`; audio inspection returned four channels, final sound 10001, and an empty queue; no choice-label or WAV decoder error occurred; a 960×600 frame was captured; and the scene stopped. The opening-AP replay separately observed two serial positive textbox requests, a passive negative textbox with no Continue button, and three concurrent final sound channels before stopping cleanly. The editor inspection contained only the existing `classic_map_presenter.gd` integer-division and shadowing warnings plus transient MCP execution diagnostics, with no new product runtime error. A subsequent installed-campaign check over three valid AOGM revisions rendered exactly one campaign row and one Play button while preserving all content-hash files.

Exit: Safe packages are cross-platform deterministic, certified routes pass, 2D/3D agree, and unsupported sandbox requirements reject cleanly.

Remaining exit evidence: execute the configured native-binary verify/export matrices on actual Windows, macOS, and Linux runners. The byte-identical cross-preset PCK is local packaging evidence, not proof that each native executable launches. The implementation does not claim Phase 6 certification until those jobs pass; additional campaign routes expand corpus confidence without weakening the completed AOGM and War route boundaries.

## Current follow-on tranche — Classic-wide application system (in progress)

The kernel, package loader, scenario VM, and bounded campaign routes are established. The current work shifts the primary effort to reconstructing Realmz as a complete application instead of adding more scenario-driver coverage in isolation.

The authoritative completeness denominator is now the generated [Classic application workflow status](classic-application-workflow-status.md), backed by `tests/fixtures/oracle/classic-application-workflow-inventory.json`. Its Classic and modern-host totals are reported separately, and its delivery states are derived from compiler, simulation, persistence, presentation, variant, and live-evidence axes. The completed items below describe delivered slices; they do not override a workflow's current audit state.

Current and future work in this tranche follows the risk-tiered delivery policy in `docs/development.md`: group 3–5 related player-visible workflows into one batch, give each workflow one focused-verified commit, and run the Tier 3 closeout once for the batch. A rolling pass therefore names a coherent workflow batch, not a tiny edit or product-side adjustment. Historical rolling-pass sections below retain their original evidence statements; only current and future planning uses this batch definition. Package/schema, save/migration/continuation, RNG, VM, topology, terminal combat/reward sequencing, and composition-root changes move directly to Tier 3.

Delivered so far in this in-progress tranche:

- `ClassicApplicationShell` map-first composition: compact menu strip, dominant map/picture stage, persistent right roster, bottom narrative/status well, and contextual command deck over the existing typed session boundary.
- Campaign-aware party setup with schema-v2 title/version/author/restriction metadata, source-authored aggregate level guidance, five Difficulty choices, three complete Monster Sets, core-computed experience ratio, six always-visible party slots, Race-left/Class-right filtering, and explicit setup errors.
- Character creator with five separately mounted stages, session-owned generated Review state, reroll/cancel without RNG rollback, typed first-twelve-slot starting-spell selection, saveable unused-point confirmation, finalization without a second roll, and complete package-backed portrait/tactical selection with race recommendations.
- Immutable `.r2char` character vault records/repository, explicit import intent, transactional persistence, complete revision history, exact target-package eligibility, confirmed archive, and exact revision recovery.
- Providence schema v2 exporter plus byte-identical runtime mirror and regenerated synthetic fixtures.
- One scene-backed component for each of the nine workspaces, with clipped scrolling and responsive stacked headers.
- Typed interaction placement in the stage or Classic textbox region for text, choices, encounters, services, and combat.
- Compact/Standard/Wide profiles, one route registry and Back stack, named input actions, independent UI/text scale, and 800×600 through widescreen reflow.
- Sixty exact-commit Remake bitmap controls with semantic scene-use evidence, byte hashes, dimensions, and 1×/2× nearest-neighbor rules. The selected SpriteCook charcoal surface and two matching frame textures are derived deterministically.
- Bundled Alegreya/Alegreya Sans fonts, pinned Google Fonts provenance, file hashes, and OFL licenses with no runtime network dependency.
- Presentation-complete honest states for all nine workspaces, CICN/ICON/PICT collision-free media lookup, safe unidentified-item views, and explicit action availability from `GameSession`.
- Typed interaction components for text/choice, selection, encounters, shops, temples, banks, and battles. The old procedural presenter is removed.
- A test-only gallery covers nine states for every route and interaction kind. Local rendered frames have been inspected at 800×600, 960×600, 1280×720, and 1920×1080, including dense inventory/services and 150% compact settings.

Still required for live visual acceptance and later gameplay completion:

- Complete the full MCP keyboard/focus matrix. The official Godot MCP Pro 1.16.0 CLI now provides live-campaign evidence for package selection and startup, while the broader gallery remains synthetic evidence.
- Inspect every interaction kind beside the Classic reference board; deterministic gallery coverage is broader than the currently reviewed representative frames.
- Gameplay implementations for the remaining audited application workflows. Ordinary rewards, carried-item equipment/trade/drop, charged spell items, field spellcasting, ordinary Camp/Rest with random interruption and automatic departure, field scroll scribing/use, and manual Pool/Share/Swap are implemented below. Timed midnight interruption, suspicious recovery ordering, combat/discard/transfer scroll branches, door/XAP items, broader item effects, bank depth, and the remaining maps/journal/character-management gaps stay explicit. Opcode 36 remains scenario-owned equipment escrow rather than a player storage service.
- The complete higher-starting-level construction path and explicit publication of newly created characters to the reusable vault still need ordinary live-campaign acceptance; package-backed portrait/combat-icon catalogs and role validation are implemented.
- Re-run the external AOGM ordinary-play route through the new shell; the committed gallery remains synthetic and is not live-campaign proof.

## Current rolling fidelity pass — blocking age updates (implemented)

- Remake supplies the live-aging mutation lead but has no equivalent blocking age-update presentation. Castle `showageupdate`, its midnight caller, and its spell callers establish the interaction: resulting band/range, character and race identity, portrait/icon, fifteen forward or reversed deltas, sound 3002, and one modal input boundary per changed character.
- Providence commit `cad17d11bd8ec798d29bf5468ed8071019f32c1f` already emits the immutable names, media identities, ranges, and change rows, so package schema v2 remains unchanged.
- `age_update` is now a supported typed interaction with a dedicated inset presenter. Direct clock work queues updates before destination AP processing; scenario spells retain the issuing VM frame. Both forms serialize through save envelope v3, reject generic acknowledgements, and resume one ordered character at a time.
- Monster attack special 17 now preserves Castle's generic potency and save-slot-seven draws, applies one source-backed age transition, and defers physical damage and the next actor behind the same saveable interaction in direct and scenario combat.
- The age-update tranche landed with 1,640 assertions across 11 suites and ten validated differential cases.

Remaining fidelity boundary: the dialog carries stable portrait and combat-icon IDs, but the current programmatic component does not yet draw those package media assets.

## Current rolling fidelity pass — monster status attacks (implemented)

- Remake's explicit status table and tests supplied the functional lead for specials 1–7 and 16, party/monster duration differences, permanent traits, and resistance. Castle `attack` and `savevs` adjudicate the details Remake's isolated helper cannot: generic potency precedes the save; save families and monster indexing remain exact; the party checks its starting duration against 30; the result may exceed 30; failed effects at 30 still report; and magic resistance above 100 turns a monster-target special into a whole-attack whiff.
- `CombatRules` now owns all eight status mappings and branches for party and monster targets. It records exact save, condition, mutation, block, and sound facts in `AttackResolution`; `CombatFlow` publishes the special and asynchronous source sound before ordinary damage feedback. Condition state and RNG remain session-owned and save without a new continuation or `.r2save` revision.
- Providence schema v2 already exports attack rows, hit dice, runtime magic resistance inputs, type flags, saves, and spell immunities. No compiler or package-contract change was required.
- The typed suite passes 1,690 assertions across 11 suites. Differential validation covers eleven cases against the pinned Castle, Remake, and Providence roots.

The later remaining-monster-specials pass completes specials 10–15 and 18–19; this section records only the earlier status tranche's narrower evidence.

## Current rolling fidelity pass — monster resource drains (implemented)

- Remake's Classic monster-special dispatcher supplied the functional lead for spell-point and experience drains. Castle `attack`, `savevs`, and the direct struct fields resolve the important distinctions: the generic potency draw still occurs first; spell drain tests save six before checking the balance, transfers hit dice times three, and may leave the attacker above maximum; experience drain tests save five, subtracts from earned experience rather than a level-progress adapter, and has no monster-target branch.
- `CombatRules` now owns specials 8 and 9 for party and monster targets. `AttackResolution` records the exact resource, amount, target balance, attacker balance, save, block, and sound facts; `CombatFlow` publishes those facts before ordinary damage and requests sound 630 asynchronously only for a failed experience drain.
- Monster spell points now restore across the central state boundary when a source-backed drain has raised them above maximum. Direct negative experience, both sides of spell transfer, RNG order, empty/saved branches, monster-only applicability, events, and whole-session restoration are covered without changing `.r2save` v3.
- Providence schema v2 already emits the attack special, hit dice, stamina bonus, spell points, saves, immunities, and magic resistance needed by the fixed runtime construction path. No Providence or package-contract change was required. The typed suite passes 1,725 assertions across 11 suites, and differential validation covers twelve cases.

## Current rolling fidelity pass — remaining monster specials (implemented)

- Special 10 now uses a persisted character allegiance field rather than a presentation trait. Party charm resistance adds fifty to save zero, undead and immunity saves remain source-ordered, charmed actors run automatically against the opposite side, battle resolution counts both character and monster allegiance, and battle cleanup restores the party's base side.
- Specials 11–15 now preserve the generic potency draw, separate elemental damage draw, save 1–5, matching protection, integer truncation, and total health loss. `FD-COMBAT-001` deliberately corrects Castle's monster-target cold-through-mental display-only protection bug; the source observation, synthetic fixture hash, player-facing problem, and chosen-result test are recorded in `fidelity-ledger.md`.
- Specials 18 and 19 now preserve save seven, permanent blindness, party-versus-monster stone sentinels, resistance whiffs, and petrification's force-kill path that skips ordinary physical damage after it was rolled.
- The session emits typed special facts for element, rolled/committed/display damage, allegiance, condition, and force-kill ordering. Mid-battle Charm survives save/restore; automatic charmed turns never yield a player action request; cleanup publishes restored allegiance.
- Providence schema v2 already carries every immutable input. No package, compiler, or `.r2save` envelope revision was required. The typed suite passes 1,775 assertions across 11 suites, including allegiance-aware hostile targeting, enemy-count participation, and rejection of battle-scoped allegiance from reusable vault records, and differential validation now covers fifteen cases.

## Current rolling fidelity pass — character melee foundation (implemented)

- Combat builds a typed equipment snapshot from immutable item definitions. A zero-plus sword remains armed, while a nonweapon damage item no longer suppresses hand-to-hand damage. Conflicting type-2 weapons and missing definitions fail explicitly instead of depending on inventory order.
- The bounded resolver includes equipped damage/luck/armor, the type-2 weapon physical die, target-tag and heat/cold/electrical ranges, condition weapons, double-to-hit, auto-hit, reflection, helpless targets, type hatred, and Castle's two critical RNG draws. Critical chance values are not fabricated because Providence does not yet export the separate character `spec[15]` table.
- `FD-COMBAT-002` makes the defending monster's save mitigate an elemental weapon. The fixture separately records Castle's attacker-save observation; runtime tests prove the chosen result.
- Castle and Divinity use two unrelated distance concepts: Data BD battle `distance` randomizes initial monster placement, while the historically named Data MD `dist` byte is a weapon-to-hit restriction. Providence now emits that monster field as `requiredWeapon` and emits `magicToHit` separately; the runtime no longer treats battle placement as an attack prerequisite.
- `FD-COMBAT-003` follows Divinity's documented Item Number semantics because Castle's `item.itemid - 1024` comparison cannot match its shipped 1–999 item records. It also requires an actual matching weapon for blunt/sharp restrictions rather than preserving Castle's unarmed bypass. Signed stored values 128–253 normalize at the rules boundary.
- The schema-v2 hash changed in place because `.realmz2` remains unpublished; `.r2save` v3 is unchanged. Fumbles, attack half-units, missile toggling, and full equipment side effects remain future cases rather than inferred behavior.

## Current rolling fidelity pass — ordinary monster melee (implemented)

- With optional fumbles disabled, the resolver now includes Castle's damage-plus accuracy term, carried-weapon accuracy and damage fields, defender-luck draw, effective equipped armor, Realmz-day bonus, attacker conditions, evil-conditional protection, ten-percent floor, zero-minimum attack-row fallback, helpless branch, target-type damage, elemental mitigation, required-weapon gate, and Dragon Hide physical reduction plus synchronous sound 694.
- Remake corroborates `hitDice + damageBonus` accuracy and signed physical damage materialization. Castle controls the exact percentage and RNG ordering. Providence schema v2 already emits signed `damageBonus`, attack rows, weapon identity, and complete item metadata, so no compiler or package-contract change was required.
- `FD-COMBAT-004` records Castle's negative-damage healing defect and the 2.0 zero floor. The source observation is synthetic control-flow evidence, not a Castle-runtime fixture.
- A monster weapon condition and Dragon Hide feedback that follow a monster-caused age dialog now remain in the central pending-attack state. Restore validates the saved condition baseline, applies both effects exactly once after acknowledgement, and preserves Castle's feedback-before-damage order without changing `.r2save` v3.
- Castle's random-weapon table 2 has an overlapping roll-85 boundary with context-dependent precedence: the summon helper returns the first match, while ordinary battle setup allows the later match to overwrite it. The current single `build_monster` path cannot represent both outcomes, so this remains an explicit spawn-origin modeling defect for the tactical construction pass rather than receiving a guessed global precedence change.
- Fumbles, reflection, missile-weapon replacement, and behind/facing derivation remain explicit tactical-state boundaries. The typed suite passes 1,823 assertions across 11 suites before aggregate closeout, and differential validation covers eighteen cases.

The following rolling pass closes the active-turn attack half-unit priority while retaining missile, spell, and fumble work as separate evidence cases.

## Current rolling fidelity pass — active-turn physical attack cadence (implemented)

- Player-controlled character melee now uses Castle's signed half-attack reserve directly. Activation carries at most one positive leftover, adds normal attacks and attack bonus, applies Speedy's source-control-flow four-unit bonus, resets movement, and spends two units plus three movement per attempt. A character with at least two units remaining keeps the same activation.
- Monster physical AI chooses one action, draws and retains one opposed target, and executes each authored attack row in order. Its active actor, action, target, and next row are central combat state; death macros and blocking age updates resume the exact remaining sequence rather than repeating row zero or advancing early.
- Providence schema v2 already emits race/caste attack values and monster attack count/rows. The runtime loader now rejects a count beyond the supplied rows or Classic's five-row storage, so no compiler change was required.
- Remake's floating `MaxActions` formula grants one extra full action relative to Castle for normal values and was rejected as a donor. Castle's own zero/one-half-unit display, Speedy/Slow display discrepancy, charmed-character cadence, monster Speedy row overflow, spell/item action limits, missile replacement, and tactical movement remain explicitly unresolved.
- The typed suite passes 1,858 assertions across 12 suites. Nineteen differential cases validate against the pinned Castle, Remake, and Providence roots.

## Current rolling fidelity pass — fumbles and battle recovery (implemented)

- Character and armed-monster physical attacks now consume Castle's distinct level/HD-scaled fumble draw after the hit roll. Character fumbles require an equipped removable melee weapon and capacity in the twenty-entry battle queue; cursed weapons and a full queue retain the original attack result. Monster fumbles clear only the active weapon, so later authored rows become unarmed without deleting the monster's authored booty slots.
- Opcode 122 now requires the active party combatant to have committed a physical action. Its `q[up] < 10` outer guard makes Castle's nested monster branch unreachable, so Remake's monster-disarm behavior was rejected. Authored opcode text and sound are suppressed with the outer guard; exact live-`inspell` suppression remains deferred until spell activation owns an equivalent lifecycle fact.
- Body-count selection precedes one-at-a-time typed recovery. Recovery remains available after victory, defeat, or retreat without inventing a survivor-only restriction; it enforces recipient capacity/load, identifies the item, and survives direct-session or VM save/resume. `FD-COMBAT-005` preserves the exact item instance and remaining charges instead of Castle's ID-only reconstruction.
- The dedicated Battle Recovery stage shows exact charge count, active/disabled recipients, and Leave Behind without exposing simulation objects. A normal-renderer 1280×720 gallery capture was inspected for overlap, reachability, and enabled-state accuracy; the local image remains under ignored artifacts.
- Providence schema v2 already emits every immutable item fact required by the runtime. No compiler, package-schema, or `.r2save` envelope revision was required; the additive battle and active-turn fields default safely when restoring older v3 state.
- The typed suite passes 1,923 assertions across 12 suites before aggregate closeout. Twenty differential cases validate against the pinned Castle, Remake, and Providence roots.

Flagged boundary: Castle's `allowfumble` preference is coupled to a five-percent XP penalty when disabled. Realmz 2.0 currently uses the default enabled behavior and has no source-backed gameplay-settings owner for that pair; no UI toggle or uncoupled rule has been invented.

## Current rolling fidelity pass — battlefield generation and placement complete

- Castle battle state owns a per-character melee/missile toggle. Setup selects melee when type 2 is equipped, otherwise missile when type 15 is equipped; returning to melee permits hand-to-hand. Switching consumes no attacks, movement, turn, or RNG. Realmz 2.0 now persists that toggle, validates unique type-2/type-10/type-15 slots, exposes the same legal action set in `CombatView` and `combat_action`, and renders a typed Switch response.
- Character missile mode no longer falls through to the melee resolver. Fire remains visible but disabled with the exact missing position/range/LOS reason. Monster AI missile selection no longer runs physical row zero under a `missile` label; it emits `combat_monster_action_unavailable` and loses that activation until the tactical resolver exists.
- Castle's positive `behind` flag is not facing. `checkforenemy(2)` gives each hostile left adjacent by movement a temporary +20 attack, while character `face` and monster `lr` only choose icon orientation. Remake's once-per-round `reaction_ready` is a functional lead but materially differs from Castle and was not ported.
- Providence now imports the shared `Combat Data BD` table, enriches old projects by exact source/landlook/tile identity, and emits complete effective battle-terrain sets. Land sets preserve active landlook 0 through 200 followed by global 201 through 400; dungeon sets preserve global 200 through 400. The runtime mirror and typed loader independently validate every range, 3×3 build, map reference, level type, and landlook. The synthetic fixture gives the two tile-200 sources different solidity values to prove the overlap is resolved correctly.
- `CombatFlow` now builds and decorates Castle's 90×90 field before direction and separation, places every party slot and held-over ally before row-major authored monsters, constructs authored monsters through the distinct `combatsetup` roll path, removes dead party positions only after monster placement, and commits the field before initiative. `CombatState` saves terrain, map shifts, actor anchors, and complete footprints; `CombatView` exposes a detached copy.
- `FD-COMBAT-006` explicitly corrects Castle's unbounded no-solution placement loops. A finite full-field search returns stable character/ally/monster placement errors, and failed setup restores the complete preexisting RNG state, draw count, scripted cursor, trace, and game state.
- Three Castle inconsistencies remain open from this slice: manual and automated character missile paths disagree about twelve-movement cost; `attack2` uses global `monsterup` while replacing a ranged weapon; and `combatmap` evaluates the top tile with `bottom.ispath`. The runtime preserves the top-tile branch pending a named decision.
- Castle's saved party-selected `howhard` setting is also not yet session-owned. Authored-monster construction implements and unit-tests its nonzero formula, but ordinary 2.0 battle startup deliberately passes the default zero rather than inventing a difficulty setting or omitting its persistence requirements.
- A newly identified topology defect remains outside this pass: terrain replacement currently updates the terrain ID overlay but exploration movement, LOS, and presentation can still consult immutable base-cell flags/render identity. Opcode 12 therefore needs a dedicated source-backed topology correction before it is considered complete.
- The typed suite passes 2,033 assertions across 13 suites before aggregate closeout. Twenty-four differential cases target the pinned Castle, Remake, and Providence checkpoints. Providence commit `7f3347cbe3073c2152004f0dfd0c0395efa82f30` passes the focused 17-test exporter lane and reproducibly generates the exact-size fixture.

## Current rolling fidelity pass — tactical movement and melee adjacency (implemented subset)

- `BattlefieldRules` owns the complete-footprint adjacency and one-step query shared by simulation, detached `CombatView`, typed battle requests, and the interaction component. Party actors use all eight directions. Cost uses Castle's maximum destination-footprint terrain time plus one for each changed axis; Remake's minimum-footprint implementation was rejected.
- At that checkpoint, melee target selection became proximity-gated for party, monster, ally, and charmed-character paths. A nonadjacent character attack failed before RNG or turn mutation, while ordinary nonadjacent monsters emitted `tactical-movement-not-implemented`; the later monster-targeting pass below replaces that ordinary-monster boundary. Charmed-character movement remains unresolved.
- At this checkpoint a hostile-perimeter step was rejected transactionally so the partial movement slice could not skip reactions. The later Guard/withdrawal pass below replaces that temporary boundary with a serializable source-ordered continuation. Rejected geometry and resource checks still leave positions, attack reserve, movement, active-turn state, and RNG unchanged.
- Ordinary defeated combatants are removed from battlefield occupancy. `FD-COMBAT-007` handles the Castle inconsistency where `killbody` clears a death-macro monster before CODE 119 revives its record without restoring field occupancy: 2.0 retains the pending footprint, removes it only if the monster stays dead, and keeps it when revived.
- The typed suite passes 2,086 assertions across 13 suites before aggregate closeout. The new differential cases distinguish the implemented player subset from disabled monster movement and projectile behavior.
- Flagged before the next pass: Castle `cansee` divides each of 128 steps by `128 + delayspeed`, apparently making LOS depend on a user animation preference and potentially preventing the trace from reaching its target. This needs a controlled Castle runtime fixture or a named correction before projectile LOS is implemented. Current 2.0 `retreat` also ends the whole battle immediately, while Castle's command is per-character and range/condition gated; it is an existing runtime defect, not a certified behavior.

Next rolling priority: adjudicate monster target selection, LOS, and movement as one source-backed slice, then implement guarding/withdrawal continuations or projectile firing only after those facts are stable. Keep the top-tile path typo, projectile movement-cost disagreement, wrong-global replacement hazard, retreat, spell-per-activation questions, Castle haste/display contradictions, and random-weapon-table spawn-origin split separate until each has its own evidence.

## Current rolling fidelity pass — monster targeting, LOS, and movement

- Monsters now preserve a valid target across turns, otherwise select from Castle's party/gap/monster slot space and switch to its ascending scan after the first unseen valid target. `FD-COMBAT-009` bounds that scan to validated live slots instead of reproducing the native 110-sentinel out-of-range access.
- `BattlefieldRules` owns Castle's 128 center-offset tactical LOS samples, complete-footprint movement, maximum terrain cost, large-monster solid-one traversal, and exact bounded `shift.c` retry branches. `FD-COMBAT-008` removes `delayspeed` from the sample divisor because a presentation preference otherwise changes monster target eligibility.
- The automatic monster activation preserves the missile-then-cast draw order, falls through a successful missile choice when already adjacent, retains movement and target state across saves, takes repeated legal steps, emits movement/sound events, and executes physical rows only after adjacency.
- Permanent Tangle remains disabled for monster movement with `permanent-tangle-movement-unresolved`: Castle subtracts the `-1` permanent sentinel and therefore increases movement. Castle also retains the maximum terrain cost across shifted movement probes, apparently preventing a cheaper alternate route after an unaffordable first direction; that observable control flow is preserved and flagged rather than silently corrected. Guarding and ordinary withdrawal are implemented in the following pass; friendly swaps, battlefield-edge flight, spell-field collision, retreat, casting, and projectiles remain separate continuations rather than being skipped or guessed.
- The typed suite passes 2,113 assertions across 13 suites before aggregate closeout. Twenty-eight differential cases target the pinned Castle, Remake, and Providence checkpoints.

## Current rolling fidelity pass — Guard and withdrawal reactions

- Battle state now owns persistent Guard identities and a serializable reaction record containing mover, origin, destination, movement cost, fixed original hostile perimeter, phase, source-ordered attacker queue, cursor, and defeated-mover flag. Character movement resolves pre-move guards, every original enemy no longer adjacent with Castle's temporary plus twenty, movement for a survivor, and destination guards. Ordinary monster movement resolves party/allied guards before attacking.
- Guard and withdrawal use Castle party-slot then monster-slot order. Guard requires the flag and skips Helpless without clearing it; withdrawal does not require Guard, is suppressed only by mover Invisible, clears attacker Guard, and can let the same enemy attack once in each phase. Monster reactions use authored attack row zero. The player Guard command preserves the exact one-based 49/51 sound split and asynchronous playback; Finish Turn is now distinct and clears movement plus Guard. Implemented monster physical attacks clear their activation Guard; later ordinary missile and spell passes now do the same on successful execution.
- Age-band dialogs persist both the deferred monster attack and reaction cursor. Death-macro interruption retains the mover footprint; if CODE 119 revives it, execution resumes with the next queued attacker rather than replaying or abandoning the sequence. Impossible reaction kind/phase combinations and invalid actor/position references fail restore.
- Remake supplied adjacency and animation-queue leads, but its once-per-round `reaction_ready` economy and temporary guarding trait were rejected. Providence schema v2 already preserves every immutable combatant, condition, attack-row, and terrain fact required by this pass, so no compiler or package-contract change was needed. `.r2save` v3 remains unchanged because Guard and reaction state are additive nested combat data.
- The typed suite passes 2,193 assertions across 13 suites before aggregate closeout. Twenty-nine differential cases validate against the pinned Castle and Remake references; combat cases also validate their Providence projections where authored data is involved.

Flagged boundaries: Castle checks guards when entering movement mode before the player chooses or cancels a direction, while Realmz 2.0's typed intent already carries one legal destination. Reproducing a Guard attack on cancelled or blocked movement requires a separate begin-movement interaction and is not inferred here. Retreating-monster withdrawal, possessed-character auto-guard, target-everybody Guard omission, friendly swaps, and spell-field collision remain unresolved. `getup.c` also appears to index `monster[q[up]]` with initiative IDs that start at ten; this unsafe status-display access is not reproduced.

## Current rolling fidelity pass — Per-character Escape and routed monsters

- Explicit Escape is now a source-backed per-character operation. It uses Castle's integer-truncated Euclidean nearest-enemy range, permits exactly ten, rejects Helpless, traitor, Confused, Tangled, and Slow actors, asks `Embrace Cowardice` versus `Stay and Fight`, removes only the accepting character, and adds exactly 200 prestige penalty. Battle continues while another living loyal character remains on the field; escaped living characters receive no battle experience.
- Battlefield-edge movement is a distinct Escape route. Entering the outer band prompts before ordinary terrain collision, while a character already in the outermost band is forced out. The detached combat view and typed battle interaction mark edge options explicitly instead of presenting them as ordinary movement.
- `CombatState` now persists sorted `retreatedCharacterIds`. Restore therefore accepts a deliberately escaped living character without weakening corruption checks for an arbitrarily missing living actor. Direct and scenario-hosted Escape confirmations use the existing typed `yes_no` shape and survive `.r2save` v3 or VM restoration without replay.
- RUNS_AWAY monsters move away from their retained target through the existing bounded topology probe, snapshot source-ordered adjacent enemies, receive withdrawal attacks before each committed step, and leave when the destination reaches Castle's edge band. Hostile flight sets zero stamina-equivalent health, removes the footprint, grants ordinary defeated-monster rewards, and does not request a death macro.
- Remake supplied the per-character `fled_battle` and edge-removal leads, but its `distance_to <= 10` rejects Castle's legal exact-ten case, omits condition/confirmation gates, and its routed AI flees from an enemy barycenter. Those differences were rejected in favor of Castle's retained-target flow. Providence schema v2 already carries all required immutable monster and terrain facts, so no compiler/package change was needed.
- Flagged rather than guessed: Castle can read `pos[-1]` when a routed monster has no retained target; 2.0 reports `retreat-target-unresolved`. Its mandatory-ally branch commits the edge step before flipping deltas that are recomputed on the next loop; 2.0 stops there and reports `mandatory-ally-edge-retreat-unresolved`. These are not claimed as corrected Classic behavior without runtime evidence and a named decision.

## Current rolling fidelity pass — Ordinary physical projectiles

- Providence schema v2 now preserves all six native monster item positions instead of compacting zero IDs. This keeps slot zero as physical fallback and slot one as projectile weapon. The runtime mirror independently requires six entries, permits empty strings, rejects unresolved nonempty item IDs, and the synthetic fixture proves an empty slot one cannot collapse.
- Character Fire now resolves ordinary physical class-nine item spells through session-owned battlefield range/LOS, projectile shield, missile dodge, finite charges, one caste-bonus draw, one damage roll reused across fixed repeated hits, and per-hit dodge. A committed manual shot spends two half-attacks and twelve movement; invalid targets and unavailable profiles fail before mutation or RNG.
- Monster missiles preserve Castle's separate missile-chance and range-power draws, exact slot-one weapon, range-power versus affordable-cost-power versus resolution-power-one split, opposed party target, zero remaining movement, and Guard clearing. An out-of-range or unaffordable missile resumes at Castle's cast/movement decision without repeating the missile-choice draw. A malformed or absent slot-one item remains an explicit unavailable action rather than loading undefined Classic resource state.
- `FD-COMBAT-010` corrects Castle's stale-global projectile-to-melee replacement: both ordinary attacks and reactions read slot zero from, and mutate, the actual attacker. Remake supplied actor-local equipment and target-selection leads but did not establish Castle's slot identities, cost split, or draw order.
- Deliberate boundary: Castle's native manual UI decrements a finite charge before target cancellation. Realmz 2.0's typed Fire intent carries a validated target and consumes the charge only with the committed action, preventing cancellation from becoming a hidden mutation.
- Explicit unresolved branches: the automated possessed-character path omits manual Fire's twelve-movement cost; random item power eight needs a serializable begin-fire continuation; elemental/special projectiles need their remaining save and spelllist flow; charmed-character and allied-monster firing need source-owned target/cadence state; monster-on-monster dodge reads stale `c[charup].missile` in Castle.
- The typed suite passes 2,218 assertions across 13 suites before aggregate closeout. Thirty differential cases cover the pinned references. Providence commit `228fe88c61245c6ef5771956629e05a7754250fd` passes the focused 18-test exporter lane and emits the regenerated deterministic fixture under schema hash `d8c59c118373b704fbf1103d7d57a76ff6e7f6144917cacdebe95cb0750d7e73`.

Next rolling priority: source-own the ordinary spell-casting activation and targeting lifecycle, including spell-per-activation limits and successful monster-spell Guard clearing. Keep random-power and nonphysical projectiles, possessed-character cadence, permanent-Tangle movement, Castle haste/display contradictions, random-weapon-table spawn-origin split, pre-direction movement cancellation, and the top-tile path typo separate until each has its own evidence.

## Current rolling fidelity pass — Ordinary character spell activation

- Providence commit `55c158d33800a246e15b8ee4cdb54404c472ed67` preserves all ten native monster spell positions. Castle samples slots zero through nine and retries empties, so the runtime independently rejects packed arrays of one through nine entries. An older empty array is normalized to ten empty slots because it cannot change selection or RNG; this keeps the installed AOGM exports readable without weakening the fixed-slot rule. The focused nineteen-test exporter lane passes and the deterministic synthetic fixture is regenerated at internal package hash `04a58fbff91f4992e5a5258f1690e49bcd146f8865c7136b9c9abd285380c7f9`.
- The existing character cast path incorrectly used caster level as `castlevel`, skipped range/LOS, combat eligibility, conditions, same-round hit state, and the caste spell limit, and always advanced the turn. The bounded replacement derives zero-based tier from Classic spell ID, validates before mutation, persists per-activation cast count and round attacked actors, spends spell points plus two half-attacks and twelve movement, and retains a caster with a usable reserve.
- Opcode 69's first two values are now named as blocking flags while retaining the save-v3 JSON keys. Castle treats nonzero as disabled; the prior positive variable names invited exactly the opposite interpretation.
- Cross-function review corrected an apparent `resolvespell.c` inconsistency before implementation: the monster branch has no local cannot check, but `savevs()` consumes its roll and then forces failure for `cannot > 1`. The runtime preserves that draw and failed save rather than trusting the local branch in isolation.
- The battle host now derives detached spell/power/target/cost combinations from the same mutation-free core probe used by execution. The typed interaction submits only stable IDs and power; presentation does not duplicate affordability, range, LOS, condition, or per-activation rules.
- Ordinary monster casting consumes the fixed ten-slot contract, thirty slot retries, initial range power, separately lowered affordability power, native target-slot draws, source-ordered resistance/save/protection, successful Guard clearing, and a persisted post-movement retry count. Multiple authored magical attacks can continue in the same activation.
- Cross-function review found and corrected a prior 2.0 resistance defect: a failed charm opposed save must continue into magic screens, Animated immunity, and magic resistance. Castle's `cannot` bypass occurs after that first draw; it does not erase it.
- The typed suite passes 2,263 assertions across 13 suites. Thirty-three differential cases distinguish the implemented ordinary player and monster subsets from explicitly disabled anomalous and broader spell shapes.

Next rolling priority: within the next workflow batch, adjudicate the remaining spell activation boundaries in bounded source-owned slices—automatic all-friendly/all-enemy/everybody targets, area targeting, specials, class nine, item/fast/random-power casting, and active spell presentation pacing. Tier 3 closes the batch once; a small branch does not create a new Tier 3 closeout. Before implementing them, resolve or explicitly retain Castle's target-everybody Guard omission, negative-cost monster SP increase, post-`getrange` size bonus, and pre-paid player target cancellation.

## Current rolling fidelity pass — Spell death-macro queue

- Castle does not dispatch each spell-killed monster's death macro immediately. `resolvespell` reaches `killbody` while `inspell` is active, `killbody` appends the macro and subject to `todoque`, and `emptyque` drains that queue before battle outcome or actor progression. Remake supplied a useful FIFO implementation lead, but Castle establishes the spell-scoped timing and authored subject identity.
- `CombatState` now owns and serializes the ordered spell death-macro IDs, original caster, and whether the caster should advance when the queue drains. The core validates the exact completed subject, removes only that still-dead subject, requests the next macro in source order, and defers victory or caster advancement until no queued macro remains.
- Both direct-session and scenario-owned death-macro continuations preserve the queue through typed interaction and `.r2save` v3 restoration. Older continuation data defaults to the prior immediate-macro allegiance behavior, while new queued entries carry an explicit host policy.
- Cross-function review prevented an overcorrection: the queued `killbody` path itself does not reset monster allegiance, but Classic opcode 119 explicitly does. The host no longer resets allegiance unconditionally; an authored revival still does so through the opcode implementation.
- Multiple-entry tests prove exact FIFO order, stale-cursor rejection, per-subject occupancy removal, and retained original caster state. A yielding macro test proves whole-session restoration and that a macro without an allegiance-changing opcode leaves the source allegiance intact.
- The typed suite passes 2,290 assertions across 13 suites. Group spell casting remains explicit because Castle rolls shared duration/base damage and charges once before iterating targets; invoking the single-target resolver repeatedly would alter both RNG and cost.

Next rolling priority: implement source-ordered automatic all-friendly/all-enemy/everybody damage resolution over one shared cast transaction. First resolve Providence's apparent target-type inventory gap for Castle value 12 and adjudicate the target-everybody Guard omission; do not silently normalize either behavior.

## Current rolling fidelity pass — Player automatic group spells

- Player target types 9, 10, and 12 now use one automatic cast option and select living positioned participants in Castle party-slot then monster-slot order. The UI labels the group without fabricating a target combatant or meaningless HP total.
- `MagicRules` spends once and rolls one duration/base-damage value before applying resistance, save, elemental protection, and damage independently to each typed target. `CombatFlow` spends two half-attacks and twelve movement once and queues defeated monsters' macros in the resulting source order.
- Providence now exposes target value 12 as `Everybody` and accepts the source-backed 0-through-12 range. Its exporter and schema already preserved the numeric field, so no package migration or fixture regeneration is required.
- Castle's monster group branch is not copied: it resolves before assigning the current cast power, skips affordability and spell-point deduction, leaves movement unchanged, and treats Guard differently for everybody. Realmz 2.0 reports `monster-group-spell-power-resource-anomaly` until a controlled Castle fixture or named correction determines intended behavior.
- Target type 11 is also flagged: the player `targettype > 8` block does not special-case it and therefore reaches the all-enemy `else`. It remains outside the ordinary-group implementation rather than being silently treated as a documented target shape.
- The focused runtime suite passes 2,305 assertions across 13 suites; the Providence browser validation test and TypeScript typecheck pass.

Next rolling priority: adjudicate player spatial target types 0, 3, and 4. Do not assume they share one transaction; keep monster group power/resource semantics, target-12 Guard, target-11 fallthrough, queued fields, reflection, and specials separate until each has source-backed ownership.

## Current rolling fidelity pass — Player spatial area spells

- Castle establishes that target types 3 and 4 are one-center spatial transactions, but target type 0 is not. Type 3 uses the spell's fixed Data AD shape; type 4 uses cast power. Type 0 performs multiple independent target selections and reruns `spelltargets` plus `resolvespell` for each selection, including fresh duration/base-damage rolls, so it remains explicitly unavailable rather than being folded into the area path.
- `SpellAreaRules` preserves all eighteen source-ordered Data AD masks as fixed engine rules data, including asymmetric and hollow patterns. Providence schema v2 already carries target type, fixed size, rotation flag, queue icon, range, and all ordinary resolution fields, so this pass requires no package change and does not embed native Data AD bytes.
- One core-probed typed option now identifies spatial mode, resolved shape, source-ordered offsets, and the caster's default center. Execution validates one explicit battlefield coordinate against range, LOS, and the complete 90x90 mask before mutation; deduplicates intersecting actor footprints; removes over-100 resistant monsters during targeting; then spends once and resolves party slots before monster slots with one shared duration/base-damage roll and the existing source-ordered death-macro queue.
- The battlefield-edge rule is intentionally bounded, not claimed as exact Classic behavior: Castle's unchecked seven-cell indexing can leave field memory. Realmz 2.0 reports `spell_area_outside_battlefield` instead of reproducing unsafe memory access. A separate named fidelity correction is not asserted because the out-of-bounds Castle result is not a stable gameplay behavior.
- Reflection, authored adjacent-mask rotation, and queued persistent fields remain explicit. Reflection can consume targeting-stage draws and redirect to the caster; rotation selects adjacent Data AD records rather than applying a geometric transform; queued fields create persistent collision state before resolution. Player and monster queued-field spells now fail explicitly rather than dealing immediate damage while silently omitting that state.
- The battle component currently returns typed X/Y coordinates through an interim selector. Classic-style pointer targeting, mask highlighting, and confirmation belong to the tactical viewport presentation pass; presentation still does not calculate affected actors, range, LOS, or legality.
- The typed suite passes 2,332 assertions across 13 suites. Thirty-six differential cases target the pinned Castle, Remake, and Providence checkpoints.

Next rolling priority: implement the target-type 0 repeated-selection continuation or take a narrower active-content spell branch after inventorying certified campaign use. Keep reflection, rotatable adjacent masks, queued field collision/lifetime, item/fast/random-power casting, class-nine spell effects, specials, and active spell presentation pacing separate.

## Current rolling fidelity pass — Repeated actor-target spells

- Ordinary size-zero target type 0 now presents one ordered selector for each spell/power combination. The core supplies all legal party and monster candidates, including self, and validates the submitted distinct sequence against the same battlefield range/LOS query before mutation. One through power targets may be committed; choosing fewer targets still spends the full chosen-power cost.
- `MagicRules` rerolls duration and base damage for every selected actor. Defenses remain target-specific, deaths feed the existing queue in selection order, and an unreflected monster above 100 magic resistance consumes that selection's duration/damage rolls but receives no effect. Ordinary reflection is now owned by the following pass.
- Monster target type 0 now rolls target power after range power, lowers it only for affordability, samples unique native party/gap/monster slots, and commits a partial sequence after a hundred failed or successful samples within one cast while still paying the chosen power. `FD-COMBAT-011` corrects Castle's leaked battle-wide counter by giving each cast an independent attempt budget; this temporary guard is not serialized.
- Remake confirmed the creature-per-power workflow and distinct 1108/3208 Magic Darts identities. Providence commit `fa0297426677552080608e1b89bad4334d323465` replaces the misleading `Multi Open Space` label with `Multiple Actors / Open Spaces`; schema v2 already preserves target type and size, so packages do not change.
- The aggregate gate passes 2,360 assertions across 13 suites. Thirty-seven differential cases validate against the pinned Castle and Remake references plus the current Providence compiler.

Flagged boundaries: Castle prepays player cost and refunds only 66 percent on Abort; the 2.0 host continues to charge only on committed Cast. Nonzero-size open-space/summoning placement, active tactical pointer highlighting, and special/queued-field effects remain explicit.

## Current rolling fidelity pass — Ordinary spell reflection and opposed-monster targets

- Castle's ordinary target types zero and one check Reflecting Spells once on the originally selected party or monster actor. Results one through thirty-three replace that actor with active caster `q[up]` before duration, damage, the original monster's above-100 exclusion, resistance, saves, or protection. The redirected caster is resolved once; its own reflection condition does not recurse.
- `SpellTargetSelection` and `GroupSpellResolution` now retain original and effective actor identity plus a typed reflection flag. Player single and repeated casts use that contract, and a reflecting actor remains visible in the repeated-target picker instead of being silently suppressed.
- Monster casting now samples the complete native space: party slots, slot nine's gap, and monster slots ten onward. Opposed monsters resolve through the same source-ordered monster resistance/save/protection path, while reflected monster casts can strike the caster. Castle queues a reflected caster's death during `inspell`, finishes its remaining authored magic attacks without another stamina check, and drains the macros afterward. Repeated post-death hits append duplicate records to Castle's fixed 100-entry queue; 2.0 now preserves that bounded order, keeps occupancy until the subject's final record, and serializes the complete caster continuation.
- The implementation deliberately does not spread this rule to every spell shape. Castle's automatic groups bypass ordinary reflection; area footprints can test one large actor repeatedly; intelligent area scoring consumes reflection RNG before the actual cast; queued fields explicitly suppress reflection. Those suspicious or distinct branches remain unavailable until separately adjudicated.
- Providence requires no new package change for reflection: schema v2 already preserves target type, ordinary spell fields, and all forty signed monster conditions at the current compiler checkpoint. The knowledge-graph format-enrichment lock targets Realmz commit `4089d550ab606172bac850ac055677c36c6ff547`, not the pinned Castle oracle, so this pass uses direct source/control-flow evidence rather than applying graph conclusions across revisions.
- The focused typed suite passes 2,388 assertions across 13 suites before aggregate closeout. Thirty-nine differential cases now distinguish ordinary reflection from area/group/field behavior.

Flagged boundaries: friendly monster targeting under `cannot == 4`, nonzero-size open-space/summoning placement, rotatable masks, queued fields, item/fast/random-power casting, class-nine nonphysical effects, monster group anomalies, and target type 11 remain separate.

## Current rolling fidelity pass — Native race/caste identity and ordinary startup

- The ordinary AOGM workflow exposed generic `Race 1`/`Caste 2` labels and more than thirty race choices. Castle `Race`, `Caste`, and `loadprofile` establish one-based character values over exactly thirty fixed records; Remake independently subtracts one before looking up rule records and names.
- Providence had emitted zero-based stable/native identities, shifted caste eligibility by one record, and parsed trailing stock `Data Race` bytes as additional profiles. Compiler commit `1d660bb6dbab175e641f33d07e8755e8ee3be24e` caps both tables, emits IDs 1–30, uses imported Realmz names, and aligns both eligibility directions. Schema v2 now makes those bounds executable contract rules.
- The runtime independently rejects incomplete, duplicate, zero-based, or out-of-range profile tables. The regenerated synthetic package and fresh local AOGM export validate against schema hash `e0ebe02f52c1fb52e83ce6e91074d82f4adf5c39db7af8e7a7512dd83e5c3f62`; AOGM constructs 30 races, 30 castes, Human/Fighter at ID 1, and its normal party-setup session at land 0 coordinate 48,15.
- This pass also found that Providence's committed schema-hash sidecar had drifted from the authoritative schema bytes. A compiler test now derives and compares that hash so a stale sidecar cannot be published again.
- Godot MCP Pro exercised the corrected package through the ordinary shell: path install, Human/Fighter creator review, character addition, explicit Begin, movement from (48,15), the opening AP at (49,15), all three staged messages, and resumed movement to (50,15). The opening picture resolved as `scenario-pict-32128`; no product runtime error interrupted the route.
- The typed suite passes 2,398 assertions across 13 suites, and the differential validator resolves all 40 cases against clean pinned Castle, Remake, and Providence worktrees.

The headless package-probe teardown warning was later traced to a strong `RealmzRules`/`CombatFlow` ownership cycle. CombatFlow now retains only the stateless rule modules it uses, the typed suite and package probe release cleanly, and the aggregate gate rejects future ObjectDB/resource-retention diagnostics. The 72 MB AOGM path-open operation still blocked the runtime inspector for more than five seconds before completing; campaign validation needs a responsive progress/caching pass even though the resulting session is correct. Startup latency is not being conflated with package or character-rule fidelity.

Next rolling priority: inventory active special and zero-damage combat spells, then take the smallest source-settled branch. Do not infer one generic spell-special pipeline from ordinary damage resolution.

## Current rolling fidelity pass — Special-57 combat healing and actor targeting

- Active-content inventory selected the narrow source-settled healing branch instead of treating all nonordinary spells alike. Remake supplied a dedicated healing adapter and stock-spell tests; Castle's manual picker, monster `getrange`, reflection, resistance, `resolvespell`, and `heal` establish the actual target and mutation rules.
- Player target-one spells now expose every living party or monster actor in Classic slot order rather than imposing a hostile-only UI rule. Strict combat special 57 resolves reflection and resistance first, then negates rolled damage. Party healing respects stone/death exclusions and maximum stamina; monster `cannot == 4` selects same-allegiance actors including self, may exceed maximum stamina, and retains Castle's otherwise surprising healed-monster attacked marker.
- Monster over-healing exposed a persistence-model defect: the loader had rejected current stamina above maximum even though Castle creates that state. Restore now accepts over-maximum values while retaining signed-16 bounds, and monster healing uses the shared signed arithmetic so an extreme result cannot create an unsaveable aggregate.
- Providence schema v2 already contains every required field, so no compiler change or package migration is needed. Spell resolution events carry both signed `damage` and positive `healing`; character/monster health, spell cost, active cast count, attacked state, and RNG restore through the existing save-v3 aggregate.
- The typed suite passes 2,413 assertions across 13 suites. Forty-one differential cases distinguish implemented special 57 from all other special and zero-damage branches.

Flagged rather than guessed: Castle special 60 appears to consume the shared damage value while some authored records may use duration fields; specials 58 through 60 need individual control-flow and active-content evidence. Providence currently emits generic `Realmz Spell <id>` names for standard spell records without project overrides. Camp healing is a separate application workflow. None of these is silently inherited from special 57.

Next rolling priority: correct the standard-spell display metadata at the Providence boundary, then inventory active specials 58 through 60 and class-nine effects by certified-campaign reachability before selecting another runtime branch.

## Current rolling fidelity pass — Staged character creation and starting spells

- Providence commit `40169270388049c355cdd133ba1a0af76a7eda64` now gives the stock spell catalog Castle's positive STR# names. The regenerated synthetic fixture and local AOGM package independently validate and are installed as immutable revisions; the compiler's existing schema-v2 race, caste, restriction, and spell fields are sufficient for this pass.
- Character generation is no longer a one-click presentation form. A typed specification enters one complete Classic creation roll, whose provisional `CharacterState`, RNG position, and spell choices belong to `GameSession` and `.r2save` v3. Review renders that exact state; Reroll advances RNG while retaining the provisional ID; Cancel discards the state without rewinding RNG; acceptance clones the reviewed state without another roll and grants caste starting items only after spell selection, matching `CharacterGen`.
- The ordinary setup overlay mounts Identity, Race & Class, Appearance, Review, and Spells one at a time. Race remains the left-hand driver for Class. The 960×600 AOGM flow was exercised through all five pages, character addition, enabled Begin, and entry into the land map without clipped controls or the previous all-fields-at-once overflow. The external Pen.dev review board was used only as a local layout study and is not runtime or committed product evidence.
- Castle's `cspells[7][12]` boundary prevented a pre-commit defect: Providence's fifteen resource-name entries per level are not fifteen learnable character slots. The runtime now offers only slots one through twelve, applies costs 1, 3, 6, 10, 15, 21, and 28, disables options that exceed the remaining budget, and preserves Castle's explicit confirmation when points remain. That confirmation and its response survive save/restore.
- Castle's separate `Startlevel` path remains incomplete. It offers levels 1, 3, 5, 7, 9, 11, 13, 15, 17, 20, 25, and 30, then invokes ordinary level-up once per additional level. The Identity page therefore exposes level one and a disabled higher-level explanation instead of silently claiming the workflow or approximating advanced characters.
- The typed suite passes 2,635 assertions across 13 suites. Forty-three differential cases now include the aligned generated-draft/starting-spell path and the explicitly disabled higher-starting-level path.

Next application-workflow priority: implement the complete source-backed higher-level character construction and then the explicit publish-to-vault decision. Portrait/combat-icon catalogs remain a coordinated Providence/display-data task; do not replace them with guessed filesystem assets.

## Current rolling fidelity pass — Advanced character construction and vault publication

- The Identity stage now offers Castle's fixed `Startlevel` choices—1, 3, 5, 7, 9, 11, 13, 15, 17, 20, 25, and 30—filtered only by the campaign maximum. The core builds the level-one base, sets prestige and the caste victory threshold, and invokes the ordinary level-up transaction once per additional level before starting spells and initial items. Presentation never synthesizes the advanced result.
- Providence commit `8f5fcf6830bee8249333c53c4da14b57e147e878` adds the previously missing fourteen-value race ability row, both caste ability rows, and thirty victory thresholds. The runtime mirror hash is `040f50ab9bd67a98a626176de876848b135fb449263fcd989664652a65bc7acd`; malformed lengths fail before content construction. The synthetic fixture was regenerated twice byte-identically and its positive/stale-hash pair now represents this contract.
- This pass caught two source-boundary defects before they became durable rules. Castle's level-up condition threshold replaces a nonnegative current value with permanent `-1`; it does not subtract one. Castle also loops across fifteen character trained-ability slots while the race/caste records author only fourteen. `FD-CHARACTER-001` keeps the unauthored slot zero and draw-free, and the runtime now keeps trained abilities separate from the twelve racial combat modifiers.
- The character's two-hand value is constructed, persisted, and displayed because Castle initializes and clamps it. No combat effect is inferred: the pinned attack source does not currently consume that field.
- Finalization now yields a typed, saveable yes/no publication decision. The session commits the new member before that request; acceptance emits a host event only after the interaction clears, and `RealmzApplication` publishes one immutable `.r2char` revision. Decline leaves the vault untouched. Stable character IDs may contain interior dots, but traversal components remain invalid.
- Godot MCP Pro exercised the fresh local AOGM package through a level-three Human Fighter roll, all five creator stages, party addition, the publication prompt, and a successful transactional vault write. The resulting current index points to a parseable immutable revision carrying level, campaign/package provenance, fifteen trained-ability values, and two-hand state. This walkthrough also caught and corrected stale Appearance guidance surviving onto the asynchronously populated Review page.
- The typed suite passes 2,662 assertions across 13 suites. Forty-four differential cases now include the aligned advanced creator and the named trained-ability boundary correction. Providence's focused 21 Rust tests, unit tests, typecheck, production build, and repeated deterministic export all pass.

Next application-workflow priority: complete package-backed portrait and combat-icon catalogs and the vault management workspace (revision history, archive, eligibility, and explicit publication feedback). Keep those media identities Providence-authored and Castle-adjudicated; do not guess filesystem assets or attach repository writes to ordinary gameplay.

## Current rolling fidelity pass — Carried-item inventory workflow

- Castle and Remake were compared before extending the UI. Remake's Honest Storage is optional host functionality; Castle's similarly named arrays belong to opcode 36 whole-party equipment escrow. The normal inventory therefore has no invented Store command.
- `InventoryRules` now supplies mutation-free source probes and committed operations for ordinary equip, unequip, exact-instance trade, and drop. Race/class admission, slots, rings, hands, quivers, unique items, curses, recipient count, and charge-aware load are core-owned and exposed as detached availability with exact disabled reasons. `FD-INVENTORY-001` permits Castle's ordinary equipped-item transfer outcome while forcing the recipient unequipped and rejecting the source path's curse bypass/stale derived bonuses.
- The inventory workspace is character-first and item-specific. It shows player-knowable CICN/detail data, action availability, and one typed Trade destination per other party member. Presentation emits only stable IDs and does not reproduce equipment or capacity rules.
- Drop uses a typed Classic yes/no request and a validated `drop-item-confirmation` continuation. Accept, decline, stale response rejection, save, restore, and exact-instance/load mutation are covered without changing `.r2save` v3.
- The existing consume-only item path was unsafe: it spent a charge despite having no effect implementation. Use now fails explicitly without mutation. Identification, item effects, unsupported passive equipment effects, and split/join remain disabled. Castle's split/join record mutation does not consistently update load even though trade uses per-record charge weight, so that branch needs an explicit fidelity decision rather than an assumed correction.
- The live AOGM pass exposed a second runtime defect: an older vault revision imported nine weighted items at `0` carried load. Finalization now creates identified starting records with authored charges, capacity-checks them in caste order, attempts source-probed automatic equipment, and derives load from personal wealth plus package item weights. Vault import normalizes that total against the target package, and save restore rejects a mismatched total without replacing the active session. Castle's premature `numitems` increment for an overweight starting record is not reproduced as a blank item.
- Providence schema v2 already preserves the required immutable item, race, and caste fields; no compiler or package migration is required.
- Godot MCP Pro exercised the installed AOGM package through vault import, adventure start, and the populated inventory workspace at 960×600. The imported level-three fighter entered play at `632/2880` carried load, and the selected item exposed its individual weight, proving the repaired value through package load, vault clone, session view, and presentation. That live pass also exposed an invisible Equip control caused by referencing a nonexistent donor bitmap; actions without verified bitmap art now render as labeled themed buttons instead of blank hit targets.
- The prior carried-item pass completed at 2,770 assertions across 14 suites and 46 differential cases.

## Current rolling fidelity pass — Ordinary shop and paid identification

- Remake supplied the application-workflow lead: a dedicated stock/party workspace, buyback handling, equipment guards, and paid-identification controls. Castle, not Remake's generic rate model, determined the rules.
- `EconomyRules` now owns Classic buy and sale valuation. Buy uses absolute cost, current shop inflation, and the 32000 cap. Sale integer-halves cost, scales positive-charge definitions by current/authored condition, caps sale inflation at 100 percent, and applies the unidentified one-fiftieth penalty. `FD-ECONOMY-001` gives a zero-authored-charge definition full condition rather than reproducing Castle's undefined `0.0 / 0.0` conversion.
- The serializable Classic shop continuation now purchases identified authored-charge instances, requires ordinary sales to be unequipped and accepted by the configured ranges, returns sold items to base or normalized buyback stock, and identifies one exact carried instance for twenty gold with sound 683. Wealth, inventory, stock, identification, and the pending request remain session-owned.
- The typed shop surface shows total payable gold, player-knowable item names, exact prices, quantities, and disabled reasons. Unidentified inventory no longer leaks the true definition name; unaffordable purchases, equipped sales, rejected goods, and unaffordable identification remain visible but disabled.
- Providence schema v2 already carries every price, charge, name, inflation, and occupied-stock field needed for the implemented workflow. It does not retain empty native shop-slot indices. Realmz 2.0 therefore preserves buyback identity and quantity in additive nested `.r2save` v3 state while recording exact first-empty slot ordering as a compiler gap instead of inventing it.
- Castle's configured-sale-range counter has another suspicious edge: one active range never reaches its two-failure rejection threshold. The runtime preserves that control flow for now and records it as unresolved; it is not generalized into a cleaner rule without authored evidence.
- The complete typed suite now passes 2,810 assertions across 14 suites, and the differential validator resolves all 47 cases against the clean pinned Castle and Remake worktrees plus the current Providence compiler.

Next application-workflow priority: reconstruct the nine Classic temple services and their exact costs, eligibility, condition mutations, curse-removal spell-list interaction, restoration service, wealth handling, sounds, and save boundaries. Treat the current one-button full-heal implementation as a known approximation to replace, not a donor. Then take ordinary treasure/loot distribution. Split/join load and native shop-slot provenance remain separate fidelity/compiler decisions.

## Current rolling fidelity pass — Classic temple and contextual services

- Classic opcode 32 now records temple availability and its signed cost percentage; opcode 49 records bank availability. Neither opcode forces an application screen open. The Services workspace exposes those contextual commands through an ordinary typed intent and clears them when movement leaves the location.
- `TempleRules` owns all nine fixed services and their exact base costs. Healing, disease, stone, poison, blindness, curse-linked equipment removal, Revive's health/trained-ability mutation, and the surprising unconditional Animated/Stone cleanup follow the pinned Castle control flow. Costs preserve short wrapping and Castle's negative-cost wealth increase instead of silently imposing a modern clamp.
- Temple payment uses pooled gold before the selected character's gold and updates carried load. Pool and Share cover all three denominations in Castle order. When banking is concurrently available, every banked denomination enters the temple pool and returns on exit.
- The temple workspace is a typed character-first interaction: it displays health, personal and pooled affordability, load, the first five conditions, all nine prices/descriptions, and explicit disabled states. Service selection, insufficient funds, Pool, Share, bank-backed exit, and nested no-bank exit confirmation all remain saveable session continuations.
- Providence schema v2 already preserves the signed action operand, conditions, trained abilities, wealth, curse identity, and equipped state. No package or compiler migration is required. The differential ledger now contains 48 validated cases.

Flagged boundaries: the later application-hook pass now preserves and runs Global macro 5 before contextual temple entry. The no-bank pooled-wealth departure gap recorded during this temple pass is now closed by the later typed Swap continuation. Negative temple percentages are source-valid but likely an authoring/input defect and should be revisited only with scenario evidence.

The application workflow audit selected ordinary treasure and terminal battle return as the next complete tranche. The implementation below closes the default reward path while retaining Castle's special battle modes and incidental random drops as explicit evidence gaps.

## Current rolling fidelity pass — Ordinary rewards and terminal battle return

- Scenario treasures and ordinary battle rewards now enter one typed, session-owned reward continuation. Wealth, shared experience, and exact pending `ItemInstance` values commit once; assignment, leave-behind, capacity failure, Pool, Share, denomination Swap, Detect Magic, Identify Objects, completion confirmation, and every pending request survive canonical save/restore.
- Dead or animated recipients are excluded from experience in source order. Each positive recipient gains at most one level during one reward close, matching Castle's caller control flow rather than automatically consuming every positive threshold. The source-backed level transaction publishes a dedicated result stage; eligible spellcasters then receive a serializable spell-selection stage that permits valid unspent points.
- Ordinary victory now proceeds through ally selection, exact fumble recovery, prevalidated monster rewards, treasure, level results, spell selection, after-message, cleanup, and one return to the originating VM or exploration continuation. Defeat and full-party retreat complete without reward draws; a character who escapes before the remaining party wins is excluded from that victory's experience division. Re-entry cannot reroll or replay a completed reward.
- `FD-ECONOMY-002` permits exact-instance treasure assignment at precisely maximum load. Castle's strict-less-than test rejects that legal boundary and separately ignores charge weight; the chosen rule uses one complete instance weight for both validation and mutation.
- The presentation gallery covers populated, empty, capacity-blocked, unidentified, oversized, level-result, and spell-selection states. This is synthetic functional evidence, not ordinary AOGM acceptance.
- The workflow audit now derives 6 blockers, down from 9, and the differential ledger validates 52 bidirectionally linked cases.
- The complete typed suite passes 2,947 assertions across 15 suites, including terminal cleanup through both direct-session and VM-owned battle continuations.

Flagged rather than copied: Castle's two less-than-ten parchment/ration drops, battle mode 5 XP-only behavior, battle mode 10 restoration behavior, opcode 48 bonus treasure, and progression beyond the thirtieth threshold still require controlled fixtures or package-contract evidence. The complete ordinary AOGM battle/reward/return presentation also remains uncertified.

Scenario-owned ordinary total defeat now crosses a typed, saveable VM-to-session handoff into Global Party Death. Unrecovered defeat closes without running later scenario instructions; opcode 119 revival suppresses rewards, releases combat once, and resumes the exact Classic or Safe caller. Castle's mode-10 encounter restart and opcode-56 target-minus-one backup/experience-loss return remain explicit unavailable branches. Providence schema v2 already retains the positional EDCD words needed for these callers, so the remaining work is runtime behavior and controlled oracle adjudication rather than a presumed schema revision.

Next application-workflow priority: use the regenerated audit queue for remaining AOGM blockers—ordinary item use/targeting, field and camp spells or scrolls, full Pool/Share/Swap access, bank denomination behavior, and complete camp/rest—while scheduling one ordinary AOGM battle acceptance pass as soon as a stable reachable battle is available.

## Current rolling fidelity pass — Charged spell-item use

- Castle `items`, `canuse`, `getchoice`, and combat choice establish the bounded common path: an item's second special field names a spell, `abs(sp1)` supplies fixed power, positive charges lose one charge and per-charge load, negative authored charges remain reusable, and the item sound is requested at its ID plus 600. Field type 5 targets the user, target types above 2 target the party, and other field forms stage a character choice; combat returns through the ordinary spell-target lifecycle. Item use spends no spell points or known-spell slot.
- `GameSession` now owns field item selection through a typed, saveable continuation. It rolls field power value 8 through the central RNG before staging the target, rejects stale/duplicate/wrong-count responses without spending a charge, and publishes exact item, spell, target, power, charge, load, and sound facts only after commit.
- `CombatFlow` exposes only source-probed fixed-power actor and automatic-group item uses for the active character. The scenario runtime and direct session both carry exact item-instance and target IDs; malformed responses are transactional. A committed use spends Castle's twelve movement and two half-attack units, does not spend spell points or count as a memorized-spell cast, and returns through the ordinary battle interaction.
- Inventory and battle presentation consume detached availability/options and emit stable identities only. The fixture gallery includes a populated charged-item combat state; no presenter derives permission, targets, power, charges, range, or spell effects.
- Providence schema v2 already emits item type, category/restriction fields, charges, per-charge weight, drop flag, sound, all five special fields, and referenced spell records. No compiler, package-schema, or save-envelope revision was required.
- The audit now reports 5 blockers, down from 6, with 53 bidirectionally linked differential cases. `classic.inventory.use-item` remains partial because distinct item families are not being hidden behind the implemented subset.
- The complete typed suite passes 2,999 assertions across 15 suites, including direct-session and scenario-host combat item requests, invalid-response transactionality, random-power rollback/staging, target-selection restoration, fixed-power continuation integrity, and unidentified-name safety.

Flagged rather than copied: Castle decrements a positive charge before the abortable field picker and ignores `getchoice`'s return value, which appears to lose a charge on cancellation. A controlled Castle fixture must settle that observable before a named preservation/correction decision. Door/XAP items, item type 13 scrolls, missile-class items, random-power combat staging, battlefield area/repeated targets, and broader spell specials remain explicit unavailable families.

Next application-workflow priority: take the highest remaining AOGM blocker from the regenerated audit—field/camp spells and scrolls, manual Pool/Share/Swap access, or complete camp/rest—after checking its Providence prerequisites and ordinary AOGM reachability. Schedule a live charged-item observation alongside the next stable AOGM route rather than upgrading synthetic evidence to certification.

## Current rolling fidelity pass — Ordinary Pool, Share, and Swap

- The audit's two money-workflow blockers now use the declared `MONEY_ACTION` boundary. Pool, Share, and exact character/pool transfers commit synchronously inside `GameSession`; presentation emits stable character and denomination identities and never mutates wealth or calculates capacity.
- The detached Services money workspace exposes pooled, banked, and personal gold, gems, jewelry, carried load, exact Classic increments, and every core-owned unavailable reason. Pool and Share are party actions; Swap selects one character and moves five gold or one gem or jewelry per action. Done returns to exploration without another session mutation.
- Castle controls denomination order and side effects. Pool removes denomination load, Share processes jewelry then gems then gold in party order, Swap uses sounds 10051 toward a character and 663 toward the pool, Pool/Share use sound 128, and every successful operation recalculates Classic movement. Existing reward and temple wealth paths now use that same movement boundary; reward money controls also publish their source sounds.
- `FD-ECONOMY-003` records a source defect rather than silently adopting it: Castle Share can add fifteen-load jewelry to a character with only one free unit, even though Swap rejects the identical transfer. Realmz 2.0 requires the complete denomination weight to fit and leaves unassignable wealth pooled.
- Providence schema v2 needs no change. The workflow consumes mutable party wealth, package-validated character identity, race, class, conditions, and load already present in the session aggregate.
- The regenerated audit reports 3 blockers, down from 5, and 55 bidirectionally linked differential cases. Pool/Share and ordinary Swap are functional from synthetic simulation, persistence, and presentation evidence; route-open/Done audio, contextual money changing, bank semantics, and movement-time pooled-wealth handling remain explicit follow-up work.

Next application-workflow priority: ordinary AOGM battle certification or the field/camp spell blocker, selected by reachable live evidence. Banking denomination semantics and complete camp/rest remain major application-depth work, not hidden extensions of this money pass.

## Current rolling fidelity pass — Field spells, Camp mode, and scroll cases

- Providence commit `202c87f1b4d856ec968760ed982f6ea6eeae3204` now preserves Castle's signed plain-character spell fields while retaining unsigned special and spell-class bytes. Negative spell costs therefore reach the runtime as fixed-power records instead of being reinterpreted as large unsigned values. The synthetic package also contains a type-13 scroll case and charged Classic item 806 parchment; schema v2 and its hash remain stable.
- Field casting validates the living caster, known slot, context, supported special, power, absolute spell-point cost, and target before mutation. One serializable character-selection continuation preserves the exact caster, spell, power, and source; duration and base damage roll once, field targets bypass combat magic resistance but retain Castle saves/protection, and committed results publish source-shaped sound and mutation facts.
- `FD-SPELL-001` corrects Castle's pre-picker spell-point loss. A cancelled or malformed target response does not spend points or consume RNG; the transaction commits only after a valid typed target response.
- Camp is now session-owned persistent mode rather than the prior invented eight-hour rest command. Entering clears location services and advances Castle's five scaled time clicks; explicit Break Camp advances two. The clock and mode survive save/restore, and movement/Search fail with exact reasons while the unresolved source continuations are unavailable.
- Every character owns five fixed typed scroll slots. While camped, a living spellcaster with an equipped type-13 case can scribe a known spell into the first empty slot by spending twice its power-scaled cost and one charge/load unit of item 806. Field scroll use spends no spell points, stages the same typed target boundary, and consumes the exact slot only after a valid response.
- `FD-SPELL-002` corrects Castle's clear-before-picker scroll behavior in both field and combat use. Invalid or cancelled targeting cannot destroy the scroll. Combat use preserves fixed power, no-SP resolution, ordinary targeting/effects, and Castle's twelve-movement/two-half-attack cost. Field-invalid discard and type-13 case transfer with its five records remain explicit gaps.
- The regenerated workflow audit reports 2 blockers and 59 bidirectionally linked differential cases. Camp/rest, item use, and field/camp casting remain partial because their named unresolved variants are not being hidden behind the completed subset.
- Godot MCP Pro opened the current synthetic package through the ordinary library, completed the five-step creator, began exploration, entered Camp, and opened the Spells workspace at 960×600. This caught and corrected two presentation defects before commit: the detached view omitted sub-hour clock minutes, and flexible scroll rows collapsed their labels into vertical text. The final frame shows `Day 1 • 00:25`, the Camped state, and full-width fixed scroll-slot rows. This is a presentation smoke test over synthetic content, not ordinary AOGM certification.

This section's original held-Rest and departure gaps are closed by the later Camp/Rest workflow pass. The former `core.party.camp` Safe Action was removed because its fixed eight-hour heal was neither the player-facing Camp command nor a source-backed reusable scenario operation.

Next application-workflow priority at this checkpoint was ordinary battle/reward acceptance; the later Camp/Rest pass closes ordinary Rest/departure while retaining timed midnight and suspicious recovery branches as explicit audit gaps.

## Current rolling fidelity pass — Ordinary AOGM battle and reward return

- A fresh Providence schema-v2 AOGM package entered Battle 46 through the ordinary campaign, creator, exploration, AP, and interaction UI. The run observed source-shaped placement, the first player actor, legal movement, complete player/monster melee exchange, save before the first action, and restoration into the same live battle.
- The terminal run completed victory, the no-eligible-allies stage, eighteen gold, 1,602 experience, twenty-four exact loot instances, leave-behind processing, Pool, Share, reward completion, combat cleanup, return to exploration, and a post-battle save. The test character did not level, so the live evidence covers experience award but not the level-result or spell-selection stages.
- Live use exposed a runtime/application inconsistency: `CombatState.outcome` serializes an active battle as `active`, while three detached-availability branches tested for an empty value. Combat Move now derives from enabled `CombatView` probes, and exploration Move/Search plus field item/spell actions remain unavailable during active combat.
- Live use also exposed a presentation routing defect: completed rewards released combat correctly but left the shell on an empty Battle workspace. The shell now returns Combat to Exploration after combat state disappears while preserving a different route deliberately opened by the player.
- The audit now reports 1 blocker, 28 major gaps, 60 differential cases, and 38 functional Classic workflows. Ordinary AOGM evidence clears the battle-entry blocker and the stale Combat Move gap; it does not create a certified workflow.

Flagged rather than accepted: the combat interaction sheet obscures the battlefield and makes actor, terrain, route, and target state difficult to read. Ordinary missile combat, defeat, retreat, and an actual level-up remain unobserved. Castle's incidental random reward items, battle modes 5 and 10, and opcode 48 bonus treasure remain source/compiler gaps rather than inferred behavior.

Next application-workflow priority: reconstruct the tactical workspace so the board and legal actions remain readable, then use the audit queue to close ordinary missile/defeat-or-retreat/level-up evidence without delaying the larger application workflows for more isolated spell formulas.

## Current rolling fidelity pass — Classic tactical battlefield

- Castle's battlefield art contract is composited rather than singular. `GWorldInit` seeds a 640×640 graphics world from PICT 302, `loadpixmap` overwrites the upper 640×320 with the active landlook, and `centerfield` presents an actor-centered 16×14 grid of native 32-pixel cells. `combatchoice` keeps commands outside that viewport.
- Providence commit `4f0ea8fedea6bca2e197c4eb900b80e25eb767e8` now exports one complete content-addressed `classic-battle-tiles-302` atlas when reachable battles exist and declares `realmz.presentation.battle-atlas-v1`. The existing active-landlook atlases supply battle IDs 1–200; shared PICT 302 supplies 201–400. The runtime rejects absent, malformed, or capability-mismatched battle art before play.
- `ClassicBattlefieldPresenter` consumes only detached `BattlefieldView` and package media. It centers and clamps the native viewport on the active actor, renders exact party/monster appearance identities, outlines the active actor and target, and displays rules-owned legal or blocked movement without answering simulation questions.
- The typed combat component now occupies the bottom Classic control region instead of mounting a full-stage interaction sheet. Attack, eight-direction movement, item use, Guard, Finish, and Escape remain keyboard-focusable while the tactical board and party roster remain visible.
- The regenerated audit keeps 38 Classic workflows functional and removes `GAP-COMBAT-002`; one unrelated Classic blocker remains. The new tactical workflow is synthetic functional evidence, not accepted or ordinary-campaign certification.

Flagged rather than hidden: pointer-owned spatial-spell targeting still uses the interim coordinate controls, and packages exported before the new Providence checkpoint do not contain the required shared battle atlas. A fresh AOGM export and ordinary battle pass are required before this presentation can gain live evidence.

Next application-workflow priority: use the remaining audit blocker and major-gap queue, with ordinary missile/defeat-or-retreat/level-up evidence and pointer-based spatial targeting scheduled as bounded combat follow-ups rather than reopening settled combat formulas.

## Current rolling fidelity pass — Classic Camp and Rest workflow

- Camp remains a mode, not a fixed-duration heal. Entry clears contextual services and advances five source-scaled time clicks; explicit exit advances two. A separate typed Rest intent represents one held-control pulse, subtracting two fatigue before five time clicks so presentation repeat cadence cannot become simulation time.
- `ClockRules` now owns Classic field-time boundaries: hourly condition reduction, fatigue, character and ally spell-point recovery, and noon/midnight character and ally health recovery. Item 877 is consumed through exact inventory charge/load rules once per injured character when available.
- Land movement from camp commits fifteen time clicks before the requested move; dungeon movement commits two. Random rectangles are checked during that departure. An interrupting random battle retains a validated post-time continuation in combat state, survives save/reload and reward interactions, and resumes the requested move exactly once after battle return.
- The old Safe Scenario Action capability `core.party.camp` was removed. It performed an invented eight-hour full-heal transaction and could not truthfully represent either Castle's Camp command or held Rest loop. Scenarios may still use the explicit source-neutral time capability when authored behavior actually requests elapsed minutes.
- The focused workflow suite covers ordinary Rest, hourly recovery, noon ration use, Camp entry/exit, random choice interruption, automatic departure, active-battle save/restore, reward drain, and post-battle movement return. The audit records these as synthetic functional evidence; no ordinary AOGM Rest acceptance is claimed.
- The regenerated audit reports 60 Classic workflows, 8 host workflows, 1 blocker, 62 bidirectionally linked differential cases, and 36 functional Classic workflows. The earlier 38 count overcredited timed-event dispatch in both the clock and encounter workflows; correcting the denominator is evidence hygiene, not a product regression.

Flagged at this stage rather than copied: Castle adds its signed Poisoned condition value to half-day healing, its timed-encounter location code guards the y comparison with `recx`, and the `continueday` loop may repeat recovery around multiple records. The later timed-midnight and poisoned-boundary passes resolve these questions through complete control-flow characterization and supersede this historical uncertainty.

Next application-workflow priority: follow the regenerated audit queue. Timed midnight dispatch is now visible as its own major gap rather than being hidden inside “time complete”; ordinary AOGM Camp/Rest acceptance and the remaining scroll branches remain separate evidence tasks.

## Current rolling fidelity pass — Party Order

- Completed the AOGM-reachable `classic.character.reorder-party` workflow through Castle/Remake comparison, typed simulation, central persistence, and the Character workspace.
- Party order is now one complete stable-ID permutation. Duplicate, unknown, omitted, solo, setup, pending-interaction, and battle requests cannot partially mutate the party; accepted requests preserve complete character state and consume no RNG or time.
- Move Up and Move Down are presentation-owned draft operations. Apply emits one typed intent, while Cancel safely discards the draft under `FD-CHARACTER-003` instead of copying Castle's apparent abort-time out-of-bounds write.
- The generated audit moves Character management from three missing workflows to two and reports 37 functional Classic workflows, 28 major gaps, one release blocker, and 63 differential cases. Evidence remains synthetic rather than ordinary-play certification.

That pass handed off to `classic.character.view-sheet`; the subsequent inspection and appearance passes below supersede this historical next-step note.

## Current rolling fidelity pass — Classic character inspection

- Reconstructed the Character workspace around Castle's complete inspection family instead of extending the old summary cards. Seven inspection tabs expose overview, conditions and saves, equipment, abilities, spells, race/class/aging, and the lifetime record from detached `CharacterView` data; the following appearance pass adds an eighth workflow tab.
- Added source-backed names for all forty condition slots, eight saves, eight documented monster-special categories, known trained abilities, three personal wealth denominations, attacks-per-round display, race/caste descriptions and traits, and all five age rows. Disabled/dead and long-content states are covered by the typed presentation fixture.
- `FD-CHARACTER-004` corrects Castle's view-time Aging mutation: the active age row is derived for display without changing the session-owned current age group. Providence schema v2 already preserves the required race/caste and aging metadata, so no compiler change is required.
- Lifetime prestige remains explicitly unavailable because Realmz 2.0 does not yet track Castle's lifetime combat-history counters. Several ability labels sourced from absent native STR# resources remain honest generic slot labels. Both are audit gaps, not silent defaults.
- The regenerated audit reports 60 Classic workflows, 8 host workflows, 1 blocker, 28 major gaps, 37 functional Classic workflows, and 64 bidirectionally linked differential cases. Character-sheet presentation is functional, but the workflow remains partial until lifetime history is owned.

That pass handed off to active-character appearance change, completed in the following rolling pass. Lifetime history remains scheduled with the combat/magic mutation audit it requires.

## Current rolling fidelity pass — Active character appearance

- Completed `classic.character.change-appearance` through Castle/Remake comparison, existing Providence schema-v2 content, typed simulation, central persistence, and the Character workspace.
- Castle's two commands remain independent: portrait recommendations use Data Race `defaulticonset`, while tactical-icon recommendations use the one-based race slot. The runtime exposes all 120 options for each exact package role and does not auto-pair an active character's other identity.
- `FD-CHARACTER-005` corrects ownership rather than visual behavior. Castle writes both the campaign slot and standalone character file when a picker closes; 2.0 stages previews locally, applies one stable-ID role through `GameSession`, discards without mutation, and leaves vault publication explicit.
- Restore now rejects unknown or wrong-role portrait/icon IDs before replacing an active session. Accepted changes consume no RNG or game time and persist through the unchanged `.r2save` v3 character fields.
- The regenerated audit reports 38 functional Classic workflows, 27 major gaps, one release blocker, and 65 bidirectionally linked differential cases. Evidence is synthetic; presentation is not yet ordinary-play accepted.

Next application-workflow priority: return to the audit's AOGM queue. The adjacent Character gaps are either party-setup inspection or lifetime combat history; the former is bounded UI work, while the latter requires broader combat/magic counter ownership and should not be filled from guessed zeros.

## Current rolling fidelity pass — Classic bank-backed Swap

- Castle does not implement banking as arbitrary gold deposits and withdrawals. Opcode 49 enables a location context; opening the ordinary Swap modal moves every banked gold, gem, and jewelry record into the shared pool once. Pool, Share, and character transfers retain their ordinary rules, and Done closes only the modal.
- Realmz 2.0 now presents that workflow through the typed contextual Bank command. The request contains detached pooled, banked, personal, load, exact-increment, and unavailable-reason facts. Responses may Pool, Share, transfer five gold or one gem/jewelry, or finish; the presenter owns no legality or mutation.
- Bank entry and every response survive the existing central save boundary without a second bank drain. Done requests Castle sound 141 and leaves banking active. A source-legal location departure returns the complete remaining pool to the bank, clears the pool, and disables the contextual service exactly once.
- The same archaeology exposed a related shop defect: Castle shop entry imports all banked denominations and shop exit returns the complete remainder. The runtime now does both without changing the shop continuation or `.r2save` v3 envelope.
- Providence schema v2 needs no change. Opcode 49 is already compiled and all banked, pooled, and personal denominations are mutable session state.
- The later application-hook pass reclassifies Shop and Temple's contextual Global hooks as compiled and session-owned while retaining their ordinary-play and native empty-slot gaps. Bank-backed Swap remains functional from source, simulation, persistence, and presentation evidence; it is not certified because no ordinary campaign bank route has been exercised. Counts belong to the generated workflow report rather than this historical narrative.

Flagged rather than hidden: Castle's bank is visually the generic Swap modal, while 2.0 keeps a separately labelled Bank command for discoverability. This does not alter the source mutation sequence. Money-changing rates and ordinary-campaign bank acceptance remain separate workflow gaps; the no-bank pooled-wealth departure workflow is completed in the following pass.

Next batch priority: finish the coherent service-workflow closeout through one ordinary AOGM contextual service route, then return to the regenerated major-gap queue rather than reopening isolated economy formulas.

### Ordinary Swap lifecycle audio

- A deliberate ordinary Services-route entry now requests Castle's Swap-button sound 141, stops existing channels before modal sound 3003, and leaving that route through Done, Back, a menu, or a shortcut requests sound 141. Repeated rerenders and same-route mutations do not replay the sequence.
- The lifecycle remains presentation-owned. Pool, Share, and exact transfers still use `MONEY_ACTION`; opening or closing the workspace does not create gameplay state, consume RNG, or alter saves.
- The shared Services route suppresses ordinary Swap audio when it was opened to host a typed Shop, Temple, or Bank interaction. Those workflows retain their own source-backed entry and exit events.
- Focused Tier 2 verification covers the route state machine and the existing audio presenter path. This closes `GAP-SVC-006`; contextual money-changing rates remain separate.

Flagged preference boundary: Castle persists `reducesound` and uses it to suppress modal ambience such as sound 3003 while retaining button, transfer, and Done sounds. Realmz 2.0's default-audio sequence is source-shaped, but the preference itself is now tracked as `GAP-SYS-003` until modal sounds are comprehensively classified.

### No-bank pooled-wealth departure

- Castle resolves a nonempty no-bank money pool before destination passability. Realmz 2.0 now yields one save-owned yes/no warning for every valid direction, including a direction that will later be blocked; malformed directions still fail before opening the warning.
- The question requests Castle sound 20005 after stopping existing audio. Declining clears all three pooled denominations and resolves the original direction exactly once. Accepting opens typed Swap with sound 3003; Pool, Share, exact transfers, and their unavailable reasons remain core-owned.
- Done requests sound 141, clears any remaining pool, and resumes the saved ordinary movement exactly once. Declining does the same after clearing the pool. Warning, distribution, empty-pool, and post-transfer states restore transactionally through the existing `.r2save` v3 continuation envelope; forged directions, stale response IDs, and mismatched request payloads are rejected.
- `classic-pooled-wealth-departure-source-observation.json` records the pinned control flow. The exact STR# 135 question-6 wording is absent from the source tree, so the current prompt is explicitly synthetic and remains eligible for replacement by an authoritative resource/runtime observation.
- Providence schema v2 needs no change because this workflow consumes only mutable session state. Remake's one-entry warning/next-entry clearing behavior is recorded as divergent rather than ported.
- Castle's separate internal `moveparty` fallback returns before coordinate mutation after accepted distribution, but ordinary land and dungeon callers invoke `checkmoneypool` earlier and continue. The ordinary application workflow follows those callers rather than substituting the fallback.
- The audit now records `GAP-EXP-006`: Castle advances tile-specific time on several blocked land and dungeon attempts, while 2.0 currently publishes `movement_blocked` without that downstream time/random processing. That is a distinct major movement gap requiring its own archaeology and fixture.
- The regenerated audit reports 36 functional Classic workflows, 29 major gaps, one host release blocker, and 67 bidirectionally linked differential cases. The completed pooled-wealth variant remains inside Travel and Swap; Travel becomes partial because the newly evidenced blocked-time variant is unresolved.

This is the third focused workflow commit in the service/money batch. Batch closeout now requires one aggregate verification, one coherent MCP walkthrough, and one final source-intelligence regeneration.

## Current workflow batch — Classic information surfaces

- Completed pre-adventure character inspection across campaign setup and the vault. The setup shell now exposes the same detached character facts used by the active Character workspace without importing a character or mutating the session.
- Completed ordinary item inspection. Identified, unidentified, concealed-curse, and revealed-curse records present source-backed facts and restrictions; disabled actions expose their reasons directly. Castle's `iscurse` field is treated as the unworn decoy identity, while equipping reveals the original cursed record.
- Audited acquired player maps end to end and rejected the previous false implementation. Castle opcode 29 addresses `Data MD2` player-map records, not playable land levels. Providence parses those records but schema v2 does not export them, so 2.0 now fails with `player_map_contract_unavailable` without persisting a false acquired-map ID.
- The acquired-map workflow remains missing, with separate compiler and presentation gaps. Its follow-up must add names, picture/scrolling-text/crop modes, markers, notes, and exact media reachability across Providence and Realmz 2.0 before enabling opcode 29 or the Journal browser. Castle's apparent ID-20 acquisition-array overflow remains an explicit fidelity decision.
- The audit reports 60 Classic workflows, 8 host workflows, 38 functional Classic workflows, 28 major gaps, one host release blocker, and 68 bidirectionally linked differential cases. The map workflow's move from partial to missing is a corrected denominator, not a product regression.
- The risk-tiered method was applied as intended: each workflow received focused Tier 2 verification and its own commit; the three-workflow batch scheduled the aggregate suite, source-intelligence regeneration, clean-reference validation, and MCP walkthrough only at closeout. Bounded archaeology was delegated where suitable, while fidelity adjudication, integration, and final status remained Sol-owned.
- The first MCP walkthrough found the Equipment heading's slot count collapsing into a one-character column at 960×600. A bounded presenter repair reserves proportional width for heading details, adds a regression assertion, and passed the policy's one post-repair Tier 3 rerun plus a second visual walkthrough. MCP input injection worked; the MCP game-inspector tree/screenshot bridge timed out independently while the responsive game was inspected through Windows Graphics Capture.

Next application-workflow priority: select the next 3–5 adjacent AOGM gaps from the regenerated audit queue. The acquired-map compiler tranche is high-risk and cross-repository; it should be scheduled as a dedicated package-contract batch rather than mixed into ordinary presentation work.

## Current rolling fidelity pass — Timed midnight encounters

- Crossing midnight now owns an ordered, serializable timed-encounter scan. Matching records advance their effective day before the chance draw and item, quest, level, rectangle, and coordinate gates; eligible records enter the ordinary scenario VM and resume the remaining scan after each typed interaction.
- Midnight health and ration recovery is deferred until timed eligibility has been evaluated. It commits once before the first dispatched record or once after a scan with no dispatch, and its cleared pending flag survives save/restore so an acknowledgement cannot replay healing or consume another ration.
- `FD-EXPLORATION-001` corrects Castle's y-field guard and inconsistent movement-location globals to the semantic attempted/current/committed party coordinate. `FD-EXPLORATION-002` corrects Castle's `continueday` loop, which can repeat half-day recovery once per eligible record.
- The session rebases the remaining timed scan and later random-region work after an active timed VM program moves the party. A dedicated location-changing timed AP with post-action destination still needs end-to-end characterization before that branch is removed from the audit queue.
- Providence schema v2 already preserves all required timed fields and source order, so this pass changes no package contract. Focused tests cover chance failure, exact gates, day mutation, event/RNG order, staged text, one-time recovery, and interaction save/resume.
- The regenerated audit reports 60 Classic workflows, 8 host workflows, 39 functional Classic workflows, one host blocker, and 70 bidirectionally linked differential cases. Timed variants and Camp/Rest are functional, while the encounter workflow remains partial for location-changing AP destination proof.

Flagged rather than copied: Castle's successful land path checks one tile beyond the committed destination, dungeon time checks the pre-move floor, y-only requirements can be skipped, and multiple eligible records can multiply healing. The first three coordinate artifacts and repeated recovery are named corrections; fatigue and torch behavior are resolved in the subsequent hourly-clock pass.

### Poisoned half-day recovery

- Complete Castle control flow resolves the apparently suspicious signed term without a runtime correction. `reduce()` first damages the character by the positive Poisoned value and decrements that value; noon or midnight recovery then adds the remaining value to the ordinary level-based recovery. Across the complete boundary, Poisoned therefore changes the result to base recovery minus one rather than granting extra health.
- The existing clock implementation already preserves that order. A source-shaped regression now proves pre-decay damage, post-decay recovery, event ordering, and the resulting unchanged health for a level-six unrationed character with Poisoned 3.
- No package, save, RNG, VM, or presentation contract changed. `classic.exploration.camp-rest` is now functional from source, simulation, persistence, presentation, and ordinary AOGM evidence; the broader clock workflow is completed in the following hourly fatigue/light pass.

### Hourly fatigue and light

- Castle's `if (timeclick)` expression is a function-symbol typo and therefore always true, but it sits inside the genuine hour-crossing branch. Its observable result is simply one bounded fatigue point per hour, which 2.0 already preserved.
- Torch Lit is deliberately reduced in both the generic party-condition loop and a second torch-specific branch. Combined with light special 50's `30 * power - 1` initialization and darkness bands, this is a distinct two-point hourly duration scale rather than an accidental duplicate generic condition tick. Realmz 2.0 now matches it without allowing the final one-point value to become negative.
- Castle can leave stale incremental torch art when a value of two reaches zero because its redraw flag is captured before both decrements. Realmz 2.0 does not copy this presentation artifact: detached light state reaches zero once and disposable presentation caches rebuild from that truth.
- Focused source-order tests cover values three, two, and one, expiration event cardinality, the ordinary Rest hour boundary, and fatigue ordering. The regenerated audit reports 40 functional Classic workflows and 71 bidirectionally linked differential cases; `classic.exploration.time-fatigue-light` is now functional.

### Exploration cadence batch closeout

- Closed four adjacent workflows under the risk-tiered policy: blocked positive land-attempt timing, timed-midnight dispatch and continuation, poisoned half-day recovery, and hourly fatigue/light decay. Each landed as a focused-verified commit before the batch gate.
- Tier 3 passed 3,512 assertions across all 20 typed suites, architecture and export-contract checks, teardown-leak rejection, 71 differential cases, and the complete 60-Classic/8-host workflow inventory. Clean pinned-reference validation also passed. Providence was unchanged, so no compiler gate was required.
- Godot MCP Pro opened the current 960×600 fixture shell, inspected the scene, played the main scene, and accepted input. Its runtime-tree and screenshot bridge timed out; Windows Graphics Capture completed the visual fallback. The ordinary UI created a character through all five steps, entered exploration, and advanced Camp/Rest from 00:00 to 00:25 to 00:50 without overlap or inaccessible controls. The editor log contained no runtime errors after stop.
- The current audit reports 40 functional Classic workflows, one host release blocker, and 71 bidirectionally linked differential cases. This visual evidence is a synthetic fixture walkthrough, not ordinary AOGM certification.

Next batch priority: use the regenerated audit queue to choose 3–5 adjacent application workflows. Preserve the unresolved movement/Swap observations—ordinary callers continue movement after distribution, while the direct `moveparty(1)` fallback returns—and settle them only in the dedicated source-backed workflow that owns movement resumption.

## Current workflow batch — AOGM combat feedback and command parity

- The workflow inventory now schedules battle entry, tactical movement, physical attacks, combat casting, and manual turn control as the active batch. Auto Turn, persistent character Auto, Delay, Bandage, Turn Undead, and Undo are separate workflows rather than hidden variants of one oversized turn-control record. The previous exploration/services certification targets retain their evidence in the queue.
- Batch 1 adds a presentation-owned combat playback transaction over the previous detached view, ordered committed events, and final detached view. Movement, melee, ordinary projectiles, spell cast/projectile/eight-frame resolution, fixed result numbers, defeat settlement, and ordered sounds cannot mutate or advance simulation. Playback retains the action actor across actorless terrain sounds; turn/focus changes recenter once, while in-window one-square movement holds the existing tactical camera until the actor reaches its edge. Space skips only visuals; reduced motion collapses the visual path while preserving audio.
- Battlefield-native targeting replaces coordinate spin boxes. Actor and repeated-actor selection uses core-provided combatant identities; area selection previews exact Data AD offsets only at core-provided legal centers. Cancel emits no response, while confirmation preserves the existing combat response ABI.
- The integrated application-media catalog now includes proven combat CIcon families in addition to all 142 Classic sounds. Exact scenario keys still override application keys. CIcon 161 is not called blood or gore, CIcon 167 lacks a traced caller, and death-effect timing remains unresolved rather than being presented as settled fidelity.
- Providence schema v2 already preserves `lookStart`, `lookEnd`, spell sounds, and scenario CIcon overrides, so this batch requires no package-contract change. Automated evidence may make these workflows functional, but acceptance and certification still require the ordinary AOGM MCP route and user review.

### Source-backed combat commands

- The follow-on command tranche implements five separately audited workflows: current-turn Auto, persistent character Auto, Delay, Bandage, and Turn Undead. Availability and disabled reasons come from the detached combat view; the command deck and roster submit only stable IDs and typed actions.
- Persistent Auto is save-owned, survives battles, and starts the active character immediately when enabled. Both Auto forms share one deterministic rules routine and stop at manual, mandatory, error, or terminal boundaries. State and RNG roll back together at the bounded operation limit.
- `FD-COMBAT-012` advances a last-slot Delay to the next round instead of reproducing Castle's duplicate-activation cursor artifact. `FD-COMBAT-013` exposes one actual living bleeding recipient instead of Castle's broad manual picker. Turn Undead preserves source target order, signed sentinel, RNG, experience, result effects, and death-macro continuation.
- The inventory now reports 46 functional Classic workflows and 91 bidirectionally linked differential cases. These command workflows remain certification targets until the integrated command deck and reachable variants receive ordinary AOGM evidence.

### Bounded Undo and remaining command parity

- Undo is not a generic state rollback. The save aggregate records only the active character, activation-start cell, round, turn index, and availability. Result-free movement retains the boundary; attacks, spells, item effects, reactions, and turn advancement invalidate it.
- The source-backed ordinary path restores an empty start cell, applies Castle's attack-allocation adjustment, recalculates movement and attacks, requests sounds 664 then ordinary activation 138, and presents the reverse step without advancing simulation.
- `FD-COMBAT-014` rejects an occupied activation-start cell instead of reproducing Castle's unchecked field overwrite. Repeated Undo and first/last-slot or round-boundary re-entry remain explicit runtime-fixture work rather than being guessed from the global cursor.
- The active batch has closed combat scroll use and numeric Fast Spell shortcuts; it next owns Auto Weapon Switch as a host preference translated into an explicit typed combat choice. That interface is not inferred from the existing charged-item, weapon-mode, or persistent-Auto contracts.

## Current workflow batch — AOGM combat commands and return

- The first workflow corrects ordinary melee input. Castle maps keypad, keyboard, and battlefield clicks into a directional `movelook` command; an opposed occupied destination runs Guard first, spends three movement and two half-attack units, attacks, and does not move. Its Target control is the charged missile/item path, not a generic melee picker.
- Realmz 2.0 now carries the hostile identity on the detached directional option, emits the ordinary typed move response, resolves contact in `CombatFlow`, and removes the duplicate melee target row. The Guard/age-update continuation restores and resolves the contact attack exactly once.
- Castle's optional Auto Weapon Switch preference can redirect a physical missile collision to melee. The current 2.0 workflow requires an explicit switch and tracks that difference as a minor parity decision instead of silently choosing a setting policy.
- The remaining batch workflows were ordinary missile Fire, representative combat spell responses, and Escape/edge retreat. Their supported rules already had broad synthetic coverage; this batch adds the missing typed-boundary proof while leaving ordinary-play certification explicit.
- Ordinary missile Fire now has an end-to-end typed boundary characterization: switch mode, regenerate legal targets, serialize and restore battle/RNG, Fire the selected stable target, spend one charge/two half-attacks/twelve movement, and return to the active battle request. This strengthens the implemented workflow without relabelling synthetic evidence as ordinary AOGM proof.
- Representative combat spells now cross the same restored runtime boundary: actor-target responses carry stable actor identity, automatic groups remain targetless and preserve party-then-monster order, and area responses carry only coordinate/rotation while core recomputes the Data AD footprint. These tests strengthen the functional workflow; ordinary AOGM spell certification remains open.
- Escape now has accepted restored-VM coverage while another loyal character remains, and an interior edge-direction command reaches the same confirmation/commit path. Decline, central-save acceptance, forced edge exit, routed-monster withdrawal, and unsafe source branches remain separately covered. Ordinary campaign retreat is still a live-proof gap.
- Batch closeout passes 3,580 assertions across twenty suites, all architecture and package/export contracts, 71 bidirectionally linked differential cases, and the 60-Classic/8-host workflow inventory with one blocker. The Castle, Remake, and Providence evidence references also validate from their clean pinned worktrees.
- Godot MCP Pro loaded the synthetic package, created a party through the typed session boundary, rendered the combat interaction, exposed `Attack E` as the hostile-collision command, moved keyboard focus from Defend to Finish turn, inspected the live interaction node, captured the 960x600 frame, and stopped cleanly. This is synthetic presentation evidence, not ordinary AOGM missile, spell, retreat, or terminal-return certification. The walkthrough also reproduced the already-audited host gap: synchronous package startup occupied the interactive frame for roughly eight to ten seconds and exceeded MCP's five-second command timeout before recovering normally.

### Ordinary AOGM combat certification closeout

- Ordinary AOGM Battle 46 now proves physical contact attacks, target-selected and automatic-group combat spells, explicit character Escape with its prestige penalty, ordinary treasure, Pool/Share, one level result, an independently saveable spell-selection stage, and return to exploration. Dead participants correctly receive no experience; an escaped character remains excluded from the later victory division.
- The level-up witness used a normal campaign-eligible immutable vault revision at level 9 and one point short of the next threshold. Victory awarded 1,842 experience per eligible living participant, advanced Brom Ascendant once to level 10, restored both the exact result request and the separate 18-point spell-selection request from saves, and confirmed the completed spell choice through the ordinary presenter.
- Tier 3 passed 3,981 assertions across twenty-two suites, teardown-leak rejection, all architecture/package/export gates, the 142-resource application-media manifest, 78 differential cases, and the 60-Classic/8-host inventory with one remaining host release blocker. Clean pinned Castle, Remake, and Providence references also passed external symbol validation. Source intelligence regenerated to 6,074 entities, 54,839 relationships, five flows, and 6,162 chunks.
- Godot MCP Pro completed the ordinary route, captured the live result/spell boundaries, confirmed return to exploration, and stopped the scene. Presentation remains functional rather than accepted, and ordinary total defeat, special battle modes 5/10, opcode 48 bonus treasure, and incidental reward quirks remain explicit—not inferred as complete.

Auto Weapon Switch now matches Castle's application-owned, default-on preference: only a qualifying manual projectile collision consults it, Guard resolves first, and the surviving actor switches once before the ordinary contact attack. The pending one-move decision survives interrupted Guard/age continuations without moving the preference itself into gameplay saves.

Next batch: certify the ordinary AOGM Camp/Rest, Pool/Share, denomination Swap, and contextual-service entry workflows before beginning another implementation tranche.

### Ordinary AOGM exploration and services certification closeout

- Ordinary AOGM play now covers the complete selected batch. Camp and Rest were previously observed through the field interface. A six-character party then pooled 1,305 gold, shared it as 218/218/218/217/217/217, and completed a five-gold character-to-pool-to-character Swap round trip without changing the total.
- The live money evidence is deliberately limited to gold. The selected AOGM party had no gems or jewelry, so those denominations retain synthetic coverage rather than being described as ordinary-play observations.
- The party reached an authored AOGM Temple action point through exploration. Its action sequence exposed both Temple and Bank as location services; entering Temple presented all nine fixed services plus Pool, Share, and Leave, and Leave returned cleanly to the field at the same location.
- This closes the contextual-service entry gap, but it does not certify the complete Temple workflow. No service purchase, no-op payment, or service-boundary reload was performed, so `GAP-SVC-004` remains the owning live-proof gap.
- No product code changed in this closeout. The workflow inventory and generated status report record only the variants observed through the ordinary application path.

## Current workflow batch — Session lifecycle and host responsiveness

- Validated save previews are complete: the System workspace indexes primary and backup records independently, shows detached identity/time/location/party facts, disables corrupt or mismatched records, and restores a selected validated source without mutating the active session on failure.
- End Adventure is now separate from Campaign Library and process Quit. Outside battle it offers explicit Save and end, End without saving, and Cancel operations; battle removes the save option. These labels are modern host copy rather than claimed Castle resource text. The host prompt is not gameplay state, noncombat gameplay interactions remain modal, save failure suppresses close, and `GameSession.close` releases the aggregate exactly once.
- `FD-APPLICATION-001` deliberately adds an outside-combat Cancel operation rather than reproducing Castle's ambiguous No-means-discard path. The application-hook pass serializes and runs Castle's Global quit macro slot 2 through a saveable continuation. Ordinary AOGM play now proves Cancel returns to exploration and Save and end returns to campaign setup with a cleared `0/6` party; scenario-owned Party Death integration remains a separate unresolved combat gap.
- Focused host-transaction tests prove save-before-close ordering, no-write discard, and no-op Cancel; save repository tests separately prove transactional write/readback. Full composition-root failure injection remains a named minor test gap rather than being described as verified persistence.
- Process Quit now remains distinct from End Adventure. Both the menu command and `NOTIFICATION_WM_CLOSE_REQUEST` use one host-owned typed confirmation; an active field session may save before quitting, battle cannot save, explicit no-save bypasses persistence, and Cancel or save failure leaves the process open. `FD-APPLICATION-002` records the safety correction from Castle's outside-combat No-means-quit behavior. Castle Quit never runs Global macro slot 2, so this workflow has no Providence dependency.
- Root-level notification and repository-failure proof remains pending until package discovery no longer blocks main-scene startup. This limitation is recorded as a minor test gap rather than treating pure host-transaction coverage as a live window-close result.
- Package discovery now inspects manifest/schema/capability metadata without hashing roughly 828 MB of installed payloads on the interactive frame. Choosing Play runs complete integrity validation and immutable installation in a host-owned worker, exposes detached determinate/indeterminate progress plus cooperative Cancel, and joins the worker during shutdown.
- The worker owns no Nodes or active-session state. The composition root consumes its result exactly once and changes package media only after a replacement session starts successfully, so rejection or failed session construction leaves the existing presentation catalog untouched.
- Focused package repository, worker, and presentation coverage proves structural discovery, deferred hash rejection, success/failure/cancellation, exact-once result handoff, clean join/teardown, and UI progress signaling.
- The MCP closeout selected the installed AOGM package through the ordinary campaign library. The application stayed queryable while complete validation ran, exposed Cancel, disabled competing Play actions, completed successfully once, and on a second run accepted cancellation without creating a session. The 960x600 frame was captured and the scene stopped cleanly. This closes the host validation-progress live-proof gap without claiming campaign-route certification.

Batch closeout: Tier 2 passed 1,017 focused assertions; Tier 3 passed 3,685 assertions across twenty-two suites, the aggregate architecture/package/export gates, 74 bidirectionally linked differential cases, and the 60-Classic/8-host inventory. Clean pinned Castle, Remake, and Providence references also passed external evidence validation. The next workflow batch is selected from the regenerated audit queue.

## Current workflow batch — Maps and Journal

- The batch owns three separate Castle workflows: player-authored location notes, discovered authored journal entries, and acquired player-map records. They must not share state merely because the modern shell presents them under one Journal route.
- Location notes are the first workflow. Castle stores them in separate land and dungeon record streams, preserves source-file order, permits editing only at the current party location, captures land darkness/torch state, clears the native record ID for empty text, and copies both streams with the playthrough save.
- Realmz 2.0 now models that source-shaped mutable state directly: stable map identity, land/dungeon kind, level, coordinate, native location ID, darkness value, record ordinal, and UTF-8 text bounded to the source's 255-byte field. Detached views expose only the current stream in ordinal order; edits consume neither gameplay time nor RNG and restore transactionally.
- `FD-MAPS-001` deliberately limits each stream to the 501 ordinals Castle can search. Castle's append path can write a later record that its own scan cannot find, so overflow is rejected before mutation instead of producing unreachable save data.
- Flagged rather than guessed: the pinned source does not identify the labels for dialog items 1 and 3, and both exit paths appear to write the current text. The modern non-mutating control is therefore labelled Revert draft rather than claiming Classic Cancel fidelity. Historical-note map recentering/darkness presentation and ordinary-play proof remain gaps.

- The second workflow reconstructs manual authored-journal discovery from Data SD2 identity. Positive Classic text can emit Take note through its existing saveable acknowledgement frame; discovered IDs persist in numeric source order and resolve immutable package text on view/restore. `FD-MAPS-002` bounds the table to IDs 0 through 2,999 rather than reproducing Castle's contradictory 5,999 cursor clamp. Auto Note and native edge rendering remain explicit gaps.

- The third workflow completes the bounded acquired-player-map path. Providence commit `2ce5a512d9754d3b3d000641edbfc96884e7e14e` established the acquired-player-map export and emits typed `Data MD2` records, acquired/unavailable names, exact display and media identities, crops, markers, and notes under schema hash `2f9b23f5b5c4c2b056226b945746bf6941436accdbbacc52b6af8507c511d855`. Repeated exports are byte-identical.
- Realmz 2.0 validates that contract independently, acquires opcode 29 records exactly once, preserves positive text and negative typed display pacing, restores the complete pending VM/session boundary, and exposes a mutation-free Maps/Notes browser for picture, scrolling-text, land-crop, and dungeon-crop modes. `FD-MAPS-003` rejects Castle's unsafe ID-20 and zero-icon-divisor paths.
- The workflow is functionally covered by synthetic package, VM, persistence, and presentation evidence. Exact Castle dungeon pixels, styled legacy scrolling text, malformed geometry, and ordinary AOGM acquisition/browsing remain explicit parity and certification gaps rather than inferred success.

## Current host batch — Providence-precompiled packages

- Providence now compiles map topology into the fixed `realmz2.compact-cell-rows.v2` runtime ABI after performing its authoritative schema, reference, topology, media, capability, and deterministic-package validation. The strict schema-v3 Realmz Rebuilt mirror uses schema hash `8911a4de6f6182e7d18a5c1ee2c8ccf2989a5bb22cfcb24c6f9fc4ab19a23602`; package v1/v2 is not adapted at runtime. Topology-v2 preserves exact Classic boat requirements, blocked-attempt timeclicks, and active-landlook tile-60/tile-147 profiles rather than reconstructing those facts in the client.
- Realmz Rebuilt independently validates an arbitrary external `.realmz2` exactly once when installing it. The installer copies the exact compressed bytes transactionally and writes an app-owned receipt carrying schema, campaign/package identity, compressed archive SHA-256, byte count, and modification identity.
- Normal Play of an unchanged app-owned installation no longer repeats package compilation, archive hashing, per-payload hashing, media/reference validation, or topology expansion. It verifies the receipt's cheap immutable-file identity and manifest identity, parses the compiled documents, and lazily materializes topology cells only when simulation or presentation queries them. Changed installed bytes invalidate the receipt and require reinstalling the package.
- On the current development machine, manifest-only discovery of the two installed v3 packages takes about 32 ms. The 6.24 MB synthetic fixture performs a cold full consumer load in about 0.91 seconds. A fresh 20.7 MB Assault on Giant Mountain export takes about 3.26 seconds for first-install validation, while three receipt-backed warm loads take 1.05–1.07 seconds. Discovery therefore remains below the 250 ms target and normal Play remains below the two-second target; first installation remains the explicit progress/cancellation path.
- The initial schema-v3 AOGM export was repeated byte-for-byte identically at Providence commit `0a3aab2e8c1951c48d2d626ee24e96fb9c7aab04`. Commercial package bytes remain local and are not committed; later compiler checkpoints add their own export evidence without rewriting that historical gate.

### Integrated scenario and party setup

- The splash now opens one full-stage setup workspace instead of a campaign modal followed by a separate party screen. Installed scenarios remain visible in a single left column, reusable Character Files occupy the center, and all six Current Party positions remain visible on the right before and after a scenario is selected.
- Scenario rows open app-owned precompiled installations and use ready/opening language; there is no per-row Play control and no suggestion that Providence compilation or complete package validation repeats on every click. External paths use an explicit Install `.realmz2` action, while deterministic seed selection is removed from ordinary setup UI.
- A party-setup session may be replaced by selecting a different installed scenario from the same workspace. Once Begin Adventure commits, the existing active-session boundary again requires ending that adventure before another scenario can open.
- Stale incompatible package revisions are collapsed by campaign identity and omitted when a current ready revision exists. Unidentified failures are grouped by failure reason, preserving a bounded readiness surface without flooding the selector with duplicate rejected rows.

## Current workflow batch — AOGM field application closeout

- Ordinary AOGM play now proves the bounded charged-item path with Classic item 805. Torch consumes one charge and its authored load once, requests the item and spell sounds in order, resolves Light special 50 as party state without opening a false character picker, and sets Torch Lit to `power × 30 − 1`.
- The camp spell workspace now presents one selected character, separate Known Spells, Fast Spells, and Scroll Case tabs, a list/detail split, and compact power controls through the same typed intents as the prior shell. Route-local actions use the established themed controls instead of mixing the old bitmap button deck with the modern workspace chrome.
- Through that ordinary workspace, Durin scribed a power-one Heal Small Wounds scroll for the source-shaped double spell-point cost. The pending party selection survived a central save/reload, healed Brom without another spell-point charge, and cleared the exact scroll slot once.
- AOGM opcode 29 at land 0 coordinate 40,6 acquired Classic player-map record 1 after its ordinary treasure continuation. The acknowledgement boundary and acquired state both survived separate save/reload checks, and Maps/Notes displayed its authored abandoned-vault note as `Map 2`. Castle's zero-based record identity and one-based menu loop prove that label; it is not an invented land name or an off-by-one runtime correction.
- Item use and field/camp casting remain partial for separately evidenced discard, case-transfer, door/XAP, random-power combat, allied-target, map-effect, and broader-special branches. Their ordinary AOGM paths are no longer certification gaps, so the remaining work returns to the parity queue. Acquired maps are functional with ordinary evidence but remain presentation-unaccepted pending user review.
- No Providence contract changed. The commercial AOGM package, saves, and route captures remain local.
- Tier 3 passed 2,483 assertions across twenty-three suites, project import and smoke launch, teardown-leak rejection, architecture and export-contract gates, the hotspot/test budget, 95 bidirectionally linked differential cases, and the 68-Classic/8-host workflow inventory. Clean pinned Castle, Remake, and Providence symbol validation passed. Source intelligence regenerated to 8,856 entities, 55,583 relationships, five flows, and 8,630 chunks.
- Godot MCP Pro completed the ordinary AOGM Torch, camp scribing, saveable party-target selection, map acquisition/reward, save/reload, and Maps/Notes route before stopping cleanly. Its editor-error history retains the known project-wide warning backlog but contains no entry for either changed spell-workspace controller.

Next batch: certify Fast Spell input through ordinary AOGM play, settle the remaining blocked-attempt timing families, and prove location-changing timed-program continuation through the complete scheduler.
