# Godot presentation contract

## Purpose

Own scenes, controls, screen presenters, topology-derived rendering caches, animation, audio, and accessibility settings.

## Ownership

- Classic-wide 2D shell and optional topology-derived 3D dungeon presenter.
- Translation of user input to typed intents/responses.
- `InteractionPresenter` is the dedicated Classic interaction layer. It owns modal/request identity and delegates text/choice, selection, encounter, shop, temple, bank, battle, and one-item recovery controls to typed components that emit `InteractionResponse.Body` variants. Combat targeting and presentation-only commands use explicit typed signals rather than a dictionary command bus.
- Consumption of `GameView`, domain events, and interaction requests.
- `ClassicMapPresenter` renders one map stage over a seamless application-owned surround, optional traveled-area preview, debug facts from detached map/cell views, and the exact direction cursor for every pointer-driven movement region. Its default non-LOS Classic visibility keeps Castle's 15×13 current window; larger Rebuilt stages may show save-derived previously revealed cells beyond that window, dimmed as memory, while unrevealed outer cells reveal the same surround without a coordinate grid. On an authored LOS map, the complete native-cell map rectangle is black, currently topology-visible cells draw normally, exact save-owned sight memory draws dimmed, and never-seen cells—including sealed interiors—remain black without applying the non-LOS 15×13 visited-window expansion. Dungeon walked-path discovery remains Castle's 3×3 derivation. Found land secrets and walked authored paths draw Castle's transparent-mode PICT 302 tiles 251 and 253 from detached saved facts. Disabling the traveled-area preference restores the complete bounded detached view only on non-LOS maps; authored LOS remains mandatory. Discovery masks extend incrementally from detached visited/seen sets and rebuild only when the map or saved set is replaced, so redraw cost does not grow by repeatedly expanding every prior step. Spatial presenters retain their custom arrow while they own map input, relinquish it before a typed interaction, and restore it from the current pointer only after the interaction owner releases the arrow slot; no presenter clears or reapplies another presenter's cursor during ordinary movement. It does not expose redundant map-level or input-tutorial headings.
- Consumption of detached app-owned view models, including `CharacterVaultRevisionView` from `src/app/view`; presentation does not define, duplicate, or compatibility-wrap those views and does not consume infrastructure records directly.
- `ClassicBattlefieldPresenter` renders the actor-centered native tactical viewport from detached battlefield facts and validated package media. It fills every complete cell available in the responsive play stage, bounded only by the authoritative battlefield, and paints the same seamless application-owned surround used behind exploration wherever no battlefield cell is drawn.
- `DungeonGeometryProjection`, `DungeonSceneMeshBuilder`, and `DungeonMap3DPresenter` adapt the project-owner-authored Remake donor into one batched first-person floor, ceiling, occluding wall, framed door/arch, recessed stair, and corner-pillar scene from the same detached topology view as 2D. The camera consumes the save-owned four-way Classic heading, renders through a nearest-neighbor 400×225 viewport with one stable square source tile per world floor/ceiling cell, preserves source cell-oriented doors and exact/user-supplied surfaces, and applies orientation-neutral yellow-sand balancing and Macintosh-palette quantization before a neutral scalar boundary-distance/dark-map shade. The door samples a centered wood-and-ring crop whose source aspect matches its 0.72×1.23 world plane, so round hardware remains round without distorting the doorway. Archways use broad lintel courses and continuous jamb crops from the same supplied sand-brick surface rather than the donor's gray patch; neither distance nor face orientation may shift one surface's hue, and no second movement or visibility model exists.
- `ClassicAudioPresenter` owns playback from the composed Classic media catalog. `ApplicationMediaCatalog` owns versioned built-in media, the pinned character-library media source owns stock character appearances, and `ClassicMediaCatalog` resolves an exact scenario-package override before those application fallbacks. All consume the core `MediaSource`/`MediaAsset` contracts and never depend on package repository or archive classes.
- Classic media identity is the exact `(resource type, ID)` key; semantic asset IDs are aliases, not admission or lookup boundaries. A positive or negative CICN referenced by a player map, initial terrain, or runtime terrain mutation resolves as the same icon-backed land overlay through scenario-before-application precedence; application-owned identities remain normalized references and scenario-owned media is not duplicated.
- `ClassicAudioPresenter` also owns one independent looping music channel beneath master and music volumes. `ClassicMusicCatalog` validates the eleven-module application-owned stock Ogg bank and resolves the distinct Desert, Swamp, and Snow preference contexts to Outdoor, while `ClassicMusicContext` selects only detached presentation contexts after Begin Adventure; the scenario library, party assembly, and character creation remain without gameplay music, and the splash may play only its separately owned click-enabled intro soundtrack. A committed interaction transition selects music only after its destination route and interaction workspace have settled, so closing Treasure, Shop, Temple, or combat cannot retain the departed context. Play switches context, Continue retains the current title, and Off stops it; when a requested Play context has no application or scenario stream, the presenter stops any different departed context instead of leaking it into the new workspace. No music state enters simulation or adventure saves. A positive package `baseScale` selects Indoor independently of `landlook`; Custom 1–3 resolve only the scenario asset carrying their explicit `scenarioMusicSlot`, never a guessed resource key.
- Godot engine boot remains plain black without engine branding. The lightweight runtime immediately reveals the project-owner-supplied card over its own opaque black field on the same frame exact application sound 624 marks entry, then begins menu and gameplay construction only after that frame draws. After a three-second hold it starts the second 624 while the card remains visible; when that complete 1.513-second cue ends, one callback removes both card layers, reveals the menu, and starts sound 20 at the saved effects volume. Graph-dependent menu actions remain disabled only when background construction is still finishing. Presenter-owned imported textures and shaders initialize with their presenter instances on the main thread rather than as script preloads so background script/scene compilation cannot claim rendering resources. Only the visible front door owns active intro video and soundtrack decoders, while the hidden shell suppresses its duplicate splash activation. The visible menu loads the silent video on activation but defers MP3 decoding until the player first enables audio; leaving it stops playback and clears both uncached streams. Returning reloads and restarts the video, and loads/restarts the soundtrack only when the player previously enabled it. Hidden setup, gameplay, and routed workspaces never retain or draw either intro resource.
- `MusicPlaylistDialog` is a locked responsive modal reachable from the source-visible Music menu and the Audio preference tab. It keeps all twenty Classic slots visible, uses Play/Continue/Off radio choices, exposes current title/global enable/music volume, reserves slots 18–20, and provides an explicit Done action at 1280×720 and 800×600.
- Picture and sound presenters resolve exact Classic resource type/ID pairs and expose developer-only resolution diagnostics with the owning catalog, asset, hash, and decoder outcome. They never search by numeric ID alone. The effective media catalog decodes each package sound asset at most once for its catalog lifetime, so repeated terrain and action cues reuse one stream instead of rereading the package or accumulating decoder churn. Re-presenting events with that same catalog does not invalidate or rebuild the mounted route workspace; only a catalog identity change propagates through the shell and router.
- `ClassicApplicationShell` is the sole scene-backed application composition. It owns the compact menu, framed map/picture stage boundary, separately framed right roster, bottom narrative/status well, and contextual command deck. Scenario media diagnostics remain developer-only; the visible picture stage never prints package-internal asset IDs. `ClassicScreenRouter` owns route registration, primary-workspace replacement, history, and focus restoration; route-local controllers own workspace construction and local selection/settings state. Neither boundary may mutate `GameSession`.
- Immediate scenario-picture displays are pointer-transparent, nonblocking overlays over exploration. They remain visible until the next committed movement step; ordinary map input is therefore the Castle-shaped dismissal and does not require Escape. In free Exploration, Escape opens the System workspace only after a more specific interaction, media overlay, target, or combat cancellation has declined the input.
- The exploration footer spans the full shell as three aligned regions: equal-width Adventure and Party side panes around a wider expandable narrative/status well. Search mode, held Area Search, Torch, Camp, Rest, Heal, the single contextual Shop/Temple/Encounter slot, and Settings belong to Adventure; Money, Items, Spells, and Maps belong to Party. Both headings are centered over command decks that remain visible beneath typed AP/Encounter interactions; each deck is centered horizontally and vertically inside its own tight content-sized framed inset so the controls remain a cohesive owned group without filling unused Party height at large Fit densities. The interaction presenter owns authored responses while the underlying simulation commands are disabled. Typed light state remains visible beside location, time, and wealth; fatigue uses Castle's visible cyan gauge over the authoritative 4–135 range while retaining its exact numeric value and tooltip. The location fact shows `?,?` while the save-owned X/Y display flag is set, and shows the current dungeon heading only while the save-owned compass flag is enabled. Camping changes only the Adventure pane to a labelled inset state; the same Camp icon remains visibly pressed and toggles camp off. Ordinary Encounter samples the exact source-proven yin-yang from application PICT 180. Shop uses the project-owner-supplied transparent isolation of application PICT 225's source-proven mark without applying a second runtime crop or circular mask. Each contextual control adds its stable live caption on Rebuilt chrome without exposing a compiler record number. Heal uses the project-owner-supplied app glyph because Castle's CNTL 172 is a blank standard control rather than an icon resource; presentation crops only its transparent padding and never resamples it. Do not expose a generic Service command or Castle's hardcoded developer Speak vocabulary in release UI.
- `UiRouteCatalog` is the single route, label, shortcut, and workspace-scene registry. `ClassicCommandCatalog` is the single command/asset/action/context/availability/focus registry. `UiLayoutProfile` owns only the optional 800x600 Compact and canonical 1280x720 Wide compositions after interface density is applied. Its Fit policy increases interface geometry at larger 16:9 sizes, bounds the application to a centered 16:9 rectangle on ultrawide displays, and leaves the root stone surface visible as pillars. User text scale remains independent and imported bitmap art remains snapped to exact 1× or 2× pixels. Larger map stages may expose save-derived previously revealed terrain outside Castle's 15×13 current window under the explicit Display preference, but never unrevealed terrain; battle stages may expose additional authoritative battlefield cells when complete native cells fit.
- Allies and Bestiary are distinct routes under the source-visible Allies menu. Allies renders only current save-owned holdovers; Bestiary renders the active monster set's ordered menu-visible immutable definitions, authored descriptions, independent Classic name bytes, source combat facts, and exact CICN identities from detached views.
- Context commands share one application-owned modern slate beveled/focus/disabled surface. Physical holds, persistent Camp, and the currently open Party/Preferences workspace use its recessed state; inactive commands remain raised. Every grouped footer command separates a centered native-scale icon stage from one live Black Chancery caption band. Proven donor controls are sampled only for their semantic imagery, with embedded captions and hotkeys excluded; text-led actions do not import word-bearing Castle slabs.
- Search renders the licensed eight-frame Searching effect at five frames per second while active and a static first frame while inactive, after removing only its six legacy neutral-gray matte keys at runtime; exact CICN 128 remains control-state evidence rather than a visible gray-backed button. Torch similarly composes exact frames 146–154 after narrowly keyed matte removal. Both source assets and their recorded hashes remain unchanged.
- `ClassicUiAssetCatalog` owns imported app-control lookup and keeps those controls separate from package media.
- `ClassicTypography` owns one theme-level role map. Classic mode uses the project-owner-approved Pen modernization of Theldrow letters with the exact FONT 1601 advances for body, narrative, menus, and dialogs; the complete utility repertoire comes from the pinned OFL Grenze Gotisch source, while Samuel's remetricked vector and the unscaled Castle bitmap strike remain reference evidence. Castle's Black Chancery supplies ornamental headings and compact command labels, Castle's Inter substitute supplies Geneva-style utility controls, and Chicago remains limited to its narrow source-backed 3D-help role. Readable mode retains the bundled Alegreya family. Typography choice and text scale are presentation settings and never enter the session or save aggregate.
- The integrated setup workspace uses Castle's stored-character-versus-party mental model: reusable Character Files remain visible in the center pane even before a scenario is selected, while the complete party capacity remains visible on the right. Add and drag/drop stay disabled until a scenario owns party eligibility. Click Add plus drag/drop both emit the same stable-ID import intent. Portrait and summary children ignore pointer input so dragging anywhere on a Character Files row reaches its one typed drag source; the complete party list owns the matching drop target and highlights while accepting a drag. Character Files and Current Party use aligned row surfaces and render the exact package-provided 44×44 in-game portrait from `CharacterView.portrait_id` at full size. The setup media cache resolves stable stock identities referenced only by vault records as well as active scenario appearance options, so selecting a scenario cannot blank an application-created character. Dragging temporarily replaces every native drag/can-drop/forbidden cursor shape with a translucent copy of that portrait and restores the system cursors when the drag ends. The picker never substitutes a tactical CICN or forces its pixel-art texture filter. The full-width setup surface is opaque so the persistent exploration roster cannot appear underneath it as a second party list. Create Character replaces the entire assembly row with exactly one of the five stages—Identity, Race & Caste, Appearance, Review, or Spells—and returns to assembly after finalization or cancellation; the assembly roster is never shown beside a creator stage. Without a selected scenario it uses the stock application catalog; with a selected scenario it uses that package's standard/custom definitions and restrictions. Race is the left-hand driver; each Race and Caste record pairs one narrow spellbook-style pressed selector with a wider selected-definition pane containing its authored description and named compatibility facts, compatible named Castes sort first, and literal compiler fallback labels such as `Race 2` and `Caste 2` remain absent from ordinary creation. Revision history, restore, and archive remain in the advanced Vault workspace. Appearance labels each six-choice row from explicit race recommendation metadata. Review reuses the complete detached character sheet without a redundant party picker. Spells reuses the ordinary spellbook's level-rail/list/detail hierarchy with normal pressed toggle state, typed costs, readable source descriptions, and the exact detached eight-frame Classic effect sequence. A preview appears only when all eight exact resources resolve and decode to meaningful pixels; incomplete, blank, or unavailable sequences are omitted rather than shown partially or replaced. No modifier-key instruction copy appears. Media choices remain explicit compiled content, never guessed filesystem paths.
- Party setup renders all six current-party slots with full-size portraits and without scrolling at the minimum viewport, removing assembly-only helper copy before shrinking portraits. It presents Monster Set in Normal, Mega, Monster order alongside source-authored recommended/maximum/current aggregate levels and the core-computed experience percentage. Difficulty and Monster Set controls emit one typed setup intent; labels and availability come from the detached `PartySetupView`, and presentation never recomputes reward scaling or monster identity.
- Appearance may sort detached options and decode their package thumbnails, but it retains stable asset IDs and never enumerates host folders. The selected portrait may pair its corresponding tactical icon only until the player explicitly changes that icon.
- Identity exposes Castle's fixed starting levels filtered by the campaign maximum. Its backed Character File preview shows only the entered name, gender, and starting level until Appearance supplies exact package-owned media. Race and Caste remain equal left/right selectors in both supported profiles; authored descriptions are visible, while absent descriptions and unproven emblems remain absent. Review renders exact portrait, tactical-icon, and starting-item identities when resolvable, states missing media explicitly, and stacks its typed records in the compact profile. Presentation submits the selected level and renders the resulting session-owned draft; it never synthesizes an advanced character, restriction, media identity, or progression result. Reroll and acceptance emit typed intents; the unspent-spell and vault-publication confirmations are session-owned interactions, not local dialog shortcuts.
- The Character Files workspace is a full-stage route that suppresses the persistent roster, textbox, and command deck. It renders detached immutable revision history and exact eligibility reasons with the complete viewport available. Archive/recovery signals cross to the host repository; presentation never opens, moves, rewrites, or deletes `.r2char` files.
- Application workspaces normally suppress the exploration/combat roster and use the full viewport. Every ordinary routed workspace owns one persistent visible Back, Done, or Cancel action wired to the same history path as Escape; nested modal or inspection tasks retain their more specific local action. Preferences and Save & Load use backed slate cards rather than full black content wells; regular named saves and selected-slot overwrites remain explicit host requests beside the stable Quick Save slot, and presentation validates only the repository's portable slot alphabet. Character and Inventory own compact task-specific member selectors. Inventory follows Castle's working-screen hierarchy: one visibly icon-led carried-item field with detached Damage/Armor line annotations on the left, one selected-character record with fixed item actions and an integrated six-member portrait selector on the right, one full-width selected-item narrative/fact record below, and one square fixed lower-right Done. Trade replaces that ordinary field with two tall paper-white item ledgers around a narrow slate spine; duplicated left/right portrait columns select both packs independently, Money/Items/Done remain visible, rows open unpressed, and an exact eligible cross-ledger drop is the only transfer gesture. Shop applies the same exchange grammar to source-categorized stock and the exact portrait-selected pack, with original category art and duplicated portrait controls in its center spine, unpressed paper-ledger rows, and fixed Buy/Sell/Identify/Leave actions beside bidirectional drag. Its Castle-derived geometry preserves two independent nine-row item ledgers above one fixed 73-pixel command/status band; the application page itself never scrolls, and only the two item ledgers may overflow. Wide rows scale to 46 pixels while the optional compact profile keeps Castle's 40-pixel pitch so neither profile clips fixed controls. Castle's Option-click item inspection is adapted consistently to Alt-hover in Inventory, Trade, Treasure, and Shop: one opaque pointer-transparent floating pane follows the hovered record without changing workspace height and renders only detached player-knowable facts. Exact CICNs, identified facts, restrictions, categories, portraits, and source-owned availability stay detached, and presentation never infers inventory or shop legality locally. Field Spells is the deliberate Castle-shaped exception: it preserves the live map/picture renderer and left narrative/footer while a 420-pixel canonical or 288-pixel compact persistent sidebar covers the complete right edge, including Party commands. Both profiles retain Castle's vertical level-and-power rail beside dense same-level rows and a bounded one-line selected record; the route omits a redundant page heading and fixes Cast, Make Scroll, and Back outside all scrolling content. Fast Spell bindings and the scroll case may scroll independently when their fixed Classic slot counts require it; each occupied slot renders detached Use and source-backed Discard availability as separate commands without inferring either rule locally. Ordinary spell records never display the combat-queue CICN as a spellbook identity icon. Encounter and combat spell selection use the same shared level-filter contract in their roster rectangle. A known combat spell remains selectable even when no tactical cast option exists, and its row, record, and disabled Cast action display the detached exact availability reason rather than a presentation-authored fallback. Mandatory party-target selection restores the numbered roster and keeps the committed spell, selected power, effective cost, and target facts visible. Exact Cast/Abort art is shown only for proven identities. Donor-proven target labels retain their original gradient glyph pixels but key away the donor gray plate onto one shared native Rebuilt inset; ambiguous target types remain ordinary semantic facts and do not receive invented badge art.
- The System workspace renders detached primary/backup save previews and exact corruption or identity-mismatch reasons. Browsing never opens save files, and choosing a candidate delegates full validation and replacement-session restore to the host. The splash Load path first selects the owning installed scenario, then opens that package-bound workspace; party setup exposes the same workspace beside Begin Adventure. Back returns to setup, while a successful restore clears setup-only route state and enters the restored play route.
- Finalized and imported setup members render only from `GameView`; Add, Remove, and Begin emit typed intents. The presenter does not retain a parallel party array or infer that a committed setup edit has begun the adventure. Because party setup suppresses the ordinary shell status region, rejected setup intents render their exact core-owned reason inside the assembly surface; the presenter does not recreate duplicate-character or eligibility rules. Setup inspection fills the setup viewport on an opaque clipped surface above assembly, so the complete character sheet never composites with the picker beneath it.
- Party setup and campaign-aware vault browsing reuse `ClassicCharacterSheet` for complete mutation-free inspection before import or Begin. Eligible and ineligible revisions keep their exact target-campaign reasons visible; inspection never changes or republishes a vault revision.
- Cosmetic-only animation and randomness.

## Local Contracts

- Presentation never mutates gameplay state directly.
- Top-row `1` through `0` belongs to the active character's ten Classic Fast Spell slots. A plain number displays the binding and Control/Command-number invokes it through the existing typed cast path. During a manual combat activation, holding Alt overlays a compact animated Fast Spell dock along the bottom of the tactical canvas whenever the character has an assignment; only assigned slots appear in 1–0 order, unavailable assignments remain dim with the request-owned reason, and clicking a legal preview or pressing its top-row number invokes the same typed cast/target path before dismissing the dock. Nonautomatic spells reveal the ordinary visible Cast/Cancel targeting controls; `T` selects/cycles supplied combatants or marks the current/default area center, and Space confirms. Releasing Alt, losing focus, playback, or leaving the request removes it. The numeric keypad remains movement, and application route shortcuts use Alt-number only outside combat. Choosing a Fast Spell in its numbered row immediately emits the binding intent, Clear immediately emits removal, and Back only navigates; presentation never edits character state directly.
- Animation completion never advances simulation; only genuine interaction responses resume it. Cosmetic Treasure transfer overlays also never delay presentation of the next already-committed request.
- `CombatPlaybackController` owns the cosmetic boundary between a previous detached battle view, ordered committed combat events, and the final detached view. It retains the previous battlefield through terminal playback, sequences source-positioned sounds and effects once, masks the combat command surface, and releases the already committed final view only after playback settles or the player skips it. A standalone persistent-Auto toggle sound plays without opening another playback mask, allowing the application-owned queued manual-control change to settle before the next activation. Playback camera focus follows the current action actor across actorless sound frames and overrides stale inspection focus. The tactical camera recenters at turn/focus changes and only when an in-progress mover reaches the current viewport edge, rather than recentering the board for every square. Battle-opening and Auto-controlled action sequences retain every ordered frame but use an accelerated presentation cadence; explicit Auto frames carry presentation-only status and identify Escape as the full-party Auto safety hatch, while accelerated entry frames do not masquerade as player-selected Auto. The persisted presentation-only 25–200 percent combat speed scales manual and Auto visual frame durations without altering event order, simulation, RNG, AI, blocking sound waits, or the reduced-motion path.
- While an interaction is pending, exploration controls remain disabled and the presenter cannot bypass the session response path.
- `InteractionPresenter` consumes `GameView.active_interaction_request()`: a VM-owned pending response wins, while an otherwise active direct-session battle supplies its detached combat command surface. The latter is translated back to typed combat intents by the host and never masquerades as a saved VM wait.
- Every newly presented interaction resets its scroll viewport to the heading and current task after assigning focus. Long interaction components may scroll internally, but they cannot inherit an offset from a previous request or focus a lower action off-screen on entry.
- A pending interaction owns its source-backed workspace: combat always enters Combat, Shop/Temple/Bank enter Services, and scenario/AP interactions return unrelated browsing workspaces to Exploration. Without a pending interaction, deliberate player browsing remains presentation-owned; route selection never affects simulation or battle cleanup.
- Committing Begin Adventure or successfully restoring from its Save & Load workspace is a presentation navigation boundary: `ClassicScreenRouter` discards setup-only Vault/save return state, inspection state, and route history before selecting Exploration. Later active-session refreshes continue to preserve workspaces deliberately opened by the player.
- Full-window structural Controls such as `ClassicApplicationShell` and `ClassicScreenRouter` use `MOUSE_FILTER_IGNORE`; only concrete controls and modal surfaces participate in pointer hit testing. Router workspaces render above the persistent roster/textbox/command regions so scrolling controls remain clickable wherever their rectangles overlap those regions. The root `InteractionPresenter` owns an explicit draw and hit-test boundary above every nested router modal because mandatory session responses must mask, and cannot leak input to, the workspace beneath them. Its dim shield remains immediately behind the presenter across every same-request rerender; changing modal content may never swap the shield above buttons in GUI input order.
- Whenever a mandatory interaction becomes visible, it reclaims final root-sibling order as well as its high draw order. This keeps mouse hit testing aligned with the visual modal boundary even after the battlefield or another presenter was rebuilt later.
- The splash separates a restrained application identity from one anchored startup command stack and reflows vertically only for the optional compact profile. Its identity pane uses Black Chancery consistently, placing `Realmz Rebuilt` above the project-owner-supplied looping Ogg Theora/Vorbis intro video and `Classic Adventures Reconstructed` below it without redundant lineage or principles copy. The video keeps its exact 832:480 aspect inside the ornamental frame but its embedded audio remains permanently at zero. A separate project-owner-supplied MP3 starts only when the player clicks the video, continues looping independently of video restarts, and a later click mutes it without resetting its playback position; its enabled level follows the persisted master-volume setting. Scenario selection then opens one aligned full-stage row of three independently framed panes: a deliberately narrow installed-scenario picker on the left, and equal wider Character Files and six-slot Current Party panes. Headings share one top band, and ordinary empty states rely on disabled actions and empty slots instead of repeating scenario-selection helper paragraphs beneath each heading. Selecting another scenario may replace a party-setup session, but never an active adventure. Continue remains absent until a typed resumable-save identity and availability exist.
- The scenario picker presents one row per campaign. Immutable package revisions are installation details, not separate campaigns; stale incompatible revisions stay out of the ordinary list when a current ready revision exists. Installed rows say ready/opening and do not claim compiler-level validation on every selection. An arbitrary external path is explicitly installed; deterministic seed controls remain developer diagnostics rather than ordinary setup UI.
- Campaign rows are fast manifest/readiness summaries, not claims of full payload integrity. After selection, the narrow pane may add the exact detached campaign title, version, author, restrictions, guidance, and package splash from `CampaignSummaryView` without making discovery decode the content document. While the host installs or opens a package, one fixed backed operation host shows detached phase/progress data, disables competing scenario actions, and exposes cooperative Cancel; presentation never opens archives or constructs content. Terminal failures cannot be retained or classified until the host supplies typed durable failure state.
- TileMap layers, collisions, AStar structures, meshes, minimap textures, and other caches are disposable derivatives of `GameView` facts produced from topology plus overlays.
- The 2D exploration viewport is clipped to its center panel and party-centered with edge clamping. Its camera rectangle uses every complete native cell available in the stage, bounded by the detached map dimensions, so maximized layouts can display all save-derived revealed terrain that fits without stretching source pixels. Land atlases render at their native 32×32 cell size from each detached cell's effective save-owned `tileset_id` and authored `render_tile`; a cell's optional positive or negative Classic icon-backed land image renders above that base tile. Stock landlook art resolves scenario-first and then through the application-owned six-atlas fallback, matching combat's active-landlook lookup. Dungeon composition uses the same detached feature and edge facts as the topology views.
- Empty width and still-unvisited outer cells around the integer-sized exploration map are application chrome, not dead black space, coordinate-grid overlay, or map truth. They share one manifest-backed low-contrast seamless ornamental tile across the complete stage without a detached rail or ornate viewport border. The six-member roster and canonical Adventure/Party command panes hide their scrollbars whenever their complete fixed content fits.
- Exploration Search renders exact CICN 128 as a persistent toggle. Held Area Search uses the licensed non-commercial Realmz eye/label bitmap from its exact tracked donor commit; exact generic CICN 130/129 released/pressed plates remain cataloged as Castle control-state evidence and are not mislabelled as the visible icon. The custom torch meter renders exact frames 146–153 over segmented body 154 only when the detached party owns Classic item 805, animates only while Light is positive, and shortens from the detached remaining-light value without changing time or inventory.
- Ordinary land travel draws exact built-in Classic CICN 175 when facing west and CICN 186 when facing east, at native 32×32 map-cell size without directional-arrow decoration. Vertical movement preserves the prior horizontal facing. Camp mode draws exact built-in CICN 178 and restores the preserved mounted facing when Camp is toggled off. While aboard, source-proven CICN 13300/13400 variants replace the mounted marker for the active landlook and retained east/west facing; an absent variant falls back to the ordinary marker rather than inventing art. The abstract yellow ring is permitted only when the selected verified app-owned asset fails to load, and base-scale tactical variants still require their own proven assets before use.
- Normal play never paints topology grid lines, AP markers, random-rectangle bounds, or abstract land edges over Classic art. Those remain opt-in debug facts. Land click/hold movement uses Castle's independent horizontal/vertical pointer regions for eight directions and switches among exact application `crsr` 138–146, including the neutral center cursor; top-down dungeon clicks and cursors remain cardinal. Both emit typed movement intent only, and leaving or hiding the map restores the ordinary system pointer.
- `DebugToolsDialog` is a centered, viewport-bounded, debug-build-only floating command surface over detached `GameView` and map labels. It enables exploration-only warp/no-clip/encounter/battle launch, party restoration in exploration and active combat, battle-only forced victory, and a compact recent Auto-action history translated from committed events at the visible boundary; warp coordinates accept direct selected text entry as well as spinner input. It emits typed `SessionDebugCommand` values and owns no gameplay mutation or saved state. `DebugActionConsole` is its selectable, copyable, bounded view of every committed domain event and remains presentation-only.
- Keyboard and mouse exploration holds share one presentation-owned scheduler. The first press and every changed held direction move immediately; the persisted 25–400 percent setting controls only repeat cadence, never simulation time. The scheduler permits one synchronous movement transaction at a time and never catches up missed intervals. An ordinary collision keeps the physical hold alive so pointer or keyboard direction changes can steer away immediately; release, focus loss, route/modal changes, transitions, combat, and scenario interactions stop it.
- Exploration cadence is measured start-to-start: synchronous transaction and presentation work consumes the current interval, and a slow frame emits at most one next step rather than a catch-up burst. Ordinary movement retains the existing command controls and updates availability in place; route, modal, and context changes remain the only reasons to rebuild that deck. Full-stage party setup owns its assembly list and must not rebuild the hidden persistent gameplay roster. Package and application image decoding is shared through the effective media catalog for its lifetime.
- Each first-person room surface faces inward with flat normals; cell-oriented door and stair-well planes that can be approached from either side are two-sided. A non-current door occupies the party-facing entry plane along its orientation normal, flush with its complete wall frame rather than recessed at the destination-cell center; the current cell omits its own door. A rendered boundary uses the same destination cell and directed entry edge as `MapTopology.probe_entry`, so every wall/opening is an adaptation of authoritative movement facts rather than source-cell inference. The 2D dungeon view reconstructs Castle's persistent 3×3 reveal around the save-owned walked path and leaves still-unmapped cells blank; this derived discovery mask never removes 3D topology geometry.
- Switching between 2D and 3D changes only presentation state. Space toggles during dungeon exploration when the authored multiview policy permits it or detached party condition 4 reports Wizard's Eye; a locked dungeon otherwise stays 3D, and Wizard's Eye expiry restores that lock. The toggle cannot create collision, movement, LOS, discovery, or door state and does not rewrite the saved host preference.
- In the optional first-person dungeon view, the center upper/lower regions request forward/reverse movement relative to the detached Classic heading and the left/right regions request source-shaped quarter-turn intents. `W`, Up, and keypad 8 are forward; `S`, Down, and keypad 2 are reverse; `A`/Left/keypad 4 and `D`/Right/keypad 6 turn without moving or consuming clock/RNG. The donor cursor set exposes the same four pointer regions.
- Presentation and accessibility settings cannot change rules.
- Auto Switch to Melee is a persisted application convenience preference, defaulted on from Castle's bundled preferences. Presentation attaches its boolean value only to a manual combat movement response; it never mutates battle weapon mode or supplies the preference to direct Attack, current-turn Auto, persistent Auto, charmed actors, or monsters.
- Realmz Rebuilt has one visual system. Verified base-game command bitmaps and integrated Classic media are application-owned, versioned assets; scenario-owned CICNs, portraits, pictures, sounds, and map art remain package content. Exact scenario `(resource type, ID)` keys override the corresponding application key. Application PICT/CICN families migrate only after their package-reference and readiness contracts are proven; numeric ID alone never selects an owner. Both catalogs use nearest-neighbor sampling where applicable and never share an ambiguous lookup. Slate backgrounds and frame centers use the manifest-backed seamless tile at native scale with repeat enabled; responsive containers crop or tile it and never stretch it. Generated raised/inset bevels retain opaque slate through their complete bounds so buttons and panels never expose accidental parent-colored corner gaps.
- If an action has no verified bitmap in the app-owned catalog, render its ordinary text label through the shared theme. Never construct a blank `TextureButton`, repurpose a status marker as command art, or make hover text the only visible action identity.
- Interface density and text scale are independent. The canonical composition is 1280x720. The optional 800x600 Classic mode uses its own compact 4:3 composition and square gameplay viewport; other window sizes may reflow but are not independently designed or accepted. Controls stack, wrap, or scroll when necessary and never fractionally scale Classic map/content art.
- Unidentified item views expose only player-knowable names and details. Presentation never reveals identified descriptions, values, curse relationships, or other hidden definition facts.
- The ordinary inventory workspace is character-first, then exact-instance-first. It renders the item view's typed action availability and trade recipients and emits stable IDs only; it never reproduces slot, restriction, curse, capacity, load, charge, spell, or targeting rules. Its Cast Identify control carries the core-selected caster, special-48 spell, and target character through the typed spell-intent family; presentation neither chooses eligibility nor identifies only the selected instance. Field item targets use the typed character-selection component; combat item options come entirely from the combat request. It has no player Store command unless a separate source-backed service contract is introduced.
- Screen workspaces live inside clipped vertical scroll surfaces so long inventories, service actions, and accessibility-scaled text remain reachable. Classic textbox interactions reuse the shell's exact narrative well instead of replacing the whole footer: positive source text advances from a click on that surface or Enter, while authored Yes/No and indexed responses occupy one content-width floating pane immediately above the well so the complete well remains available to source text. Complex Encounter follows Castle's docked composition: its idle original-art command row sits directly above the active Encounter's unobstructed authored prompt, Action replaces the roster with the request-owned toggle-set sidebar and fixed Done/local Stop controls, and Speak alone adds its input to the well. An action result temporarily removes the dock/sidebar; acknowledging a continuing click-result restores the idle dock. A transparent shield blocks unrelated input without dimming the map or roster. Skills opens a content-sized transparent-shield dialog wholly inside the map stage; its previous/portrait/next selector is one centered cluster. Encounter Spells mounts the standard scroll-bounded spellbook in the full-height right workspace that ordinarily replaces the roster, while Encounter Items mounts the full application Inventory workspace; closing either restores the issuing Encounter. Pick Lock, Level Result/Spell Learning, surviving-allies selection, and host lifecycle confirmation retain their owned modal regions and explicit Escape-compatible typed cancellation. Treasure, Shop, Temple, Bank, and pooled-departure distribution remain opaque full application workspaces spanning the play stage and lower command region. Treasure's leave-behind warning is a nested locked modal that retains the current distribution workspace beneath it. There is no persistent Chronicle column.
- Campaign selection and party setup use the already aligned root stone as one full-stage surface. They suppress the map frame, play textbox, and command deck until an active workspace returns, avoiding a second texture origin, structural seams, or overlapping play controls.
- Before campaign selection, the presentation-owned menu is the complete pre-session front door. It appears only after the launch card, offers scenario selection, reusable Character Files, and process Quit without constructing a campaign `GameSession`, and keeps every action requiring the gameplay graph disabled until that graph is ready. The stock Character Files creator is available without a scenario; selecting a scenario switches creation to that package's custom definitions and eligibility context. Scenario installation uses one file picker restricted to `.realmz2`; ordinary players never type a filesystem path. Classic Theldrow uses a 17-pixel default before the user text-scale multiplier; dense records may use smaller explicit sizes only when they remain legible at both supported compositions. Creator identity fields, setup selectors, and creator actions use the same Theldrow family as the surrounding Classic controls.
- Ordinary party assembly shows reusable characters and all six current-party positions directly. At 1280x720 all six party rows and primary actions remain visible together; at 800x600 the active creator owns the full width and the reusable-character list uses bounded pagination rather than pushing actions below the viewport. Revision history, archive operations, and recovery remain optional secondary content in the full-stage Character Files workspace and do not occupy the party-builder footer.
- Menu routes are rebuilt whenever session, overlay, or route ownership changes. No-session, party-setup, mandatory-interaction, and active-combat states expose only commands that are valid in that host state; a disabled child-only menu is itself disabled. Popup and option-menu panels are opaque through their complete border bounds.
- Spatial map and dungeon presenters follow that same play-stage visibility boundary. They remain hidden beneath campaign selection and party setup even when Explore is still the active route.
- A positive Classic message reuses the shell's exact narrative-well bounds and underlying outer frame at the compact 18-pixel interaction role; the interaction presenter supplies a frameless opaque center fill so AP/Encounter text replaces the prior narrative without either compositing over it or introducing a second edge across the unified footer. It installs exact Classic color cursor 129 and advances on any application left click or Enter/Space, matching Castle's global acknowledgement loop; it does not invent a formal Continue button or render generic Auto Note, journal, or continuation helper lines while authored text is visible. An authored empty positive message retains its click boundary but exposes one explicit `Continue` action instead of leaking an internal interaction-family label. Auto Note and the source-proven `N` shortcut still determine the typed acknowledgement payload. That surface has no redundant "Classic Textbox" heading or stage-modal minimum size. A negative Classic message uses the same narrative surface without introducing an interaction and remains until the next committed step, matching Castle's no-click path. Textbox history excludes internal event names and sound/picture request diagnostics. The shell may briefly show a native-size app-owned disk or journal glyph at the stage's upper-left only after the host reports the corresponding committed operation.
- The debug-only game-action console uses an opaque dark surface and the readable application font for selectable event text and controls; it never allows the live battlefield or custom display face to reduce log legibility.
- Opcode 62 replaces the map stage with one opaque `Scrolling Text` surface using the exact authored message. It starts at the top, advances at Castle's 20 pixels per second, moves by one 25-pixel step for each vertical drag update, and closes on double-click, Enter/Space, Escape, or the visible Done correction. Ordinary single clicks never dismiss it. These controls emit one typed acknowledgement; scroll offset and timing remain presentation-only.
- A live age-band change uses the inset `Age Update` component, not the Chronicle or Classic textbox. The component renders only typed character/race, resulting band/range, and fifteen-delta payload facts; each current request has one Continue response and independently presents sound 3002.
- Post-battle fumbled-item recovery uses a typed `treasure_distribution` stage component after body count. It displays exact remaining charges and rules-owned recipient availability, then emits assign or leave-behind without mutating inventory directly.
- The same treasure component owns ordinary distribution mode: pending item, pooled denominations, rules-owned recipient availability, Pool/Share, exact Classic Swap increments, stable Detect/Identify controls, and explicit remainder confirmation. Lore caster pickers distinguish authoritative remaining SP from the action cost; a completed lore action stays visibly disabled with its supplied reason while the sibling action remains independently usable if its recomputed caster list permits it. Identified magical items retain both facts in their visible state. Done retains that ordinary component until a committed response either rerenders it or replaces it with the nested completion warning. `LevelUpInteraction` owns result acknowledgement and an application-region spell-selection modal using the shared level rail; it consumes the available canonical or compact height, caps excess height on tall displays, and remains vertically centered. Both emit typed responses only and remain independently gallery-testable at long-list and inaccessible-action boundaries.
- The Services route owns the ordinary money workspace. Exploration exposes the verified Money bitmap as the ordinary route into Pool/Share/Swap and one contextual location-service command whose title, stable ID, action, and availability come from `GameView`; presentation never infers a shop, temple, or bank from map identity. The workspace renders detached pooled, banked, and personal denominations, character load, Pool/Share, and exact Swap increments; every action uses the core-provided availability and emits one `MONEY_ACTION`. Character selection and Done are presentation-only state and never mutate wealth. The contextual bank uses its own save-owned `BANK` interaction because an active request blocks ordinary intents. No-bank movement departure uses the sibling save-owned `POOLED_WEALTH_DEPARTURE` interaction after its yes/no warning; both render the same detached operations and emit typed responses while the session owns bank import, pool clearing, and exact-once movement resumption.
- The Character route owns a presentation-only Party Order draft behind a compact secondary Reorder control, so formation editing never displaces the ordinary character sheet. Move Up, Move Down, and Cancel never mutate gameplay; Apply emits one complete stable-ID `REORDER_PARTY` permutation. The core owns availability and validation, and a changed campaign or detached member set resets the draft.
- The Character route's Lifetime Record tab renders only the detached save-owned counters and computed prestige supplied by `CharacterView`. It never derives permanent history from battle events, current combat state, or presentation playback.
- The Allies route is a read-only view of current held-over ally instances. Its menu entry is disabled when the party has none, and list/detail selection remains presentation-only. Bestiary stays unavailable until package content preserves Castle's complete `hd != 0 && !notonmenu` catalog and normalized descriptions; presentation must not replace that contract with discovery state or reachable-monster inference.
- The Character route also owns independent portrait and tactical-icon previews. It sorts each complete detached catalog by that role's source-backed race recommendation, emits one typed stable-ID mutation only on Apply, and makes Discard a presentation-only reset. Active editing never inherits the creator's temporary portrait-to-icon pairing.
- The Journal route keeps player location notes, authored journal entries, and acquired player maps visibly separate. Places uses cool slate with restrained parchment only for the editable field-note surface; selecting a saved record renders its detached authoritative 15-by-13 topology crop centered on the historical coordinate with the saved darkness intensity, without relocating the live party. Authored journal entries use a scrollable two-page parchment treatment with a leather spine, red headings, and dark ink; acquired maps sit on a low-luminance cool map table so exact source pixels remain dominant. It may edit only the detached current-location note and may browse only the current land/dungeon note stream in source ordinal order. Authored entries render immutable message text in numeric Data SD2 identity order; eligible positive Classic text carries its typed journal choice through acknowledgement without adding another text-well control, while an already-recorded message is read-only. Auto Note defaults off from application PRFN 128, persists only as a host presentation setting when explicitly enabled, and leaves Castle's manual `N` path available. Revert draft is a deliberate presentation-owned correction and emits no intent; Castle's unavailable native labels remain a visual-only wording gap, while source proves both native exits commit. The acquired-map list preserves unavailable source slots and enables only acquired records; its presenter centers exact PICT assets, signed scrolling text, topology-derived land crops, Castle's fixed 20-by-20 16-pixel dungeon crop, ordered markers at their independent authored scale, notes, and eligible party marker 138 on the map table with presentation-only 1×–6× inspection zoom, without mutating simulation. Dungeon cells compose exact PICT 302 tile 16 plus source-ordered bit layers 1 through 12, including revealed-secret, unmapped, and directional allow-move pixels. Each positive map award enters an ordered compact flash queue with fixed compact geometry, blocks underlying pointer input until click/key acknowledgement, and requests its event-supplied stock cue only when that record becomes visible. Exact PICT darkness-mask pixels and styled legacy scrolling text remain explicit visual gaps.
- The Spells route renders only core-provided field-cast, Make Scroll, and scroll-slot Use actions. It emits the existing typed spell intent with stable character/spell/slot/power identities and never calculates costs, context eligibility, targets, or scroll consumption. Exploration reflects Camp as one persistent bitmap toggle and exposes Rest and Heal only from rules-owned availability. Holding Rest, Heal, or Area Search uses a presentation timer that emits independent typed pulses through footer rerenders; a button release caused only by replacing the footer while the left pointer remains down cannot stop it. The hold stops on physical mouse release, keyboard activation release, focus loss, interaction, or lost availability. Symbol-backed bitmap captions fit within their fixed command cell rather than truncating the command identity. Presentation never advances time, heals, leaves camp, searches, or resumes movement itself. Dungeon instrumentation derives its compact facing label from the detached last-move direction and retains cardinal pointer movement; it never maintains a second facing state.
- A newly mounted route resets its workspace scroll after focus restoration. Route-local rerenders preserve the prior offset after focus restoration, so neither stale workspace state nor focus order can hide the active controls.
- The battle action component renders the session-owned melee/missile mode. It emits the no-cost typed switch response, never offers melee target buttons in missile mode, and labels each rules-owned legal projectile target as Fire. Unsupported or unavailable Fire remains visible with the request's exact reason.
- Active combat keeps its tactical board visible above one full-window-width Classic command deck. A click anywhere outside the active actor's cell is projected through the exact camera transform that rendered the current frame to one of Castle's eight signed directions and submits one adjacent typed move; keyboard/keypad input uses those same directions, and the deck does not duplicate them as permanent buttons. Turn changes restore active-actor focus, and the camera recenters when that actor reaches the visible edge rather than waiting until the actor has left the viewport. Holding Shift reveals core-provided movement costs on adjacent battlefield cells, and opening a spell, item, or target subworkflow suppresses spatial input. Detached combatant facts drive target inspection and source-side Reveal Friends lines; inspection, camera focus, and overlay dismissal remain presentation-owned. Reveal Friends carries a red-hostile/green-friendly/blue-helpless legend, while the separate modern Shift aid carries a gold movement-cost legend; neither is visible by default or styled as the other. The presenter composes battle IDs 1–200 from the detached active-landlook identity and IDs 201–400 from the validated shared PICT 302 atlas, then draws only core-provided actors, targets, movement availability, and persistent-field cells. Persistent fields use Castle's exact `200 + queue icon` PICT 302 tile on each core-projected nonsolid mask cell; missing media gets one visible presentation fallback without changing collision. The presenter never derives passability, relationship, range, LOS, action cost, or target legality.
- Combat casting follows the Classic staged flow: choose a spell, choose its admitted power, then choose actors or a battlefield center. Actor and repeated-actor targeting happens on request-provided battlefield footprints; spatial spells preview the request's exact Data AD mask under the cursor, and a rotatable mask exposes one visible Rotate Area action that cycles only the request-provided orientations. No default or hovered center becomes committed until the player clicks it. Selection order, rotation index, hover state, cancellation, and preview are presentation-owned, but presentation does not claim legality: confirmation emits the existing typed response and the core validates the selected actor, center, and rotation once before mutation. Playback uses proven application or scenario-override CIcons for cast/result frames, fixed target-centered numbers, and explicit unavailable diagnostics; it does not name red art as blood or invent an unproven death effect.
- The combat command deck renders on the application-owned raised open-right slate rather than the opaque narrative textbox surface. It presents the request-ordered current/upcoming initiative sequence with media-catalog combat icons resolved from detached character and monster views, plus core-provided Auto Turn, Delay, Bandage-recipient, and Turn Undead availability and reasons. Its two compact command rows retain fixed slots across actor and availability changes and must fit the default combat region without scrolling; persistent Auto is a separate roster-row toggle carrying only the stable party ID and desired boolean, and its indicator is detached state rather than a presenter-owned preference. The deck may choose a typed Bandage recipient but may not infer bleeding, fresh-turn, priest, undead, allegiance, or once-per-battle eligibility.
- Scenario-picture overlays place a native-scale repeating stone backing under the complete raised bevel. Transparent frame pixels may never expose the map between the surface and bevel.
- Classic opcode 3 carries button labels, not a question prompt. Its yes/no presenter keeps the latest source-authored Classic textbox message visible as context, while an explicit typed request prompt remains authoritative.
- Classic sound playback rotates across four presentation-owned channels. Positive sound requests may overlap; a negative request waits for that channel's completion before the presenter drains later sound events, without introducing a simulation wait. Reduced Sound suppresses only an explicitly tagged Castle-gated modal or workspace-opening event before channel or media side effects; it never filters a numeric sound ID globally or mutes action, transfer, completion, or scenario-authored cues.
- Ordinary Swap route entry and exit are presentation-owned lifecycle audio: one deliberate Services-route entry requests button sound 141, stops existing channels before modal sound 3003, and leaving requests sound 141. Same-route rerenders do not replay them, and a Services route opened for a typed Shop, Temple, or Bank interaction is not treated as ordinary Swap. Pool, Share, and transfer sounds remain simulation events.
- Deliberately opening Items or field Spells requests stock workspace sound 20001 or 20002 once after the route changes. Same-route rerenders and automatic workflow routing remain quiet; combat entry and action cues stay in the ordered domain-event stream.
- Starting a physical Rest or Area Search hold requests stock sound 6001 once. Presentation-owned repeat pulses remain quiet until release and a later new hold, matching Castle's sound-before-`StillDown` loop without moving time or search behavior into presentation.
- Cosmetic RNG cannot enter saves, replays, oracle traces, or simulation decisions.

## Work Guidance

- Inventory inspection renders detached Classic record facts and explicit disabled-action reasons. It must not reconstruct bonuses, restrictions, or curse-decoy behavior from package definitions.
- Put tweakable visual values in scene/inspector properties.
- Use MCP to inspect scene trees, properties, errors, runtime state, interactions, and screenshots.

## Verification

- Playable slices follow: open/build scene, inspect editor errors, play scene, run input/test scenario, inspect runtime, capture screenshots, stop scene.
- Runtime MCP tools must never run before `play_scene`.

## Child DOX Index

- `assets/AGENTS.md` owns presentation-owned chrome assets and provenance.
- `controllers/AGENTS.md` owns route-local controller inputs, signals, and mutation boundaries.
- `interaction_components/AGENTS.md` owns typed request-kind control surfaces and exact payload emission.
- `screens/AGENTS.md` owns scene-backed workspace layout, scrolling, and focus containment.
