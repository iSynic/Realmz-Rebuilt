# Purpose

- Own scene-backed screens, reusable panels, and roster surfaces used by the canonical Classic-wide shell.

# Ownership

- Each route owns a registered scene. Scenes own durable header/layout, scrolling, focus containment, and the persistent Back action for ordinary reversible routes; `ScreenNavigator` supplies detached view content and routes that action through the same history path as Escape. Exploration, Combat, and Vault omit the generic action because their spatial or nested return surfaces own navigation explicitly. Inventory also omits the generic route action because its controller-owned Done action is integrated into the lower item record and emits that same Back path.
- The field Spells route is the persistent-sidebar exception to ordinary inset route geometry: its 420-pixel canonical or 288-pixel Classic scene is deliberately wider than the roster, covers the right edge from the menu to the window bottom, and preserves the map and narrative/footer stage to its left. It suppresses the redundant route heading and owns a fixed bottom context-action strip beside Back so Cast and Make Scroll never enter route scrolling.
- `SpellsScreen` embeds the complete `spells_workspace.tscn` composition: caster and section selectors, blocked and empty states, Classic level and power rails, known-spell browser, selected record, Fast Spell grid, Scroll Case, and action host. `spell_action_dock.tscn`, `fast_spell_row.tscn`, and `spell_scroll_slot_row.tscn` own their reusable stable records. `SpellsScreenController` binds detached values and instantiates only those exported variable record scenes; Encounter spell selection reuses the same workspace rather than maintaining a second hierarchy.
- `ClassicPartyRoster` owns the persistent six-slot right rail and Castle's single current-character marker. No ordinary row latches independently: clicking another member moves the marker and changes character-scoped commands, while clicking the already-current member opens that exact Character record. Hourly exploration updates refresh matching existing rows in place from the new detached party records, preserving controls and selection while updating stamina, spell points, conditions, and portrait state. Its portrait stage follows Castle's `updatepictbox`: stamina below one receives exact CICN 2019 shading, and stamina below negative nine additionally receives exact CICN 2015 as the death marker through scenario-before-application media resolution. In combat, persistent Auto uses one compact `A` toggle with pressed-state and tooltip/accessibility explanation so all six rows retain an unobscured `HP current/max` summary and the stable party ID is the only mutation identity. A typed `CHARACTER_SELECTION` request switches that rail into Castle's separate countdown picker: a readable project-font cursor shows the initial picks remaining before any click, portraits receive descending numbers, repeated clicks renumber locally, and the exact count emits one Party-ordered response without opening the Character workspace. The request is mandatory under `FD-SCENARIO-002`: the picker exposes no cancel control and Back or Escape leaves the blocking request pending. Detached character-effect requests temporarily animate their exact CICN frame sequence over the matching portrait and then restore its current base presentation.
- The persistent roster owns one complete inset panel boundary around its title, slots, and scrollbar; it never appears as unframed text floating beside the spatial stage.
- During memorized combat spell selection, `ClassicPartyRoster` temporarily becomes a full-height spellbook rail covering the otherwise inert Party-command footer. It owns only level/spell/power selection and returns the exact request-owned `CastOption`; closing or resolving the interaction restores the current detached party view and ordinary roster depth. One bounded records column contains the dense internally scrolling spell list, compact selected record, and power choices above the fixed action footer, so no detail can extend below the roster. Every detached known combat spell remains present: request-owned legal options supply cast powers, while known spells without an option remain selectable for inspection with an explicit unavailable state and a disabled Cast action. The list reuses the persistent pressed-row selection language from character creation, while the inset record presents the selected character's detached Realmz-owned description, spell-point cost, range, scaled damage, duration, resistance, and saving-throw facts. Its target badge shares the field/selection native inset and preserves only catalog-proven gradient target lettering from the donor plate. The proven original Cast Spell and Abort controls live in a fixed footer outside the spell list's scroll region so both actions remain visible in the canonical 1280x720 composition and the optional 800x600 Classic mode; their tooltips distinguish immediate casting from battlefield aiming without altering the submitted option.

# Local Contracts

- `classic_scrolling_text_surface.tscn` owns the tiled background, margin, and text viewport. `ClassicScrollingTextSurface` decodes and binds application media, style runs, and scrolling behavior without reconstructing that stable hierarchy.

- Workspaces are designed for the canonical 1280x720 composition and remain reachable in the optional 800x600 Classic composition through deliberate compact reflow or scrolling. The 4:3 mode keeps a square gameplay viewport. Intermediate sizes are fallback reflows, not additional design targets; route headers stack below the compact threshold, bound descriptions to two lines, and truncate instead of expanding a workspace off-screen.
- Screens present only detached `GameView` facts and explicit action availability.
- Inventory selects one party member and one exact carried item before presenting actions. Trade recipient rows, Cast Identify's source-selected caster/spell identity, and every disabled explanation come from the detached item action view; the workspace cannot expose scenario-owned opcode-36 escrow as a player stash.
- Inventory inspection renders visibly sized exact CICNs plus `ItemView` facts, properties, and restrictions in the full-width lower record while the selected character, fixed action dock, and integrated six-member portrait selector occupy the right command rail. Its ordinary square Done action stays locked to the lower-right route edge above scrolling content. Disabled controls expose source-owned reasons. Enabled verbs enter one stable exact-item operation stage with only visible state, charge/load facts, conservative consequence copy, and fixed confirm/cancel; committed events own the result, Identify remains inventory-wide, and Drop retains its core confirmation. Trade replaces selection-driven transfer staging with two independently selected ledgers: duplicated left/right portrait controls choose both packs, rows open unpressed, and dropping an exact eligible instance on the opposite ledger immediately emits Trade while leaving the workspace available for another drop. Its embedded Money/Items/Done spine replaces rather than duplicates the ordinary route action. Current, resulting, and maximum recipient load remain sourced from `ItemTransferTargetView`; presentation never recomputes capacity. It never reconstructs identified or curse-decoy facts from package content.
- `InventoryScreen` owns the ordinary route's stable item-browser, character-command, item-inspector, and alternate-content regions in `inventory_screen.tscn`. The reusable `inventory_item_browser.tscn`, `inventory_command_rail.tscn`, `inventory_item_inspector.tscn`, and `inventory_done_column.tscn` scenes expose their editable headings, collection hosts, record orientation, and persistent Done control in the Godot editor. `InventoryScreenController` clears and repopulates only their variable record hosts; it must not reconstruct or replace that hierarchy.
- `inventory_workspace.tscn` is the reusable complete Inventory composition embedded by the route and instantiated for Encounter item selection. `inventory_character_record.tscn`, `inventory_action_panel.tscn`, `inventory_selected_item_record.tscn`, `inventory_empty_state.tscn`, and `inventory_trade_workspace.tscn` own all stable normal, unavailable, operation, and Trade layouts. Their parent scenes export the item, fact, character, portrait, and trade-row scenes used for variable detached records; no Inventory controller may construct a Control.
- Shop interactions show total payable gold, identified stock, player-knowable carried-item names, exact buy/sell offers, and paid identification. They submit stable stock/character/instance identities and render typed disabled reasons; they do not calculate prices, acceptance, equipment legality, or affordability.
- The Party Wealth workspace always exposes ordinary money management after party setup, even when no location service is active. Wide composition separates Party Pool, all six adventurer records, and the selected-character Swap pane; Classic composition condenses selection into Swap. It renders all three denomination transfers without deriving balance, increment, or capacity rules and never pads the page with unavailable hypothetical services.
- `ServicesScreen` owns the stable pool, party ledger, exchange, location-service, and alternate-state regions in `services_screen.tscn`. `ServicesScreenController` fills those regions from `MoneyWorkspaceView` and rules-owned availability without replacing their hierarchy.
- `JournalScreen` owns the stable summary and Places, Maps, and Journal tab regions in `journal_screen.tscn`. `MapsJournalScreenController` fills those named regions with detached records while preserving the scene-authored tab structure across refreshes.
- `SystemScreen` owns the stable campaign summary and Save & Load, Display, Audio, Pacing, Accessibility, Controls, and Diagnostics tab regions in `system_screen.tscn`. `SystemScreenController` binds save records and presentation settings without replacing that scene-authored navigation.
- `VaultScreen` owns the complete stable Character Files header, availability summary, empty state, current-record grid, revision-history controls, and immutable-inspection regions in `vault_screen.tscn`. `vault_character_card.tscn` and `vault_history_row.tscn` own the repeated records. `CharacterScreenController` binds these scenes and switches list/inspection visibility without replacing the hierarchy.
- Newly mounted route screens begin at the top after keyboard focus is restored; same-route rerenders restore their prior offset only after focus restoration, and scroll state must not leak across workspaces.
- Exploration and Combat share the shell's spatial stage rather than mounting explanatory body cards. Combat controls stay in the bottom interaction region so the actor-centered battlefield and persistent roster remain visible.
- Exploration and Combat are typed shell modes declared by `UiRouteDefinition` resources. They mount no workspace scene; their visible map, roster, narrative, and command structure is owned once by `game_shell.tscn`.
- The Character workspace uses `ClassicCharacterSheet` for presentation-owned portrait-backed character and tab selection. Its Overview keeps portrait and tactical identity, Classic stamina/SP/AR/attacks/load, Attributes, Combat profile, Resources and wealth, and current detached conditions in stable regions: three aligned columns at 1280x720 and a readable stacked record at 800x600. Conditions & Saves keeps named current effects and all eight detached saving throws in two aligned wide records and stacks them without clipping in Classic mode. Abilities separates detached special modifiers from special abilities in aligned wide records and stacked compact records; it does not invent icons or categories. Race, Class & Aging compares the two exact authored definitions beside one another in wide mode, stacks them in compact mode, and keeps the five source-backed age bands in a distinct current-band table. Spells separates the exact known-spell records from the five fixed Classic scroll slots, resolves exact CICNs through the shared media chain, and uses an explicit neutral fallback when identity art is unavailable. Appearance keeps package-owned portrait and tactical-icon roles independent, uses large exact previews and role-local Apply actions, aligns both panes at 1280x720, and stacks them at 800x600; application CICNs never substitute for character identity media. Lifetime Record renders the detached save-owned counters and computed prestige only; it never derives permanent totals from transient combat events or playback. Equipment separates exact equipped instances from the carried pack side by side at 1280x720 and stacks those regions at 800x600; it does not invent named body slots from unresolved Classic item-type values. Party Order is a secondary expandable editor rather than the route's default dominant content.
- `CharacterScreen` owns the stable party-order summary, reorder editor and actions, empty state, and persistent shared character sheet in `character_screen.tscn`. `party_order_row.tscn` owns each variable party member record. `CharacterScreenController` binds those controls and must not rebuild the route-level hierarchy.
- `classic_character_sheet.tscn` owns the shared sheet's persistent empty state, character selector, portrait/tactical identity header, complete tab rail, content frame, and all eight tab compositions. Every Character, Character Files, setup-inspection, and creation-review caller instantiates that scene rather than constructing `ClassicCharacterSheet` directly.
- `character_sheet_stat_tabs.tscn` owns Overview, Conditions & Saves, Abilities, and Lifetime Record. Their fixed regions remain scene-authored; variable metrics and lifetime counters instantiate only exported `character_metric_row.tscn` and `character_record_card.tscn` components.
- `character_sheet_inventory_magic_tabs.tscn` owns Equipment and Spells. Their fixed headings, split regions, empty states, item hosts, known-spell grid, and scroll-case host remain scene-authored; variable exact records instantiate only exported item, spell, and record-card scenes.
- `character_sheet_identity_tabs.tscn` owns Appearance and Race, Class & Aging. Portrait and combat-icon previews, pickers, role-local Apply controls, Discard, authored definition records, and all five age bands remain scene-authored; the sheet script binds package choices and detached definition values without constructing Controls.
- The Allies workspace owns a separately backed current-ally list and detail pane. It renders current stamina, spell points, conditions, source identity, and exact CICN media from detached views, with an explicit empty state and no mutation path. It suppresses generic route helper copy, keeps the list and record side by side at 1280x720, and deliberately stacks them at 800x600 rather than compressing fact labels into unreadable columns.
- `creature_library_workspace.tscn` gives Allies and Bestiary one shared, complete list-and-detail structure: identity header, facts, state cards, and empty state remain directly editable. `creature_library_row.tscn` owns the repeated selector. `CreatureLibraryScreenController` may instantiate that exported row scene and switch authored regions to compact orientation, but must not construct or replace stable controls.
- Party setup and vault revision inspection reuse that same complete sheet. Setup inspection is a full-stage child surface with an explicit Back action; vault inspection retains the selected revision's eligibility reasons and cannot apply appearance changes.
- Its Appearance tab previews package-backed portraits and tactical icons independently. Apply emits the exact character, role, and appearance IDs; Discard resets local state and never publishes to the character vault.
- `PlayerMapPresenter` and `PlayerMapCanvas` consume only detached `PlayerMapView` plus package media. They may render exact PICT/TEXT assets, topology crop cells, markers, note, and current-party marker eligibility on `PlayerMapCartographicStage`'s repeated shared charcoal-slate tile, but they never acquire a map, query mutable topology, or answer simulation questions. Scrolling player maps bypass parchment and zoom, fill the remaining map stage with the same `ClassicScrollingTextSurface` as opcode 62, and resolve offset-preserving same-ID `TEXT`/`styl` resources over tiled application `ppat` 129 with Castle-sorted style starts, exact font/color/face/size interpretation, one-space tabs, and Castle's automatic and drag scrolling. `PlayerMapParchmentMat` repeats the project-owner-supplied native parchment texture in both axes and exposes exactly a 12.5-percent border around the 320-pixel drawn map at every supported zoom; it never stretches the texture or fills the complete stage. The controller, not the presenter, owns the inset selected-map title/zoom header and the centered authored description so map art begins at the top of the remaining scroll region. Dungeon topology cells use the detached fixed 16-pixel scale while authored CICN and party-marker coordinates retain the record's independent icon size. The Maps/Notes selector preserves source slots and disables unavailable entries instead of hiding them.

# Work Guidance

- Prefer reusable typed scenes for durable layout structure and keep gameplay mutation outside this subtree.

# Verification

- Run `tools/verify.ps1` and the presentation fixture gallery.

# Child DOX Index
