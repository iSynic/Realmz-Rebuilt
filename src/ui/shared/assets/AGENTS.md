# UI asset contract

## Purpose

Own Realmz 2 application chrome assets and their provenance.

## Ownership

- App-owned textures, verified original bitmap controls, mounted/camp map markers, the exact PICT 302 four-heading dungeon arrow strip, the six stock landlook PICT atlases, exact scrolling-text `ppat` 129, the complete integrated Classic sound and stock music banks, shared item CIcon library, exact 14000–14063 party-effect animation bank, proven built-in combat CIcon families, and bundled fonts used by the Classic-wide shell.
- SpriteCook, OpenAI ImageGen, donor-commit, Castle resource-fork, Google Fonts, dimension, license, modification, and local hash provenance.

## Local Contracts

- Scenario-owned pictures, icons, portraits, sounds, and map art remain package media. Built-in resource families enter this folder only through an exact-commit catalog with explicit ownership, license, modifications, and byte hashes; scenario packages may override an exact application key.
- Scrolling-text `ppat` 129 remains one exact 64-by-64 application-media asset. Presentation tiles it without stretching and resolves a same-key scenario override before this fallback.
- `ui/map-parchment-tile.png` is the byte-exact project-owner-supplied 243-by-226 player-map mat texture recorded in `ui/map-parchment.json`. Presentation repeats it in both axes without stretching and confines it to the 12.5-percent border behind the drawn map; it never becomes the full cartographic-stage background.
- `ui/fog-of-war-tile.png` is the byte-exact project-owner-supplied 32-by-32 unrevealed-map texture recorded in `ui/fog-of-war.json`. Presentation uses it by default at native cell scale with nearest filtering for every projected map cell withheld by LOS or Classic-window discovery; the user may replace it with opaque Castle black through the presentation preference. Seen or revealed terrain remains fully rendered, and decorative surround art remains outside the projected map.
- The startup Realmz Rebuilt card and front-door intro are app-owned project-owner-supplied art, not scenario media. Preserve the exact supplied 1024-square JPEG launch card for centered aspect-preserving display over an opaque black runtime field; engine boot remains plain black so the card first appears with its entry cue, remains through a three-second hold and the complete exit cue, and disappears in the same callback that starts the menu music. Commit the byte-exact native Ogg Theora/Vorbis menu stream and separate byte-exact MP3 soundtrack, and record all source hashes, byte lengths, media shapes, licenses, and playback contracts in `ui/intro/intro-video.json`. The OGV retains its source audio bytes but runtime playback keeps them permanently silent; the MP3 alone is click-enabled and loops independently. Its app-owned SpriteCook ornament remains separate generated chrome; `ui/spritecook-assets.json` records the style asset, generation job, source hash, transparent-center derivation, dimensions, and production hash.
- Verified base-game UI controls and map markers may enter only through the exact-commit catalog/importer. Preserve each imported source bitmap byte-for-byte. Runtime footer composition may crop or mask the untouched source to isolate its semantic icon while replacing embedded donor captions and hotkeys with live project typography. Record semantic evidence per asset: donor-scene use for controls or pinned Castle resource-fork ownership plus source/control-flow for built-in PICTs and CICNs. A numeric resource ID without its owning fork is insufficient provenance.
- `classic-dungeon/` contains the project-owner-authored Remake donor's exact surface atlas, Macintosh-palette reference, and four relative-navigation cursors plus the project-owner-supplied square floor/ceiling and horizontal-brick wall JPEG bytes. `classic-dungeon-assets.json` records every original supplied name or donor path, ownership, commit where applicable, and production hash; runtime may orient, palette-quantize, and matte-shade these unchanged pixels but cannot silently replace or repaint them.
- The ordinary land map-marker set is exact built-in CICN 175 west, 186 east, and 178 camp. Camp presentation preserves the prior mounted facing for restoration; it never derives or repaints a tent icon. Castle's aboard branch uses exact CICN 13300/13400 families; the committed set contains only source-present landlook variants 0, 3, 5, 6, and 7 and never fabricates missing variants. The 2D dungeon party marker is the untouched PICT 302 source rectangle containing tiny tiles 17–20; presentation keys only its exact-white QuickDraw transfer matte and selects north/east/south/west from the save-owned Classic dungeon heading shared with first-person projection. Positive Classic textbox acknowledgement uses exact color cursor 129 with its authored hotspot until the response commits.
- Integrated stock item icons include exact Family Jewels CICN 6195 so identifying Battleaxe +2 changes its name and knowledge without losing the application-owned image.
- Generated structural chrome must contain no text, symbols, scenario content, or executable metadata. App-owned status and command glyphs are separate UI categories: each may depict one semantic symbol, contain no embedded words, record its provider provenance and exact hashes in `ui/spritecook-assets.json`, and render only at native integer scale. Generate a command glyph only after the stock application library and source control flow prove that no owned icon exists.
- The four compact scrollbar direction glyphs are project-authored SVG assets recorded by exact hash in `ui/spritecook-assets.json`; the shared theme assigns them to Godot's native vertical and horizontal decrement/increment regions.
- The cataloged spell-target plates remain byte-exact donor evidence. Runtime target badges may key away only their neutral gray plate pixels through `classic_spell_target_text.gdshader`, preserving the original saturated gradient lettering over native Rebuilt inset slate; the source PNGs are never rewritten or presented as application chrome.
- Command bitmap provenance is semantic, not stylistic: current donor controls remain implementation leads unless the catalog proves their Castle resource ownership. App-owned bevel, focus, disabled chrome, and live captions unify footer controls while the compositor samples only the icon portion of untouched donor files. Ordinary Encounter uses exact application PICT 180 as source evidence and masks only the surrounding legacy plate. The catalog retains exact application PICT 225 unchanged as Shop evidence; the visible Shop mark is the project-owner-supplied 31×33 transparent isolation recorded by exact hash in `ui/spritecook-assets.json`, with no further crop, mask, or resampling. Heal uses the project-owner-supplied exact PNG because source inspection proves its Castle control has no icon payload, cropping only its transparent alpha bounds at presentation time. A generated reconstruction cannot be called exact without a supplied reference and explicit provenance.
- Selected generated chrome records its provider, remote asset or generation ID, label, source hash, deterministic derivation, and production hash. Runtime slate uses its deterministic cosine-feathered seamless derivative; frame centers and complete bevel bounds contain that same native-scale tile. The exploration stage uses its separately provenance-bound ImageGen surface as one low-contrast seamless tile only across surplus space outside projected map cells; it is never stretched or composed as a viewport frame. Opposite tile edges must match exactly, no container may stretch a stone center, and generated frame corners must remain opaque.
- Selected SpriteCook status glyphs remain their direct production outputs. The save disk and journal book/quill retain their recorded native dimensions; the project-owner-supplied Heal glyph retains its exact opaque pixel region. None may be stretched to a shared display size.
- Original controls use nearest-neighbor filtering at exact 1x/2x only. Fonts ship with pinned source, hashes, and licenses and introduce no runtime network dependency. `Theldrow-Classic.fnt` and its atlas are deterministic, metric-preserving conversions of licensed Castle `FONT` 1601; keep that native strike monochrome, nearest-neighbor sampled, and unstretched as reference evidence. `Theldrow-Classic-Vector.ttf` preserves Samuel's scalable CC0 outlines as reference. Runtime Classic text uses deterministic `Theldrow-Rebuilt.ttf`: the approved Pen letter contours, machine-verified production-sheet proportions, sidebearings, and mixed-case optical weight, exact FONT 1601 horizontal advances, zero kerning, and pinned OFL Grenze Gotisch numerals, punctuation, and extended utility glyphs. Numerals keep their Castle advances but are normalized to the declared cap-height figure zone. The reviewed lowercase `h` deliberately overshoots both the cap line and baseline; all other vertical variants follow their declared class zones. Black Chancery, ChicagoFLF, and Castle's Inter Geneva substitute retain their exact pinned bytes. Alegreya remains the readable-mode fallback family.
- The licensed Searching atlas remains byte-exact and records its eight-frame row in the manifest. Exact Torch frame files also remain byte-exact; presentation may key only each asset's documented legacy neutral-gray matte colors while composing them onto Rebuilt chrome.
- The eight Effects slots resolve exact application CICNs 14000–14063 as eight source-authored animation frames per condition. Keep the fixed condition-to-resource formula, scenario-first override semantics, and native 32-by-32 pixels. Search frames 14032–14039 may key away only the same documented six neutral-gray matte colors as the licensed Search control; do not redraw, recolor, or infer replacement effect art.
- Integrated sounds retain exact `snd ` IDs and load from Godot-imported WAV resources. The canonical importer reproduces Castle's 48 kHz, 16-bit linear interpolation from each decoded Macintosh sample stream and stores the one unique mono waveform instead of Castle's two byte-identical channels. Godot imports every Classic WAV in lossless PCM mode with mono forced so the audio mixer duplicates it only at playback; nearest-neighbor expansion and lossy QOA or Vorbis re-encoding are not permitted for the source-fidelity bank. The generated manifest and `THIRD_PARTY_NOTICES.txt` preserve the Castle commit, source fork, copyright, license, and decoding/resampling modification.
- Stock Plains, Subterranean, Castle, Desert, Swamp, and Snow landlook atlases retain exact application PICT 300, 303, 304, 305, 309, and 310 identities. Scenario media may override an exact PICT key; opcode-driven landlook changes resolve every other stock atlas through this application fallback.
- Integrated stock music retains eleven unique Castle modules and loads them from Godot-imported looping Ogg resources. Desert, Swamp, and Snow remain distinct playlist preferences but resolve the Outdoor module; `classic-application-music.json` records exact module commits and hashes, deterministic OpenMPT/FFmpeg conversion settings, output hashes, durations, and the pinned-MADG-to-later-standard-MOD Outdoor replacement. Custom 1–3 are scenario-owned exact resources; never copy them into the application bank.
- Integrated combat CIcons retain exact `cicn` IDs and native dimensions. The selected catalog is limited to source-backed active cues, result effects 159–167, bounded death-effect candidates 2015–2019, and existing spell cast/resolution frames 12000–12127. Standard spells with `lookStart` zero make Castle request the absent 11992–11999 family; presentation reports that exact unavailable identity rather than substituting art. Scenario media overrides the same exact key. Red pixels or numeric adjacency alone do not establish blood, gore, or a semantic identity.
- Integrated item CIcons retain the exact shared Data ID record identity, application-owned stock-supply identities referenced by active-scenario `Data NI`, and Castle's unidentified-range substitutes from `lookupicon`. Core view/request projection selects the player-knowable exact key; Inventory, Equipment, Trade, Shop, and Treasure render that key without exposing its numeric resource ID. Presentation never derives an icon from an item stable ID.
- Keep only selected production assets. Rejected candidates remain external evidence.

## Work Guidance

- Use low-contrast chrome so source art and readable text remain dominant.

## Verification

- Verify each manifest hash, dimension, semantic ID, source commit, rendering rule, and seamless edge pair against the committed file.

## Child DOX Index

- No child AGENTS.md files are currently required.
