# Deterministic Realmz core contract

## Purpose

Own the pure Realmz model, fixed Classic rules, topology, game clock, randomness, and complete mutable playthrough state.

## Ownership

- Direct Realmz definitions and mutable playthrough state, including characters, equipment, wealth, conditions, encounters, battles, shops, treasures, spells, monsters, races, castes, and immutable compiled scenario programs.
- Detached party views include current held-over ally instances separately from hostile combatants. The active monster set's menu-visible immutable definitions project into a separate ordered Bestiary catalog with their authored descriptions; presentation may inspect either view but cannot mutate ally ownership or infer the catalog from current party state.
- Typed intents/events/interactions/views and the core state carried by session snapshots.
- Pure host-facing value contracts shared across adapters and presentation, including immutable media descriptors/read ports and presentation settings. These records never enter `GameSession` or alter Classic rules. `PresentationSettings` schema 12 may retain an optional stable last-campaign identity used only for host prewarming; it never stores an install path or enters an adventure save. Auto Note defaults off from application PRFN 128; an explicit saved host preference may enable it without entering adventure state.
- `RealmzRules`, `RealmzClock`, `RealmzRng`, topology queries, and world overlays.

## Local Contracts

- `ClassicSpellCapabilityCatalog` classifies every application-library spell definition as a stock player spell, application effect, reserved standard slot, or explicit unknown and derives a deterministic mechanical behavior signature from rules fields only. The signed `queueIcon` field participates because zero versus nonzero selects one-shot versus persistent battlefield behavior; look and sound identities remain separate presentation facts. A shared signature is an inventory key rather than proof that any legal context is implemented. Character/monster projectile profiles are distinct from learned, scroll, charged-item, and monster spell-casting contexts because Castle reaches physical class-nine/damage-nine records only through equipped missile items. Application-owned target-three class-nine/damage-nine area records are charged-item projectiles only: resolution retains the loaded item's damage/to-hit bonus, one shared damage roll, the caster's missile statistic and caste bonus, and projectile defenses; learned, scroll, and monster spell-list contexts are not applicable. The negative-target Arrow of Transportation is likewise item-only: its first open coordinate uses the shared Phase transaction, while its nonzero size preserves the remaining attack allocation.
- All classes are pure `RefCounted` or value-like data. They never extend or retain Nodes.
- Rule modules may retain the smaller rule dependencies they use, but may not strongly retain their owning `RealmzRules` aggregate; core ownership graphs must remain acyclic so headless and host teardown release complete sessions.
- No scenes, autoloads, filesystem/resource APIs, audio, OS services, wall-clock time, or Godot randomness.
- JSON dictionaries stop at validating infrastructure factories. Domain state is typed and does not expose writable backing dictionaries.
- `MediaAsset` is the immutable content descriptor and `MediaSource` is the read-only byte-source port. Presentation may consume only these core contracts; archive/file access remains in infrastructure adapters.
- `PlayerIntent`, `InteractionRequest`, and `InteractionResponse` use closed typed payload families. Request bodies may serialize only at the save/event wire boundary; core, scenario, and presentation behavior consumes their typed variants directly. A Classic scrolling-text acknowledgement carries one exact signed `(resourceType, resourceId)` pair rather than copied bytes or a scenario-message identity, and that pair survives the pending-request save boundary. Level-up spell choices carry the application-owned description supplied by the candidate definition; its optional decoder field keeps older pending save-v4 requests readable without inventing absent prose. A field spell, scroll, or charged-item character-selection request carries one strict `SpellTargetContext` with the authoritative caster, spell, selected power, effective SP cost, icon resource identity, and target facts; presentation never reconstructs those values from prompt text. A request may carry a revision-local detached projection solely as a nonserialized performance hint; codecs and restore ignore it, and consumers must reconstruct from authoritative state when it is absent or its identity does not match.
- Core supplies the typed state, RNG, intent, interaction, and view records carried by a detached session snapshot. `src/session` owns the aggregate snapshot; infrastructure alone encodes `.r2save` JSON and filesystem persistence.
- Classic option-label records are immutable typed content distinct from ordinary messages; runtime choice resolution may query them but never mutate their source table.
- Every gameplay mutation is committed synchronously by the `src/session` transaction coordinator.
- Core never imports scenario, session-orchestration, infrastructure, presentation, or app classes.
- `core/scenario` owns only immutable compiled definitions. VM frames, execution, handlers, and runtime coordination remain under `src/scenario` and depend inward on these records.
- Session state owns encounter attempts/type flags, equipment escrow, mutable base-shop quantities, native-slot buyback stock, combat, and scenario-program replacement. These are save data, never mutations of installed package definitions.
- Session state also owns opcode-driven map appearance, random-rectangle geometry, and the one exact party-position bookmark. Signed Classic tile replacements retain their raw value in the existing terrain overlay; land projection normalizes positive marker bands and renders positive or negative icon-backed identities over the effective landlook base tile, while battlefield construction uses that same base beneath icon-backed land. Package maps and authored rectangle membership remain immutable; an explicit rectangle override replaces only that region's effective inclusive bounds, while landlook/darkness overrides feed exploration projection and future battlefield construction. `WorldState` may retain derived nonserialized flags and a revision for effective random-region bounds so projection can answer boolean membership without materializing ID arrays; deserialization reconstructs those hints from authoritative saved region state.
- Session state owns the selected Classic difficulty and Monster Set from party setup. Difficulty uses Castle's five signed choices; party commitment freezes the source-authored experience multiplier, while money continues to use the saved difficulty multiplier. Monster Set resolves every constructed monster through the package's complete typed replacement catalog. New sessions deliberately reset both choices to Normal instead of preserving Castle's stale process globals.
- Combat state owns the active actor's action, retained target, and next authored monster attack row. Its nonterminal outcome sentinel is the explicit `active` value; an empty outcome is not an active-battle test. Character attack capacity remains a signed integer half-unit field on `CharacterState`; save/resume may not recompute or round either boundary.
- `PersistentCombatField` is additive save-owned battle state for Castle's 60-slot spell queue. It retains the lowest available queue slot, exact spell/caster identity, center, Data AD shape and rotation, queue icon, power/cast level, remaining duration, and the initiative phase that owns once-per-round decay. Queue lifetime rolls and state creation precede the spell's separate immediate resolution; `CombatView` detaches its exact nonsolid mask coordinates and queue icon so presentation never retains or reconstructs field lifetime, shape, or terrain admission. Movement collisions use the field mask against the actor's complete battlefield footprint, bypass reflection and resource payment, and save the source-backed damaging-field collision slots consumed during the current activation. Every actor boundary clears that ledger. A new round first decays fields for the returning phase, then checks each living party character and monster independently so occupied fields can apply again before ordinary round effects.
- The Auto button owns one deterministic state/RNG checkpoint across its current activation and any immediately following persistent-Auto characters. Nested automatic activations reuse that transaction instead of serializing the complete battlefield again; any failure still restores the whole chain exactly.
- Monster automation scans through the fixed 100-monster capacity plus the live party roster. Summons and death-macro additions may grow initiative during that scan but may never expose a completed monster activation as a player command boundary.
- Session state owns non-VM interaction continuations as well as VM continuations. Random-rectangle surprise choices serialize with their owning region and resume only through `GameSession.respond`.
- Party condition slot 5 owns Castle's persistent Search mode. Detached party summaries expose all ten exact condition slots for the source-shaped Effects bank while retaining Torch separately, and `GameView` exposes the scenario-owned character-spellcasting block without leaking mutable `GameState`. Area Search is a separate held action: each submitted pulse performs the forced-secret scan and four-click clock/random phase before a save-owned continuation performs the outer one-click clock/random phase. Neither presentation nor the command catalog may merge those operations or aggregate the two Castle random checks. The Torch shortcut resolves exact Classic item 805 through the ordinary charged field-item contract. Classic door items are the other active Use family: field use admits absolute type 23 or Special 1 `-23`, combat admits only Special 1 `-23`, and both consume the ordinary exact-instance charge before dispatching Special 5 as a scenario XAP. The contextual Encounter command consumes its ordered random-door draws and continuation through session-owned state.
- Start Game, Party Death, End Adventure, contextual Shop, and contextual Temple execute as package-owned application hooks through the session VM. Their exact hook identity, program, resume operation, service identity, revival result, pending request, and VM frame form one typed save-v4 continuation. Direct scenario Shop and Temple opcodes bypass the contextual service hooks.
- Live age-band changes yield ordered `age_update` requests. Movement clock work suspends a post-clock continuation whose nested resume remains the unstarted post-move operation, so age acknowledgement and any midnight timed scan precede destination AP discovery. Each typed response advances one character before the owning session operation resumes.
- Monster death macros execute before battle resolution. Their combatant identity, VM interaction, and direct-session continuation belong to the save aggregate and resume only through `GameSession.respond`. Monsters killed by one spell enqueue death macros in source target order; combat state owns the queue, original caster, and deferred turn decision, validates the exact completion cursor, and drains every macro before battle outcome or caster advancement.
- Direct session-owned total-party defeat runs the Party Death hook before aggregate release. Opcode 119 revival records a no-reward retreat, publishes one terminal battle return, and releases combat exactly once without closing the session. Scenario opcode-2 mode 10 is the source-backed exception: it never enters Party Death, retains equipment escrow, revives every character to one stamina, clears Animated, and restarts its issuing program without RNG or a false terminal return.
- Combat Undo owns a narrow save-owned activation checkpoint rather than a transaction snapshot. A legal Undo restores the empty activation-start cell, recalculates only Castle's turn allowance, consumes no RNG, and re-enters the same round and initiative slot with Undo available again; attacks, spells, item results, reactions, turn advancement, prohibited conditions, and an occupied start cell still block it.
- A placed Action Point's Classic header is a post-action map/coordinate destination. A header that points back to the AP's own source cell is a no-op even when the program moved the party; otherwise `GameSession` applies it only after that AP completes, rechecks the destination cell once, and serializes the recheck depth so save/resume cannot repeat or skip it.
- An ordinary placed Action Point becomes disabled after its complete timeline. Classic opcode 24 Keep Codes preserves it; opcode 25 may disable it explicitly. An initially inactive packaged AP remains dormant until opcode 13 gives it a runtime chance override; a positive override makes it eligible without mutating immutable content. This world-overlay state is serialized across every interaction and save boundary.
- Every gameplay random draw goes through the session-owned `RealmzRng` and is serializable. Production RNG diagnostics retain the newest 4,096 draws; explicit scripted/oracle sources may opt into an unlimited trace. Saves own generator state and draw count, never diagnostic trace history.
- Classic field time advances as an ordered stream of individual timeclicks: five minutes per land click and one minute per dungeon click. Each click publishes its intermediate clock state, while condition decay, fatigue, spell-point recovery, half-day recovery, and timed/random continuation work remain attached to the exact boundary that crosses them; callers may stage source-separated click groups but may not collapse them into one coarse clock mutation.
- Classic thief actions map their eight authored action slots to character trained abilities 5 through 12. Timed Pick Lock previews a deterministic clone of the session RNG, then commits only the player-selected frame prefix through the real RNG; the request preserves Castle's 30 Hz updates and final static countdown second without saving presentation timer state.
- Each character owns exactly ten Fast Spell bindings. A binding stores one known stable spell ID and valid power, persists inside character state across campaign saves and vault revisions, and invokes the ordinary field/combat cast contracts; shortcut activation may not duplicate cost, target, RNG, or effect rules. `FD-SPELL-003` permits exact-cost activation and commits only after valid targeting instead of reproducing Castle's prepayment and greater-than-or-equal cost defects.
- `RealmzRng` uses the documented QuickDraw 16807/mod-2147483647 state transition and Castle's inclusive scaling; raw scripted values are test-only branch controls.
- Snapshots and restores detach typed state so callers cannot mutate an active session through a prior envelope.
- `GameSession.close` releases the complete aggregate exactly once at an ordinary or active-combat command boundary and publishes only detached campaign identity. Other gameplay interactions remain modal and reject close transactionally. Host confirmation and repository writes remain outside the simulation; a closed session has no save envelope or pending interaction.
- `MapTopology` plus `WorldState` overlays is the only source for simulation map facts; movement, pathfinding, LOS, search, triggers, and views reuse its explicit cells, edges, and features. Providence's validated fixed row-major compact topology is retained immutably and materialized into typed cells only when queried. An unoriented hidden land secret preserves the underlying terrain's passability while concealed, including solid collision even when a placed AP shares the cell; once discovered, the revealed entrance is passable regardless of that terrain, and the same discovery overlay controls marker visibility and colocated placed-trigger eligibility. Directional dungeon secrets remain edge-owned traversal facts. Ordinary LOS uses a bounded radius-eight supercover probe: blocking cells remain visible, blocking cells or edges occlude every farther cell, and discovered secrets/open doors alter that same topology. The documented Rebuilt Wizard's Eye correction ignores occluders and doubles that bounded exploration radius to sixteen. `WorldState` saves exact per-map seen cells separately from walked cells; a current-schema save without that field safely seeds sight memory from its walked cells only.
- Player-authored location notes are mutable `WorldState` records, separate from immutable acquired-player-map definitions and scenario-authored journal discovery. They preserve independent land/dungeon record streams, stable source ordinals, map level/location, native location ID, and Castle's numeric saved darkness level. Only the party's current validated topology cell may be changed. Each detached note may carry a bounded historical `MapView` centered on its saved coordinate without relocating the party or retaining mutable topology. `FD-MAPS-001` rejects a 502nd record per stream rather than reproducing Castle's append-beyond-search defect; editing consumes no clock or RNG.
- Authored-journal discovery is mutable `GameState`, not copied narrative text or a chronological event log. It stores unique Data SD2 message IDs 0 through 2,999, detaches their immutable package text in numeric source order, consumes no clock or RNG, and validates every identity against content before restore. `FD-MAPS-002` rejects Castle's unsafe 5,999 cursor range rather than indexing beyond its 3,000 saved flags; browsing position remains presentation-only.
- Acquired player maps are immutable `WorldDefinition` records plus stable acquired IDs in `WorldState`. Detached views expose all twenty menu slots in source order, but attach topology cells, exact media IDs, markers, notes, and current-party eligibility only to acquired records. Land crops retain the authored icon size; dungeon views detach Castle's fixed 20-by-20, 16-pixel topology crop while retaining the independent authored marker scale. `FD-MAPS-003` rejects IDs outside zero through nineteen and nonpositive icon sizes before either value can reach Castle's unsafe array/division paths. Viewing remains presentation-owned and consumes no time or RNG.
- Land travel accepts all eight adjacent destination cells and follows cardinal or diagonal Layout neighbors at map edges and corners. Castle Layout crossing validates only that neighbor, wraps the preserved edge coordinate, and does not re-admit the target tile through ordinary terrain collision. A placed Data DD cell that is not an undiscovered land secret likewise enters through Castle's door-band branch before ordinary boat/terrain rejection; the secret 3000 band instead preserves underlying collision until discovery. This exception follows the exact attached trigger identity and never admits adjacent ordinary land while aboard. Movement within one land level, detached availability, and land pathfinding use the shared destination probe without corner blocking. Dungeon movement and pathfinding remain cardinal and edge-based.
- A topology cell retains immutable authored presentation metadata for its base tileset/tile and optional positive or negative icon-backed land overlay asset. Detached views replace those facts from save-owned signed Classic tile mutations when present; neither authored nor effective presentation metadata becomes an alternate movement, LOS, or trigger model.
- Immutable `BattleTerrainSetDefinition` catalogs preserve Castle's signed mapstats and exact 3×3 combat builds for battlefield construction. Classic atlas artwork is one-based: a zero combat-build cell remains empty and never aliases tile one. They are map-referenced construction inputs, not a second exploration topology. Battle start generates one session-owned `BattlefieldState` from the current map plus overlays before initiative; terrain, source shifts, actor anchors, positions, and complete footprints serialize with `CombatState`, while `BattlefieldView` is detached presentation data.
- Combat pursuit is route-first for Party Auto and monsters. `BattlefieldRules` derives four nonserialized navigation profiles from the authoritative battlefield for 1x1, 1x2, 2x1, and 2x2 footprints; each caches static passability and the maximum destination terrain charge across the complete footprint and invalidates only when the battlefield instance, terrain catalog, or nonserialized terrain revision changes. The retained typed search workspace uses generation counters instead of clearing full-grid distance, first-step, closed, goal, heuristic, queue, and heap storage. Current actor occupancy blocks only the immediately committed step, except that a rules-legal size-zero friendly swap remains a five-point route edge; later occupancy remains a forecast. Exact charged movement cost and stable direction/heap tie order remain authoritative, and Castle's bounded RNG shifting runs only when no weighted contact route exists. `AStarGrid2D` remains tool-only comparison infrastructure. Party Auto prepares the fresh character activation before scoring so prior-turn spent movement and half-attacks cannot suppress a legal opening pursuit.
- Battlefield setup follows Castle's terrain-decoration, formation, party, ally, authored-slot, monster-construction, dead-position cleanup, and initiative order. An omitted participant list enters the complete party; a validated stable participant-ID subset enters only those party characters, initializes and orders only that subset, and serializes the boundary through the ordinary battlefield and turn-order state. It is transactional. `FD-COMBAT-006` requires a complete finite placement search and exact state/RNG rollback when no legal cell or footprint exists; no setup failure may leave speculative draws or partial combat state.
- Random rectangles follow Castle's reverse region order, 1-in-10,000 chance scale, three ordered random-door draws, signed one-shot door state, surprise choice, and battle selection through the session RNG. Battle selection preserves Castle's signed-short `randrange` width, including authored inverted ranges; package validation requires every actually selectable battle identity rather than normalizing the authored endpoints.
- A moved-to coordinate selects only its lowest `classic_record_index` placed Action Point. Disabled or failed selected records do not fall through to another same-cell AP; positive sub-100 chances consume one tagged draw, while percentages below one consume none.
- `GameView` and its map/cell views are detached read models for presentation and never expose mutable simulation objects. Its nonserialized `ViewChangeSet` identifies roster, status, inventory, magic, exploration, service, combat, and system changes plus affected stable character IDs; restore, replacement, unknown events, and incompatible interaction changes request a complete refresh. `CharacterView` reuses immutable identity, definition, inventory, and magic records across status-only snapshots while preserving its public read-only surface, and equipment results are keyed by the character inventory revision. `MapWindowView` is a disposable 8x8 copy-on-write chunk window over authoritative topology; unchanged chunks/cells may be shared by later snapshots, while `cells()` remains an on-demand compatibility materialization. `MapView` preserves the effective save-owned `landlook` art identity, Castle's independent nullable `base_scale` fact, and the save-owned coordinate-display and compass flags needed by presentation.
- Detached Race/Caste creation options carry authored descriptions only when the source record provides them, plus concise display facts derived from typed rule fields that current Classic rules actually consume. They never invent prose, friendly meanings for raw arrays, or claim dormant source flags as active modifiers.
- An active direct-session battle with no VM-owned pending interaction projects a revision-local typed `combat_action_request`. `GameView.active_interaction_request()` gives a real pending interaction precedence, so rewards and other mandatory responses replace—not overlap—the battle command deck. The projected combat request is detached UI legality, not save-owned continuation state.
- Save-slot previews are detached read models under `src/core/view`; infrastructure constructs them and presentation may display them without depending on save repository types.
- Combat rules publish detached, non-save-owned presentation facts in ordered domain events: source actor/target coordinates, semantic result kind and amount, automatic-action identity, and exact proven Classic effect resource identities. These facts describe an already committed mutation; they never create a playback continuation or allow animation to advance the turn. Character spell choices are staged by spell and power without enumerating every actor or battlefield center. They expose the exact Data AD mask for preview; the final selected actor, center, or ordered summon-space sequence is range, LOS, footprint, mask, and boundary validated once by the authoritative combat probe before mutation.
- `CombatFlow` is the stable combat-rule facade. Lifecycle/turn sequencing, character actions, combat magic and summoning, reactions/retreat, and monster automation live in named collaborators sharing one typed `CombatFlowContext`; direct and automatic actors must reach the same rule operations and event ordering through that facade. Classic learned, fixed-power scroll, charged-item, Party Auto, and monster summoning select native monster identity through the session RNG, construct each instance through `MonsterRules`, place its complete footprint transactionally, append it to initiative, and persist summoned provenance on the instance. Monster AI chooses its exact legal placement deterministically after the serialized weighted action-category draw and stops summoning once its allegiance has the configured force advantage.
- Classic combat target type seven is an automatic party-condition cast for learned spells, fixed-power scrolls, and charged items: it rolls duration once, retains the greater of the existing party slot and the new duration, and uses the authored special as the party-condition index. Party Auto uses the same route only when it improves the current duration. Castle's zero-range monster selection cannot reach these records, so the monster context is not applicable.
- Signed negative Classic condition durations remain negative, persistent potencies: resolution adds them to a nonnegative actor-condition slot, later casts cannot replace an already negative slot, and condition ticks use their absolute magnitude without decaying them. Learned, fixed-power scroll, charged-item, Party Auto, and monster sources share this rule.
- Classic damage type eight is direct damage rather than an eighth elemental defense channel: ordinary resistance still applies where the record permits it, but no elemental save, protection, or monster vulnerability adjustment applies. Queued type-eight fields retain Castle's unmarked collision behavior and may affect the same actor on another collision check; zero-cost monster fields remain legal and spend zero spell points.
- Scenario-owned group spells roll one duration and one damage value before resolving stable party-order targets through the shared noncombat save, protection, condition, health, age, and attribute path. Thief traps use that group contract at their authored power after ordinary trap damage and spend no spell points.
- Developer forced victory may remove hostile combatants only before calling `CombatFlow.finish_debug_victory`; the facade then uses the ordinary resolved-battle lifecycle. Reward construction, scenario continuation, terminal events, and RNG ordering remain unchanged.
- Classic opcode 100 is a distinct rules-owned terminal transaction: it defeats living hostile monsters without death macros, records Castle's slot-eight post-battle sentinel, completes victory, and consumes no RNG. The sentinel round-trips with `CombatState`; scenario reward ownership uses it for experience-only construction and the terminal caller unwind.
- Successful battle setup publishes stock sound 10049 before `battle_started` and any automatically processed opening turns. This is a detached presentation request inside the committed event sequence, not an audio service dependency.
- Detached item/spell/monster views carry exact content icon IDs and resource types. Immutable monster definitions retain their independent Classic name identity, authored bestiary description, and menu-exclusion fact. Item views retain player-visible item type and icon while revealing identified names, descriptions, definition identity, and values only after simulation marks the instance identified.
- A detached Shop request carries each shopper's exact portrait and load facts plus player-knowable carried/stock descriptions, weights, and item facts; each active stock entry retains its native slot category. Save-owned buyback entries retain the first empty native slot chosen at sale; presentation may filter and select these facts but never derives item details, categories, or legality from names, IDs, or icons.
- `SpellView` carries Castle-visible immutable spell-record facts needed by the spellbook: level identity, damage and duration scaling, range, target type/size, resistance and save adjustments, rotation, exact media identity, and Castle's exact eight-frame resolution-effect resource sequence. It also carries separately source-probed combat, field, and scribing availability so a known spell remains inspectable with its exact current-context reason. Draft `CharacterSpellOptionView` records carry the same sequence for creation previews; neither view exposes mutable spell definitions or grants casting legality.
- Each detached carried-item view owns source-probed Equip, Unequip, Use, Identify, Split, Join, Drop, and exact Trade-target availability. Every Trade target also carries its current, post-transfer, and maximum load so presentation never recomputes capacity. Ordinary equip/unequip/trade/drop mutation goes through `InventoryRules`; presentation cannot infer slots, race/class admission, hands, quivers, curses, capacity, or load. `FD-INVENTORY-001` permits an equipped ordinary item to transfer as unequipped while blocking Castle's equipped-curse bypass and stale derived bonuses. `FD-INVENTORY-002` preserves Castle's charged-stack Split and Join results while immediately restoring exact derived load, rejecting signed-short overflow, and keeping the selected stable item instance as the Join survivor. Drop uses a save-owned yes/no continuation naming the exact character and item instance. Charged spell-item use validates the exact owner/instance, permission, context, charge, spell, power, and target before mutation; positive charges remove their per-charge load, negative charges remain infinite, and a pending field target selection plus rolled random field power are session/save-owned. Restore rejects a fixed-power continuation that does not match the item's authored power, and request text retains the player-known identified or unidentified item name. Combat item use spends Castle's movement/half-attack resources but no spell points; fixed-power actor, ordered one-through-power repeated actor, group, ray, and rotatable area targets share the ordinary spell geometry and transactional rollback. A combat item with authored power eight first rolls one through seven into activation-owned `CombatTurnState`, spends no charge or action resource, and exposes only that retained power through the next target request; invalid targeting preserves the staged state and RNG position, while commit spends one charge and clears it. Repeated items validate a distinct ordered target sequence before mutation, reroll each target independently, and spend one charge for the complete transaction. Paid identification is available through an active shop continuation; inventory Cast Identify selects the first party-order caster who knows special 48, can cast, and has at least 25 spell points, then spends exactly 25 and identifies every item carried by the selected character. Type-13 case transfer moves the one physical item unequipped together with all five ordered scroll records, clears the source records, and rejects a recipient who already carries a case or assigned scrolls. Door/XAP items use their source-backed scenario continuation; unsupported passive effects stay disabled until their own source-backed implementations or fidelity decisions exist.
- `EconomyRules` owns Classic shop buy and sale valuation. Sale integer-halves absolute cost, applies charge condition, caps sale inflation at 100 percent, and applies the unidentified one-fiftieth penalty. `FD-ECONOMY-001` treats a zero-authored-charge item as full condition instead of reproducing Castle's undefined zero-divided-by-zero conversion.
- `EconomyRules` also owns ordinary Pool, Share, and Swap probes and mutations. Pool removes all personal denomination load; Share processes jewelry, gems, then gold in party order; Swap moves exactly five gold or one gem or jewelry per action. Bank-backed Swap imports every banked denomination once on entry, leaves the pool untouched on Done, and returns the complete remainder to the bank when movement clears location services. Without a bank, a valid movement attempt with pooled wealth owns a typed saveable direction/choice continuation before passability: decline clears the pool, while accept opens Swap and Done clears the remainder; both resume that ordinary movement exactly once. Castle's distinct internal fallback return is not the ordinary caller contract. Bank-backed shops and temples import and return the same complete pool at their source exit boundary. Every committed wealth distribution recalculates Classic movement. `FD-ECONOMY-003` requires the complete denomination weight to fit instead of reproducing Share's jewelry-overload defect.
- `TempleRules` owns the nine fixed temple services and their signed percentage costs. Temple payment and Pool/Share mutation remain session-owned through `EconomyRules`; presentation receives prices, status, and exact availability but never applies healing, conditions, equipment, abilities, or wealth.
- Personal carried load is a derived invariant: gold plus gems plus fifteen times jewelry plus every item's base and charge weight from immutable package definitions. Character finalization materializes identified starting items with authored charges in caste order, tries ordinary Classic equipment admission, and retains only records that pass capacity. Every item-instance ID has one exact owner across character inventories, party storage, equipment escrow, and combat fumble custody. Vault import rejects an incoming ownership collision before mutation; save restore enforces the same aggregate invariant even outside combat. Vault import recomputes the total against the target package; save restore rejects unknown item identities or a mismatched total transactionally.
- Opcode 36 equipment escrow is scenario-owned party state, not a player storage service. Do not expose `PartyState` escrow or interim-find storage as an ordinary inventory destination.
- `GameView` reports action availability as `enabled` plus an explicit reason. Presenters cannot infer availability from the existence of a control. Combat Move availability derives from enabled detached `CombatView` movement probes while outcome is `active`; a fresh activation with no active turn transaction probes and displays the character's maximum movement even if committed state retains the prior remainder. The same active boundary suppresses exploration Move/Search and field item/spell actions.
- `MapView` carries one bounded party-local cell projection: 25×25 by default for headless consumers, or the live host's exact complete-cell viewport span after responsive layout. It also carries the complete walked-coordinate set needed by the minimap, the exact saved sight-memory coordinates needed by LOS presentation, save-owned four-way Classic dungeon heading/view policy, and movement availability computed through the same topology query as movement: eight directions on land and four in dungeons. A strictly ordinary adjacent revision may inherit those immutable visibility segments through a typed nonserialized delta containing only viewport shift and newly visited/seen coordinates; the chain is periodically flattened and never enters saves or simulation. Projection sizing is nonserialized read-model configuration and remains bounded by the authoritative map rather than duplicating the full 90×90 topology when the viewport needs less.
- `GameState.dungeon_heading` is the dormant/saveable Classic north/east/south/west value 1–4. A validated quarter-turn changes only that heading; it consumes no clock or RNG and leaves absolute topology movement to the ordinary movement intent. `GameState.dungeon_multiview` is the save-owned authored permission to leave 3D; detached party condition 4 reports Wizard's Eye as Castle's temporary override without changing that authored policy. `xy_display_hidden` and `compass_enabled` are independent save-owned Classic application flags; they alter detached location facts and consume no clock or RNG.
- `PartySummaryView` exposes authoritative aboard state as one detached presentation fact; renderers may select source-proven party art from it but cannot infer boat ownership, passability, or movement legality.
- Classic-visible Realmz behavior is the fixed ruleset. Fidelity corrections require a documented decision and source/oracle tests.
- Campaign display metadata, restriction summaries, race/caste eligibility, and character-creation drafts are detached read models. They may explain a legal choice, but only `GameSession` and fixed Classic rules decide whether a character can enter a party.
- Character appearance definitions retain distinct portrait and combat-icon roles. Scenario content resolves an exact local role/resource first and then the pinned application character catalog, so a scenario's unrelated reuse of a stock CICN cannot invalidate a reusable stock appearance ID. Creation and vault import validate stable IDs against the composed roles; `FD-CHARACTER-002` aliases Human set zero to portrait 257/tactical 9000 rather than exposing missing source-formula resources.
- Party setup begins empty and remains session-owned across any number of finalized-character, vault-import, removal, save, and restore boundaries. Exploration intents fail until explicit Begin commits a nonempty party; no placeholder character or view-revision shortcut may stand in for that state.
- Active Party Order is one synchronous `PartyState` permutation of every current stable character ID exactly once. It preserves the existing character objects, consumes no RNG or clock time, and persists through the aggregate character array. Setup, solo-party, pending-interaction, and combat requests remain unavailable; malformed permutations are transactional failures. `FD-CHARACTER-003` makes cancellation presentation-owned instead of reproducing Castle's apparent cleared-track out-of-bounds write.
- Character generation commits one session-owned provisional `CharacterState` after the complete creation RNG sequence. Review, reroll, cancel, starting-spell edits, unspent-point confirmation, final acceptance, and vault-publication confirmation all preserve that boundary; accepting a reviewed character never rolls it again. The provisional state has no starting inventory; final acceptance grants identified, authored-charge caste items after spell selection and derives personal load, matching `CharacterGen`'s `addinitialitems` order without reproducing its premature `numitems` increment.
- `CharacterCreationSession` is the pure application-workshop adapter around the same `GameSession` creation transaction. It admits only creator intents, uses the app's stock content and RNG seed, and exposes one detached completed character for host publication; it cannot assemble a party, begin exploration, or create a second rules implementation.
- Starting-spell selection follows Castle's seven costs and only the first twelve `cspells` slots per eligible level. Resource-name slots thirteen through fifteen are not character spells. Unspent points require a typed, saveable yes/no confirmation.
- Advanced creation accepts only Castle's fixed `Startlevel` choices through the independent Data RI per-character maximum, where zero means uncapped. The Data SC aggregate maximum remains source guidance and is not a registered-game admission rule. Creation constructs the level-one base, applies the target-minus-one prestige penalty and caste victory threshold, then invokes the ordinary level-up path once for every additional level before spells and initial items. Every registration, combat-stat, condition, spell-point, stamina, resistance, and ability draw remains in the session RNG trace.
- Character creation initializes all forty condition slots from the race, then applies only caste condition-level `1` as permanent `-1`; larger caste values are level-up thresholds. When reached, a threshold replaces a nonnegative current value with permanent `-1`; only an already-negative sentinel decrements. Initial saves use the serialized race/caste bonuses and the `CharacterState` `-99…120` bounds.
- `CharacterState.specials` owns twelve racial combat modifiers. Its separate fifteen-slot trained-ability array uses fourteen authored race/caste inputs; `FD-CHARACTER-001` keeps the unauthored final slot zero and draw-free. Two-hand is retained as a source-owned character statistic but has no inferred combat effect.
- Character creation preserves Castle's complete random sequence: six assigned attribute rolls plus the seventh discarded loop roll, gender adjustment, cumulative race aging rows through the caste minimum age group, final race/caste bounds, three threshold-based special bonuses, stamina, age, and optional spell points. Aging table columns 8 through 14 adjust only saves 0 through 6 during creation.
- Character inspection is detached and mutation-free. `CharacterView` owns named display projections for conditions, saves, special modifiers, abilities, race/caste traits, all five aging rows, and the complete lifetime combat record; an active age row is derived without reproducing Castle's view-time `currentagegroup` write under `FD-CHARACTER-004`. `CharacterLifetimeRecord` is signed save-owned state. Physical attacks, character spell commits, learned spell commits, Turn Undead outcomes, and combat defeat transitions update only their source-backed central owners, while prestige uses Castle's exact weights and truncates signed damage division toward zero.
- `CharacterState` persists exact age days and the independent current age group. Each crossed midnight and each source-backed age-changing spell invokes one Castle age operation: it may apply or erase only one adjacent row, does not reuse creation bounds, floors maximum movement at two, and adjusts only saves 0 through 6. Maximum age reduces battle experience to Castle's truncated two-thirds share; `doesNotDie` remains stored content but has no invented death or exemption behavior.
- A party-target monster attack with Classic special 17 consumes the generic special-potency draw after ordinary damage, tests save slot seven, and on failure applies the attack-row lifespan formula through the same one-transition aging rule. A changed band pauses combat at a session-owned age update before the next actor; the combat continuation, interaction, character mutation, pending weapon condition, Dragon Hide feedback, damage, and RNG boundary all survive save/restore and resume once in source order.
- Monster status specials 1 through 7 and 16 consume generic potency before their source-family save and mutate the exact Classic condition slot. A negative duration is permanent. Party targets add once only when the starting duration is below 30; monster targets stack without that gate. A monster target above 100 runtime magic resistance converts the complete special attack to a miss after potency and before its save. Special and source-sound events precede ordinary damage feedback.
- Monster resource specials 8 and 9 consume the same generic potency draw before their source-family branch. Spell drain tests save six, transfers up to three points per attacker hit die, and permits the attacker balance to exceed its maximum; experience drain tests save five, subtracts twenty times attacker maximum stamina from direct earned experience, and has no monster-target case. Both mutations and above-maximum monster spell energy remain session-owned and saveable.
- A source-shaped combat spell special 57 uses ordinary reflection and resistance before healing. Target-one, class-eight, positive-cost records expose living party and monster actors; monster `cannot == 4` selects same-allegiance actors including self. Stock target-five, class-seven, zero-cost effects instead resolve automatically on the caster through learned, fixed-power scroll, charged-item, Party Auto, and monster sources. Party healing observes stone/death exclusions and caps at maximum health. Monster healing retains signed-16 arithmetic, may leave current health above maximum, and marks that monster attacked. Restore validates signed bounds but may not reject this source-valid over-maximum state.
- Monster charm is battle allegiance, not a generic condition: save zero includes party charm resistance; affected party actors are session-owned automatic combatants, target only the opposite side, remain valid hostile targets for loyal actors, participate in outcome checks, survive mid-battle restore, and return to base allegiance at battle cleanup.
- Monster construction copies all forty immutable Data MD starting conditions into session-owned state before combat. Negative permanent values remain active; no constructor may synthesize or discard authored condition slots.
- Monster elemental specials 11 through 15 retain the generic potency draw followed by elemental damage, source-family save, and matching protection. `FD-COMBAT-001` consistently applies protection to committed monster-target damage instead of preserving Castle's cold-through-mental display-only ordering defect.
- Character melee resolves from a validated snapshot of equipped immutable definitions. Armed state comes only from an equipped Classic type-2 item, never from a nonzero damage bonus; conflicting melee slots fail explicitly. `FD-COMBAT-002` makes weapon elemental damage use the defender's save while retaining Castle's immunity, protection, and RNG order. Monster `requiredWeapon` and `magicToHit` are independent attack gates; battle `distance` is placement-only. `FD-COMBAT-003` normalizes specific signed-byte requirements to Divinity Item Numbers and disallows Castle's unarmed family-restriction bypass.
- Ordinary monster melee receives a detached attack context containing the resolved carried weapon, Realmz day, effective defender luck/armor, and party Dragon Hide state. Its bounded fumble-disabled resolver preserves Castle's luck-before-hit and weapon/unarmed damage order, evil-conditional protection, ten-percent floor, typed weapon gates, and Dragon Hide's physical-only reduction plus synchronous sound 694. `FD-COMBAT-004` floors negative physical damage at zero instead of allowing Castle's subtraction to heal the defender. `FD-COMBAT-010` replaces a projectile weapon from the actual attacker's native slot zero instead of Castle's stale global monster; reflection remains unavailable. Castle's positive `behind` is a withdrawal-attack modifier, not character/monster icon facing.
- Battle state owns a bounded twenty-entry queue of exact fumbled `ItemInstance` values and the active turn's committed-physical-action fact. Character attacks consume the level-scaled fumble draw even while unarmed; cursed weapons and a full queue suppress the fumble without changing the attack result. Armed monsters consume the HD-scaled draw, clear only their active weapon on a fumble, and continue later authored rows unarmed. After body-count selection, exact fumbled instances lead the ordinary reward queue and are identified there; `FD-COMBAT-005` preserves remaining charges. The fixed runtime currently uses Castle's default enabled behavior; do not add an `allowfumble` toggle without also source-owning its five-percent XP coupling.
- `ClassicRewardState` owns one bounded ordinary reward transaction: origin, source identity, exact pending instances, pooled experience and recipient awards, detection/identification knowledge, completion confirmation, level cursor/result, and spell-selection cursor. It must round-trip canonical JSON without accepting fractional integer fields. Battle state separately owns exact-once `rewards_started` and `rewards_completed` facts; package validation must finish before either reward RNG or that one-shot ownership advances.
- Treasure capacity uses the exact current `ItemInstance` weight and permits a result equal to maximum load under `FD-ECONOMY-002`. Presentation receives the core-owned availability/reason and may not recreate Castle's inconsistent base-weight strict-less-than check.
- Player-controlled character melee activations normalize any positive prior reserve to one half-unit, clear a negative reserve, add normal attacks plus attack bonus, add Castle's four Speedy half-units, and spend two units plus three movement per attempt. A reserve of at least two retains the active actor. Monster physical activations choose once, retain a living target, and execute each validated authored row in order. Charmed-character cadence, spell/item limits, monster Speedy overflow, and Castle's contradictory zero/one-unit display remain unresolved rather than sharing this bounded rule implicitly.
- Battle state owns each party character's Castle melee/missile toggle. Setup prefers an equipped type-2 weapon, otherwise type 15; switching costs no attacks, movement, turn, or RNG and returning to melee permits hand-to-hand. Equipment projection rejects duplicate type-2/type-10/type-15 slots. Missile mode cannot enter the melee resolver.
- Ordinary physical class-nine projectiles use integer-truncated Euclidean anchor range, the battlefield LOS query, projectile-shield immunity, missile dodge, one damage roll reused across fixed repeated hits, and one player caste-bonus draw. A committed player Fire spends one finite charge, two half-attacks, and twelve movement; a monster uses exact native slot one, retains initial range power while lowering unaffordable cost power, resolves at power one, spends all movement, clears Guard, and targets an opposed party character. Out-of-range or zero-affordable-power missiles continue into the source-ordered cast/movement decision without repeating the missile roll. Random-power, elemental, special, charmed-character, and allied-monster projectile paths remain explicit unavailable boundaries. Monster spell definitions preserve ten native positions; only a wholly empty legacy package array may normalize to ten empty slots, while any one-to-nine-entry array remains invalid because it can change slot selection and RNG.
- Ordinary memorized combat spells cover combat-enabled damage spells without specials plus the strict source-shaped condition-cure family. The positive-cost, non-physical class-nine Flame Missile record uses ordinary spell resource accounting with projectile shield/dodge defense; a charged item may invoke a source-authored zero-cost nonphysical class-nine application effect such as AOGM Flask of Oil because the item spends its charge instead of spell points. These application-family class-nine elemental attacks are not native learned or scroll records, remain legal monster slots, and resolve monster range, target, damage, and event power at Castle's forced power one. Zero-cost physical projectile records remain owned by weapon/projectile actions. Player admission uses one mutation-free probe shared by the detached picker and execution, including Castle's flags, conditions, round-hit state, per-activation limit, known spell, cost, tier, range, and LOS. Monster spell resolution retains exact fixed slots, multiple magical attacks, Guard/movement clearing, and one persisted post-movement retry; deliberate AI scoring chooses the legal spell, affordable power, and target instead of Castle's random slot/power/native-gap sampling. Other source-authored zero-cost ordinary monster effects such as breath and spit attacks remain legal at zero spell points, spend zero, and use the scored power; negative-cost records remain unavailable. Failed charm saves continue through Castle's later defenses. Friendly/charmed-monster, non-cure specials, item/fast/random-power, unsupported zero-damage effects, and other anomalous nonpositive-cost branches remain explicit.
- Heroism is the strict application-effect target-five, class-eight, damage-type-eight, special-zero signature with zero cost and damage, a positive fixed duration roll, and no power scaling or defenses. Castle rolls duration before damage and then performs no target mutation. Learned, fixed-power scroll, charged-item, monster, and field sources retain that inert resolution and their source-owned payment/action rules; Party Auto has no viable plan for it and monster AI assigns it no tactical weight.
- A strict condition cure is combat enabled, queue zero, size zero, target type zero, one, or self, class eight, damage type eight, zero damage/duration/scalars, and a condition special above 100. It clears exactly `special - 101`, bypasses reflection and resistance in Castle's source order, and reports `clearedCondition` in the committed result. Manual learned spells admit actor target types zero and one; fixed-power combat scrolls and charged items may also use self. Field casting, repeated combat targeting, combat scrolls, charged items, Party Auto, and monster AI share that deterministic resolution; items spend one charge and no caster spell points. Auto scores only an afflicted legal ally, giving poison and disease greater urgency; it never invents a cure target or spends a draw when no cure is viable. Group/area cures and other zero-damage specials remain separate.
- A strict single-target friendly condition effect is combat enabled, queue and size zero, target type one, `cannot` four, class and damage type eight, zero damage, a positive finite duration, and special one through forty except twenty-eight. Learned spells, fixed-power scrolls, charged items, Party Auto, and monster AI share the same condition-slot resolution and publish `appliedCondition`; save state preserves the exact duration. Automatic actors consider only a living positioned ally whose slot is zero, avoid reflecting allies other than self, and do not repeatedly stack an already-active buff. Group, area, hostile, permanent, and non-condition special effects remain separate capabilities.
- Classic combat damage types one through seven share the ordinary save, protection, resistance, negative-save modifier, targeting, payment, event, and automatic-action paths. Damage type seven is a real source-defined non-elemental channel, not an unsupported or presentation-only spell signature.
- A combat-enabled queue-zero special 27 or 49 uses Castle's ordinary resistance and damage-save order, then replaces the damage with ten plus the target's current stamina. A zero-damage spell that saves receives no effect. Special 27 additionally writes permanent Turned to Stone after the failed save and before lethal damage. Character, scroll, charged-item, Party Auto, and monster-AI sources share the same resolution, terminal outcome, and monster death-macro path.
- A combat-enabled queue-zero special 28 uses the same ordinary defense order, then replaces damage with the signed rolled duration and writes condition slot 27 (`BLIND`) when its prior value is nonnegative. A zero-base successful save cancels both effects. Negative duration heals character slots only to maximum health but may raise monster stamina above maximum; positive duration can defeat the target. Repeated actor and fixed-area records use their native target geometry through learned, scroll, charged-item, Party Auto, and nonnegative-cost monster sources. Application Salt spell 4612 is owned only by item 747 at fixed power one: it spends a charge rather than spell points and is not a learned, scroll, or monster context.
- A combat-enabled size-zero, queue-zero special 59 with friendly target type one or self converts its resolved positive damage roll into spell points, capped at the target's maximum, and deals no stamina damage; Castle and War records may use either the stock class/damage-type-eight metadata or alternate authored metadata. Casting cost is paid before the capped restoration. Learned spells, scrolls, charged items, Party Auto, and monster AI share the same resolver and publish the actual `spellPointDelta`.
- Combat-enabled queue-zero charm specials 51 and 52 use Castle's ordinary reflection, charm-save, immunity, and resistance order, then assign the affected actor to the caster's allegiance; a charmed monster also clears its stale target. Learned spells, scrolls, charged items, Party Auto, and monster AI share the same resolver and publish the changed allegiance.
- A combat-enabled queue-zero special 1 through 40 except special 28's signed-duration Blind path may combine ordinary resolved damage with its positive-duration condition instead of being restricted to zero-damage friendly buffs. Castle specials 2, 3, and 7 additionally set current character movement to zero or halve it after defenses; later character activations derive movement from Helpless, Tangle, and Slow just as monster allowance already does. Learned spells, scrolls, charged items, Party Auto, and monster AI share the same condition resolver and source order.
- `FD-COMBAT-015` preserves stock Tangle Weed's authored unsigned special 253 in immutable content and behavior signatures but resolves that impossible out-of-bounds condition reference as special 3, Tangle. Its target-type-three area, field queue, defenses, duration, movement effect, automation, and save behavior remain generic.
- Ordinary player automatic-group spells support target types 9, 10, and 12. They select living positioned party slots before monster slots, spend cost and action resources once, roll duration/base damage once, apply defenses per target, and enqueue defeated monster macros in that order. Under the documented improved-AI allowance, monster target types 9 and 10 use a legal scored affordable power instead of Castle's incomplete power/resource branch: target nine selects every living positioned same-allegiance actor, including the caster, while target ten selects every living positioned opposed actor. Both retain party-before-monster order, spend once, and resolve through the same deterministic group transaction. Target type 11 and monster-everybody groups are not folded into this contract.
- Ordinary character and monster spatial spells support target types 3 and 4. `SpellAreaRules` owns the eighteen source-ordered Data AD masks; fixed areas use the authored size and power areas use cast power. A rotatable record exposes the authored mask plus the next three source masks as orientations zero through three; a nonrotating record accepts only zero. Manual character casting submits one typed battlefield center and orientation; Party Auto and monster AI score deterministic range/LOS-valid placements that contain an opposed actor, exclude allies, hard immunity, active reflection, and targets already carrying a field's condition, and reuse geometry-equivalent candidates across powers. Intersecting footprints are deduplicated, over-100 monster resistance is removed during targeting, and party slots resolve before monster slots under one cost/duration/base-damage transaction. A mask that crosses the validated 90x90 field is rejected instead of reproducing Castle's unsafe indexing. Target type 0 remains a separate repeated-selection transaction. A source-shaped nonzero queue icon, fixed or power area, positive duration, supported ordinary damage or condition special one through forty except disease, and signed-byte queue identity creates a save-owned field before immediate resolution; rotation is optional metadata rather than a queue prerequisite. A target-type-five queued field instead uses a one-cell mask centered on the caster's authoritative cast-time coordinate; learned, scroll, charged-item, Party Auto, and monster sources queue it before the separate immediate self resolution. The field retains caster kind, selected rotation/mask, power, tier, creation phase, and duration; later footprint collisions resolve through the original character or monster caster without another resource payment, once per actor activation, and expiry reuses the lowest Classic queue slot. Castle ray target type six and automatic-group target types above eight bypass `spelltargets`, so their authored queue icon remains a mechanical-signature fact but never allocates a persistent-field slot; manual casting, Party Auto, monster AI, and save state use their ordinary ray/group resolution. Area reflection remains explicitly unavailable, so automatic placement excludes reflectors and manual intersection rejects before commit.
- Combat special 53 or 54 is Helpless only for the source-proven target-zero repeated, target-four power-area, and target-ten enemy-group shapes. It adds the rolled duration to condition slot `HELPLESS`; character targets immediately lose current movement and attacks and retain zero at later activation preparation, while the condition itself prevents monster action because monster state has no separate persistent movement balance. Target four creates its ordinary power-shaped persistent field before immediate resolution. Target ten bypasses the queue for both character and monster casters. Target zero inherits the actor-field queue contract below. Learned, scroll, charged-item, Party Auto, and monster sources share these contracts.
- `CombatFlowPhase` owns character combat Phase as the source-shaped target-type-eight, special-56 coordinate transaction. Learned casts pay spell points; charged items and combat scrolls spend only their source resource. Every source requires the activation's still-available Undo checkpoint, validates the selected point through ordinary range and line of sight, consumes duration then damage RNG even though neither value affects the result, and spends twelve movement plus one attack. An open point moves the caster and marks it attacked for the round; an occupied or solid point removes it at minus ten. Size zero clears the remaining attacks, while War's authored nonzero size preserves them after the ordinary cost. Castle's resolver operates unconditionally on the active party character, so monster Phase is explicitly not applicable rather than inheriting unsafe stale-character behavior; automatic character Phase remains unavailable because it requires an authored destination choice.
- Ordinary size-zero target type 0 is an ordered repeated-selection transaction. A player chooses one through power distinct positioned actors, including self or either allegiance, pays once at chosen power, and rerolls duration/base damage for each selection. Monster AI deterministically selects up to the scored affordable power from legal opposed actors, while the common resolution still pays that chosen power and resolves each selected actor independently. For ordinary target types zero and one, each originally selected reflector consumes one 33-percent draw before duration/damage, redirects once to the active caster, and retains both selected and effective target identity. An unreflected over-100 monster consumes that repeated selection's pre-resolution rolls but receives no effect. A monster killed by its own reflected cast retains its battlefield anchor, completes the authored magic-attack count, then enters the serialized death-macro continuation. Castle's record-based queue may contain the same dead caster more than once; occupancy remains until that subject's final queued record completes, and the queue is bounded to Castle's 100 records. Nonzero-size open-space placement remains explicit; `FD-COMBAT-011` remains the historical correction record for Castle's leaked local retry counter.
- A size-zero target-zero or target-one actor spell with a signed nonzero queue icon, positive duration, and supported ordinary damage or condition effect creates a queue record before its separate immediate resolution. Target one stores the selected native party/monster selector as a pseudo-center with default shape one. Target zero attempts one record and duration roll per ordered selection: its first record uses the first selector, while later records consume queue slots and draws but use a valid noncolliding shape sentinel to represent Castle's cleared `target[0]` coordinates without changing the save schema. A full queue suppresses only record creation and its lifetime draws; the immediate actor resolution continues. Manual, fixed-power scroll, charged-item, Party Auto, and monster sources share this source order and emit exact field records.
- Ordinary target type 6 is a source-ordered ray from the caster anchor to one selected actor anchor. `BattlefieldRules` takes Castle's fixed 128 center-offset samples, excludes the caster, deduplicates encountered actors, lets an occupied footprint mask its underlying terrain, and stops at blocking terrain only when the authored range requires LOS. Learned, fixed-power scroll, and charged-item casts pay or spend their source resource once and reroll duration/base damage for each encountered actor without ordinary spell reflection; manual casting may therefore harm allies. Party Auto and monster AI admit only endpoints whose complete ray contains opposed, nonimmune actors, then score every actor that the same authoritative traversal will resolve.
- `BattlefieldRules` is the single owner for tactical footprint adjacency, deterministic LOS, projectile range/target legality, one-step legality, and deterministic pursuit routing. Party movement accepts eight directions, validates the complete destination footprint, and never mutates turn resources on rejection. Pursuit routing uses a deterministic target-goal grid and bounded A* search with one precomputed occupancy map; it respects current occupancy for the immediate step and treats later actor cells as temporary while retaining terrain walls, footprints, and movement costs, so a moving crowd cannot make an existing wall detour appear permanently unreachable or force roster-wide occupant scans for every explored cell. A legal same-allegiance size-zero cell remains temporary occupancy but carries the real five-movement swap cost throughout a Party Auto route; each Auto activation excludes its already-visited anchors, and only a true no-route result may enter Castle's bounded random shifting without revisiting one. An opposed occupied destination remains an enabled directional command: Guard resolves first, then ordinary melee spends three movement and two half-attack units without moving; open movement instead charges the maximum destination terrain time plus changed axes. A same-allegiance occupied destination is enabled only for another size-zero actor while the active character retains more than four movement. Manual movement opens Castle's saveable Swap Positions/Attack Friend choice; swapping atomically exchanges anchors, spends five movement, invalidates Undo, plays sound 654, and performs no field collision, while Party Auto selects that swap branch without an extra RNG draw. A manual missile-mode hostile collision may carry the host's default-on Auto Weapon Switch decision only when the equipped type-15 item has `sp2 > 1100` and resolves to spell class nine plus damage type nine. That one-move decision survives an interrupted Guard continuation, switches to melee or hand-to-hand after Guard, plays sound 141, and performs exactly one ordinary contact attack. Direct attacks and automatic actors never consult the host preference. `CombatState` owns Guard flags plus the source-ordered, serializable pre-move Guard, fixed-origin withdrawal, committed-move, destination-Guard cursor, and living characters who explicitly left battle; interruption by age update, Escape confirmation, or a reviving death macro resumes without replay. Explicit Escape uses integer Euclidean range, Castle's condition/allegiance gates, per-character removal, and the exact 200 prestige penalty. Edge movement prompts or forces Escape before collision gates. Ordinary monsters retain or source-select one visible target, try a direct step, use deterministic pursuit routing, reserve bounded `shift.c` probes for a no-route fallback, resolve party/allied Guard at contact, and attack only after adjacency; RUNS_AWAY monsters instead move away from their retained target, receive withdrawal attacks, and leave at the edge. Physical attacks, reactions, successful ordinary monster missiles, and successful ordinary monster spells clear monster Guard. Monster target-12 spells use the complete living party-then-monster target order and are viable only when their opposed-target advantage exceeds friendly exposure; zero-cost unscaled Shatter resolves every selection through the shared save/death transaction and clears Guard/movement as a deliberate correction to Castle's inconsistent action state. Fixed-power combat scrolls reuse typed spell targeting and effects without spell-point or spell-attack-limit ownership, charge twelve movement plus two half-attack units after validation, and clear the exact slot only after a successful transaction under `FD-SPELL-002`. `FD-COMBAT-008` removes presentation delay from Castle's 128-sample LOS, `FD-COMBAT-009` bounds its unseen-target scan to validated slots, and `FD-COMBAT-010` makes projectile-to-melee replacement actor-owned. Castle's pre-direction movement-submode Guard check, possessed-character auto-guard, permanent-Tangle monster movement, and nonordinary spell/projectile branches remain explicit unresolved boundaries. A routed monster without a retained target and a mandatory ally at the edge report narrow unavailable reasons because Castle's corresponding branches are unsafe or internally ineffective.
- `CombatState` owns bleeding recipients and once-per-battle Turn Undead use; `GameState` owns sorted persistent-Auto character IDs. Both are optional backward-compatible fields inside the existing save aggregate. Castle clears Auto when `killbody` removes a defeated character from battle, including the above-minus-ten bleeding range; party removal does the same, and restore rejects stale or nonliving IDs.
- Auto Turn and persistent character Auto share one rules-owned activation routine. A bounded one-turn scorer admits only currently legal actions and assigns positive relative likelihood weights: actual bleeding remains highest-weight; critical healing and urgent condition cures outweigh Turn Undead, useful missing friendly condition effects, safe multi-target spell value, force-balancing summons, melee, missile, and movement; moderate healing and lower-urgency cures are lower; spell cost reduces otherwise equal value. The serialized session RNG chooses among the viable action categories, while the exact target, spell/power/placement, and route remain deterministic within the selected category. A summon uses the same source selection and transactional complete-footprint placement as manual casting and stops receiving weight once the friendly force has an advantage or enough allied summons. Forced retreat and single-option turns consume no choice draw, and oversized totals are proportionally normalized to the RNG's bounded range. The scorer never projects future turns. Area damage is eligible only when the resolved footprint contains an enemy and no ally; hard immunity and active reflection exclude a damage target. Area scoring derives candidates from hostile footprints and reuses geometry-equivalent placements across spell powers instead of scanning the complete battlefield. Monster AI uses the same bounded weighted principle for advance/physical contact, authored missile/cast preference, critical healing, condition cures, missing friendly condition effects, legal spell/power, target, and ally-safe spatial field placement. Each persistent-Auto transaction commits at most one character activation so the host can yield to presentation and accept manual control before continuing. Auto stops at manual, mandatory, error, or terminal boundaries and rolls state plus RNG back together on a failed bounded activation. Delay is fresh-activation-only; `FD-COMBAT-012` advances a last-slot delay into the next round instead of reproducing Castle's duplicate activation. Manual Bandage exposes one actual living bleeding recipient under `FD-COMBAT-013`; Turn Undead retains source target order, threshold, experience, effect, death-macro, and once-per-battle state. Observed-resistance memory remains unavailable rather than inferred by the scorer.
- Combat Undo is a narrow activation-start position/cursor contract, not generic rollback. Movement without a result retains it; attacks, spells, item effects, reactions, or turn advancement invalidate it. Save state records only actor, start cell, round, turn index, and availability. `FD-COMBAT-014` rejects an occupied start cell instead of reproducing Castle's unchecked field overwrite. Repeatability and initiative-edge re-entry remain fixture-required.
- `BattlefieldView` may expose the stable active-landlook asset identity needed to render Castle's composited battle art. This is detached presentation metadata only; terrain values, passability, LOS, occupants, targets, and movement legality remain simulation-owned battlefield facts.
- Typed combat interaction records may expose detached player-visible item rows, active condition names, and authoritative attack rows for read-only battlefield inspection. These facts never enter saves, replay traces, or simulation state; authored monster loot stays private and only visible equipped monster gear may appear.
- A defeated monster with a death macro retains its battlefield footprint until the nested macro completes. `FD-COMBAT-007` removes the footprint only if the monster remains dead and retains it after CODE 119 revival, so a live combatant can never become unpositioned or make the save aggregate invalid.
- Opcode 125 selects the bounded stable-order living monster set by independent Classic name identity and allegiance, then uses the ordinary death-macro, occupancy, terminal-outcome, and reward lifecycle. Its saveable nested continuation adds no gameplay RNG or turn advance.
- Classic random monster-weapon tables have one rules-owned definition shared by construction and package-readiness validation. Every possible generated item ID must exist in immutable package content; combat never treats an unresolved generated weapon as unarmed.
- Monster blindness and petrification use save seven. Blindness writes permanent `-1` before physical damage; petrification writes Castle's target-specific sentinel, sets health to zero, and skips the already-rolled physical damage through the force-kill result.
- Character vault imports arrive as detached validated state plus provenance. The active session clones the state; it never writes back to the vault implicitly. Finalized new characters may request explicit publication only through a typed, saveable confirmation; acceptance emits a committed host event and never performs repository I/O inside the core.
- Active portrait and tactical-icon changes are separate stable-ID session mutations. Every nonempty role is validated against immutable package content during submission and restore; an explicit empty identity remains empty rather than receiving substituted art. Changes consume no RNG or time and never publish a vault revision implicitly.
- Camp is session-owned exploration mode. Entry/explicit-exit time, service clearing, and persistence follow Castle; the Camp toggle itself never heals. One Rest intent is one source-held pulse: fatigue changes before five scaled time clicks, and the clock owns hourly conditions/fatigue/spell recovery plus half-day health and exact item-877 charge/load consumption. One field Heal intent advances one source pulse, then each party-order type-one/type-two caster who knows special 57, can cast, and retains ten spell points spends ten per eligible wounded, non-stone recipient and restores Rand(8) stamina capped at maximum. Held-over healing remains outside this bounded party implementation. Poison damage and positive-condition decay precede recovery, so Castle's signed Poisoned term offsets the earlier damage rather than granting a net bonus. Land and dungeon movement leave camp through their distinct pre-movement time costs. Timed and random interruption plus battle return persist the owning Rest, Heal, or requested movement exactly once.
- Crossing midnight creates one save-owned post-clock continuation after ordered age acknowledgement. It scans timed records by stable source order, advances a same-day record before chance and requirement gates, persists the scan cursor and RNG, and resumes its AP interaction before later random rectangles. `FD-EXPLORATION-001` guards y with the authored y field and uses semantic attempted/current/committed party coordinates; `FD-EXPLORATION-002` performs half-day recovery once per midnight rather than once per dispatched record. A timed AP may rebase the remaining scan after moving the party. Location-changing AP destination semantics still require their dedicated end-to-end fixture.
- Complex Encounter Action responses preserve Castle's complete toggled slot set and authored required selection count. Runtime validates every supplied slot and selects the authored Action result only when the selected set exactly matches the nonzero group bytes; a same-sized nonmatching set routes to fallback result four. Item and spell requests carry exact living owner identity; item entries additionally carry exact instance plus player-visible icon, charge, and equipped facts, and responses return the selected owner/instance instead of a party-aggregate record number. Complex Encounter result elimination is save-owned by encounter and zero-based result index. Immutable package result programs remain unchanged; later selection of an eliminated result executes the source-equivalent slot-seven Keep Codes termination.
- At each genuine field-time hour, party fatigue increases by one within Castle's 4–135 bounds. Every positive party condition decays once, then Torch Lit decays once more when still positive; this preserves the source's distinct `30 * power - 1` light-duration scale. Expiration is a state event, and presentation must rebuild from that state rather than reproduce Castle's stale incremental-redraw edge.
- Providence's ordinary movement cost and blocked-attempt timeclick count are distinct Classic facts, not elapsed minutes. Successful and blocked land attempts apply the five-minute outdoor scale and full hourly rules; nonpositive native cells retain Castle's fixed three-click blocked cost, while an ordinary solid dungeon wall consumes no time. Exact `needboad` values govern boarding, water traversal, solid collisions, and shore attempts. Session-owned ordinary terrain replacements project the active landlook's exact mapstats profile, then boat-presence overlays apply Providence's tile-60/tile-147 profiles through `MapTopology.effective_cell_at`, so rendering, LOS, movement, save restoration, and battle terrain share one topology truth. Castle's transient `hitland` counter is intentionally save-owned and bounded to zero through two so a pending third-attempt choice restores deterministically. Special dungeon wall bits and attempted-coordinate timed gates remain explicit fixture work.
- Every character owns five fixed typed scroll slots. Field scribing and use validate exact case, parchment, spell, power, target, cost, and slot before mutation. A rejected or cancelled target cannot spend spell points, parchment, or a scroll. Fixed-power combat scrolls support the catalog-admitted actor, group, ray, summon, repeated, and rotatable area contracts without spending caster spell points. Selecting a scroll that is invalid in the field opens Castle's explicit save-owned Discard/Keep boundary; acceptance clears only that exact slot, while decline, restore rejection, and stale responses preserve every record and consume no spell points, time, or RNG.
- Unsuspended held-over allies are consumed into the combat roster as non-traitors. After combat, surviving friendly monsters with nonzero `canSummon` eligibility return only through the typed Classic body-count selection; negative eligibility is mandatory. Monster targeting compares allegiance, so hostile monsters can attack party characters or friendly monsters and friendly monsters target hostiles.
- An ordinary scenario-owned total defeat is a session handoff, not a completed VM operation. The save aggregate owns the advanced issuing frame, caller metadata, combat defeat, exploration continuation, and Party Death hook together. Opcode 119 revival resumes the ordinary caller without rewards and publishes one battle return; unrecovered defeat closes. Opcode-2 mode 10 is completed inside the VM runtime instead: it revives and restarts the current program while preserving escrow. Other non-equivalent special Castle callers remain explicitly unavailable.

## Work Guidance

- `ItemView` resolves Castle-visible Describe facts before crossing the session boundary. Unworn cursed records use their native `iscurse` decoy definition for text and statistics while retaining the original icon; wearing reveals the original record. Presentation must not infer this link or receive the hidden original identity through visible fields.
- Prefer small domain modules behind a fixed `RealmzRules` facade; do not add registries or profile selectors.
- Keep character, condition/time, inventory/economy, combat, magic, and monster behavior in their owned rule modules. Opcode handlers adapt Classic records to these rules instead of duplicating formulas.
- `ClassicSpellCapabilityCatalog` is the application-owned mechanical-family and current-context disposition authority for every stock spell definition. Casting sources may add tactical legality after consulting it; they must not invent parallel special-ID capability lists. Combat special 61 clears every positive target condition while preserving nonpositive permanent markers. Castle stores charm allegiance outside the condition array: special 61 resets that separate flag only for player-character slots, while summoned and NPC monster slots retain allegiance and require Charm to reverse it. Learned, scroll, charged-item, Party Auto, and monster sources share this resolver; automation scores the complete post-effect allegiance and condition state. Special 62 clears Cursed and force-unequips only equipped cursed definitions in inventory order. Field, learned-combat, scroll, and charged-item sources share the character resolver and publish exact affected item-instance IDs; combat rejects monster targets and classifies monster casting not applicable because Castle's unguarded character-array access is unsafe outside player slots.
- Combat special 46 polymorphs only monster targets; a character target completes ordinary defenses and is otherwise unchanged. The replacement retry draw is the exact Classic application monster range 1 through 200 and admits only a same-size, positive-hit-die, summonable definition. `MonsterRules` owns the source-ordered agility, stamina, armor, difficulty, and day rebuild, copies nonnegative prior conditions over the replacement definition, clears its weapon and retained target, and preserves battle allegiance, summoned provenance, and authoritative battlefield footprint. Learned, scroll, charged-item, Party Auto, and nonnegative-cost monster sources share this resolver; automatic area scoring ignores character cells for this monster-only mutation.
- Combat special 90 follows ordinary defenses, then tests only living hostile undead or nether monsters whose summon eligibility is not the source sentinel. It consumes one tagged draw per eligible target against Castle's level, power, and hit-die threshold: a positive margin below thirty destroys the target, while thirty or more assigns the caster's allegiance, clears retained targeting, and requests application sound 630. Learned, scroll, charged-item, and Party Auto sources share the ordered group resolver. Monster casting is not applicable because Castle's special unconditionally reads a player-character caster slot while its target-ten path selects the party.
- Special 63 leaves character targets unchanged and, after ordinary resistance in combat, marks every nonempty loot slot on a monster target. The aligned slot marks are save-owned combat state and follow target-one or queued target-four resolution through learned, scroll, charged-item, Party Auto, and monster sources. Outside combat, target one selects one party character and target four selects the whole party; the cast spends its authored cost and rolls duration then damage but changes no character state. Automation values only opposed monsters with undetected loot. Reward construction retains a mark only when the resolved item definition is magical; the mark is local to that Treasure item instance and does not become an inventory identification fact after assignment. Treasure Detect Magic remains a separate five-point reward action.
- Application item 609 Vaporous Form and item 723 Refresh Potion are target-free field-item transactions. The former preserves Castle's target-type-7 special-zero write to party condition zero for its fixed duration; the latter preserves special 68's earlier fatigue-four clamp. Both spend one charge and no spell points, retain duration-before-damage RNG order, and save the exact party-state result.
- Negative-cost, field-enabled, noncombat encounter-utility spells use fixed power one and pay their absolute authored cost. Target type 11 preserves Castle's whole-party selection and completion sound, while target type 0 preserves one selected character; the stock special-zero records roll duration then damage without inventing a terrain, language, movement, or encounter mutation. Field target types 3 and 9 resolve party characters followed by every held-over ally through one shared roll and ignore ally magic resistance outside combat while retaining saves and protections; target type 7 exits through party-state mutation before per-creature effects. Complex Encounters separately consume the selected packed spell identity through their authored branch table.
- Combat special 60 negates the resolved post-defense damage value and subtracts that amount from the target's spell points, clamped at zero, without changing stamina. Learned repeated targets, single targets, target-six rays, scrolls, charged items, Party Auto, and monster AI share the resolver and publish the signed actual `spellPointDelta`; automation rejects zero-potency and already-empty targets. The stock Weakness ray deliberately retains Castle's zero-damage, zero-drain result after its ordinary duration and defense rolls.
- Preserve 16-bit and 32-bit arithmetic semantics explicitly where Castle behavior depends on them.
- Keep serialized IDs stable strings and use `StringName` only as an internal lookup optimization.

## Verification

- `tools/verify_architecture.ps1` rejects forbidden host dependencies and direct randomness.
- Core behavior requires headless unit tests and, when source-backed, an evidence-labelled oracle fixture.

## Child DOX Index

- No child AGENTS.md files are currently required; subdirectories remain governed here.
