# Human-maintainability architecture record

The first boundary reorganization is complete, but the human-centered architecture overhaul is not. Realmz Rebuilt has six sound source areas and a verified behavioral baseline; many systems inside those areas still retain prototype-sized scripts, forwarding surfaces, and runtime-built UI. Beta 1 remains blocked until the stricter acceptance targets in [Human-centered architecture overhaul](human-centered-architecture.md) reach zero and the Godot scenes are independently recognizable.

This record describes the foundation inherited by the active migration. Current ownership is machine-checked in [the system manifest](system-manifest.json), and the newcomer tour remains [The Realmz Rebuilt Builder's Manual](builders-manual.md).

## The six source areas

| Area | Owns | Must not own |
|---|---|---|
| `src/app` | Startup, dependency construction, host lifecycle, and input translation | Realmz rules or screen layout |
| `src/game` | Pure definitions, mutable state, fixed rules, topology, clock, RNG, and detached views | Nodes, files, audio, or platform services |
| `src/playthrough` | `GameSession`, transactions, continuations, restore coordination, and player workflows | Presentation or filesystem access |
| `src/scenarios` | Classic instruction execution, Safe Scenario Actions, and the scenario VM | Host UI or package I/O |
| `src/storage` | Package loading, saves, Character Files, settings, validation, and external adapters | Gameplay decisions or screen behavior |
| `src/ui` | Scenes, screens, dialogs, components, rendering, animation, audio, and themes | Mutable game truth or direct storage access |

Dependencies point toward the model and command boundary:

```text
game <- scenarios <- playthrough <- app
game/playthrough/scenarios <- storage <- app
game/playthrough <- ui <- app
```

The architecture verifier enforces these directions and the typed boundaries between them. A later move includes its scripts, paired `.uid` files, resource paths, tests, verification rules, and DOX ownership contract in one coherent change.

## Foundation already in place

- Internal source moved from framework-shaped `core`, `session`, `scenario`, `infrastructure`, and `presentation` folders into the six product-facing areas above.
- Major routes gained named `*_screen.tscn` scenes, but several expose only a frame or mounting regions. They are migration anchors, not evidence that the complete runtime composition is editor-authored.
- Reusable inventory regions, party-setup rows, Fast Spell controls, exchange controls, Debug Tools, the action console, music playlist, shared content and spell visuals, scrolling text, player maps, Pick Lock, Age Update, lifecycle and text choices, Bank, Temple, and Thief workspaces now have editable scenes. Request-sized item, spell, character, statistic, transfer, service, and tumbler records use reusable row scenes.
- Exploration and Combat are typed shell modes and no longer have placeholder scenes. Their persistent visible structure remains authored once in `game_shell.tscn`.
- Realmz Builder recognizes each existing major scene and links its guide, controller, detached view, and tests. Registration remains distinct from production-bound preview completion, so the 24-surface preview debt is still reported honestly.
- Campaign selection and the retained party-assembly/character-creation workspace now have editor-authored scenes under `src/ui/setup`; their controllers bind stable nodes and create only variable campaign and Character File records. This lowers missing major scenes from eight to five and runtime-created Controls from 664 to 629.
- Level Up now has one recognizable two-mode scene: committed gains and spell learning share the scene-authored header, with their complete stable pane structures visible in Godot. Only the level-filtered spell buttons remain dynamic, lowering missing major scenes to four and runtime-created Controls to 611.
- Complex Encounter now exposes its six-command dock in `encounter_interaction.tscn` and keeps its editable Action, Items, Spells, and Speak structures in neighboring workspace scenes. Its script selects and binds those scenes and creates only authored action records, lowering missing major scenes to three and runtime-created Controls to 587.
- Shop now exposes its wide two-ledger and compact tabbed browsers, five category controls, transaction strip, shopper selectors, route controls, and detail wells in `shop_interaction.tscn`. Item and portrait records are reusable scenes, lowering missing major scenes to two and runtime-created Controls to 549; the controller also falls below both the file-size and class-method limits.
- Treasure now exposes its ordinary distribution workspace, legacy exact-item recovery adapter, completion confirmation, wealth transfers, loot cells, recipient rows, and lore controls through editable scenes. Its controller binds detached request values and creates only scene instances, lowering missing major scenes to one and runtime-created Controls to 483; it also lowers the outstanding file, function, and class limits to 26, 99, and 36 respectively.
- Combat now exposes the complete turn summary, initiative host, View/Action/Tactics deck, fixed command slots, attack/spell/scroll/item/bandage modes, inspection record, and reusable targeting controls in `battle_interaction.tscn` and `battle_targeting_controls.tscn`. The controller binds supplied legality and detached records without constructing Controls, lowering missing major scenes to zero and runtime-created Controls to 442.
- Allies and Bestiary now share the fully authored `creature_library_workspace.tscn`: list and detail panes, identity header, facts, empty state, and three state cards are visible in Godot, while `creature_library_row.tscn` supplies the only variable record. Their controller only binds detached creature data and exact media, lowering runtime-created Controls to 417.
- The Character route exposes its party-order summary, reorder editor, persistent shared sheet, empty state, identity, complete tab rail, and content frame through authored scenes. Character Files likewise exposes its complete library, empty state, history, eligibility, and inspection composition; exported `party_order_row.tscn`, `vault_character_card.tscn`, and `vault_history_row.tscn` provide the variable records.
- Overview, Conditions & Saves, Abilities, and Lifetime Record now expose their complete stable sheet regions through `character_sheet_stat_tabs.tscn`. Metric rows and lifetime cards instantiate exported component scenes, lowering runtime-created Controls to 361; Equipment, Spells, Appearance, and Race/Caste/Aging remain the explicitly incomplete sheet tabs.
- Equipment and Spells expose their fixed headings, split records, list hosts, empty states, and scroll-case regions through `character_sheet_inventory_magic_tabs.tscn`. Exact item and spell records use exported card scenes.
- Appearance and Race, Class & Aging now expose their complete preview, picker, action, definition, and age-band structures through `character_sheet_identity_tabs.tscn`. All eight character-sheet tabs are therefore recognizable and editable in Godot; their controller binds detached values and instantiates only the exported metric, record, item, and spell components. This completes the character-sheet scene migration and lowers runtime-created Controls to 324.
- Inventory now shares one editor-authored `inventory_workspace.tscn` between its route and Encounter selection. Its normal split, empty state, character record and selector, seven-action dock, confirmation stage, item inspector, two-ledger Trade workspace, portrait matrix, and fixed Money/Items/Done spine are visible in Godot; only exported party, item, fact, and portrait record scenes vary at runtime. The controller constructs no Controls, lowering runtime-created Controls to 274 and the outstanding file/function limits to 25 and 95.
- Spells now shares one complete editor-authored `spells_workspace.tscn` between the route and Encounter selection. Its caster and section selectors, blocked and empty states, Classic level/power rails, known-spell browser, selected record, Fast Spell grid, Scroll Case, and action dock are visible in Godot; only exported spell, Fast Spell, and scroll record scenes vary at runtime. The controller constructs no Controls, lowering runtime-created Controls to 229 and the outstanding file limit to 24.
- Maps / Notes now embeds the complete `maps_notes_workspace.tscn`: saved-place and current-note panes, historical-map viewport, acquired-map selector, cartographic stage and zoom tools, scrolling-text host, and two-page journal are all visible in Godot. Location, map, and journal records instantiate exported row scenes, while map canvases remain algorithmic presenters. The controller constructs no Controls, lowering runtime-created Controls to 193, oversized functions to 93, and generic preload aliases to 130.
- System now embeds the complete `system_workspace.tscn`: Save/Load columns, selected-record and empty states, fixed host actions, every preferences tab, picker, slider, toggle, help card, and Wide/Compact reflow are visible in Godot. Only detached save records instantiate the exported `save_slot_row.tscn`; the controller constructs no Controls, lowering runtime-created Controls to 161.
- Party Wealth now embeds the complete `services_workspace.tscn`: pooled and banked wealth, adventurer ledger, selected-character exchange, all three denomination transfers, Done, location services, alternate states, and Wide/Compact reflow are visible in Godot. Detached characters, transfers, and location services instantiate exported row scenes; the controller constructs no Controls, lowering runtime-created Controls to 138.
- The persistent Party roster now authors its member record, empty position, countdown cursor, and complete combat spellbook as scenes. `ClassicPartyRoster` retains incremental ordinary-movement updates and portrait compositing, while `CombatRosterSpellbook` owns level/list/detail/power binding; neither constructs Controls. This lowers runtime-created Controls to 111 and removes the former 800-line roster from the oversized file and class-method budgets.
- Startup now authors the complete deferred `front_door_menu.tscn`, and campaign selection authors its selected record, alternate library states, package-operation status, install dialog, and refresh actions. `CampaignLibraryController` binds both scenes and instantiates only the campaign selector's exported package-row scene; it constructs no Controls, lowering runtime-created Controls to 91.
- Character creation now exposes five recognizable step scenes from the retained party-setup workspace. Identity, Race/Caste, Appearance, Review, and Starting Spells expose every stable preview, selector, detail, alternate state, level rail, and allowance in Godot; the production controller constructs no Controls and instantiates only exported variable rows or the shared sheet.
- Party assembly exposes its Character Files heading, row host, empty state, and pager through `party_assembly_browser.tscn`; party inspection exposes its complete overlay through `party_setup_inspection_overlay.tscn`. Both controllers construct no Controls and instantiate only exported records or the shared sheet.
- Character targeting and surviving-allies selection now share the complete editor-authored `selection_interaction.tscn`; the latter instantiates only its exported candidate-row scene. The selection binder constructs no Controls, lowering the remaining classified runtime-created Controls to 34. The current structural debt is 22 oversized files, 88 oversized functions, 35 oversized classes, 192 cross-object private calls, and 128 generic preload aliases elsewhere in the repository.
- Interaction presentation now exports scene-authored modal shields, flash messages, nested Treasure confirmation, Encounter dock, side and application workspaces, and hint components from `interaction_presenter.tscn`. The presenter owns only typed request routing, placement, retention, and teardown, lowering the remaining classified runtime-created Controls to 19.
- Shared item-detail records, Party-effect slots, combat initiative entries, interaction hints and character pickers, and Race/Caste option hosts are now exported or directly scene-authored components. Their binders construct no Controls, lowering the remaining classified runtime-created Controls to 10 and generic preload aliases to 127.
- Three warmed startup samples for that shared-component batch kept first-frame median at 144.587 ms versus 149.084 ms and application-ready median at 6,713.574 ms versus 6,512.257 ms. The latter 3.1-percent change remains below the joint five-percent/100 ms rejection rule; all first-frame, retained-intro, and queued-transition contracts passed.
- Navigation hosts, generic summary cards/messages, and the field Spells footer are now scene-authored. `ScreenNavigator`, `ScreenContentPresenter`, and `ScreenFrame` bind those components without constructing Controls, lowering runtime-created Controls from 10 to 2. The two remaining calls are named algorithmic render-target allocations in the retained map and first-person dungeon presenters; no route, screen, interaction, or controller script creates stable layout.
- Realmz Builder now previews all 24 registered major surfaces—including the application shell, campaign selection, party assembly, and character creation—by placing an ownerless clone over the edited scene and feeding all six deterministic profiles through each production binder. The actual scene is never mutated or made dirty, variable records come from the production row scenes, and the owning test exercises the same fixture catalog. No registered major surface lacks a production-bound preview.
- Three warmed startup samples for the navigation batch improved first-frame median to 147.017 ms from 149.678 ms, application-ready median to 6,411.604 ms from 7,101.360 ms, and background-load median to 6,272.779 ms from 6,951.907 ms. All retained-intro and queued-transition contracts passed.
- `ScreenNavigator` owns navigation and mounting through its production scene. Named screen controllers bind detached views and populate genuinely variable collections from exported row/card scenes.
- The application composition root and its package, save, Character Files, and standalone-creator controllers now construct dependencies through their public class names. Removing all 22 `*Type`/`*Script` preload aliases from this boundary lowers generic alias debt from 127 to 105 without changing dependency construction or startup timing.
- Every remaining production preload alias pointed to an already registered public class. Replacing those 105 indirect names with the actual class names removes the generic-alias debt entirely across game rules, playthrough workflows, scenario runtime, storage, and presentation; dependency construction and serialized identities remain unchanged.
- Party setup now has explicit collaborator contracts: assembly, inspection, creation, and shared-state operations are public where another controller genuinely depends on them, while dead proxy helpers were removed. This eliminates all 24 cross-object private calls from that feature and lowers the repository debt from 192 to 168.
- Application input now crosses three named host operations for intent submission, field fast spells, and the full-party Auto abort; the router no longer invokes composition-root private methods. This removes seven more cross-object calls and lowers that debt to 161.
- Package assembly now uses explicit decoder and validator APIs for story records, rule catalogs, world records, scenario construction, media resolution, and cross-reference checks. Internal parsing helpers remain private, while the 34 operations used between package collaborators are named contracts; cross-object private-call debt is now 127.
- Ten remaining shared codec, projection, exploration-event, scenario-handoff, and shell/row helpers now have narrow named contracts instead of being called as another object's private implementation. Only the session and combat seams retain cross-object private calls, lowering that debt to 117.
- `GameSession` now owns and addresses one durable `SessionContext` directly, containing content, mutable state, deterministic RNG, rules, VM/action state, runtime API, typed continuations, pending interaction, and revision. Exploration, scenario, response, and debug coordinators receive that same context by reference through named operations; the former copy-in/copy-out coordinator context and all 35 session-to-private calls are gone. Only the combat seam remains, at 82 calls.
- Combat now shares one explicit `CombatContext` instead of retaining its root facade in every collaborator. `CombatFlow` exposes only mutation commands; callers use the named round, action, reaction, magic, field, summoning, Phase, or automation owner for probes, and physical-action event policy has its own `CombatActionEvents` collaborator. This removes the remaining 82 production cross-object private calls and lowers the outstanding file and class limits to 21 and 34.
- Combat AI now enters through a 15-line `CombatAiScoring` facade. Party Auto policy, monster policy, shared deterministic scoring, and read-only target facts have separate named owners without changing their public calls or RNG order. This lowers the outstanding file and class limits to 20 and 33.
- Physical combat now enters through a small `CombatRules` facade. Character attacks, monster attacks and specials, shared weapon/save calculations, and initiative have separate named rules owners without changing the existing command API or RNG order. This lowers the outstanding file and class limits to 19 and 32.
- Magic now enters through a small `MagicRules` facade. Character casts, monster casts, projectiles, field/scenario effects, and shared resolution mechanics have separate named owners without changing the public calls, effect ordering, or RNG tags. This lowers the outstanding file and class limits to 18 and 31.
- Combat automation now separates party Auto activation and pursuit, monster phases and actions, and battlefield occupancy cleanup behind its stable entry point. The split preserves command order and RNG use while lowering the outstanding file limit to 17.
- Scenario reward handling now separates battle settlement from Treasure distribution and progression behind the opcode-facing reward entry point. Shared transaction rollback is explicit, and the evidence inventory points to each real owner. This lowers the outstanding file and class limits to 16 and 30.
- Package content decoding now routes campaign story records, character/rule catalogs, and encounter catalogs to separate strict decoders that share one diagnostic boundary. Character, encounter, map, and player-map collection operations preserve source order while delegating each record to a focused validator/builder; player-map marker validation is likewise isolated. Safe Scenario Action bytecode construction dispatches named instruction and expression families while retaining the same bounded recursion, node count, capability checks, errors, and wire format. Scenario-reference validation now names its Simple Encounter, Complex/Thief Encounter, and program-instruction stages. Package installation names existing-install resolution separately from temporary-copy, readback, atomic-rename, and receipt commit. The public APIs and failure semantics are unchanged.
- Restore validation keeps `SessionRestoreValidator` as the transactional entry point and session-continuation owner, while `SessionRestoreStateValidator` validates detached game truth and `SessionScenarioRestoreValidator` validates pending VM workflows. Header, core, and continuation stages preserve the former failure order and never mutate the live aggregate. The former 800-line validator is now a 484-line coordinator, lowering the outstanding limits to 14 files and 76 functions; 30 top-level scripts still exceed the final method-count limits.
- Monster automation now names turn-action dispatch, fallback casting, contact reactions, target refresh, route probing, spell targeting, spell execution, and character-versus-monster attack resolution as separate private operations. Its transient spell target plan replaces parallel local arrays without entering state or wire formats. Combat characterization remains 279 assertions; three warmed AOGM Battle 46 samples measured median Auto activation at 26.448 ms, complete monster phase at 7.082 ms, and warm monster phase at 6.974 ms, all below the branch baseline. This lowers oversized-function debt to 72 while file and class debt remain 14 and 30.
- Acknowledge, age update, yes/no, indexed choice, character selection, general selection, Complex Encounter, Thief Encounter, and Pick Lock requests now use feature-named top-level payload files under `src/game/session/requests`. `InteractionRequest` remains the stable kind registry and wire envelope rather than an oversized nested protocol catalog; serialized kinds, fields, response matching, and save forms are unchanged. Focused session, presentation, scenario, persistence, and field-spell characterization passed 1,228 assertions, and oversized-file debt falls to 13.
- Shop, Temple, Bank, Treasure, Level Up, combat, and application-lifecycle payloads now join the other top-level request bodies. The registry contains no feature payload implementation or compatibility alias; each caller names the actual value it consumes, while the strict central codec preserves the exact wire representation. Focused transaction, UI, scenario, and persistence characterization passed 1,155 assertions.
- Classic banking now has a named request-and-transfer owner, while Shop and Temple remain in the opcode-facing service handler. A shared money-transfer collaborator owns package-backed movement validation and recalculation for both Bank and Temple; no wealth behavior or event order changed. Scenario and money characterization passed 427 assertions, lowering architecture debt to 12 oversized files and 71 oversized functions.
- The battle command deck now delegates only responsive sizing to `BattleCommandScaleController`; its scene remains the complete stable hierarchy and `BattleInteraction` remains the request/response binder. Wide, Compact, and all Realmz Builder preview profiles passed 728 presentation assertions. The binder now satisfies both the 600-line and class-method limits, lowering debt to 11 oversized files and 29 oversized classes.
- The 2D map renderer now delegates decoded atlas, overlay, darkness-mask, and land-marker retention to `MapTextureCache`. `ClassicMapPresenter` remains the drawing and pointer-input owner, and `ClassicRetainedMapSurface` uses the same transparent-tile operation without a presenter dependency. Focused map geometry and presentation characterization passed 486 assertions, lowering oversized-file debt to 10.
- Map viewport, discovery, LOS-mask, debug-outline, pointer-region, and facing calculations now live in the stateless `MapPresentationGeometry`; exact map asset identities and tile composition live beside their decoded cache. Renderers and the application host call those named public collaborators instead of treating `ClassicMapPresenter` as a utility namespace. Focused map characterization passed 486 assertions, and the presenter falls from 58 methods to 33, lowering oversized-class debt to 28.
- Battlefield combatant lookup, camera tracking, footprint interpolation, click sectors, and coordinate calculations now live in `BattlefieldPresentationGeometry`; `BattlefieldTextureCache` retains the base and upper atlases, combatant textures, and keyed effect matte results. `ClassicBattlefieldPresenter` remains the tactical drawing and input owner rather than a shared utility namespace or decoder cache. Focused combat and presentation characterization passed 694 assertions, lowering oversized-file debt to 9.
- `GameSession` is now a 200-line transaction boundary rather than a feature dispatcher. `SessionContext` owns initialization, accepted restore assignment, teardown, and snapshot construction; `SessionIntentCoordinator` routes validated commands to the existing feature workflows and returns typed rejected or committed results. Dead session proxy methods were removed, failure revision semantics remain explicit, and focused session, combat, exploration, persistence, inventory, magic, camp, and money characterization passed. This lowers oversized-file debt to 6 and oversized-function debt to 68.
- Classic VM program transfer is now a named control-flow collaborator. `ScenarioClassicControlFlow` applies XAP/program branches, GOSUB depth, encounter entry, result/repeat loops, and encounter exit to the existing typed frame array, returning `ScenarioDirectiveTransition` for the VM-owned halt/return/trace boundary. Focused VM, exploration, and persistence characterization passed without changing snapshots or trace order, lowering debt to 5 oversized files and 67 oversized functions.
- `GameState` now exposes scenario truth through two recognizable state owners instead of implementing more than twenty unrelated progress operations itself. `ScenarioProgressState` owns searches, quests, journal discovery, and character selection; `ScenarioEncounterState` owns timed records, elimination, attempts, thief flags, and program redirects. The collaborators preserve the exact existing top-level save fields and lower architecture debt to 4 oversized files and 66 oversized functions while keeping class debt at 27.
- Interaction request binding is now separate from conditional overlay hosting. `InteractionPresenter` preserves request identity, binds one typed component, and translates its response; `InteractionOverlayHost` owns the scene-authored shield, nested confirmation, Encounter dock, and side/application workspace lifecycles; `InteractionFlashController` owns queued Classic flash messages. Focused shell, workspace, and Realmz Builder characterization passed 816 assertions, lowering debt to 3 oversized files and 65 oversized functions while keeping class debt at 27.
- Scenario battle lifecycle now owns only battle start, response dispatch, retreat, macro, age, and terminal transitions. `CombatInteractionRequestBuilder` separately projects the authoritative battle into legal actions, movement, targets, combatant inspection, spell/item/scroll choices, and the transient detached `CombatView`. Battle response dispatch now names Auto, explicit retreat, edge retreat, direct command execution, and post-command continuation instead of combining them in one 90-line branch. Focused combat, scenario, session, reward, and persistence characterization passed 1,150 assertions, lowering architecture debt to 8 oversized files and 69 oversized functions.
- The misleading `InventoryMagicServicesWorkflow` is gone. `InventoryWorkflow` owns carried-item transactions, `FieldItemWorkflow` owns Identify/Torch/door/charged-item operations, and `FieldMagicWorkflow` owns Fast Spells, scrolls, and learned field spells. `FieldMagicTargetRequestBuilder`, `FieldMagicResolver`, and the top-level `MagicTransitionResult` provide their shared typed seams without compatibility aliases. Focused inventory, field-spell, scroll, session, and persistence characterization passed 805 assertions, lowering architecture debt to 7 oversized files and 27 oversized classes.
- The obsolete `ClassicWorkspaceView` mounting-point scene was removed. Product files use screen, panel, dialog, row, card, canvas, renderer, definition, state, rules, repository, loader, decoder, result, and flow according to their actual role.
- Every internal rename was decisive. Stable wire identities—including `.realmz2`, `.r2save`, `.r2char`, package keys, opcode identities, save fields, and the user-data directory—were intentionally preserved.

## Scene and script agreement

Stable layout belongs in `.tscn`. Scripts bind data, respond to input, coordinate behavior, and build only variable collections. Repeated rows and cards should become small `PackedScene` components when their structure is stable enough to edit visually.

Custom map, battlefield, first-person dungeon, animation, and effect drawing remain valid script-owned exceptions because their structure is algorithmic. Their viewport, cameras, layers, materials, masks, and surrounding controls still belong in scenes. Shell modes are typed resources rather than duplicate or placeholder scenes.

## Readability budgets

The repository rejects growth from the verified baseline and is converging on stricter final limits:

- final production scripts over 600 substantive lines;
- final functions over 60 substantive lines;
- final classes over 40 total or 20 public methods;
- test suites over 1,200 substantive lines;
- a regression in the measured test-line ratchet or test-to-production ratio;
- any compressed statement line or production script without a purpose header;
- any runtime-created ordinary control outside an exact reviewed function classification, any stale classification, or any per-owner count change that has not been reviewed;
- class/file-name mismatches and unapproved one-node route scenes.

The former 800/100 caps and 664-call construction inventory remain useful no-growth guards during migration, but they are not final acceptance. `verify_architecture_overhaul.ps1` separately ratchets the 600/60/method limits, cross-object private calls, generic preload aliases, missing major scenes, missing preview registrations, and one-node shell-mode markers toward zero. A converted UI collection instantiates an exported row scene; only named algorithmic renderers retain code-created rendering nodes.

## Acceptance trail

The final acceptance exercise requires a newcomer to locate and explain fatigue movement, character state, portable item resolution, one scenario opcode, Inventory layout, and combat Auto flow without private guidance. That exercise has not yet passed. Each migration batch must lower its checked-in ceilings and leave source, scene, guide, tests, and system-manifest ownership in agreement.
