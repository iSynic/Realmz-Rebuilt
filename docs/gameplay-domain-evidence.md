# Gameplay domain evidence

This document records the Phase 4 evidence boundary. Castle source observations guide the fixed rules and Classic opcode adapters; runtime tests prove the new implementation. Content inventory does not by itself prove Castle executable parity or campaign reachability.

## Castle source/control-flow

Pinned oracle: Realmz Castle commit `491816ad60037394f92c428e99c004494d3c28b3`.

- `src/realmz_orig/newland.c:1966-2003` loads the packed spell ID and applies power, save adjustment, and force-affect fields for opcodes 17 and 18. `src/realmz_orig/resolvespell.c:13-61` rolls base duration, one duration contribution per power level, base damage, and one damage contribution per power level in that order. Lines 286-365 apply saves, elemental protection, condition additions, and condition clears. The 2.0 rule keeps that draw order and packed identity rather than treating scenario-spell slots as zero-based runtime IDs.
- `src/realmz_orig/modify-cancast.c:47-69`, `getup.c:58-101,257-267`, `castspell.c:210-237,714-734`, `combatinfo-combatchoice.c:529-540`, `loadspell.c:5-18`, `combat.c:124-379`, `getrange.c:5-89`, `resist.c:5-101`, `resolvespell.c:13-61,270-365`, and `misc.c:1188-1229` establish ordinary character and monster spell admission and resolution. A nonzero scenario flag blocks casting; the source conditions, same-round attack, and per-activation limits also gate it. The Classic ID encodes zero-based tier independently of caster level. Character commits spend spell points, two half-attacks, and twelve movement. Monster commits sample ten fixed slots, preserve initial range power while lowering only affordability power, sample native party-gap-monster target space, clear Guard/movement, and can retry once after moving. A failed charm opposed save continues into screens, Animated immunity, and magic resistance; `cannot` bypasses those later checks only after consuming the first draw. Realmz 2.0 exposes typed player combinations and implements ordinary single-actor damage for players plus ordinary opposed-party-or-monster damage for monsters; the following records own bounded group, spatial, and healing branches. Friendly `cannot == 4` outside strict special-57 healing, class-nine, other special, zero-damage, and anomalous nonpositive-cost paths remain explicit. Castle's pre-paid player cancellation, negative-cost SP increase, post-range size bonus, and target-everybody Guard omission remain recorded rather than normalized.
- `src/realmz_orig/resolvespell.c:220,396,424`, `killbody.c:4-127`, `emptyque.c:5-13`, `combat.c:1091`, `getup.c:69`, and `newland.c:292-307` establish the spell-death-macro lifecycle. Every monster killed while `inspell` appends its death macro to `todoque`; Castle drains those records in order before normal combat progression. The queued `killbody` branch does not reset `traiter`, whereas authored opcode 119 independently revives the macro subject and explicitly clears it. Realmz 2.0 therefore owns an ordered spell macro queue, original caster cursor, deferred turn decision, and exact completed-subject identity in `CombatState` and the session/VM continuation. The queue survives typed interaction and save/restore; each still-dead completed subject loses occupancy before the next macro, and battle outcome or caster advancement waits for the queue to empty. This fixes the prior immediate-single-macro host path without attributing opcode 119's authored allegiance change to every death macro. Automatic groups and bounded spatial areas feed that queue through one shared cost/base-duration/base-damage transaction.
- `src/realmz_orig/combatinfo-combatchoice.c:663-705`, `resolvespell.c:13-61,254-433`, and `spelltargets.c:61-92` establish player automatic-group targeting. Types 9, 10, and 12 build friendly, enemy, and everybody tracks in party-slot then monster-slot order. One `resolvespell` call rolls duration and base damage once before per-target resistance, save, protection, damage, and `killbody`; player cost is already committed by `castspell`. Realmz 2.0 implements that bounded ordinary-damage path with one targetless typed picker option and the existing ordered spell death-macro queue. Castle's separate monster branches in `combat.c:162-207` jump to resolution before assigning `powerlevel`, checking affordability, deducting spell points, or clearing movement; type 12 also omits the Guard clear used by 9 and 10. Those likely stale-global/resource bugs remain explicitly disabled as `monster-group-spell-power-resource-anomaly`. Providence's editor correction adds source-backed target value 12 without changing schema v2's existing numeric projection. Target type 11's fallthrough into the player all-enemy `else` remains outside this case.
- `src/realmz_orig/main.c`, `combatinfo-combatchoice.c:663-705`, `cast.c`, `spelltargets.c`, and `resolvespell.c:13-61,254-433` establish player spatial targeting. Castle loads eighteen 7x7 masks from Data AD. Type 3 selects one center and uses the authored `size`; type 4 substitutes cast power; `spelltargets` scans the selected mask in row-major order, deduplicates occupied field IDs, removes monsters above 100 magic resistance, and `resolvespell` rolls duration/base damage once before party-slot then monster-slot defenses. Type 0 is materially different: it permits `powerlevel` independent selections and calls targeting/resolution for each, so it is not implemented as an area. Realmz 2.0 owns the masks as fixed rules data, exposes one typed center descriptor, validates center range/LOS and full-field safety before mutation, and uses the existing shared multi-target and death-macro transaction. Providence schema v2 already preserves target type, size, rotation, queue icon, range, and ordinary spell fields; no compiler change is required. Castle's unsafe edge indexing, reflection draw/redirect, adjacent-mask rotation, and persistent queued-field lifecycle remain explicit unavailable boundaries. The present numeric coordinate controls are functional presentation scaffolding, not the final Classic viewport targeting interaction.
- `src/realmz_orig/combatinfo-combatchoice.c:645-648,1227-1423`, `cast.c:18-44`, `spelltargets.c:5-105`, `resolvespell.c:13-61,254-433`, and `combat.c:9-12,209-370` establish repeated target type 0. With size zero, a player chooses up to `powerlevel` distinct party or monster actors in an explicit order and may cast after the first selection; the already chosen power determines the one spell-point cost. `cast` invokes targeting and resolution separately for each selection, so duration and base damage reroll per actor. A monster rolls range power, then target power from the current in-range count, lowers it only for affordability, and samples unique party/gap/monster native slots. Castle's local `pass` leaks across the whole battle and can make a later cast abort before sampling. `FD-COMBAT-011`, recorded by `monster-spell-target-retry-scope-correction.json`, scopes the hundred-attempt guard to one cast while preserving a genuine within-cast partial result and full chosen-power cost; the guard is not persisted. Providence commit `fa0297426677552080608e1b89bad4334d323465` corrects the editor's open-space-only label while retaining schema-v2 numeric fields. Nonzero-size open-space/summoning placement remains explicit. Castle's partial abort refund is not reproduced: the typed host mutates only after an explicit committed Cast response.
- `src/realmz_orig/spelltargets.c:94-111`, `cast.c:18-44`, `combat.c:147-378`, `getrange.c:5-58`, `resolvespell.c:254-433`, `resist.c:5-101`, `killbody.c:87-97`, `emptyque.c:5-13`, and `getup.c:42-69` establish ordinary spell reflection and mixed monster target slots. A selected party or monster actor with Reflecting Spells consumes `Rand(100)` and results below 34 replace that actor with active caster `q[up]` before duration, damage, the original monster's over-100 exclusion, resistance, saves, or protection. The redirected actor is resolved once and cannot reflect recursively. Target type zero repeats the complete check per selection; monster casters may select opposed monsters at native slots ten onward. If reflection kills a monster caster, Castle queues its death macro while `inspell` is true, finishes the remaining `noofmagattacks` iterations without another caster-stamina check, and drains the queue in `getup`. Because monster damage calls `killbody` for every nonpositive result, later reflected hits append duplicate records for that caster to the fixed 100-entry `todoque`; `emptyque` executes records, not unique subjects. Realmz 2.0 preserves selected and effective target IDs, that bounded record order, subject occupancy through its final duplicate, and the complete saveable caster continuation. Castle's automatic groups bypass this check, area footprints and intelligent scoring can consume anomalous repeated reflection draws, and queued fields pass `noreflect`; those branches remain separate rather than inheriting ordinary behavior.
- `src/realmz_orig/combatinfo-combatchoice.c:1227-1423`, `combat.c:209-370`, `getrange.c:5-89`, `spelltargets.c:5-111`, `resist.c:5-101`, `resolvespell.c:13-61,186-190,254-433`, `heal.c:5-47`, `killbody.c:26-52,149-152`, and `structs.h:159-170` establish special-57 combat healing. Manual player targeting permits any living party or monster actor; monster `cannot == 4` reverses allegiance and may select the caster itself. Reflection and resistance occur before special 57 negates rolled damage. Party healing rejects stone and dead characters and caps at maximum stamina. Although `heal` itself can revive an unconscious character, combat `killbody` has already removed that actor from the targetable field; the combat picker therefore exposes living actors only. Monster healing subtracts negative damage directly, may exceed maximum stamina, uses signed-short storage, and still sets `beenattacked`. Realmz 2.0 implements only the source-shaped target-one, class-eight, damage-type-eight, positive-cost special-57 form, exposes detached party-then-monster options, publishes signed damage plus positive healing, and persists over-maximum monster stamina without weakening signed-range validation. Camp healing, specials 58 through 60, other zero-damage effects, and generic stock spell display names remain separately flagged.
- `src/realmz_orig/structs.h:159-170`, `combatsetup.c:277-315`, and `spelltargets.c:101-104` establish monster starting-condition ownership. Data MD stores forty signed bytes directly inside each monster record; battle setup reads the complete struct before runtime adjustments, and ordinary targeting consults the resulting Reflecting Spells slot. Providence commit `b045a6317712f95932b7334490ecd8f756d43e5b` corrects schema-v2 data loss by requiring and emitting all forty values. The runtime validates their signed-byte range and copies them into both ordinary and authored-battle monster instances before play. The regenerated synthetic fixture uses permanent reflection to prove the compiler, loader, and construction path without embedding commercial records.
- `src/realmz_orig/misc.c:1651-1667`, `race.c:5-47,178-206`, and `class.c:5-41,183-207` establish character profile identity. Character race and caste values start at one; each creator enumerates thirty slots, and `loadprofile` subtracts one before seeking the fixed Data Race or Data Caste record. Race eligibility stores thirty caste columns at zero-based record positions. Providence commit `1d660bb6dbab175e641f33d07e8755e8ee3be24e` caps both native tables at thirty records, emits stable one-based identities and imported names, and aligns both eligibility directions. Schema and runtime validation independently require one unique profile for every Classic ID 1 through 30.
- `newcharacter.c::CharacterGen`, `getnumspells.c::getnumspells`, and `spellselect.c::Spellselect` establish the staged level-one creator boundary. One generated character reaches Review before acceptance; reroll repeats the source RNG path, while acceptance retains the reviewed result. Spell points use the caste start-level sum, current level, and Knowledge or Judgment; costs are 1, 3, 6, 10, 15, 21, and 28. The character table is exactly seven by twelve even though the resource-name catalog has fifteen entries per level. Realmz 2.0 therefore excludes slots thirteen through fifteen, persists the provisional character and spell choices, and yields a typed confirmation when points remain.
- `misc.c::Startlevel`, `newcharacter.c::NewCharacter`, `levelup.c::levelup`, and `misc.c::getexp` establish the advanced-character workflow over fixed levels 1, 3, 5, 7, 9, 11, 13, 15, 17, 20, 25, and 30. Castle constructs the level-one base, sets prestige to ten times the square of target-minus-one and direct experience to the negative caste victory threshold, then invokes ordinary `levelup` once per additional level before starting spells and initial items. Realmz 2.0 uses the same fixed choices through the campaign maximum, preserves each registration-era draw and level-up field order, and retains the generated character as the session-owned Review draft.
- `structs.h:60,94,314` and `updatespec.c:49-97` establish separate twelve-slot racial combat modifiers and fifteen-slot trained abilities, but only fourteen race/caste ability inputs. Castle's fifteenth loop iteration reads adjacent struct fields. `FD-CHARACTER-001` keeps the unauthored character ability slot zero and draw-free while preserving all fourteen authored initialization, Strength/Dexterity adjustment, level-up dice, and clamp rules. `levelup.c:35-44` also establishes that a reached caste condition threshold replaces any nonnegative condition with permanent `-1`; only an already-negative sentinel decrements.
- `src/realmz_orig/newland.c:2597-2662` pools and removes party wealth/equipment for opcode 36, then restores the exact stored records and returns items acquired in the interim separately. The 2.0 equivalent stores detached item instances and wealth in `PartyState`, persists the escrow, restores equipment, recalculates load from immutable item definitions, and moves interim finds to party storage.
- `src/realmz_orig/newcharacter.c:13-39`, `misc.c:824-839`, `wear.c:5-226`, `removeitem.c:5-102`, `moveicon.c:221-259,353-440,615-655`, `dropitem.c:5-29`, `items.c:470-549,885-1000`, and `showitems-showspecial.c:256-351` establish starting and ordinary carried-item workflows. `addinitialitems` visits the caste's authored slots in order, admits only records that fit, marks them identified, preserves authored charges, attempts to wear each admitted item, then adds starting gold; `calcw` derives load from gold, gems, jewelry at fifteen units each, and every item's base-plus-charge weight. Castle increments its separate `numitems` before the capacity test; Realmz 2.0 does not manufacture blank item records when an overweight record is skipped. Finalization uses package definitions for these identities and weights, vault import recomputes load against the target package, and restore rejects a mismatched saved total transactionally. Race and caste both admit the item's first category bit before specific race/caste and descriptor/class exclusions; equipment then enforces one slot per type above one, two nonduplicate rings, two hands, an equipped quiver for ordinary bows, party-unique negative-cost items, and curse identification/removal lock. Trade checks the recipient's thirty-item and load limits, accepts an equipped record, and writes it unequipped on the recipient. `FD-INVENTORY-001` follows that ordinary record outcome but prevents Castle's direct-delete path from bypassing a curse or retaining stale derived source bonuses. Drop removes one exact unequipped record after confirmation. Realmz 2.0 owns those operations through typed probes, intents, detached disabled reasons, and a saveable drop continuation. Passive effects that need unimplemented character-state mutation, item use, identification, and split/join remain unavailable instead of partially mutating state. Castle split/join changes stack records without consistently changing carried load even though trade weighs each record; that contradiction requires a named fidelity decision before exposure. Remake's Honest Storage is not Castle's opcode-36 escrow and is not presented as a Classic player service.
- `src/realmz_orig/newland.c:1736-1823` copies XAP programs over encounter/AP programs or redirects the active AP. The 2.0 runtime records equivalent program mappings and active-frame redirects without modifying immutable package definitions.
- `src/realmz_orig/attack.c:199-238,608-625`, `newland.c:351-404`, `getbattleorder.c:5-80`, `removeitem.c:5-92`, `dropitem.c:5-29`, `booty.c:130-177,320-340,470-510,690-710,1440-1600`, and `pref.c:518-740` establish natural fumbles, forced fumbles, and post-battle recovery. Character attacks always consume `Rand(1000 + 100 * level)` when fumbles are enabled; only 51–59 with a removable melee weapon and queue capacity drops it and forces a miss, calling synchronous 10121/10123 before `dropitem` adds asynchronous 655. Armed monsters use `Rand(600 + 50 * hit dice)` and fumble on 21–34 by clearing only the active weapon with synchronous 10121/655. Opcode 122's outer `q[up] < 10` guard excludes monster initiative IDs despite its nested monster branch, so it affects only an active party combatant after a physical action. Castle runs body count before booty, inserts fumble IDs first, retains them after retreat, identifies them, and lets the player choose a recipient subject to inventory/load limits. `FD-COMBAT-005` preserves the exact item instance and remaining charges instead of Castle's ID-only reconstruction. The global `allowfumble` preference and its linked five-percent XP adjustment, plus the live `inspell` opcode guard, remain explicit unresolved settings/lifecycle boundaries.
- `src/realmz_orig/spelllist.c:105-123`, `136-166`, and `412-475` provide the source branches used for the selected scenario-spell specials, including death/Flesh to Stone, resurrection, dispel, curse removal, movement, animation, poison, haste/age, and youth. Implemented specials fail or mutate explicitly; there is no script-name fallback.
- `src/realmz_orig/combatsetup.c:201-204` inserts unsuspended held-over allies into the monster roster and lines 467-468 consume them. `booty.c:130-154` then invokes `bodycount()` unless opcode 105 suspended allies; `bodycount.c:13-88` identifies living friendly `cansum` candidates, preselects mandatory allies, and applies the four-plus-mandatory/18-total limits, while lines 178-199 rebuild `holdover` from the player's selection. The 2.0 runtime therefore yields a serializable typed ally-selection request after combat instead of either silently deleting or automatically preserving allies. `src/realmz_orig/checkforenemy.c:47-60` and `combat.c:401-442` choose targets whose `traiter` allegiance differs. The 2.0 combat roster and monster AI preserve those ownership and targeting facts without presentation-side combatants.
- `src/realmz_orig/killbody.c:87-127` executes or queues `todoondeath` macro code with the dying monster's identity and then removes it from the enemy side. The 2.0 host runs a serializable nested VM before outcome calculation; opcode 119 can revive the exact combatant, and a pending player interaction survives whole-session save/restore.
- `src/realmz_orig/attack.c:695-697,785-793,976-998,1201-1222` resolves ordinary monster damage before special processing, consumes a generic special-potency draw for every nonzero special, and routes party-target special 17 through save slot seven. On failure it adds the truncated `race.maxage * .01 * (attack damage maximum * hit dice)` value to exact age days, invokes one adjacent `age()` operation, and blocks at `showageupdate` only when the band changes; physical damage and the next actor follow that boundary. Providence schema v2 already preserves every required monster, race, and caste field. Differential case `combat.monster-aging-special` covers the pure draw/formula vector plus scenario and direct-session save/resume.
- `src/realmz_orig/attack.c:785-889,1011-1084,1224-1229` and `misc.c:1188-1229` establish monster status specials 1–7 and 16. The shared potency draw precedes every save. Party conditions use the pinned Castle correction: a failed save adds potency once when the starting duration is below 30, may exceed 30, still reports a result from 30 or above, and cannot replace a negative permanent sentinel. Monster conditions have no duration gate; their save-seven chance is the integer average of six saves, while other save families retain the shifted save byte, spell immunity, and undead auto-save after consuming the roll. Runtime monster magic resistance above 100 jumps to the attack's whiff path after potency, removing ordinary damage. Party status feedback uses sound 630; monster disease uses 684. Differential case `combat.monster-status-specials` covers the exact table, draw order, state branches, sound/event order, and persistence.
- `src/realmz_orig/attack.c:785-917,1086-1097,1224-1229`, `misc.c:1188-1229`, and `structs.h:150-171,300-326` establish resource specials 8 and 9. Both follow ordinary damage and the otherwise-unused generic potency draw. Special 8 then tests save six before checking spell points, transfers the smaller of the target balance or hit dice times three, and does not clamp the attacker's signed-short balance to `maxspellpoints`; it applies to party and monster targets without a sound. Party-target special 9 tests save five, subtracts maximum stamina times twenty from direct signed 32-bit experience without a zero floor, and requests sound 630. There is no monster-target case 9 and therefore no save draw there. Differential case `combat.monster-resource-drain-specials` covers transfer limits, direct experience semantics, event order, and whole-session persistence.
- `src/realmz_orig/attack.c:918-1179`, `misc.c:1188-1229`, `checkforenemy.c:5-75`, `getup.c:205-214`, and `booty.c:94-111` establish the remaining monster specials. Charm uses save zero, including party charm resistance, copies the attacker's `traiter` side, makes charmed party actors automatic, targets only differing allegiance, and clears party allegiance after battle. Elemental specials consume an additional one-to-attack-maximum draw before saves 1–5 and matching protection. `FD-COMBAT-001` corrects the pinned monster-target cold-through-mental ordering defect, documented in `fidelity-ledger.md`; all other elemental ordering and truncation remain source-conformant. Blindness writes permanent `-1`; petrification writes party `-1` or monster `TRUE`, sets stamina to zero, and reaches `forcekill` before ordinary damage commits. Differential cases `combat.monster-charm-special`, `combat.monster-elemental-specials`, and `combat.monster-permanent-affliction-specials` cover the complete result, RNG, allegiance, event, and persistence boundaries.
- `src/realmz_orig/wear.c:20-218`, `attack.c:43-443`, `misc.c:1188-1229`, `loaditem.c:5-34`, `combatsetup.c:109-115`, and `share-movecost-dialog.c:43-51` establish the source-settled character melee subset. Classic item type 2 is the single melee slot; all equipped item damage, luck, and armor contribute to derived combat values independently from that slot. A legal attempt rolls an optional weapon-condition precheck, signed-range luck and hit, weapon gates, reflection, weapon elements, target-tag ranges, the physical small-target die, and two critical checks. The monster-target elemental branches anomalously call `savevs` with the attacker while neighboring character-target branches use the defender; `FD-COMBAT-002` corrects that target identity. Data BD battle `distance` controls initial monster placement and is unrelated to Data MD's required-weapon byte. `FD-COMBAT-003` applies Divinity's documented Item Number meaning because Castle's minus-1024 comparison cannot match the shipped Data ID, and it closes Castle's unarmed bypass of blunt/sharp restrictions. Differential cases `combat.character-melee-resolution` and `combat.character-required-weapon-correction` own these bounded results.
- `src/realmz_orig/attack.c:459-750,1185-1219` establishes ordinary monster melee with optional fumbles disabled. Accuracy includes five times hit dice and signed damage plus, optional weapon magic plus, source conditions, Realmz day divided by 70, party defender `Rand(luck + magical luck)`, effective armor, and evil-conditional protection before a ten-percent floor. Damage comes from either the selected attack row or the carried weapon's magic plus, physical small-target die, elements, and matching monster-type ranges; Dragon Hide reduces only the physical component and requests asynchronous sound 694. Providence schema v2 already preserves the complete monster/item projection, and `CombatFlow` supplies effective equipped luck and armor through a detached `MonsterAttackContext`. A weapon condition and Dragon Hide feedback following special 17 remain pending across the blocking age update and are committed once before deferred damage. Castle can subtract a negative physical total and heal the target; `FD-COMBAT-004` floors that component at zero. Differential case `combat.monster-ordinary-melee` owns the bounded result and explicitly excludes fumbles, reflection, missile replacement, and derived behind/facing state.
- `src/realmz_orig/combatsetup.c:133-196`, `getup.c:257-323`, `combatinfo-combatchoice.c:690-712,1698-1710`, `combat.c:957-963,1086-1095`, and `checkforenemy.c:124-135` establish physical attack cadence. Character `attacks` is a signed half-unit reserve: battle setup clears it, activation carries at most one positive unit and adds `normattacks + attackbonus` (plus four for Speedy), melee costs two, and control advances below two. A monster resets `attacknum`, selects one target, and runs the remaining authored rows in order, retargeting only after a kill. `CombatTurnState` now persists the active actor, chosen action, retained target, and next row; a monster age dialog commits its deferred row once and resumes later rows. Providence already emits race/caste attack inputs plus monster count/rows, and the loader rejects counts beyond supplied rows or the native five-row bound. Remake's `1.0 + halfAttacks / 2.0` floating budget overgrants a full action and is recorded only as divergence evidence. Castle's zero/one-unit display, Speedy/Slow display arithmetic, monster Speedy overflow, missile, spell, item, and movement interactions remain unresolved.
- `src/realmz_orig/combat.c` calls `combatmap()` before `combatsetup()`. `combatmap.c:5-224` clamps a 30×30 source window, expands it threefold into the 90×90 field, applies live terrain replacements, consumes dungeon-decoration draws, and preserves the apparent top-neighbor `bottom.ispath` branch. `combatsetup.c:100-468` then draws direction and bounded Data BD separation, places all party slots and held-over allies before row-major authored monsters, constructs those monsters with its own AC/DX/SP/HD/save/difficulty/weapon roll order, removes dead party positions, and only then reaches `getbattleorder`. The runtime persists the generated terrain, map shifts, actor anchors, and complete footprints before initiative. `FD-COMBAT-006` replaces Castle's unbounded no-solution placement retries with a finite complete-field search and transactional state/RNG failure.
- `src/realmz_orig/combat.c:124-141,385-510,702-757`, `cansee.c:5-52`, `movemonster.c:4-132`, `shift.c:5-62`, `checkforenemy.c:5-137`, `combatinfo-combatchoice.c:1497-1852`, `getrange.c:5-89`, `checkrange-checkforitem.c:9-25`, `getup.c:5-167`, `booty.c:51-108`, and `share-movecost-dialog.c:70-94` establish tactical targeting, LOS, stepping, proximity, Guard, withdrawal, and Escape. Party input derives both axes independently; monster movement takes the sign of its retained target delta and retries blocked directions through `shift`. Both use complete footprints and the maximum terrain contribution plus changed axes. Castle does not reset that maximum between shifted probes, so an unaffordable first direction can prevent a cheaper retry; 2.0 preserves and flags this likely source bug pending runtime evidence for a named correction. Monsters retain a valid target, otherwise draw in party/gap/monster slot space, switch to an ascending scan after the first unseen target, move repeatedly, and attack only after adjacency. `FD-COMBAT-008` removes `delayspeed` from the fixed 128-sample LOS divisor; `FD-COMBAT-009` bounds the fallback scan to validated slots. `checkforenemy(0)` scans party then monster slots, clears and executes every non-Helpless hostile Guard, and is called before a player step and after both player and monster movement. Codes one and two retain the original hostile perimeter, suppress only an Invisible mover's withdrawal queue, clear each attacker Guard, and issue attack row zero with `behind = TRUE`, adding twenty without consulting icon facing. A Guard can therefore strike once before movement and again during withdrawal. The player Guard command uses one-based `Rand(100) < 50` to choose asynchronous 10121 versus 10123; Finish clears Guard and movement. Explicit Escape permits nearest integer Euclidean range ten, blocks four conditions plus traitor allegiance, confirms, removes only the acting character, and adds 200 prestige penalty. Edge-band movement prompts or forces the same per-character removal before collision checks. RUNS_AWAY monsters move away from their retained target, receive source-ordered withdrawal attacks before each committed step, and leave after entering the edge band without invoking a death macro. Realmz 2.0 persists Guard/reaction state, escaped-character IDs, and direct or VM-owned Escape confirmations. Castle's no-target `pos[-1]` path and ineffective mandatory-ally edge reversal remain explicit unavailable branches. Ordinary monster missile and spell success now clear Guard. The pre-direction movement submode, possessed-character auto-guard, target-everybody omission, friendly swaps, permanent-Tangle movement, and nonordinary ranged branches remain unresolved rather than importing Remake's reaction economy, barycenter retreat, minimum-footprint cost, or Castle's apparent `-1` speed increase.
- `src/realmz_orig/killbody.c:87-154` clears monster battlefield cells before running a death macro, while `newland.c:292-307` can revive that record without restoring its field occupancy. `FD-COMBAT-007` retains the pending footprint, removes it only when the macro leaves the monster dead, and preserves it for a revived monster so all live battle state remains position-complete and saveable.
- `src/realmz_orig/newland.c:2010-2052` relocates the party for opcode 20, emits Extra Code sound/text, and rechecks a destination AP. The runtime commits those events in that order and bounds the destination recheck to one session-owned continuation.
- `src/realmz_orig/newcharacter.c:106-145` consumes six assigned attribute rolls plus one discarded roll; lines 173-262 apply race aging through the caste minimum age group, then perform special bonuses, stamina, and age in source order before the optional spell-point branch. `src/realmz_orig/age.c:123-154` adjusts six attributes and seven saves during creation. The exact comparison and proof boundary live in differential case `character.creation-aging-and-rng-order`.
- `src/realmz_orig/age.c:5-31` scans the authored year ranges but advances or erases one neighboring age row per call; lines 110-154 apply live Brawn/strength, five other attributes, magic resistance, maximum movement, and saves 0 through 6 without creation bounds. `textbox-time.c:199-217` increments every character's age at midnight. `booty.c:403-410` truncates a maximum-age character's battle share after multiplying by `.6666666`, and `spelllist.c:446-513` routes haste/age/youth effects through the same one-transition function. No pinned gameplay path consults the stored `doesnotdie` field for these rules. The exact comparison is `character.live-aging-and-maximum-age`.
- `src/realmz_orig/age.c:31-120` renders `showageupdate` from the resulting band/range, race, character portrait/icon, and fifteen forward or reversed row values, requests sound 3002, and blocks in `ModalDialog`. `textbox-time.c:205-216` invokes it in party order before timed-encounter processing; `spelllist.c:448-511` invokes it before returning from haste/age/youth resolution. The typed `age_update` request now owns those clock, post-move, and VM click boundaries; exact evidence is `character.age-update-interaction`.
- Additional War-active adapters are grounded in bounded `newland.c` branches for wealth/selection, encounter flow, time, shops, party mode, map state, allies, combat macros, and monster mutation. Exact ranges and proof labels remain source/control-flow unless a separate Castle runtime fixture exists.

These are source/control-flow observations. They do not claim that every formula branch has a Castle runtime fixture yet.

## Current character-melee boundary

The implemented melee subset builds a validated `CharacterCombatEquipment` snapshot from immutable item definitions. An equipped Classic type-2 item is the weapon even when its magic plus is zero; damage bonuses on rings or armor continue to contribute without changing an unarmed character into an armed one. The resolver covers equipped damage/luck/armor, weapon condition prechecks, hit chance, type hatred, reflection, weapon physical and elemental ranges, target-tag bonuses, helpless damage, and the two post-damage critical RNG draws. `FD-COMBAT-002` uses the defending monster's save for weapon elemental mitigation.

This is not complete tactical combat. The package now exposes `requiredWeapon` and `magicToHit` independently from battle placement `distance`; combat enforces magical-plus, blunt, sharp, and corrected specific Item Number requirements before auto-hit. The later active-turn cadence case owns attack half-unit preparation. The separate character `spec[15]` ability table, fumble preference/recovery, missile toggling, and full wear/remove side effects are not inferred or claimed by the melee-resolution case.

### Weapon mode and tactical boundary

Castle `combatsetup`, `combatchoice`, `toweap`, `tomissle`, and `wear` establish one battle-local character toggle over distinct type-2 melee, type-10 ammunition, and type-15 missile slots. Melee is the setup default when present; missile is selected only when melee is absent. The active character can toggle without spending an attack, movement, or the turn, and melee mode remains valid as hand-to-hand without a type-2 item. Realmz 2.0 now owns that state in `CombatState`, restores older save-v3 battle data to the safe melee default, rejects duplicate Classic slots, and carries the exact legal actions through `CombatView` and the typed battle interaction.

This does not implement projectile combat. Battle setup constructs and saves Castle-shaped terrain, positions, and footprints; `BattlefieldRules` owns complete-footprint adjacency, one-step legality, bounded monster shift retries, and deterministic 128-sample tactical LOS. Party actors can make eight-direction reaction-free moves; ordinary monsters retain/select visible targets, move through repeated legal steps, and resolve physical rows only after adjacency. Steps involving a party actor's old or new hostile perimeter remain disabled so missing guard/withdrawal reactions cannot be skipped. Euclidean projectile range, item/spell projectile identity, projectile-specific saves, and exact charge/action costs remain unimplemented. Schema v2 preserves Data BD distance, authored monster coordinates, each active landlook's exact 0-through-200 mapstats, and the global 200-through-400 combat table. Character Fire and nonadjacent monster missile resolution still report an explicit unavailable state rather than impersonating melee.

Castle's positive `behind` flag belongs to withdrawal attacks from `checkforenemy(2)`: every hostile left adjacent by the move receives a temporary +20. Character `face` and monster `lr` only select icon orientation. Remake's adjacency comparison is a useful lead, but its consumed once-per-round `reaction_ready` is not Castle behavior. No facing, flanking, or backstab mechanic is inferred from those rendering fields.

## Providence compiler evidence

Providence commit `028a587d8f99bd687d279d3abfdb74663ac4819f` owns the current schema-v2 exporter tranche; the preceding exporter lineage owns the direct opcode 88/89 monster reachability fix, Classic column-major land decoding, negative-land image overlays, required package-backed map atlases, Classic option labels, runtime-reachable shared Classic sounds, complete monster conditions, and battle terrain. The current tranche preserves the thirty one-based race/caste profiles, each race's exact 5×15 signed aging table, fourteen authored race abilities, both fourteen-value caste ability rows, all thirty caste victory thresholds, and the complete role-labelled portrait/tactical catalogs. The schema mirror hash is `040f50ab9bd67a98a626176de876848b135fb449263fcd989664652a65bc7acd`. The synthetic package contains three 90×90 maps, the bundled Classic race/caste/item/spell libraries, scenario supply/custom overlays, explicit battle slots, random rectangles, a source-correct AP destination pair, a picture, reachable shared sounds, two synthetic map atlases, one synthetic special-land overlay, all 240 character appearance assets, source-shaped character progression records, explicit monster weapon-gate fields, a permanent synthetic monster reflection condition, and deliberately distinct land/global tile-200 values proving Castle's load precedence.

The bounded Assault on Giant Mountain audit is recorded in `tests/fixtures/oracle/aogm-active-opcode-inventory.json`. Its source hash is `23e5a33dcf06e020d9efdde12ff1b5a85a97d42d732b9bc1bfb464f4fa1f7ce0`; it records 717 triggers, 32 Complex, 18 Simple, 9 Thief, and 12 Timed Encounters and an exact set of 58 active normalized opcodes. The committed fixture contains counts, hash, and opcode identities only—no commercial records. It proves bounded inventory/readiness coverage and explicitly makes no reachability claim.

## 2.0 proof

- `runtime-unit`: fixed rules cover signed arithmetic, character construction/leveling, live age-band transitions and reversal, midnight aging, maximum-age rewards, conditions and camping, inventory/economy/treasure, combat, all nineteen monster attack specials, scenario-spell draw ordering and selected specials, monsters, and combat flow. VM tests cover equipment escrow, program replacement/redirect, fumble state, packed opcode 17/18 spells, unknown-spell failure, and save/restore.
- `runtime-contract`: the loader independently constructs and cross-validates direct gameplay/encounter definitions, rejects unsupported active opcodes and malformed references, and verifies the mirrored schema and package hashes.
- `runtime-integration`: the Providence-authored fixture drives encounter and gameplay-domain operations through `GameSession`, the one public `RealmzRuntimeApi`, domain executors, and whole-session persistence. The current typed suite passes 2,662 assertions across 13 suites, including player automatic-group spell shared-roll and target-order coverage, source-ordered land/dungeon battlefield generation and decoration, party/ally/authored-monster placement, `combatsetup` monster construction, footprint/view/save restoration and cross-reference rejection, transactional impossible-placement and late-identity failure, complete battle-terrain catalogs and tile-200 precedence, eight-direction player movement, deterministic tactical LOS, retained/slot-selected monster targets, bounded randomized shift retries with Castle's retained cost, repeated monster movement into adjacency, persistent movement/target state and target-reference rejection, source-ordered Guard/withdrawal with fixed-origin hostility, exact Guard sound split, per-character Escape and routed-monster edge flight, direct/VM Escape-confirmation save restoration, age/death-macro cursor restoration and CODE 119 revival, ordered spell-triggered death-macro queueing and restoration, helpless-monster skip, explicit unsafe retreat targets and mandatory-ally edge behavior, Speedy/Tangle boundaries, adjacent-only melee, death-macro occupancy, source-ordered random-region draws, typed surprise interaction save/resume, one-shot random doors, age-update-before-AP restoration, complete monster-special draw and mutation coverage, source-settled character and monster weapon damage, half-attack carry, ordered monster multiattack and cursor restoration, explicit and range-validated monster weapon gates, signed Castle luck ranges, charmed automatic turns, hostile target exposure, enemy-count participation and save restoration, vault rejection of battle allegiance, status/resource event ordering, ordered save v1-to-v3 migration, generated-character Review/Reroll/acceptance, complete fixed-level advanced construction, separate trained abilities, twelve-slot starting spells with saveable unused-point and vault-publication confirmations, and exact/legacy current-age-group restoration.
- `content-inventory`: all 58 active normalized opcodes in the bounded campaign inventory have one declared owner, pass package readiness, and dispatch without the top-level unsupported-opcode fallback. Submode/argument coverage and completion-critical reachability remain Phase 5 route work.
- `runtime-unit`: held-over allies enter combat as non-traitors, opposed monsters attack through the fixed rules, death macros run before outcome calculation, and both nested scenario-battle and direct-session macro continuations are tested. Body-count defaults, mandatory retention, typed response validation, and saving at the post-battle selection boundary are covered. The direct-session test saves while opcode 14 is pending, restores the complete combat/VM/RNG aggregate, resumes opcode 119, reaches ally selection, and then resolves the battle.

The prior local Assault on Giant Mountain route package contains 9 maps, 72,900 cells, 623 runtime-reachable triggers, 1,956 Classic instructions, 983 messages, and 21 decoded media assets. Runtime package hash `7561aa2d2f1c2acf4dc57bc12cef1d79049473a2e9bd19fe8c6e377acf087e84` independently validates and starts at land 0 coordinate 48,15. A generic 2.0 route harness executed local route hash `b84ff7d68d2d203cb03aca65f94600d4f7c87e01054d8b9a97aba6d68ef19a95` through the normal session/VM boundary: opening, Battle 60, Baron macro, Battle 274, all four terrain-155 mutations, quest 17, rewards 68/467/625, and final land 0 coordinate 84,8 passed. This remains `live-route` evidence for that deterministic completion spine, not a claim about the newer atlas-bearing package or every campaign branch.

The prior local visual/audio package at hash `45dd3caa73b58b29afdbe071ea120a812f64595b716468ecede61b27c1e69ae3` carries 119 media assets, including all map-referenced Classic atlases, required special-land icons, and 40 scenario/shared Classic sounds referenced by the runtime-reachable program inventory. Its world document is the same corrected AOGM topology whose land 0 base layer matches the established 8,100-cell reference layer with zero mismatches; start (48,15) is tile 155, and 59 negative cells retain separate overlays. The runtime independently validates and starts the package. MCP moved through the ordinary session boundary from (48,15), fired the opening AP at (49,15), and reached the public-well AP at (51,8): the four-channel presenter ended the authored Horn/Angry Mob/Camp sequence on sound 10001 without a WAV decoder error, and the typed interaction rendered `Yes` and `No` instead of message-zero `Do Not Use`; declining cleared the pending request. A 960×600 prompt capture was inspected. This is `runtime-integration` and visual/control-flow evidence, not a human auditory comparison or a rerun of the certified completion route.

The prior local AOGM export at package hash `1e93716b593ba26466d27a9bf3f4f974572432494ad95d5a4f65c6bd03a87350` proved package startup against the preceding schema but predates the required monster-condition projection. It is intentionally stale and must be re-exported before further AOGM combat-route claims. All AOGM packages, routes, screenshots, and reports remain local and uncommitted.

The fresh local War in the Sword Lands export independently validates at package hash `4509fcda9f3684292b5406c151b7108979f4f6211eb1ee198b808ce798e464dd`. External route hash `fa08b47bd53e0f506333c3c488ccb609b7387fc324b7784586b88d9899e5b1d3` passes `war-opening`, `defeat-taroth-sark`, `recruit-naryl`, `battle-of-sharranth`, `naerun-war-ending`, `overlord-last-stand`, `maximus-revelation`, and `completion-runtime`; it observes Battles 377/473/474/212 and ends at land 16 coordinate 78,36 with quest flags 1/42/75. The ending specifically proves that Naryl is consumed into Battle 473 and selected back into the held-over party before the later opcode 87 branch. This is `live-route` evidence for the bounded completion spine. Direct ED3 checkpoints prove their compiled macro behavior, not unplaced-map reachability, and all commercial inputs/reports remain local.
