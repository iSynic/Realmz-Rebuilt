# Classic compatibility gap register

Realmz Rebuilt aims to load and play ordinary Castle Realmz scenarios through Providence packages without silently changing authored behavior. This register tracks known gaps below the coarse application-workflow denominator. A workflow can be generally functional while a specific item, spell, opcode branch, combat condition, or presentation sequence remains incomplete.

## Status and evidence

- **Confirmed blocker** means a current production path explicitly rejects, skips, or misrepresents a Castle-valid operation.
- **Coverage gap** means the runtime has an owner, but the relevant authored variants or ordinary routes have not been demonstrated.
- **Reported defect** means ordinary play exposed a symptom that still needs a retained reproduction or causal proof.
- **Implemented, awaiting ordinary replay** means production behavior and focused public-boundary coverage exist, but the original player route has not yet been repeated successfully on the repaired build.
- Corpus counts below describe stored definitions or instructions. They do not establish ordinary-route reachability.
- Package validation, controlled execution, ordinary play, and Castle comparison are separate evidence levels.

## Prelude repair and complete bundled-corpus intake (2026-09-21)

- Providence revision `2ded3e0ecd2e6b41ac49893e27f23704246d2bb1` regenerated the affected packages from the pinned Castle source. Prelude is `7f18df4eecc935dd7c1481b0c2419cfc224e8e03950b2263e121c5236073c95c` / `728c06887eaf9bde5f2117b3e8d06f0b6c69b2671a9972d762dd7ef603904d93`; Wrath is `b2019c2b6cec90ff2c0dfd780828e3ed303b1f34db7f2e3ba369363509a22bd4` / `ff198638b6c8b6a474d4c1be9b62067a02d7b05149d8cf785712e854554da5c7`. Compiler, binary, application-library, source, and package identities are recorded in the provenance and correction catalogs.
- Prelude retains authored `xap:97` (message `-248`, Keep Codes), `xap:149` (messages `368`/`369`, opcode 7 replacement to XAP 31, Keep Codes), and Simple Encounter 7 result 3's opcode 44 (`id 4`, Extra Code `[4,0,0,0,5]`). The package-pinned `tools/prelude_payment_probe.gd` passes both AP 9 payment branches through ordinary session movement and typed meal/payer choices, including restoration at the pending payer selection and west/east re-entry. With the six starters' original gold, messages are `65,66,67,69,100,101,102,103`, five gold is debited in party order, and AP 9 becomes XAP 31. With explicitly prepared zero wealth, messages are `65,66,67,69,100,101,102,368,369`, no gold is debited, and AP 9 also becomes XAP 31. Both re-entries offer XAP 31's Yes/No choice and No backs out to `(22,12)`. The zero-wealth route is controlled session integration evidence; its visible meal/payer controls remain to be replayed in an isolated fixture.
- The package-pinned `tools/prelude_riter_probe.gd` enters Mayor AP 10 and then Riter AP 6 by ordinary session movement. Interjecting for Riter, declining Marnin's warning, and accepting his challenge starts authored Battle 75, not retained Battle 103. The source narration promises Riter's 100 gold; Battle 75's controlled victory treasure contains 21 gold and four gems, then XAP 90's opcode 33 **takes** 100 gold. Castle `newland.c` opcode 33 calls `takegold`; `takegold.c` debits pooled/character wealth or fails. No preceding 100-gold grant exists in this compiled caller chain. With starting gold, the probe shares battle wealth, pays 100 in party order, disables AP 6, and returns quietly. With an explicitly zero-wealth baseline, the 21 battle gold cannot cover the payment; message 248 follows message 247, AP 6 is kept, and return runs the AP again. The probe restores the pending pre-battle choice and post-battle save and uses forced victory, so ordinary combat and Castle runtime observation remain open; the reported Battle 103 identity may describe a different source or route and is not silently equated with this one.
- Wrath's regenerated content, scenario, and media index match the prior Providence export. The compiler correction changes four player-map records and thirteen marker identities from generic `realmz-application-cicn-*` names to the exact application catalog identities (`realmz-portrait-*` and `realmz-special-land-neg-*`). Package validation and startup now pass; ordinary map presentation remains separate route proof.
- The current full-pipeline intake report is retained outside Git at `F:\Providence-vNext-build\scenario-audit-item-text-full-r1\intake-report.json`. It records separate import, complete diagnostics, readiness, direct compilation, Providence application-ownership finalization, package validation, and startup stages for all 13 pinned scenarios. Castle in the Clouds, Half Truth, Prelude, and Wrath pass every stage; the other nine retain exactly one readiness blocker each, with complete diagnostics, and do not proceed to package generation. The earlier r5 report probed direct compiler archives only; those are intermediate and their stock-rule duplication is removed by Providence's existing `slim-scenarios` pass, not a new package-ownership defect. The prior finalized baseline is `F:\Providence-vNext-build\scenario-audit-455b081-29e745b3-full-r3\intake-report.json`. The current compiler recovers scenario item `STR#` 800/801/802 from the owning `Scenario.rsrc` when `Data NI.rsrc` lacks that family; all four finalized packages now retain 200 canonical scenario items. Relative to r3, only `content.json` changes (its `items` field); `scenario.json`, `world.json`, and `assets/index.json` remain byte-identical. The current outputs still need shipped-archive adjudication before integration: Castle has 595 versus 853 shipped triggers and 121 versus 154 shipped Monsters; Half Truth has 843 versus 1298 triggers and 192 versus 209 Monsters, alongside changed programs, content, and media. The removed trigger entries in those raw counts are XAP/macro pseudo-triggers without map positions, not proved missing placed APs; the non-trigger deltas remain open. Successful startup is not parity.
- The item-text compiler repair is local Providence commit `c0b53d7` (no configured remote). The full intake ran its release binaries before that commit; the only intervening source change was an additional regression test. SHA-256 identities are CLI `21e08a63da2de80c52b4f0e2f67a20af106f260ef234eb6f4fe407be40f21538`, native adapter `0f7254d19230eb0a4c382fd3019db3289015d5cd59f0b051b05edd26922c5156`, and application-library slimmer `575d3acf749641562afcb4d3c7cbb724d146a3d3706778b2c98b3fd4c6c61499`. The generated archives are audit evidence, not replacements for the shipped packages.
- The bounded earlier batch snapshot is retained outside Git at `F:\Providence-vNext-build\scenario-audit-e777690-batch1-final.json`; it carries all 13 source/package identities, stage-separated outcomes, five workflow dispositions, the Jev receipt, and the deterministic queue at that point. Its AP 9 insufficient-funds status predates the current controlled replay above.
- The five-workflow batch is therefore partial: Prelude AP 9 has controlled session proof, and Riter has forced-victory continuation proof; visible zero-wealth controls and ordinary Riter combat remain open. Wrath package identity is verified but presentation is open; Assault's battle-sound compiler selection has a focused repair, Necronomicon's distinct battle operand remains unresolved, combat-reference adjudication is partial, and Half Truth ordinary opcode-33 journeys remain open. The live Jev trial used 13 requests against `jev-1.13.0` at `$0.00053626`; its within-tier suggestions were recorded but its value against adjudicated controls and measured triage time remains unproven, so the deterministic queue remains authoritative.
- Castle `newland.c:1451-1465` literally passes opcode 2 Extra Code slot 3 to `textbox(-1, extracode[3], ...)`; `textbox-time.c:32-41` seeks that record in scenario `Data SD2` without a checked read. Assault Land 6 AP 42, action slot 1, row 354 is `[238,0,-1,30002,0]`; its `Data SD2` has only 983 256-byte records. The existing `FD-SCENARIO-008` decision and runtime test deliberately interpret this exact legacy shape's fourth word as application sound 30002, with no message lookup. Providence local revision `2b33533a400dcc6d32866e61bc18fdff7814418c` gives the compiler selector that same narrow rule. Providence `455b081` then fixes `Data SD` foreign-suffix detection: Assault's 117,078-byte source (SHA-256 `648d6a8d...`) owns Shop rows 0–19, followed by dense foreign rows 20–37 and an all-zero terminal row 38, all preserved byte-for-byte. The previous Shop 26/item 16236 problem was a false import of those compatibility bytes. The full-pipeline intake now names the remaining authored target as Land 3 AP 37, action slot 0 → missing Shop 26. No replacement Shop is inferred; Castle observation or stronger source evidence is required. Destroy the Necronomicon Dungeon 0 AP 28, action slot 1, row 98 is `[23,0,-1,10090,0]`; its file has only 1171 records. Although application sound 10090 exists, that row lies outside `FD-SCENARIO-008`; its intent and Castle-observable result remain unresolved, so it must not be silently admitted as the same correction.
- City of Bywater's designated snapshot is identified by the bundled provenance file catalog, not a fixed local directory. The r5 intake's `F:\Realmz\base\Realmz\Scenarios\City of Bywater` matches all 35 catalog files byte-for-byte; its `10001` finding is in scope. The older `F:\Realmz\corpus-fixtures\City of Bywater` has only 32 files and differs, so the intake now rejects that directory (or any other candidate) when its files do not match the designated catalog. The audit report's distinct tree-hash algorithm does not change the file-level match.
- Bywater's first `10001` candidate is Extra Code row 38 `[0,84,12,10001,0]` at the opcode-3 prompt field, reported on Land 1–4 APs 56/78. Castle `newland.c:1530-1532` passes slots 3/4 to `question2`; `question.c:95-128` opens scenario `Data OD` if present and reads 25-byte option-label records there, falling back to `Data SD2` only when `Data OD` is absent. The designated source has a 1275-byte `Data OD` (51 records), so label `10001` is out of range in this caller. No replacement option text or Castle-observed display is established. Castle `newland.c:611-623` compares opcode-127 Monster presence operands with the active living battle roster and exits cleanly when absent; Providence `2b33533a` no longer selects missing source Monsters 441, 439, and 400 for this test-only relation, and Rebuilt now returns false instead of `unknown_monster`. The full-pipeline intake confirms one remaining readiness blocker: Complex Encounter 4 result 3's context-unsafe within-Simple result branch. Its loaded Encounter lifetime still needs Castle adjudication before Bywater can be regenerated.

## Confirmed runtime and presentation gaps

| ID | Area | Current gap | Known exposure | Player consequence | Closure evidence |
| --- | --- | --- | --- | --- | --- |
| `CCG-005` | Monster turns | Closed in controlled public combat: Speedy doubles movement and repeats attack row zero twice after the authored rows; signed permanent Tangle is subtracted exactly as Castle does and survives save restoration. | 19 Speedy and 27 permanently Tangled stored definitions across bundled catalogs; ordinary-route battle placement remains unmeasured. | The explicit activation stops are removed. | Recheck open and obstructed ordinary campaign battles with named Speedy/permanent-Tangle actors before upgrading route reachability. |
| `CCG-006` | Monster spells | Admitted in rules classification and covered in controlled public combat for valid corpus spell signatures: single and group healing/cures (target type 9 and healing condition spells), condition buffs, attack/damage spells, direct-damage spells, and zero-cost actor field spells. Monster AI evaluates group healing for wounded allies and zero-cost field spells without artificial cannot, cost, or class guards. Classifier admission and controlled unit execution are demonstrated; ordinary campaign-route execution across all scenario battles remains unverified. | Normalized package probes report zero unsupported-pending monster spell-slot references across all 13 bundled packages and loadable third-party packages. They separately identify 21 not-applicable references to party-only or field-only records in Destroy the Necronomicon, Grilochs Revenge, and Half Truth. Conversion-blocked third-party sources cannot yet be audited at the runtime package boundary. | Monster automation selects and casts legal authored spells according to deterministic tactical weights and ally status without skipping or misrouting valid spells. | Controlled public combat flow tests prove monster AI group healing evaluation, zero-cost field execution, and cast event resolution. Ordinary campaign-route battle execution remains separate. |
| `CCG-008` | Combat spells | Closed as an invalid Classic record shape, not an unimplemented targeting family. Castle uses nonzero `size` to enter open-space picking, but only special 58 Summon owns those coordinates; every other special passes a battlefield coordinate into actor-index resolution. Rebuilt rejects the malformed combination consistently for learned, scroll, item, and monster casting rather than reproducing the unsafe access. | No such scenario-owned definition was found in the 13 bundled packages or either loadable third-party package. All stock nonzero-size target-type-0 records are recognized summons. City of Port Hyrtin's one custom target-type-0 spell has size zero; Journey into the Mire has no custom spell. | Valid summons and repeated-actor spells remain available; a malformed third-party record receives the source-specific unavailable reason without mutation. | Controlled public probing covers the synthetic malformed signature across all four casting-source dispositions. Rerun the corpus probe after Providence clears the ten blocked third-party conversions. |
| `CCG-009` | Projectiles | Character missile-item random power, the zero-cost monster spell-slot signature, and all standard and special projectile records are adjudicated and supported in rules classification and projectile resolution: special 0 (normal), special 7 (half movement), special 28 (Blind: duration dealt as damage, with shared permanence and accumulation guards), and special 49 (death/slay: target health plus ten). Physical damage spells (damage type 9) in charged missile weapons (such as Mithril Vault laser pistols) are admitted as ordinary combat spells for charged items and execute through `use_spell_item`. Classifier admission and focused command regressions are proven; campaign-wide physical missile journey certification is not claimed. | The monster signature has 90 stored references across Castle in the Clouds, Grilochs Revenge, and Prelude to Pestilence; all package probes report it executable. Public command regressions cover stock and scenario-owned projectile specials 0, 7, 28, and 49, physical projectile items, and charged missile items with damage-type-nine spells. | No referenced application or package projectile record remains blocked in rules classification. | Controlled public regressions prove character and monster projectile resolution with Blind (special 28) and death (special 49), and charged missile weapon combat flow with physical damage spells. Ordinary campaign journeys remain open. |
| `CCG-010` | Monster macros | Immediate area, queued area-field, side-group, and repeated-target macro spells resolve through the shared spell owners, including field creation, damage, healing, conditions, spell-point changes, allegiance, transformation, defeat cleanup, and nested death-macro queuing. Repeated-target spells (targetType 0) select living actors within authored range and require LOS only for positive-range signatures, friendly if `cannot == 4`, opposed otherwise, in deliberate deterministic party-then-monster order up to power/fixed-count, spending no spell points and preserving the dead source anchor and active turn. Single-target (targetType 1) and ray (targetType 6) signatures fail explicitly with typed error codes (`unsupported_macro_single_target` and `unsupported_macro_ray`) due to Castle source bugs (coordinate aliasing causing out-of-bounds `pos` reads in `newland.c`/`spelltargets.c`, and ray traversal bypass in `spelltargets.c`). | An exact static call-graph audit of the 13 bundled packages finds 157 stored opcode-17 rows and 68 reachable from battle/death-macro roots: 24 immediate, 36 queued area fields, three repeated-target, and five other-target rows. Controlled public Scenario VM tests execute the exact packaged XAP programs for all three repeated-target rows (Castle in the Clouds `xap:273` Spell 3208, Trouble in the Sword Lands `xap:1605` Spell 1108, Mithril Vault `xap:9` Spell 1304). Packaged Mithril Vault `xap:393` and `xap:337` prove representative single-target and ray typed rejections. AOGM's missing in-range XAP 165/169 are confirmed stale-package omissions; White Dragon's out-of-range XAP 9363 is a dangling foreign-suffix source row that current Providence rejects. | All three reachable repeated-target signatures execute cleanly from packaged programs in controlled battles without spending active-character resources or losing continuation; representative other-target rows fail safely without game-state mutation instead of corrupting memory or bypassing ray traversal. | Controlled public Scenario VM tests cover all three repeated-target signatures and representative packaged typed rejections under `FD-SCENARIO-009`. Ordinary campaign-route execution and the remaining three packaged rejection rows remain open. |
| `CCG-012` | Combat movement presentation | Closed in controlled playback: `combatants_swapped` changes both cached anchors in one causal frame, so later attacks originate from the committed square. | Directly observed in Half Truth with Brom and Durin; controlled presentation regression added. | The stale-square cause of apparent two-space melee is removed. | Inspect an ordinary Half Truth Auto swap plus skip and reduced-motion convergence before upgrading live-route evidence. |
| `CCG-013` | Combat movement | Closed in controlled public combat. Castle resolves Guard after a direction is supplied but before terrain, cost, hostile-contact, or friendly-collision handling. Rebuilt now creates the serializable reaction at that boundary: a blocked direction commits ordered Guard attacks and reports the unchanged position, while a friendly collision presents its saveable Swap/Attack/Back-out choice only after Guard settles. | Controlled combat covers a solid destination and a guarded friendly collision, including event order, unchanged blocked position, exact pending-choice restoration, typed cancellation, and one final swap. Ordinary route reachability is not inventoried. | Blocked attempts, the collision prompt, and cancellation no longer bypass or replay Guard. | Replay blocked movement, Back out, Attack Friend, and Swap Positions through the ordinary battle controls before upgrading route evidence. |

## Implemented repairs awaiting ordinary replay

| ID | Area | Current evidence | Remaining acceptance |
| --- | --- | --- | --- |
| `CCG-001` | Equipment | Castle wear/remove ordering is implemented through `EquipmentRules`, including strength, movement, magic resistance, spell points, attack bonus, conditions, trained abilities, special abilities, party conditions, and forced removal. The exact stock Robe of Speed (206) equips for the converted Brownie Minstrel David through the public Inventory session, applies permanent Speedy, restores through save v5 without double application, removes cleanly, and now has rendered Inventory evidence after actual Treasure assignment, Equip confirmation, restore, and Identify controls. | Broaden package representatives for overlapping and lossy party-condition effects. |
| `CCG-003` | Equipment | `CharacterState` now owns exact equipped-instance order. Controlled public tests distinguish reversed armor-floor and damage-cap results, and save v5/Character Files v2 reject missing or inconsistent order. | Exercise a naturally obtained order-sensitive combination through actual Inventory, combat, removal, and save/load controls. |
| `CCG-015` | Inventory presentation | Detached item actions carry the rules-owned reason and the Inventory controller binds it for focus/hover. The exact Robe case is enabled because its negative permanent-condition duration is legal, not because the UI invented an exception. A rendered restored fixture shows David's equipped item, Speedy condition, and enabled Unequip action; an actual right-trigger sequence now changes the Inventory owner and retains controller focus through the item ledger. The reproduced shell defect that changed roster selection without rebinding Inventory is repaired. | Physical-pad comfort remains separate; broaden visible unavailable-reason evidence across ordinary campaign items. |
| `CCG-004` | Combat outcome | Castle source and application STR# 3 settle opcode 56 loss target `-1`. The revived handoff now preserves both stock warnings and sound 26260, subtracts `2,000 × level` experience in party order, reverses the last land step, finishes the issuing timeline, and round-trips the completed state. Invalid backup state rejects before mutation or RNG consumption. | Exercise a reachable authored opcode-56 defeat through its ordinary application hook, acknowledgement queue, and post-move owner; the controlled public-runtime proof does not establish campaign reachability. |
| `CCG-011` | Monster retreat | A routed monster with a stale target now reacquires through the deterministic visible-target owner before moving. At the edge, an ordinary monster leaves battle; a scenario-mandatory ally remains living and positioned and publishes an explicit blocked-retreat reason. The latter deliberately stops movement instead of reproducing Castle's unsafe repeated offscreen walk. Both retained states serialize. | Exercise ordinary and required allied monsters through routed movement, Guard/withdrawal reactions, large footprints, and restored battles before upgrading route evidence. |

## Corrected baseline claims

- `CCG-002` was invalid as recorded. Pinned Castle `wear.c` accepts normalized item type 1 through its default slot path, Rebuilt now does the same, and the current stock-plus-13-package content contains no type-1 item definition. The earlier claim of two Griloch definitions came from an incorrect field/count interpretation. Type 1 remains a synthetic controlled coverage case, not an exposed campaign gap.
- `CCG-014` was invalid as a production gap. The package decoder accepts only `realmz2.compact-cell-rows.v2` maps and constructs them through `MapTopology.from_compact_rows`, so a package cannot enter the noncompact/no-map-identity path. Movement and transitions, LOS, retained rendering, restoration, faced-cell condition checks, and battlefield construction all consume `MapTopology.effective_cell_at`. Remaining direct `cell_at` callers intentionally inspect immutable bounds, trigger placement, authored metadata, or stable cell identity rather than terrain facts. The existing public exploration fixture proves that one replacement changes movement, LOS, sound, render tile, and battle terrain through the shared effective cell without rebuilding package content.
- `CCG-023` was invalid as a runtime defect. Destroy the Necronomicon contains six monster spell-slot references to target-type-8 special 56, but Castle `resolvespell.c:192-233` implements Phase entirely through the active party slot (`charup`, `c[charup]`, `attacks`, `beenattacked`, and `canundo`) even when the generic monster cast path reaches it. Rebuilt correctly classifies this context as not applicable rather than teleporting or killing a stale party character. Fresh probes of all 13 bundled packages now separate these six records plus nine fatigue, three Identify, and three Torch field-only references from genuine unsupported-pending monster spells; the latter count is zero for this corpus.
- `CCG-007` is corrected through the shared learned, scroll, charged-item, and monster area resolver. Shaped target types 3 and 4 test each occupied reflecting actor before the shared duration and damage rolls, redirect successful results to the active caster, admit a reflecting monster before its over-100 magic-resistance exclusion, and collapse duplicate effective recipients exactly once. Automatic side and whole-group target types 9, 10, and 12 bypass reflection, while later persistent-field collisions retain Castle's `noreflect` behavior. Controlled public combat proves multiple reflectors, resource accounting, event identities, RNG order, group bypass, and save restoration; ordinary route replay and reflected caster death-macro presentation remain open acceptance evidence.
- The previously reported controller commerce and inventory routes are implemented and covered at their actual control owners. Shop Items and Money mount their complete browse-only workspaces and return to the unchanged Shop; focused stock and carried rows select the same record shown by the inspector and activate the existing typed Buy/Sell responses; inventory item focus survives a rebind by stable instance identity; party portraits are focusable; and radial Rest and Heal retain the footer's held cadence until controller Confirm releases. Maps/Notes, spell selection, encounter item/spell workspaces, top-menu activation, acknowledgements, text editing, and save/load likewise have controlled input-owner coverage. These are controlled application/UI facts, not a claim that every ordinary campaign route or physical controller has been replayed.
- Rapid controller top-menu navigation no longer queues scroll requests against row controls replaced by the next menu render. The deferred presentation owner resolves the current selected row after layout, preserving clamped repetition and stable command identity without Godot's stale-control diagnostic.
- The reported battle-before-map Auto defect is closed in consecutive rendered Half Truth battles. Battle 34 entered at committed/deferred revision 47 while revision 46 remained the last drawn battlefield; no Auto activation committed until the combat surface had drawn. After seven rounds, Treasure, and actual level-up controls, Battle 70 entered with the surviving roster Auto settings already enabled. Its entry revision 106 contained only battle setup, remained deferred behind drawn revision 105, and committed the first Auto activation at revision 107. Both battles kept each later activation behind its applicable playback/draw boundary.
- The reported lethal-spell `0` label is closed. Half Truth Battle 34 produced a final Flame Missile result with `damage: 8`, `healing: 0`, and `defeated: true`; the detached playback contract selects damage for that result, holds the target through the numeric frame, then removes it at defeat. The same rendered battle created and later expired Solor Winds fields at their event boundaries, and its retained 1280-by-720 frame shows the active field on the battlefield. Focused presentation coverage keeps multi-target results distinct, coalesces only eligible shared visuals under Hurry, and makes reduced motion converge without gameplay mutation.

## Coverage and compatibility gaps

| ID | Boundary | Missing proof | Why it matters |
| --- | --- | --- | --- |
| `CCG-016` | Third-party package intake | A pinned twelve-scenario local intake records complete source-tree hashes and runs atomic Providence import, Rebuilt readiness, compilation, and public startup probing. City of Port Hyrtin and Journey into the Mire are now `opening-tested` through rendered controller movement and authored acknowledgements; ten scenarios retain named compiler/readiness blockers. The superficially identical one-reference failures are now split: Araman's Ring and Lachis are blocked by Providence globally requiring stock door item's absent scenario-local XAP, while Elemental Strife has a placed 100-percent AP that calls a semantically empty Simple Encounter. Normalized feature reports, save/resume, and broader ordinary journeys remain pending. | The 13 bundled packages cannot establish compatibility with the wider authoring ecosystem; the intake and two bounded openings provide an executable queue without overstating opening proof as main-route playability. Compiler selection may not turn an optional unavailable portable-item action into a package-wide failure, while genuinely placed missing content must remain blocking until adjudicated. |
| `CCG-017` | Opcode variants | All 119 behavior-bearing opcode identities have handlers, but handler ownership does not prove every Extra Code mode, sign, selector, branch target, caller, or malformed-but-tolerated Castle value. Opcode 73 has controlled public proof for active restricted-shop ranges under `FD-SCENARIO-006`. Opcode 90 mode 1 has action-slot regression proof under `FD-SCENARIO-007`. Opcode 7 now has public-VM and save-owned proof for source-authoritative `-1` Simple, `-2` Complex, and nonnegative Action Point replacement; unknown negative imports fail atomically. Opcode 13 has public-runtime proof for combined single-plus-inclusive-range mutation, signed land/dungeon selection, silent unplaced-row handling, canonical disablement, save restoration, zero RNG, and atomic invalid-map rejection. State and Progression Control-Flow Branch Family (opcodes 46, 72, 77, and 86) now has comprehensive public-VM variant proof under `FD-SCENARIO-011`: opcode 46 covers condition selectors 0 (unset), 1 (set), 2 (force), out-of-bounds safe completion (*Wrath* `xap:489` `[274, 274, 304, 30003, 420]`), destination modes 0 (XAP), -1 (dropout), 1 (Simple result), 2 (Complex result), 3 (Keep Codes), and signed GOSUB (*Wrath* `xap:377`); opcode 72 covers inclusive range all-set and fallthrough, inverted range (`first > last`) vacuous truth matching Castle `newland.c:2933`, destination modes 0..2, signed GOSUB, out-of-range quest rejection, and bundled representative *Wrath* `xap:898` `[16, 17, 0, 0, 899]`; opcode 77 covers threshold evaluation, dual-target and zero-target routing, destination modes 0..2, signed GOSUB (*Wrath* `xap:380` `[8, 3, 0, 0, 383]`, `xap:437`), and out-of-range quest rejection; opcode 86 covers test kinds 0..8 (caste, race, gender, boat [*Wrath* `xap:1997`], camp, caste class, race descriptors 1..32, total and selected level sums), negative selected-only expectations, target 0 fallthrough, destination modes 0..2, signed GOSUB (*Wrath* `xap:792` `[0, 2, 0, 796, 0]`), and *War* `xap:2815` `[1, 12, 0, 2816, 0]`; opcode 30 has reinforced five-value Extra Code validation rejecting truncated 4-value rows. Opcodes 21, 40, 76, 77, 86, and 87 branch destinations support Simple and Complex encounters matching Castle `forcebranch` and destination switches; opcode 40 mode 0 matches Castle `newland.c` by terminating the issuing script via `ScenarioVmDirective.finish_timeline()` without Keep Codes while modes 1..3 branch with signed GOSUB; opcode 33 supports test mode `-1` dropout to slot 7 and destination modes 0..3 with paid status preserved; shared branch destination mode -1 (opcodes 33, 38, 42, 58, 59) executes slot 7 via typed `ScenarioVmDirective.dropout()`, while destination mode 3 uses established Keep Codes (`ScenarioVmDirective.finish_timeline()` with `action_point_kept`) terminating the entire timeline rather than returning only the current frame; opcode 42 passes GOSUB on mode 1 and supports keep-codes (mode 2) and trigger erasure (mode -2); opcodes 84 and 98 remain preserved control markers without simulation mutation (registration intentionally excluded). Bundled opcode occurrences remain stored definitions rather than demonstrated ordinary routes. | A package can pass opcode identity readiness and still fail only when a rarer authored branch executes. |
| `CCG-018` | Scenario reachability | All 13 bundled archives pass strict loading, but most stored definitions and instructions are not tied to demonstrated ordinary routes. | Static presence overstates practical playability and cannot prioritize progression blockers accurately. |
| `CCG-019` | Scenario-owned spells | Classified and tracked via the authoritative machine-readable denominator (`tests/fixtures/oracle/classic-spell-signature-denominator.json`). An exhaustive inventory across the stock application library, all 13 bundled packages, and all raw third-party scenarios in `F:\Scenarios` discovered 508 unique behavior signatures: 495 implemented in Rebuilt, 12 not-applicable, 1 malformed-safe, and 0 pending runtime gaps. The seven raw third-party signatures identified in conversion-blocked third-party sources (specials 48, 60, 89; field death on touch; physical area/touch damage) were adjudicated against pinned Castle source: valid signatures are supported across their applicable casting and resolution owners (combat Identify for characters, opposed group SP Drain, camp touch death magic, and physical touch/area damage), while special 89 is an unassigned reserved opcode in Classic Realmz safely classified as not-applicable with an explicit typed diagnostic under reserved special rules. Compiler readiness remains separate from runtime capability. | 48 sources scanned (32 verified, 16 absent/inheriting stock). Authoritative denominator pins every source path, size, and SHA-256 hash. | All valid stock, bundled, and raw third-party scenario signatures execute across applicable casting contexts; the single malformed signature (target type 0 with nonzero size and non-summon special) receives an explicit typed diagnostic rather than undefined behavior. | The authoritative JSON denominator and verification runner (`verify_spell_signature_denominator.ps1`) pass with zero pending runtime gaps. Classifier acceptance is not labeled as verified campaign execution. |
| `CCG-020` | Scenario media | The complete package audit covers all 515 restored scenario CICNs. The exact-key lookup remains scenario-first, but Half Truth Battle 52 exposed a live presenter gap: the Runic Cheiroballista pair (`cicn` 513/821) was present and decodable while the detached facing state could remain stale after vertical movement or a melee target change. The presenter now retains Castle's `lr` states, updates them at movement and melee playback boundaries, synchronizes new monster identities on every committed view, and reports the requested state, owner, hash, and fallback. Controlled coverage loads the pinned Half Truth archive and resolves the scenario-owned right-facing 821. Other monsters, animation stages, crops, text encoding, and ordinary-route battle display remain broader gaps. | A scenario may run while showing stock fallback, stale orientation, malformed crops, or unreadable text. |
| `CCG-021` | Failure presentation | Package installation and session-start failures now retain the complete host message, typed error code, exact affected package path, and operation identity after the worker result is claimed. The campaign selector offers same-package Try Again and nonmutating Choose Another actions. Runtime scenario-operation failures already preserve transaction rollback and the typed `SessionStep` detail, but ordinary third-party failure routes still need gameplay replay. | Package/content rejection is now actionable at the front door; broader ordinary-route proof remains separate from this controlled host/UI closure. |
| `CCG-022` | Platform proof | Windows and Linux have native evidence; macOS remains unexecuted on an Apple runner. | Cross-platform compatibility remains incomplete even when simulation is identical. |
| `CCG-024` | Third-party monster records | Dagger of Shine's normal monster 141 stores physical and magical attack counts of seven in all three difficulty files, although the Classic record contains only five attack rows. The authored chain is statically reachable: land level 3 AP 33 at `(56,46)` calls XAP 168, opcode 56 starts battle 205, and battle slot 84 places monster 141. Providence therefore rejects the package before Rebuilt can load it. | Castle declares `attacks[5][4]` but iterates to `noofattacks + bonusattack`; `attack2` indexes the supplied attack number directly. Counts six and seven consequently read adjacent non-attack fields. This is a demonstrated unsafe source path, not evidence that clamping, fabricating two rows, or reproducing out-of-bounds reads is correct. A controlled Castle execution and a coordinated Providence representation decision are required before runtime implementation. Ordinary campaign reachability beyond the authored AP graph remains unproven. |
| `CCG-025` | Third-party message references | Hax XAP 567 starts surprise battle 128 through Extra Code row 2038, whose exact fourth word is pre-battle message 3001. Its complete `Data SD2` contains only message IDs 0–1080. | Castle's textbox loader initializes an application fallback string, seeks beyond EOF, ignores the failed read, and displays the fallback. Providence correctly blocks rather than fabricating scenario message 3001. An authoring-source correction or explicit malformed-input fidelity decision is required; static XAP reachability is not ordinary-route proof. |

## Reported defects awaiting closure or renewed reproduction

- Skip-mode audio/result convergence and physical listening of the final Combat-to-Treasure music boundary remain presentation acceptance work; the current rendered ordinary path and controlled Normal/Hurry/reduced-motion paths no longer reproduce delayed fields, zero lethal damage, early removal, or pre-settlement Treasure presentation.
- The earlier Half Truth Wrecker fallback was closed for its pinned base-facing render, but Battle 52 reproduced a separate orientation failure with the Runic Cheiroballista. Its package pair was intact; the failure was stale presenter `lr` state rather than compiler loss or resource-chain lookup. The narrow repair now has a package-backed presentation regression selecting 821 after a neutral move and rightward melee target. Ordinary replay of Battle 52 and an all-monster orientation sweep remain open before this broader media gap is closed.

Some of these areas have received repairs. They stay listed until the repaired build reproduces the original route successfully and the retained trace proves committed and presented state agree.

## Latest repair-batch closeout

The current Half Truth replay closes the reported David/Party Auto incident for now. The exact v5 Battle 70 route reached Treasure, and the same party crossed five complete rendered rounds in Battle 34 before a mixed manual/Auto configuration returned to David in round six. No stall, failed revision, controller suspension, or lost continuation appeared; the expanded observation contract distinguishes committed, presented, drawn, deferred, playback-frame, and transport revisions. Reopen the incident if it recurs with a retained checkpoint and observation trace. No campaign status is promoted, and no save, Character Files, settings, package, rules, or gameplay operation changed.

## Prioritization

1. **Progression and deterministic-state blockers:** `CCG-004` and any package compiler loss.
2. **Common authored mechanics:** `CCG-005`, `CCG-006`, and `CCG-010`, followed by ordinary replay of `CCG-001`, `CCG-003`, and the corrected `CCG-007` area-reflection route.
3. **Misleading or inaccessible presentation:** `CCG-012`, ordinary replay of `CCG-015`, combat chronology, controller reachability, and media orientation.
4. **Rare signatures and fidelity edges:** `CCG-008`, `CCG-009`, `CCG-011`, and ordinary replay of `CCG-013`.
5. **Certification breadth:** `CCG-016` through `CCG-022` after the blocking mechanics have executable representatives.

## Closure workflow

For each gap:

1. Record a normalized definition or instruction signature without copying third-party scenario content.
2. Separate Providence preservation, Rebuilt package readiness, controlled runtime execution, ordinary-route reachability, presentation, save/restore, and Castle comparison.
3. Reproduce the smallest representative through a public gameplay owner. Do not close a gap from private-handler coverage alone.
4. Implement through the existing typed operation; do not introduce a compatibility mode or a second gameplay path.
5. Add the missing variant and owned gap to `classic-application-workflow-inventory.json`, regenerate the workflow status, and close the register entry only when its required evidence exists.
6. Rank the next scenario by new executable feature coverage after reported progression failures are addressed.
