# Test and oracle evidence contract

## Purpose

Own deterministic tests, synthetic fixtures, oracle provenance, route evidence, and clear labels for what each observation proves.

## Ownership

- Unit, contract, integration, oracle, MCP/runtime, and certified-route test lanes.
- Synthetic Providence-authored fixtures and golden traces.
- Fixture manifests with source commit, function/range, inputs, RNG, hashes, and observations.

## Local Contracts

- Label evidence as `source-control-flow`, `castle-runtime`, `runtime-unit`, `runtime-integration`, or `live-route`.
- Never treat source reading as runtime proof or a parser round trip as independent layout proof.
- Castle fixtures use pinned commit `491816ad60037394f92c428e99c004494d3c28b3` and synthetic data.
- Do not commit extracted commercial campaign payloads, user saves, generated Castle installations, or copyrighted media.
- Tests requiring identical branches use injected scripted RNG/input.
- `tests/fixtures/packages/fixture-provenance.json` owns the hashes and Providence commit for both the positive `.realmz2` fixture and its valid-ZIP stale-hash negative derivative. The current fixture contains two 90×90 land maps, one 90×90 packed-source dungeon map, explicit topology features and Layout transitions, complete 401-entry land and 201-entry dungeon battle-terrain sets with deliberately distinct tile-200 source values, the complete shared PICT 302 battle atlas, three random rectangles covering ordinary checks, a surprise battle, fixed six-item and ten-spell monster slots, all forty monster starting conditions with one permanent synthetic reflection value, thirty one-based race/caste profiles with aging, authored ability rows, and victory thresholds, a one-shot XAP, an AP-to-AP post-action relocation pair, direct Complex/Thief/Timed Encounters, an authored Data OD option-label pair, 799 bundled Classic items plus scenario supply/custom items, 420 normalized standard spells plus one packed scenario spell, five route/map media assets, complete 120-portrait and 120-tactical character catalogs, and an AP to Simple Encounter to result to Scenario Action to XAP/CODE 111 route.
- `tests/fixtures/oracle/aogm-active-opcode-inventory.json` is a bounded content inventory only. Its source hash, counts, and normalized opcode set may prove compiler/runtime readiness coverage; it does not contain commercial records and makes no reachability or live-route claim.
- `tests/fixtures/oracle/classic-functional-differential.json` is the machine-readable rolling gate for Classic-visible behavior. Each case identifies the Remake donor behavior, Castle authority, Providence projection, 2.0 owner, expected trace, decision, and tests without embedding commercial payloads.
- `tests/fixtures/oracle/classic-application-workflow-inventory.json` is the authoritative application-completeness denominator. It maps every audited Castle application entrypoint, Realmz 2.0 route, intent, interaction, application-transition opcode, and differential case to stable Classic or host workflows. Workflow delivery state is derived by tooling; do not hand-author a second status table.
- `aogm-ordinary` records only the variants directly observed through ordinary UI. A workflow may retain that label while unresolved variants, presentation acceptance, or other-campaign proof remain gaps; the label never implies certification by itself.
- `tools/route_acceptance.gd` is a generic local harness. Certified route JSON, campaign packages, and emitted reports stay outside this repository when they contain or identify commercial campaign content.
- Direct ED3/XAP route checkpoints may name a compiled program explicitly; this proves that macro's ordinary VM behavior, not map reachability for an unplaced record.
- Verification follows the delivery tier: Tier 1 runs only the affected suite or suites; Tier 2 adds the applicable architecture, differential, inventory, scope, and DOX checks; Tier 3 closes the workflow batch with the complete local gate and its required evidence. Do not widen a focused test run to unrelated routes without a Tier 3 reason.
- Post-battle tests preserve Castle's distinction between consumed held-over allies and the subsequent `bodycount()` survivor selection; route defaults must use the request's source-backed preselection rather than silently retaining all friendlies.
- Presentation tests compare 2D topology facts with the 3D geometry projection. Local MCP screenshots are visual evidence only and remain outside the repository.
- The Classic UI fixture gallery covers every route and interaction kind in nominal, empty, loading, error, unavailable, oversized, missing-media, unidentified, and six-member states. It verifies profile boundaries, scale migration, safe hidden-item display, explicit availability, and request identity; screenshots remain local visual evidence.
- Exploration integration tests require detached movement cues and minimap coordinates to agree with the same secret/door/world overlays used by simulation. They cover eight-direction land movement, diagonal save state, and cardinal-only dungeon movement; presentation tests retain native 32-pixel land cells and Classic mouse/keypad direction translation.
- Scenario and exploration tests preserve opcode 1's signed textbox behavior: positive message IDs pause at a serializable acknowledgement request, negative IDs continue, and later AP/XAP actions cannot execute before acknowledgement.
- Package-media tests preserve exact Classic type/ID identity, including case and trailing spaces, same-number cross-type collisions, ambiguous-key rejection, effective package hashes, and presentation decoder diagnostics.
- Package-host tests distinguish manifest-only discovery from full selected-package validation, exercise detached worker progress and exact typed failure handoff, and require every started Thread to be joined without teardown diagnostics. Cancellation is deterministic at the repository seam; timing-sensitive mid-file cancellation is not claimed by the synthetic fixture.
- Exploration tests preserve Castle's placed-AP lifecycle across direct and resumed timelines: ordinary APs become one-shot, opcode 24 Keep Codes APs remain active, and disabled state survives save/reload.
- Duplicate-coordinate AP fixtures deliberately reverse package references and prove selection by native record index, including success, chance miss, percent-zero, and restored-disabled paths without later-record fallthrough.
- War in the Sword Lands certification uses an external route, package, and report. Only package/route hashes, stage names, and resulting observations may enter documentation.
- Schema-v2 fixture tests cover campaign display metadata, contact shape, restrictions, race/caste eligibility, complete race aging/ability tables, caste victory thresholds, and malformed progression lengths. Character-vault tests cover immutable revisions, typed readback, archive, backup/atomic replacement, target-campaign eligibility, dotted stable IDs, traversal rejection, and complete ability/two-hand round trips.
- Campaign-startup integration tests begin from an empty party, preserve setup across repeated typed edits and save/restore, and prove that only explicit Begin enables gameplay. Gameplay fixtures import a deterministic synthetic member and Begin through the public session API without consuming RNG.
- Party Order tests require an exact complete stable-ID permutation, unchanged character-owned state, unchanged RNG, event provenance, duplicate/unknown/solo transactionality, and save/restore of array order. Presentation tests stage moves without an intent, emit one permutation on Apply, make Cancel mutation-free under `FD-CHARACTER-003`, and expose core-owned unavailable reasons.
- Character inspection tests require named detached Castle metrics, all five derived age bands without a state write under `FD-CHARACTER-004`, explicit lifetime-prestige unavailability, and presentation-only character/tab selection. Unknown native STR# labels remain visibly generic and cannot be guessed by fixtures.
- Character-rule tests distinguish racial starting-condition values, permanent caste condition-level `1`, reached higher-level caste thresholds, bounded initial saves, cumulative creation aging, Castle's discarded seventh attribute draw, conditional special-bonus draws, target-minus-one prestige, caste victory thresholds, complete level-up ordering, separate racial/trained ability arrays, `FD-CHARACTER-001`, and the resulting RNG order; keep those assertions tied to `newcharacter.c`, `levelup.c`, `updatespec.c`, `age.c`, and the serialized race/caste records.
- Character-creation integration tests preserve the generated Review boundary, reroll/cancel RNG ownership, finalization without a second roll, fixed source-backed starting levels, post-spell identified starting-item grant with authored charges and package-derived load, Castle's starting-spell point formula and seven costs, the first twelve `cspells` slots per level, saveable spell edits, saveable unused-point confirmation, saveable exact portrait/tactical IDs, and saveable explicit vault-publication confirmation. Presentation tests keep one creator page mounted, Race left of filtered Class, race-recommended appearance choices first, and starting levels filtered only by the campaign maximum. Vault tests cover immutable history, exact identity validation, wrong-role appearance rejection, target-package load normalization, archive, and exact recovery.
- Carried-item integration tests cover source-probed race/class admission, slots, rings, hands, quiver dependency, curse identification/removal lock, exact-instance trade and load transfer, complete wealth-and-item load derivation, tampered-save load rejection, equipped ordinary transfer as unequipped, `FD-INVENTORY-001` equipped-curse rejection, and saveable accept/decline drop confirmation. Charged spell-item tests cover field self/party targeting, staged `Rand(7)` power, exact target-selection restoration, stale response and forged fixed-power continuation rejection, unidentified-name safety, positive/infinite/depleted charges, per-charge load, item sound, combat actor and automatic-group targets, no spell-point cost, action cost, host request/response identity, and presentation payloads. Door/XAP items, random-power combat, spatial/repeated combat item targets, broader specials, field identification, unsupported passive effects, and split/join remain explicit; tests must not normalize Castle's contradictory split/join load behavior or target-abort charge loss by assumption, or reinterpret opcode 36 as a player stash.
- Field spell, Camp/Rest, timed-midnight, and scroll-case tests preserve signed fixed-power records, target/cost/RNG transactionality, field saves/protection without combat magic resistance, Camp entry/exit scaling, service clearing, one-pulse held Rest, hourly poison damage and positive-condition decay before noon/midnight recovery, one-point fatigue gain, generic-plus-specific Torch Lit decay, exact ration use, timed eligibility before one corrected midnight recovery, exact item/quest/level/x/y gates, staged timed AP save/resume, random interruption, land/dungeon departure timing, saveable battle return before movement, five fixed scroll slots, twice-cost scribing, item-806 charge/load consumption, no-cost field scroll use, `FD-EXPLORATION-001`, `FD-EXPLORATION-002`, `FD-SPELL-001`, and `FD-SPELL-002`. Timed AP relocation/destination behavior, combat scrolls, invalid-field discard, and case transfer remain explicit; tests may not infer them from the verified subset.
- Movement tests treat the packaged cell cost as a Classic timeclick count: land applies five-minute clicks and dungeon applies one-minute clicks through the hourly rule path. Blocked-attempt tests retain the attempted topology cell, charge its positive land cost without moving, consume downstream current-cell random checks in source order, and preserve ordinary solid dungeon walls as zero-time rejection. `FD-EXPLORATION-001` owns the corrected timed-location coordinate policy. Do not generalize the bounded movement assertions to zero/negative land cells, boat/shore cancellation, or special wall bits without controlled Castle evidence.
- Shop tests cover identified purchases, half/charge/inflation/unidentified sale valuation, equipped-item rejection, fixed-cost identification, base and normalized buyback stock, and save/restore. Native empty-slot ordering remains labelled unresolved until the compiler preserves that provenance.
- Temple tests cover all nine fixed services, signed percentage costs, healing exclusions, direct condition cures, forced curse unequip, Revive's anomalous cleanup, pool-first payment, three-denomination Pool/Share/bank transfer, contextual entry, nested interaction save/restore, and detached presentation affordability. Global macro 5 remains outside that claim; manual Swap and movement-time pooled-wealth handling belong to their separate economy workflow evidence.
- Ordinary money tests cover three-denomination Pool, source-ordered Share, exact Swap increments, load and movement recalculation, transfer sounds, typed detached availability, presentation-only open/Done sounds without duplicate rerender audio, typed-service suppression, and whole-session save restoration. They also cover no-bank departure before blocked/open movement, question/open/Done sounds, decline-and-continue, accept-and-continue, bank-before-block ordering, camp departure, stale-response rejection, exact transfer rejection, and save/restore at warning, distribution, empty-pool, and post-mutation boundaries. `money-share-capacity-correction.json` records Castle's jewelry overload separately from `FD-ECONOMY-003`; `classic-pooled-wealth-departure-source-observation.json` records the pinned ordinary sequence, distinct internal fallback, and unknown prompt text. The bounded positive-tile blocked land cost is source-backed; zero/negative tiles, boat/shore cancellation, and special dungeon walls remain explicit gaps.
- Bank tests cover opcode-49 contextual availability, one-time import of all three denominations, Pool/Share/exact Swap, arbitrary-increment rejection, ordered button/quiet/modal-open and Done sounds, save restoration without a second drain, Done without re-banking, movement-time return, and bank-backed shop entry/exit. `classic-bank-swap-source-observation.json` records the pinned source sequence; ordinary campaign proof remains distinct.
- Presentation route tests require a newly mounted workspace to reset focus-driven scroll to the top and same-route mutations to preserve it; MCP verification must confirm the Money heading and primary Pool/Share controls remain visible after leaving setup and after Pool/Share rerenders.
- Tactical presentation tests require the native 16×14 actor-centered camera, active-landlook/PICT-302 composition, stable combatant identities, rules-owned movement outlines, and a compact command region that leaves the board visible. Local gallery captures remain visual evidence and cannot upgrade the workflow to accepted or ordinary-play proof.
- Live-aging tests keep `ageDays` and `ageGroup` distinct, cover one-row forward/reverse transitions, unbounded live stat/save changes, movement-floor reversal, midnight progression, haste/youth effects, maximum-age experience truncation, and legacy nested-character restore. Separate interaction tests require ordered `age_update` requests, sound 3002, exact response kinds, VM and session save/resume, and age acknowledgement before destination AP execution.
- Combat cadence tests prove integer half-attack carry across activations, two-unit melee cost, repeated same-actor actions, one target draw per monster physical sequence, ordered authored rows, active-row serialization, and continuation of remaining rows after a saveable monster age update. Do not use Remake's floating `MaxActions` result as a Castle oracle.
- Combat spell tests distinguish the spell ID's zero-based tier from caster level, persist per-activation cast count and same-round attacked state, reject illegal casts before RNG or mutation, preserve carried half-attacks, and trace `savevs` through its post-draw cannot gate. Monster package tests require ten fixed spell positions; an empty slot is authored selection state, not an absent list element. The package boundary may normalize only an entirely empty legacy list to ten empty slots because no spell can be selected from either representation.
- Monster-aging attack tests preserve Castle's hit/damage/potency/save draw order, save slot seven, truncated attack-row lifespan formula, party-only target, ordinary physical damage, one-band update, and exact direct-session and scenario-combat continuation after save/restore.
- Monster-status attack tests preserve all eight special/save/condition identities, potency-before-save ordering, save-before-permanent ordering, the party starting-duration gate and over-30 result, uncapped monster stacking, monster immunity/undead/averaged-save branches, target-specific sounds, resistance-whiff timing, ordered domain events, and committed-state restoration.
- Monster-resource attack tests distinguish Classic's direct experience from Remake's experience-to-next-level adapter, preserve save and potency ordering, cover empty and saved balances, above-maximum spell transfer, the party-only experience branch, asynchronous sound 630, and whole-session restoration of both mutated combatants.
- Monster-charm tests cover save zero and party resistance, undead immunity, attacker-allegiance adoption, automatic opposed-side targeting, loyal attacks against charmed characters, mid-battle persistence, outcome participation, and battle-end restoration.
- Monster-elemental tests cover all five save/protection identities and draw order. The synthetic `monster-elemental-protection-correction.json` fixture records Castle's monster-target display/commit anomaly separately from the `FD-COMBAT-001` chosen result; it is source-control-flow evidence, not Castle-runtime evidence.
- Character-melee tests identify the actual equipped type-2 weapon separately from its damage bonus, retain nonweapon equipped bonuses during unarmed attacks, preserve signed effective-luck draws and weapon physical/elemental/critical order, and distinguish monster weapon gates from battle placement distance. `character-weapon-elemental-save-correction.json` records `FD-COMBAT-002`; `character-required-weapon-correction.json` records Castle's impossible minus-1024 specific-item comparison and unarmed family bypass separately from `FD-COMBAT-003`. Both are source-control-flow evidence, not Castle-runtime evidence.
- Ordinary-monster-melee tests run the fumble-disabled source path and preserve defender luck before hit, effective equipped armor, day scaling, damage-plus accuracy, evil-conditional protection, the ten-percent floor, weapon/unarmed damage order, Dragon Hide reduction/sound, and monster weapon gates. The age-special fixture saves and restores the following weapon condition and Dragon Hide feedback before committing deferred damage exactly once. `monster-negative-damage-correction.json` records Castle's negative-damage healing observation separately from the `FD-COMBAT-004` zero floor; it is source-control-flow evidence, not Castle-runtime evidence.
- Package tests reject a random monster-weapon table unless immutable content contains every item that the rules-owned table can generate; an unresolved generated weapon may never fall back to unarmed combat.
- Permanent-affliction tests cover blindness and petrification saves, target-specific sentinels, resistance whiffs, event order, and petrification's skipped physical commit.
- Fumble tests preserve character and monster draw ranges/order, unarmed draws, cursed/full-queue suppression, character synchronous 10121/10123 then asynchronous drop 655, monster synchronous 10121/655, exact battle-owned item state, natural monster disarm without authored-loot deletion, and opcode 122's party-only physical guard. Post-battle tests enforce body count before one-item recovery, defeat/retreat recovery without a fabricated survivor restriction, recipient capacity/load, exact-charge `FD-COMBAT-005` behavior, and save/resume through direct and VM continuations. Castle's allow-fumbles preference/XP coupling and live `inspell` guard remain labelled unresolved rather than receiving fixture assumptions.
- Reward tests cover one-time roll/commit, exact pending instances, zero and exact-maximum capacity under `FD-ECONOMY-002`, rejected-response transactionality, Pool/Share/Swap denominations, Detect/Identify costs and knowledge, canonical JSON, one level per reward close, level/spell request restoration, terminal victory exact-once completion, and defeat without reward draws. Battle modes 5/10, opcode 48 bonus treasure, random parchment/ration checks, and post-level-30 thresholds remain explicit gaps.
- Weapon-mode and projectile tests preserve Castle's melee-first/type-15-only fallback, no-cost toggle, hand-to-hand return, unique type-2/type-10/type-15 slot validation, whole-state restoration, detached-view/request parity, exact presenter payload, integer range/LOS, charge/action/movement costs, repeated-hit damage/dodge order, projectile shield, player missile bonus, monster range-power split, exact native slot one, Guard clearing, and actor-owned slot-zero melee replacement. Unsupported projectile classes remain explicitly unavailable; a monster missile decision may never call or label an ordinary melee row. Tactical evidence explicitly records that positive `behind` is withdrawal, not icon facing.
- Battlefield tests preserve Castle's 30×30-to-90×90 expansion, live terrain-overlay input, dungeon decoration draw count, apparent top-neighbor path branch, direction/separation ordering, party-before-allies-before-row-major-authored-monsters placement, full footprints, distinct authored-battle monster construction, dead-party cleanup timing, initiative boundary, detached view, and save round trip. `battlefield-placement-termination-correction.json` records `FD-COMBAT-006`; impossible placement must return a stable error and restore complete game and RNG state instead of hanging.
- Tactical combat tests use explicit battlefield placement. They cover eight-direction one-step probes, complete large footprints, maximum terrain cost, collision-initiated melee without movement, three-movement/two-half-attack cost, Guard-before-contact ordering, collision save/resume after a blocking age update, manual missile-to-melee mode enforcement, rejected-action transactionality, movement save/restore, monster action-draw fallthrough, retained and Classic-slot target selection, fixed 128-sample LOS, bounded `shift.c` retries, repeated monster movement into adjacency, and source-legal placement in scenario continuations. Guard/withdrawal tests cover Castle slot order, pre/post movement Guard, same-enemy double reaction, withdrawal-only plus twenty, Invisible suppression, Helpless retention, exact 49/51 Guard sound split, Finish clearing, monster contact, fixed-origin hostility, age-update save/resume, and death-macro revival without cursor replay. Retreat tests cover exact range ten, every explicit-command gate, per-character removal/penalty, edge prompting/forcing through the typed host, explicit escaped-ID restoration, accepted continuation from a restored Classic VM frame while another loyal actor remains, routed-monster withdrawal, hostile edge flight, direct/VM confirmation restoration, and explicit no-target/mandatory-ally boundaries. `death-macro-battlefield-occupancy-correction.json` records `FD-COMBAT-007`; `tactical-los-delay-correction.json` records `FD-COMBAT-008`; `monster-target-scan-correction.json` records `FD-COMBAT-009`; and `monster-projectile-weapon-replacement-correction.json` records `FD-COMBAT-010`. Castle's optional Auto Weapon Switch collision behavior, Permanent-Tangle movement, and pre-direction movement substate remain explicitly unresolved.
- Combat-spell tests cover mutation-free player picker parity, fixed monster slot retries, separate range and affordability power, native target-slot draws, Guard clearing, SP/damage mutation, charm-save continuation, cannot ordering, active-turn persistence, and typed presentation payloads. Broader or anomalous spell branches must remain explicit rather than borrowing ordinary-path expectations.
- Special-57 combat-healing tests cover party-then-monster player actor options, same-allegiance monster selection including self, reflection/resistance ordering, capped party healing, uncapped signed monster healing, the healed-monster attacked marker, separate damage/healing facts, and save restoration of over-maximum current health. Other specials and camp healing remain separate cases.
- Spell-death-macro tests cover source-ordered queue insertion and draining, original-caster retention, exact completion cursors, stale-cursor rejection, per-subject occupancy cleanup, nested typed interaction, whole-session restore, and the distinction between host completion policy and opcode 119's authored allegiance reset. Group spell tests must prove one shared cost/duration/base-damage roll before per-target defenses rather than looping the single-target cast path.
- Player automatic-group spell tests require a targetless typed option, party-then-monster target order, one cost and shared duration/base-damage rolls, per-target defenses, and ordered death-macro queue insertion. Monster target types 9, 10, and 12 must remain explicitly unavailable while Castle's stale-power and missing-resource branch is unresolved.
- Player spatial-spell tests preserve all eighteen source-ordered Data AD masks and their unusual hollow/asymmetric forms, distinguish fixed type 3 from power-sized type 4, require one typed center, enforce range/LOS and the bounded-field safety limit before mutation, commit shared cost/rolls even on empty ground, deduplicate footprints, exclude over-100 monster resistance during targeting, share cost/duration/base damage, and retain party-then-monster death-macro order. Reflection, authored rotation, and queued fields remain explicit; queued fields are rejected for both player and monster casting.
- Repeated-spell tests preserve ordered distinct player selections, self and mixed-allegiance candidates, early full-power payment, per-target duration/damage rerolls, over-100 pre-resolution draws, typed payload order, and monster target-power and partial-cast behavior. Ordinary reflection tests cover success/failure boundaries, selected-versus-effective target identity, per-selection RNG order, monster-caster redirection, native opposed-monster slots, post-death completion of the authored monster magic-attack count, duplicate record-based caster macros, retained occupancy through the final duplicate, the safe 100-record queue bound, and saveable monster spell death macros. `monster-spell-target-retry-scope-correction.json` records `FD-COMBAT-011`; tests prove a cast succeeding on its hundredth sample cannot suppress the next cast, while one genuinely exhausted cast can still resolve partially at full chosen-power cost. Nonzero-size open-space placement remains explicit.

## Work Guidance

- Keep failures deterministic and include stable IDs and draw indices in diagnostics.
- Add save/reload coverage at every interaction boundary.
- Active-character appearance tests cover complete detached catalogs, independent role mutation, wrong-role rejection, RNG/time stability, canonical save restoration, corrupt-role transactional rejection, and explicit vault non-ownership.
- Player-map characterization distinguishes `Data MD2` record identity from playable map topology. Until the package contract lands, opcode 29 must reject without mutating acquired-map state; tests may not bless the previous land-level substitution.
- Report the exact suite fragments used for focused verification. A passing Luna result is input to Sol's review, not an audit-status upgrade; unresolved or ambiguous Castle behavior remains explicit until Sol adjudicates it.
- When test categories, fixture provenance, evidence labels, or copyright boundaries change, update this contract in the same documentation pass. Do not hand-edit the generated workflow status report or codemap.

## Verification

- `godot --headless --path . --script res://tests/test_runner.gd` runs the complete typed GDScript suite; repeated `-- --suite <fragment> --suite <fragment>` arguments select the union of matching suites for Tier 1 or Tier 2.
- A passing full-suite process must also exit without ObjectDB leak or retained-resource diagnostics; `tools/verify.ps1` treats either teardown warning as a failure.
- `tools/verify.ps1` runs import/script checks, tests, architecture guards, and `git diff --check`.
- Package contract coverage verifies that multiple immutable files for one campaign collapse to one current selector entry without deleting revisions.
- Save-preview coverage indexes current and backup records independently, exposes corrupt and package-mismatched records without enabling them, and proves that loading a backup returns the exact prior committed boundary.

## Child DOX Index

- `presentation/AGENTS.md` owns UI fixture/gallery coverage, responsive profile assertions, and local screenshot evidence boundaries.
- Fixture subtrees otherwise inherit this contract.
