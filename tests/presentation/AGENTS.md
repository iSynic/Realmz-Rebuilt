# Purpose

- Own deterministic tests and typed fixtures for presentation-only behavior.

# Ownership

- Canonical 1280x720 and optional Classic 800x600 profile composition, bounded 16:9 Fit geometry at representative HD/4K/ultrawide sizes, independently snapped legacy bitmap scale, route scenes, full-stage overlay and spatial-renderer suppression, exact full-height roster-replacement field and standard-controller Encounter spell workspaces with bounded scrolling, full-application standard-controller Encounter item selection with exact owner/instance return, Castle-shaped idle Encounter docking plus roster-replacement Action choices, fixed Done/local Stop, result-message interstitial removal, and continuing-dock restoration, content-sized map-stage Skills with a centered previous/portrait/next cluster, transparent Encounter input shielding, application-centered lifecycle/reward modal ownership and Escape dismissal, roster-owned mandatory character selection with an initial readable countdown cursor and no duplicate picker, nested Treasure confirmation, pointer hit-test ownership, land mouse/keypad diagonals, exact built-in mounted/camp/aboard Classic party-marker provenance, unadorned marker presentation, Castle's shade-and-skull dead portrait composition, explicitly framed title-free exploration map and roster, optional-off non-LOS traveled-area preview plus mandatory LOS black surround with dimmed exact sight memory and no sealed-room disclosure, seamless/non-stretched stone surfaces and picture-bevel backing without package media labels, stable menu-state text metrics, helper-free edge-to-edge narrative replacement, exact packaged signed opcode-62 `TEXT` resolution over native-scale tiled application `ppat` 129 with complete-stage expansion and real automatic viewport movement, floating AP choice geometry, transient native-size save/journal glyph composition, default-off Auto Note plus manual `N` acknowledgement, inset age-update composition, asset/font provenance, focus/navigation, exact scenario-before-application media resolution, safe detached display, interaction identity and Classic choice context, fixed maximized-map camera geometry, detached save-owned dungeon heading/view policy, stable square donor/user-surface UV selection, singular cell-oriented door projection on the party-facing entry plane, camera-relative yellow-sand-balanced scalar distance/darkness shading, broad sand-brick archway crops, Castle 3×3 walked-path discovery, inward flat room normals, destination-directed boundary equivalence, retained world-coordinate first-person dungeon geometry with entering-cell patches, exploration-speed-scaled camera transitions, Space/Wizard's Eye view policy, relative forward/reverse/turn mapping, direct-text debug warp input, typed exploration light facts, native tactical turn-centering plus edge-triggered movement recentering, stable action-actor focus across actorless playback frames, atlas composition, battlefield-owned actor/sequence/area targeting with progressive setup/confirmation visibility, ordered combat playback, synchronous-audio visual blocking, textured combat command region distinct from the opaque narrative textbox, fixed combat command slots with icon-backed current/upcoming initiative inside the default combat region, permanently reachable spellbook actions, and 2D/3D projection tests.
- `ClassicUiFixtureGallery` supplies nominal, empty, loading, error, unavailable, oversized, missing-media, unidentified, and six-member states for all ten routes and all seventeen typed interaction families, including locked surviving-ally selection, source-framed pick lock, a legacy exact-charge fumble adapter, the ordinary combined treasure field and nested completion warning, capacity blockers, Theldrow detection/identification caster selectors, hollow unidentified-item naming, a populated six-shopper two-ledger Shop with exact portraits and source-category filters, locked level results, pressed Theldrow long spell-selection lists, spatial eight-direction combat input with an on-demand cost overlay, and host-owned End Adventure choices. The runtime never emits the empty ally-selection fixture during normal battle return.
- Inventory presentation coverage keeps Castle's visibly sized exact-CICN carried-item field, detached Damage/Armor line facts, selected-character command rail, integrated six-member selector, full-width exact-item record, fixed lower-right Done, action dock, one stable exact-item operation stage, and item-local portrait Trade recipients separate; it verifies detached enabled/disabled actions, conservative operation context, source-projected recipient load, explicit confirm/cancel and Transfer/Cancel, and rejects an ordinary Store command.
- Party Wealth coverage owns one public controller proof for the shared-inset pool summary, slate-readable adventurer ledger, exact application-owned gold/gem/jewelry CICNs, selected Exchange composition, all three exact-increment transfers, core-owned blockers, and typed intent emission. Wide and Classic gallery states cover the deliberate character-selection reflow; do not add per-denomination presentation tests.
- Allies coverage owns populated wide and compact captures plus the empty read-only workspace invariant, including separate list/detail panes, profile-specific composition, exact-CICN affordance, and detached current-state facts. Bestiary coverage is deferred until the package catalog contract is corrected; tests must not fabricate discovery semantics.
- Shop component coverage includes native category-art binding, left-pack/right-stock ordering, independent right-shopper selection plus Shop Keeper restoration, the wide nine-row and compact five-row ledger minima, fixed bottom-aligned control and description/stat strips at both supported profiles, equal footer regions that center the portrait matrix beneath the filter spine, disabled outer-page scrolling, detached load/item facts, square Shop Keeper/Items/Money stages, the pointer-transparent Alt-hover detail pane, unaffordable stock, unidentified names, paid identification, equipped-sale rejection, and buyback stock so service regressions remain visible without commercial campaign data.
- `capture_classic_ui_gallery.gd` writes representative local frames under ignored `artifacts/ui-gallery/`, including every route and interaction family plus the splash, fixed package-progress host, canonical and compact party setup, all five creator stages, exploration with an active native-pixel masked Search effect, acknowledgement and exact-label choice panes at both supported profiles, character, dense Inventory and its item-operation and item-local Trade stages, Party Wealth, roster-replacement field spellbook and its saveable numbered Party-roster target stage, narrative, standard-controller Encounter spell selection plus modal word/item and thief pickers, source-framed Pick Lock, exact-media Age Update, Treasure plus its actual Done-to-retained-workspace completion-modal handoff, full-height level spell learning with shared level filtering, locked Level and ally decisions, Shop, exact-icon combat records, separate Reveal Friends and Shift-aid legends, original-art combat spellbook actions, a source-shaped queued Plane of Force and rotatable board-native targeting at both supported profiles, battle entry, explicit Auto playback, retained-board terminal cues, populated Places/Player Maps/Journal, valid and corrupt save records, every preference tab, the complete Music playlist modal at both profiles, compact high-scale stress, End Adventure, compact Quit, selected-slot Save and Quit, and 1920×1080/3440×1440/3840×2160 Fit smoke frames; it is visual evidence, not a golden-image test or ordinary-play acceptance.

# Local Contracts

- Fixture facts are synthetic and cannot become gameplay or live-campaign evidence.
- Local screenshots may prove visual layout only and remain under ignored artifact storage.
- Battlefield presenter coverage proves that committed playback retires temporary inspection or targeting focus before consecutive action events return to the active actor.
- Exploration and Camp gallery frames cover the title-free icon-stage/live-caption footer compositor, exact PICT 180 Encounter source art, the project-owner-supplied transparent PICT 225 Shop-mark isolation, the project-owner-supplied native-pixel Heal glyph, persistent pressed Camp control, exact CICN 178 map marker, and light/fatigue/time facts at both supported profiles. Scene composition additionally owns Castle's visible 4–135 fatigue-gauge bounds and the canonical Party footer's left two-by-two command block beside its compact right four-by-two Effects bank. The deterministic package currently has no playable transition into its dungeon map, so a representative dungeon-stage capture remains an explicit gap rather than a fabricated detached map.
- Exploration geometry coverage mounts the real retained surface, requires its clipped SubViewport, Camera2D, base/six-feature/marker/fog TileMapLayer graph, proves camera movement preserves those same layer nodes, verifies darkness covers guard-cell margins while restoring only discovered runs beneath the 80-percent memory veil, verifies remembered LOS terrain has no extra veil while never-seen cells stay black, and verifies that found-secret and walked-path marker tiles discard their white transfer background before composition over authored terrain. `classic_ui_test_support.gd` owns reusable shell queries plus the player-map and scrollbar proofs inherited by the application-shell suite, keeping each suite below the permanent size cap without dropping assertions. Map/cell equivalence and old-snapshot immutability remain core/session integration proof; gallery captures remain visual evidence rather than pixel-golden tests.
- The capture gallery records Temple, Bank, and pooled-departure workspaces at both canonical 1280×720 and optional 800×600. These frames are visual evidence only; typed workflow tests remain the owning behavioral proof.
- Test navigation must not mutate a session. Automatic-route coverage proves that combat, services, and scenario/AP interactions replace unrelated browsing workspaces while interaction-free browsing remains presentation-owned. Pending mandatory responses disable manual route changes, and full-stage interaction fixtures assert an opaque, pointer-owning layer above nested router overlays. Locked-modal coverage reapplies the same request layout and proves each dim or transparent shield remains behind the presenter in GUI hit-test order.
- Music coverage owns schema migration, twenty-slot mode validation, stock-bank integrity, detached context precedence, separate effects/music channel volumes, modal input ownership, and responsive reachability. Gallery frames own appearance; ordinary campaign transitions remain manual acceptance rather than MCP replay.
- Workspace-audio coverage owns explicit Items/Spells entry, same-route suppression, automatic-route suppression, and the ordinary Swap lifecycle. Held-command coverage owns Castle's one-tick Rest/Area Search repeat interval, one sound per physical start, silence on repeat pulses, and no catch-up queue. Simulation-owned battle, Camp, Heal, and action cues remain public core/session workflow evidence.

# Work Guidance

- Cover canonical 1280x720 and optional Classic 800x600, including supported text/interface settings and exact integer-sampled original art. Representative 1920×1080, 2560×1440, 3440×1440, and 3840×2160 checks prove only the shared bounded-Fit policy; they are not separate authored compositions.

# Verification

- Run `godot --headless --path . --script res://tests/test_runner.gd` and `tools/verify.ps1`.
- Party Order presentation fixtures use the real Character route workspace. They prove local move/cancel staging, one typed Apply intent, and visible core-owned disabled reasons without creating test-only gameplay mutation paths.
- Character-sheet fixtures cover disabled/dead and long identity content, named conditions and eight saves, personal wealth, special modifiers and abilities, race/caste metadata, all five age bands, mutation-free `FD-CHARACTER-004` highlighting, detached lifetime counters and exact prestige, plus separate equipped/carried regions. Gallery captures own the three-region Overview, aligned/stacked Conditions & Saves, Equipment, separate Special Modifiers/Special Abilities, Race/Class/five-band Aging, known-spell/fixed-scroll-case, independent package-owned Appearance, and Lifetime Record compositions at 1280x720 and 800x600; tab, inspected-character selection, and appearance drafts remain presentation-only. Overview captures use `character-overview`, not the formerly misleading `character-record` filename.
- Party-setup inspection fixtures cover assembled members plus eligible and ineligible vault revisions, exact eligibility reasons, visible core-owned import failures while the shell status region is suppressed, the shared complete sheet, and mutation-free Back navigation.
- Party-setup route fixtures prove that Begin Adventure dismisses a setup-opened Vault, clears its setup-only return history, and enables Exploration without changing ordinary active-session route ownership.
- Inventory workspace fixtures cover identified and unidentified fact visibility, Castle's concealed/revealed curse decoy, source-backed detail fields, shared Alt-hover detail rendering, and inline disabled-action reasons. The public inventory-session proof owns Cast Identify's party-order caster, fixed cost, all-carried-item mutation, event order, and restore behavior instead of duplicating those rules in UI tests.
- Treasure fixtures cover the responsive white-field item grid, centered native-size item art with the exact blue-green glow only behind request-revealed magical items, recipient-first initial focus without a false first-item hover ring, detached item records, privacy-safe Alt-hover details, exact assignment identities, stable authored slots that become empty holes after assignment, source-cell pickup-ring ownership in a non-layout overlay, pooled wealth, distinct remaining-SP and lore-action cost text, completion-confirmation retention, and the save-v4 fumble adapter.
- Spell workspace fixtures own the wider persistent caster/level/list/record sidebar at both supported profiles, the vertical level rail, eleven-row dense same-level fixture, route-external fixed Cast dock, opcode-69 persistent warning plus visibly muted but browsable spell records, shared native-slate target badge across field/combat/mandatory selection, the standard controller's application-centered Encounter-selection lifecycle and exact typed owner/spell submission, ordinary exact typed caster/spell/power submission, and separate detached Use/Discard controls for a fixed scroll slot. Fast Spell coverage additionally owns immediate picker assignment, separate Clear, absence of a Set step, Alt-versus-route input separation, the exact eight-frame detached preview identity, bounded battle-canvas dock geometry, assigned-slot order even when every assignment is unavailable, request-owned disabled reasons, pointer activation, visible Cast/Cancel targeting from a dock-originated spell, `T` selection for combatant and area targets, Space input identity, and release dismissal. Synthetic gallery captures cover populated known spells, Fast Spell bindings, the five-slot scroll case, and right-sidebar field plus application-modal Encounter compositions; field effect, discard semantics, and target legality remain owned by public session proofs.
- Maps/Notes fixtures cover selectable saved location records, detached historical-map rendering with the saved exact zero-through-six darkness-mask identity, current-note editor isolation, tab retention across map selection, presentation-only journal text filtering, and the shared Rebuilt-font search field without mutating detached records. Acquired-map coverage additionally proves the single tiled-slate stage, inset corner title/zoom ownership, centered pre-map description, 12.5-percent repeated-texture parchment mat, Castle's fixed 20-by-20 16-pixel dungeon crop, independent authored marker scale, and exact PICT 302 wall, door, unmapped, and directional layer identities through public detached views. Scrolling-map coverage proves the shared opcode-62 surface, exact packaged `TEXT`, native tiled `ppat` 129 background, strict same-ID `styl` run decoding, 20-pixel-per-second automatic advance, and signed 25-pixel drag steps. System workspace fixtures cover Rebuilt-font Display pickers, detached current and backup save facts, distinct load operations, disabled corrupt-record reasons without reading host files, the complete fixed keyboard/mouse reference, live-labelled Pacing sliders, scroll-reachable wide/compact preference layouts through 150 percent text scale, schema migration, and source-classified Reduced Sound without private audio state access.
- Interaction fixtures keep opcode 26's saveable click request in the narrow half-height blocking modal above the narrative well, keep longer flash notifications independently readable, and prove global pointer acknowledgement independently from the ordinary narrative textbox; its stock sound event remains Scenario VM coverage.
- Battle interaction fixtures prove that the same read-only Items, Conditions, and Attacks surface opens for party and monster identities, defaults to Attacks, shows explicit empty sections, suppresses the command overview, and retains a visible Back route without emitting a combat response. They also prove that canonical commands remain 1×, live resize/reflow preserves the interaction while surplus-region View/Action/Tactics geometry and text reach the 2× cap together, large-monster playback preserves the footprint offset from its anchor and selects Castle's right-facing CICN, defeated-combatant masks begin on the lethal result and survive the defeat and next-turn cues, named spell playback retains source effect identities while removing only border-connected opaque white mattes, readable debug records identify movement/attacks/spells/rejected Auto choices, and lifecycle modals disable the unnecessary outer scrollbar.
- Startup fixtures prove one retained OGV preparation, active playback before menu reveal, temporary suspension without reopening, final release, and suppression of the hidden shell's duplicate decoder. Embedded video audio remains permanently silent, the independent MP3 alone follows click/master-volume state and stays demand-loaded, both new and saved adventures enter through scenario selection, package-bound saves open from the splash and party setup, and Back returns from pre-adventure saves to setup. Lifecycle fixtures distinguish the player-facing Main Menu/End Adventure path from process Quit, cover each field/combat/no-session option set, and drive the real application scene through menu Quit, injected save failure, `NOTIFICATION_WM_CLOSE_REQUEST`, and configured exact-once termination without closing the test process or treating the host prompt as gameplay state.
- Package-library fixtures distinguish manifest availability from selected full campaign metadata and cover the exact selected summary record, determinate worker progress in its fixed host, competing-action suppression, Cancel signaling, and terminal cleanup without opening archives from presentation. Creator coverage owns the full-stage/no-roster lifecycle, named Race/Caste filtering, explicitly labelled appearance rows, the shared complete Review sheet, and one starting-spell level/list/record/allowance invariant with description, exact animation-frame identity, and ordinary pressed toggles. Level-up coverage additionally owns application-height modal composition, the shared exact level rail, supplied application-description display and refresh, filtering across supplied spell levels, and preserved hidden-level selections; individual spell costs, eligibility, and explanatory helper copy remain public session or manual-acceptance concerns.
- Startup-shell fixtures cover the pre-session splash, one aligned and separately backed Scenarios/Character Files/Current Party row, the narrower Scenarios share, six stable pre-session slot controls retained through an insertion rerender, slot-to-list drag/drop forwarding with exact stable Add/drop identities, installation rather than Play behavior, absence of ordinary seed controls, full-stage Character Files entry without the persistent roster, and public return navigation. Helper wording remains gallery/manual acceptance under the regression-admission policy. Party-setup fixtures assert that the same three-pane workspace survives selected-package startup, keeps all six occupied party rows visible at canonical 1280x720, prepares one reusable drag cursor before activation while preserving exact Add/drop IDs, and leaves advanced revision/archive controls out of ordinary assembly; the 800x600 Classic composition may deliberately reflow or scroll. Shared-theme fixtures require native horizontal and vertical scrollbar end controls, useful custom steps, and arrow-region geometry used by held repeat without stealing the track. Surface fixtures require shared root slate through pre-session/setup overlays and opaque bevel corners; menu-state fixtures cover no-session and party-setup route suppression, disabled parent menus, and opaque popup borders.
- Appearance fixtures cover both exact package roles, recommendation-first creator thumbnail browsing, independent tactical override, presentation-only preview, one typed Apply request, no-op Discard, long labels, and visible core-owned unavailable reasons.
- Use MCP after `play_scene` to confirm mouse activation for campaign selection, the AP narrative acknowledgement surface, and original-bitmap encounter actions; keyboard activation alone is insufficient pointer evidence.

# Child DOX Index
