# Classic compatibility batch report — 2026-09-15

This report closes one three-workflow repair batch. Controlled rules, VM, and presentation evidence are not ordinary campaign certification.

## Delivered workflows

1. **Guard direction ordering — `988d26f5`.** Movement supplies its direction before Guard and resolves Guard before destination terrain, cost, hostile-contact, or friendly-collision gates. Blocked destinations keep the committed Guard results while preserving position and movement.
2. **Friendly-collision cancellation — `7c38122a`.** The post-Guard collision is a serializable indexed Swap Positions / Attack Friend choice with an explicit Back out. Cancellation clears only the pending collision and does not move, spend movement, replay Guard, or end the activation.
3. **Queued monster macro fields — `c521e7de`.** Opcode 17 accepts source-authored fixed and power-area queued fields. It rolls and creates the persistent field at the retained macro-source coordinate before immediate resolution, spends no active-character resources, and continues the issuing program through the existing field and death-macro owners.

## Compatibility register result

- `CCG-013` is closed in controlled public combat for Guard ordering, blocked movement, stable save/restore, Swap, Attack Friend, and cancellation. Ordinary battle-control replay remains required.
- The queued-area portion of `CCG-010` is closed in controlled VM execution. The corrected exact corpus audit finds 157 stored opcode-17 rows and 68 statically reachable from authored macro roots: 24 immediate, 36 queued area fields, three repeated-target, and five single-target/ray-family rows.
- The previous claim that the bundled corpus had no directly referenced opcode-17 macro row was false and has been removed.

## Verification

- Focused Guard and collision verification: 1,411 assertions across combat, session, persistence, and UI suites, plus architecture, maintainability, differential-evidence, and whitespace checks.
- Focused macro verification: 520 Scenario VM assertions. The Noxious Cloud signature creates one persistent field at the retained dead source, emits its creation, preserves the active activation and resource totals, and reaches following code.
- Deterministic package audit: all 13 bundled archives; 157 stored and 68 statically reachable opcode-17 rows. Two identical runs produced byte-identical JSON. The script parses under Windows PowerShell 5.1.
- Aggregate gate: 4,475 assertions across 29 suites; Providence alignment; application media and music; all 13 bundled packages and 515 scenario CICN descriptors; architecture, maintainability, schema, provenance, workflow, gameplay-parity, and Windows/Linux/macOS export-contract checks.
- The main-scene smoke launch retains the known single ObjectDB exit warning.
- Optional live Castle-worktree validation was skipped because that separate checkout contains unrelated tracked QuickDraw changes. The inspected `newland.c`, `spelltargets.c`, and `resist.c` paths are unchanged from pinned Castle commit `491816ad60037394f92c428e99c004494d3c28b3`; the repository's 101 pinned differential cases passed.

## Failed and open cases

- Three repeated-target and five target-type-1/6 opcode-17 rows are statically reachable but remain explicit failures. Castle aliases the macro coordinate through unsafe or bypassed selector logic for these families; controlled Castle fixtures must establish observable behavior before Rebuilt chooses aligned or deliberately corrected semantics.
- The audit originally found called programs `xap:165` and `xap:169` absent from Assault on Giant Mountain and `xap:9363` absent from White Dragon. Subsequent exact-source and current Providence inspection separates them: AOGM's two rows are valid in-range authored programs omitted by stale package lineage, while White Dragon's value is an out-of-range dangling reference inside an aligned foreign suffix. Neither is runtime-family closure, and neither package can be honestly regenerated until its independent strict-readiness blocker receives an explicit source decision.
- The two previously loadable third-party packages were not retained for this audit, and ten other third-party sources remain Providence conversion-blocked.
- The reported David/Party Auto stalls, delayed battlefield reveal, ordinary playback chronology, and scenario-facing symptoms were not reproduced in this batch.

## Campaign and format status

- No scenario status is promoted. The 13 bundled scenarios remain strict-load verified, while City of Port Hyrtin and Journey into the Mire remain third-party `loadable` only.
- No `.r2save`, Character Files, settings, or scenario-package schema changed in this batch.
- Native export contracts passed, but this Windows host did not execute Linux or macOS artifacts. Physical-controller evidence is unchanged.

## Next executable cases

1. Build controlled Castle fixtures for opcode-17 target types 0, 1, and 6, starting with the exact reachable Magic Darts, Heal Wounds, and Daze signatures.
2. Trace the three absent called XAP identities back to source ownership and Providence preservation before changing runtime behavior.
3. Replay the retained David and battle-start Auto cases through an observed application host, then inspect committed/presented revision chronology.
4. Begin ordinary campaign certification with Assault on Giant Mountain, then War in the Sword Lands; neither can be promoted without its complete save/resume start-to-ending route.
5. Re-run macro and normalized feature audits for each third-party package as Providence clears its named conversion blocker.

## Subsequent focused workflows

- **Missing macro-program classification — `1c3f1c6e`.** The deterministic package audit now separates missing called programs within the emitted program extent from out-of-range references. AOGM XAP 165/169 are stale-package omissions; White Dragon XAP 9363 is an out-of-range foreign-suffix reference. This classification does not waive either package's independent Providence readiness failure.
- **Opcode 90 picked targets.** Rebuilt already followed the documented mode-1 meaning. Public Scenario VM coverage now fixes the regression boundary at action slots 0 and 7 with a mixed picked set, and `FD-SCENARIO-007` records the deliberate correction to Castle's `track[t]` source typo. Stored corpus occurrence and ordinary-route reachability remain open.
- **Opcode 7 replacement modes.** Public Scenario VM evidence now covers the shipped source meanings: `-1` Simple result, `-2` Complex result, and nonnegative Action Point replacement. All three persist through save-owned encounter state without RNG; unknown negative imports fail atomically. Bundled occurrences are statically nonnegative, with route reachability still open.
- **Opcode 13 trigger mutation modes.** Public runtime evidence now covers one explicit record combined with an inclusive range, positive land and negative dungeon range selection, silent unplaced native rows, signed chance override persistence, canonical disablement, zero RNG, and atomic invalid-map rejection. Ordinary authored-route reachability remains open.

## Opcode and corpus batch closeout

- Focused commits: `1c3f1c6e` classifies missing macro programs, `a05bc3f5` protects corrected opcode 90 targeting, and `0c493eaf` verifies opcode 7 replacement modes.
- Aggregate verification passes 4,479 assertions across 29 suites; Providence package alignment; the 648-entry application media catalog; all 13 bundled packages and 515 scenario CICN descriptors; workflow, gameplay, schema, provenance, architecture, maintainability, and Windows/Linux/macOS export contracts.
- The main-scene smoke retains the known single ObjectDB exit warning. No package, save, Character Files, settings, or public-operation schema changed.
- No campaign status is promoted. AOGM and White Dragon still require their independent Providence source/readiness decisions before package refresh; opcode 7 and 90 ordinary-route reachability remains open; the ambiguous opcode-17 target families still require controlled Castle execution.

## Auto observation and opcode-evidence batch closeout

- **Persistent Auto observation — `883cba34`.** A rendered Half Truth Battle 70 fixture enabled all six roster Auto controls through actual UI input, committed five consecutive automated activations, and reached Treasure in round 2 without controller suspension, a failed revision, or an engine error. The observation contract now exposes active/outcome state and clears the terminal actor and round so completed combat cannot resemble a stall. This does not reproduce David's exact revision or satisfy the three-round acceptance case.
- **Exact macro signatures — `afa199b4`.** Schema 3 of the deterministic macro audit adds prose-free class, target, special, restriction, damage, duration, range, size, count, and queue fields. The 13 bundled packages still contain exactly eight reachable unresolved rows: three target-type-0 rows, one target-type-1 row, and four target-type-6 rows. They are now separable as the exact Magic Darts, Destroy Magic, Heal Wounds, Daze, Flame Tongue, and Acid Splash mechanical cases rather than one unsupported bucket. No ambiguous behavior was implemented without controlled Castle evidence.
- **Opcode 13 variants — `eb28297c`.** Public runtime coverage proves combined single-plus-range mutation, inclusive endpoints, positive land and negative dungeon selection, silent unplaced rows, canonical disablement, save restoration, zero RNG, and atomic invalid-map rejection. Stored corpus presence and ordinary-route reachability remain separate.
- **Aggregate.** The batch passes 4,485 assertions across 29 suites; Providence package alignment; the 648-entry application media catalog; all 13 bundled packages and 515 scenario CICN descriptors; workflow, gameplay, schema, provenance, architecture, maintainability, and Windows/Linux/macOS export contracts. The main-scene smoke retains the known one-object teardown warning.
- **Formats and certification.** No save, Character Files, settings, or package schema changed. No scenario status is promoted. The next blockers remain an exact David/full-three-round Auto replay, controlled Castle execution for macro target types 0/1/6, Providence resolution of the AOGM and White Dragon source-readiness cases, and ordinary AOGM then War route certification.

## Third-party opening evidence

- **Fixture controller release — `f5828bee`.** A fixture button release or exact axis-neutral event can cross an intervening gameplay revision, while initiating input retains optimistic concurrency and every live-session mutation remains read-only. The focused controller suite passes 105 assertions, the workflow gate passes, and a rendered Journey fixture accepts a deliberately stale D-pad release after scheduled movement advanced the session.
- **City of Port Hyrtin.** Exact archive SHA-256 `5a8960eb1c79712eedb9eb189aaccad31a8148620cce8f6772ebc8e00eea7e84` and package hash `b684c976d125af8ad539ec2859dc0a354a20af4e6e4e299b9df9e65e6f15c6a2` ran in fixture `a4d1fa7d86e5d987a077700a3b199cab`. Controller movement from land 6 `(41,1)` fired the authored opening at `(41,4)`; South advanced message IDs 690, 691, and 692 and returned to exploration. The final 1280×720 capture SHA-256 is `7f57696ae915eaa613c1081138fd753380379a172e55a3bf08d8a14c660bb7e0`.
- **Journey into the Mire.** Exact archive SHA-256 `53ace211a18dae91382c8da393b615beedf4f4c8a496e89dc00b81611c085bea` and package hash `c2794134c6993541deb313cb397f5b95625bc2cfb01d8bd4c1afbbc5df09cc9b` ran in fixture `09809063b330720a74322a9d9ab6e9a2`. Controller movement from land 3 `(8,64)` fired the nearby authored chest program at `(6,64)`; South cleared message ID 12 and returned to exploration. The final capture SHA-256 is `eaa15f8ad9cea152287ba1a4e4c64fddb512262eae88d0837449fdabf9eee387`.
- Both journeys used Rebuilt `f5828beeef83cdbac6ad32488b94f7c0637ce62f` with the unrelated `project.godot` fingerprint `62ddce81684286d172439b3b1dc1b6c165f481bd71a8f8fff9aecb3f221e2415`. They promote exactly two intake entries from `loadable` to `opening-tested`. They do not establish ordinary party setup, save/resume, distinctive mechanics, an ending, or main-route playability; ten intake sources remain conversion-blocked.

## Dagger of Shine compiler-blocker evidence

- The pinned source's normal monster 141 stores physical and magical attack counts of seven in `Data MD`, `Data MD1`, and `Data MD-1`, with zero bonus attacks and only five serialized physical attack rows.
- The exact authored reference chain is land level 3 AP 33 at `(56,46)` -> XAP 168 -> opcode 56 / Extra Code 415 -> battle 205 -> grid slot 84 -> monster 141. This proves stored and static authored reachability; it does not prove an ordinary campaign route reaches the AP.
- Pinned Castle's monster struct has five attack rows, while its activation loop uses the stored count and `attack2` directly indexes the supplied row. A count of seven therefore reads beyond the array into adjacent fields. Source control flow cannot adjudicate whether the author expected that unsafe result, intended five repeated rows, or simply authored a bad count.
- Rebuilt receives no package because Providence rejects the reachable projection. No runtime clamp, save/package schema change, or compatibility bypass was introduced. Closure requires controlled Castle execution and a coordinated Providence representation that retains the raw byte while assigning safe explicit semantics.

## Controller, third-party opening, and compiler-evidence batch closeout

- Focused commits are `f5828bee` for stale-revision-safe fixture controller releases, `ce680139` for the two rendered third-party openings, and `3edf89f4` for the Dagger of Shine compiler-blocker adjudication.
- The aggregate gate passes 4,486 assertions across 29 suites; Providence package alignment; the 648-entry application media catalog; all 13 bundled packages and 515 scenario CICN descriptors; gameplay and workflow inventories; architecture, maintainability, schema, provenance, and Windows/Linux/macOS export contracts.
- The short main-scene smoke retains the known single ObjectDB exit warning. The complete test suite reports no retained-resource failure.
- No save, Character Files, settings, or package schema changed. No scenario is promoted beyond the two exact `opening-tested` entries, and no third-party payload entered the repository or public bundle.

## Subsequent compiler-reference triage

- Direct Providence reachability inspection split three identical readiness summaries. Araman's Ring and Lachis fail only because application door item 662 points at scenario-local XAP 69, absent beyond their exact 51-row and 34-row `Data ED3` extents. Rebuilt already keeps such an imported portable item safe and nonmutating by disabling Use when its scenario program is unavailable. Providence should retain the item without globally requiring or fabricating the program.
- Elemental Strife instead has a 100-percent placed land AP at `(41,76)` that directly starts Simple Encounter 13. The physical target row decodes as semantically empty, so this remains a genuine source/oracle blocker rather than being waived with the optional item case.
- This is source/control-flow and compiler-selection evidence. No candidate package was produced, no runtime or schema changed, and no campaign status is promoted.

## Subsequent exact-party Auto diagnostic

- `combat_auto_reproduction_probe.gd` now accepts caller-supplied package, checkpoint, battle ID, and bounded seed count. It restores only diagnostic game state in memory, starts the selected package Battle through `RealmzRules`, enables every living party participant, and records the exact actor, round, error, activation count, and RNG count if automatic progression stops.
- The retained Half Truth party checkpoint SHA-256 `fa09e9d871982668355a6f3e95cce229f63e37adfc3fe16e8c8a13ed2a08b79c` includes the reported David revision. Its save-v4 representation lacks the authoritative equipment order now required by save v5, so the probe explicitly reports its six inventory-order assumptions and neither rewrites nor restores the old save through production. Against installed archive SHA-256 `a197fe8977e583f71f97d4d25cfaab36ec2515a622bbad50894263cc2e752e78`, the checkpoint's saved RNG reached Battle 70 victory in nine party activations; independent seeds 1–250 also all reached victory.
- This narrows the defect away from a readily reproducible rules failure for David's character state. It does not close the report: headless direct rules execution omits presentation settling and application-owned continuation, and the assumed equipment order prevents calling this an exact save restoration.

## Subsequent Hax message triage

- Detailed readiness inspection pins Hax's only current blocker to XAP 567, opcode 2, Extra Code row 2038. Its exact signed values are `(-128, 0, 0, 3001, 0)`, requesting surprise battle 128 with pre-battle message 3001. The source `Data SD2` SHA-256 `b44d28f2e361eb1ae7a0b44f9b3db6ea73308083e5a7f43ed690e9c85fc6f4c4` is exactly 1,081 complete records, IDs 0–1080; the referenced message does not exist.
- Castle source initializes its textbox buffer from application `STR#` 3 entry 1 and then ignores failed seek/read results. The observed source path would therefore show stale fallback text, not authored Hax message content. Providence remains correct to block package generation pending an authoring-source correction or explicit malformed-input fidelity decision.
- No Rebuilt runtime or schema change is justified, no package was produced, and ordinary-route reachability to XAP 567 remains unproven.

## Third-party blocker and exact-party Auto batch closeout

- Focused commits are `b945e7fb` for compiler-reference classification, `a8ee8306` for the exact-party Auto reproduction probe, and `97d022c1` for the Hax message blocker.
- The aggregate gate passes 4,486 assertions across 29 suites; Providence package alignment; the 648-entry application media catalog; all 13 bundled packages and 515 scenario CICN descriptors; gameplay and workflow inventories; architecture, maintainability, schema, provenance, and Windows/Linux/macOS export contracts.
- The short main-scene smoke retains the known single ObjectDB exit warning. No runtime behavior, package, save, Character Files, settings, or schema changed in this evidence batch.
- No campaign status is promoted. Araman's Ring, Lachis, Elemental Strife, and Hax remain conversion-blocked for their separately documented reasons. The retained David report remains open at the rendered host/presentation-continuation boundary despite the direct-rules negative reproduction.

## Half Truth Auto and David rendered replay

- The new five-workflow batch is pinned to baseline `1b0502b1`, Half Truth package hash `952b9705460e117b7b914e143134c895e04405d686de39e7d58bf2790d52b870`, archive SHA-256 `a197fe8977e583f71f97d4d25cfaab36ec2515a622bbad50894263cc2e752e78`, and v5 checkpoint SHA-256 `4e42b976bee03303252d31fa3dfba4220b1d10367fdb0e888bee675ef6d89fb7`. The historical v4 checkpoint remains diagnostic-only.
- Fixture `0dfea5859729e02324868439027e0df2` restored that v5 save, directly prepared Battle 70, enabled all six roster Auto controls through actual pointer input with no controller involved, and reached Treasure without a stalled revision. Its terminal capture SHA-256 is `750c9521b3af65fa3a2b2389f82ebce6c019376cbeb0b5e9d8eb06a4553f042f`. Battle 70 ended in round one for this stronger current party, so this is an exact-David negative reproduction rather than the required duration case.
- The same party's deterministic direct probe selected Battle 34 as the explicit duration fixture: its saved RNG requires 47 party activations. Rendered fixture `03704298c8cfe9553fb9bb9fdd75fe26` crossed five complete rounds without a lost continuation, then settled on David as the manual actor. A mixed configuration left David manual, ran one explicit Auto Turn for him, continued the other five persistent-Auto actors, and returned to David at the round-six boundary. The settled capture SHA-256 is `32d595d5568e1d08c359c4fa48e856e5ff85e91dcf862799586176d31b091f3f`.
- The bridge now reports committed, presented, presentation-drawn, deferred, and current playback-frame identities separately from its transport revision. A fresh rendered fixture observed committed/presented/presentation-drawn revision 46 together while transport draw revision was 1, removing the earlier diagnostic ambiguity.
- These matching replays verify the current no-controller all-Auto and mixed-manual paths. The reported David/Party Auto incident is accepted closed for now; recurrence with a retained checkpoint and observation trace reopens it. No save, Character Files, settings, package, rules, or gameplay operation changed in this workflow.

## Consecutive combat entry and draw acknowledgement

- The same isolated v5 session directly prepared Half Truth Battle 34 at application revision 47. The immediate rendered-host observation recorded committed and deferred revision 47, presentation-drawn revision 46, active `sound` playback, and `visualReady: false`. No automatic activation crossed that boundary.
- All six roster Auto settings were then enabled through actual pointer controls. The battle advanced through seven rounds with each committed activation deferred behind the preceding presented/drawn revision and `combat-playback` reported as the continuation blocker. Victory opened Treasure at revision 99 and settled at revision 100.
- Actual Done, Continue, and Confirm Spell Selection controls completed Treasure and two level-up sequences through revision 105. Surviving Auto identities remained enabled. Consecutive Battle 70 then entered at revision 106 with only battle setup events: committed/deferred revision 106, presentation-drawn revision 105, active `sound` playback, and `visualReady: false`. Its first `combat_auto_started` event committed at revision 107, after the new combat presentation boundary; the battle reached Treasure at revision 112.
- This closes the reported battle-advancing-behind-exploration symptom for the current build. Direct Battle invocation is identified preparation rather than ordinary encounter-entry proof, but the route, visible controls, draw acknowledgement, persistent Auto settings, and terminal transitions all ran through the application host. No production behavior or format contract changed.

## Spell results, persistent fields, and terminal chronology

- The rendered Battle 34 replay exercised Solor Winds field creation and expiry, repeated field damage, multi-target Energy Storm and protection results, healing, and lethal spells. Capture SHA-256 `2c1fd471975ff219cf09813a6153d0d9fd2507579f216074b5277d91f499949a` records the active Solor Winds field on the 1280-by-720 battlefield at settled application revision 55.
- The terminal action at revision 98 is an exact Flame Missile sequence: cast, projectile, authored result sound, `damage: 8`, `healing: 0`, `defeated: true`, battle completion, Auto completion, reward construction, and reward opening. Presentation remained deferred until the lethal numeric result and defeat frames settled; revision 100 was then presented and drawn with no deferred frame or active playback.
- The focused presentation suite passes 647 assertions. Its existing chronology cases prove that `damage` wins over an explicit zero-valued `healing` field, the defeated combatant remains visible through the `8` result and disappears at the defeat frame, field creation and expiry mutate the detached battlefield at their own boundaries, Hurry shortens only eligible shared group effects while retaining target results, and reduced motion settles on the same final detached view without simulation mutation.
- This closes the reported zero-damage lethal-spell symptom and the reproduced ordinary field/defeat ordering defects. Skip-mode listening and physical confirmation of the Combat-to-Treasure music handoff remain presentation acceptance, not a known deterministic-state defect. No gameplay state, RNG, public operation, or format contract changed.
